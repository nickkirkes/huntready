# Implementation Plan: S03.9 — reporting_obligation ingestion

Plan-format-version: 1

## Context

S03.9 ingests Montana's `reporting_obligation` rows and `regulation_reporting` link rows. **PM-locked V1 scope (2026-05-19):** 3 reporting_obligation rows + 70 regulation_reporting link rows. All 3 entity rows are artifact-backed from `ingestion/states/montana/extracted/black-bear-2026.json` top-level `reporting_obligations` list (no upstream extraction changes).

Bear ID coursework — originally a candidate for S03.9 — was carved out as S03.6.1 (`MT-STATEWIDE-bear` regulation_record, queued post-S03.9). CWD sampling deferred to Q18 + M2. Epic line 807 ("all V1 species" mandatory reporting) marked as wrong; correction surfaced as PM-approval diff at S03.9 close.

Side artifacts already written during Stage 3:

- Working note: `docs/planning/epics/E03-confidence-findings/S03.9.md`
- Q18 open question: `docs/open-questions.md`
- Deferred-items stub: `docs/planning/epics/E03-deferred-items/cwd-sampling-modeling.md`
- S03.6.1 placeholder: in epic between S03.9 and S03.10 sections
- Merge order updated: S03.0 → … → S03.9 → S03.6.1 → S03.10 …

This plan covers ONLY S03.9 implementation. The 4 side artifacts are written and committed-with-S03.9.

**Inherits from S03.6/S03.7/S03.8 precedent:**

- Three-phase main: build → guards (pre-`db.connect()`) → conn/phase-loops/commit/rollback/close
- OQ7 row-count guards: ±30% bands, fire before `db.connect()`
- State-agnostic AST guard: `TestNoLibImports` test class scans `load_reporting_obligations.py` for state-adapter-name imports and fails if found in `ingestion/lib/` files (and vice versa — adapter cannot import from another state)
- Caller-controlled txn pattern in DB helpers (no internal commit)
- Fail-loud on FK lookup miss (RuntimeError naming the missed key)
- Adapter invoked as `ingestion/.venv/bin/python ingestion/states/montana/load_reporting_obligations.py` (NOT `python -m`; state package is non-subpackage per S03.1 pitfall)

## File Table

| File | Action | Task(s) |
|------|--------|---------|
| `ingestion/ingestion/lib/db.py` | Modify (append) | T1 |
| `ingestion/states/montana/load_reporting_obligations.py` | Create | T2, T3, T4, T5 |
| `ingestion/tests/test_db.py` | Modify (append) | T6 |
| `ingestion/tests/test_load_reporting_obligations.py` | Create | T7 |

Side-artifact files written in Stage 3a (already on disk, do NOT re-touch in Stage 5):

- `docs/planning/epics/E03-confidence-findings/S03.9.md`
- `docs/planning/epics/E03-deferred-items/cwd-sampling-modeling.md`
- `docs/planning/epics/E03-deferred-items/README.md` (Contents table updated)
- `docs/open-questions.md` (Q18 added)
- `docs/planning/epics/E03-regulation-text-ingestion.md` (S03.6.1 placeholder + merge-order updated)

`.roughly/known-pitfalls.md` is touched at Stage 8 wrap-up (per pipeline), NOT during Stage 5.

## Tasks

### T1: Add `upsert_reporting_obligation` + `write_regulation_reporting` to db.py (~4 min)

**Files:** `ingestion/ingestion/lib/db.py`

**Action:** Add two new public helpers at the end of `db.py`, mirroring the canonical patterns from `upsert_regulation_record` (S03.6) and `write_regulation_season` (S03.7).

**Details:**

1. Add import for `ReportingObligation` and `RegulationReporting` to the existing schema-import block at the top of `db.py` (the block that imports `RegulationRecord`, `SeasonDefinition`, etc. — find by inspection).
2. Add a module-level SQL constant `_UPSERT_REPORTING_OBLIGATION_SQL` for the entity table. INSERT with ON CONFLICT (id) DO UPDATE. Columns (per DDL at `supabase/migrations/20260425000000_initial_schema.sql:138-158`): `id, kind, deadline, deadline_hours, submission_method, submission_url, submission_phone, applies_to_regions, what_to_present, verbatim_rule, source` (the `source` column is jsonb — pass via `Json(citation_dict)` from `from psycopg.types.json import Json`). The UPDATE clause sets all non-PK columns. Do NOT include `ingested_at` (auto-timestamped — exclude per S03.6 precedent).
3. Add `def upsert_reporting_obligation(conn, obligation: ReportingObligation) -> None:` with full docstring (mirror `upsert_regulation_record`'s docstring: caller-controlled txn, idempotent UPSERT, no internal commit). The function:
   - Serializes `obligation.source` via `obligation.source.model_dump(mode="json")` then wraps in `Json(...)`. The `Json` import already exists in `db.py` at line 40 (`from psycopg.types.json import Json`) and is the convention used by every existing helper that writes a jsonb column (`upsert_regulation_record`, `upsert_geometry`, `upsert_season_definition`, `upsert_draw_spec`). Do NOT use `Jsonb` — that class is not imported and is not the project convention.
   - Passes `applies_to_regions` and `what_to_present` directly (psycopg adapts Python `list[str]` to PG `text[]`); use `None` for null.
   - Logs at `_logger.debug` with the obligation id.
4. Add a module-level SQL constant `_INSERT_REGULATION_REPORTING_SQL` for the link table. INSERT ... ON CONFLICT DO NOTHING. Columns: `state, jurisdiction_code, species_group, license_year, reporting_obligation_id`.
5. Add `def write_regulation_reporting(conn, link: RegulationReporting) -> None:` with full docstring (mirror `write_regulation_season`). Caller-controlled txn, no internal commit, idempotent.

**Conventions:**

- Type annotations: `psycopg.Connection[tuple[object, ...]]` (per existing helpers).
- Cursor managed via `with conn.cursor() as cur:`.
- No internal commit. Caller controls txn.
- Docstring includes: DDL table name, what it does, no-commit posture, args list, behavior on conflict.

**Verify:** `cd ingestion && .venv/bin/mypy ingestion/lib/db.py && .venv/bin/ruff check ingestion/lib/db.py` — clean.

**UI:** no

---

### T2: Create load_reporting_obligations.py scaffold (~3 min)

**Files:** `ingestion/states/montana/load_reporting_obligations.py`

**Action:** Create the adapter scaffold with module docstring, imports, module-level constants, and stub function signatures. No business logic yet — T3, T4, T5 fill those in.

**Details:**

1. Module docstring — match the depth + style of `load_seasons_and_licenses.py`'s docstring. Sections to include:
   - **Purpose:** Read `black-bear-2026.json` `reporting_obligations` list, build 3 `ReportingObligation` entity rows + 70 `RegulationReporting` link rows, write atomically.
   - **Inputs:** `ingestion/states/montana/extracted/black-bear-2026.json` (top-level `reporting_obligations` list + `rows` for region lookup + `sources` for citation), `ingestion/states/montana/sources.yaml` (citation metadata).
   - **Outputs:** 3 `reporting_obligation` rows + 70 `regulation_reporting` rows; both written in one atomic transaction.
   - **Row construction rules** (verbatim — copy from working note § "Row-by-row construction decisions"). For each of the 3 rows, list the dispatch-dict-keyed structured fields (id, kind, deadline_hours, submission_method, etc.).
   - **Cleanup rules:** none — verbatim_rule preserved byte-faithful per ADR-008 (no whitespace collapse, no normalization).
   - **Confidence:** none. `reporting_obligation` has no confidence column (ADR-017 inheritance).
   - **Three-phase transaction:** build → guards → conn/loops/commit/rollback/close. Both guards fire BEFORE `db.connect()`.
   - **Fail-loud disciplines:** (a) Pydantic validation failure raises at-row, naming the row; (b) FK lookup miss (regulation_record not present for a bear `jurisdiction_code`) raises `RuntimeError`; (c) row-count guards raise `RuntimeError` naming the expected band and actual count.
   - **State-agnostic posture:** this adapter imports ONLY from `ingestion.lib.*` and `ingestion.lib.schema`; it does NOT import from sibling state adapters, and `ingestion.lib.*` does NOT import this file. Locked by `TestNoLibImports`.
   - **Invocation:** `ingestion/.venv/bin/python ingestion/states/montana/load_reporting_obligations.py [--dry-run] [-v]`.
   - **Relevant ADRs:** ADR-001, ADR-008, ADR-009, ADR-010, ADR-017.

2. Imports (sorted by ruff): stdlib (`argparse`, `datetime`, `json`, `logging`, `pathlib`, `sys`, `typing`), third-party (`psycopg`, `yaml` if needed), `ingestion.lib.*` (`db`, `schema`).

3. Module constants:
   - `_LOGGER = logging.getLogger(__name__)`
   - `_MT_STATE_CODE = "US-MT"` (matches `load_regulation_records.py:67` — the ISO 3166-2 code is the canonical state-column value per DDL line 37; the bear artifact's top-level `state="MT"` is artifact-internal metadata, NOT the DB column value; `regulation_reporting` FK to `regulation_record(state, …)` requires byte-equality)
   - `_LICENSE_YEAR = 2026`
   - `_BEAR_ARTIFACT_PATH = pathlib.Path(__file__).parent / "extracted" / "black-bear-2026.json"`
   - (NO `_SOURCES_YAML_PATH` — the bear artifact embeds its `sources` list inline; T5 confirms `_load_citation_from_sources_yaml` is dropped from the stub list.)
   - `_R1_REGIONS = frozenset({"R1"})`
   - `_R2_TO_R7_REGIONS = frozenset({"R2", "R3", "R4", "R5", "R6", "R7"})`
   - `_BEAR_SPECIES_GROUP = "bear"` (matches S03.6's bear regulation_record `species_group` — NOT the artifact's top-level `"black_bear"`)
   - `_EXPECTED_REPORTING_OBLIGATION_COUNT = 3`
   - `_EXPECTED_REGULATION_REPORTING_COUNT = 70`
   - `_REPORTING_OBLIGATION_COUNT_GUARD_BAND = (2, 4)` (strict ±30%: floor(3 * 0.70) = floor(2.1) = 2; ceil(3 * 1.30) = ceil(3.9) = 4)
   - `_REGULATION_REPORTING_COUNT_GUARD_BAND = (49, 91)` (computed: floor(70 * 0.70) = 49; ceil(70 * 1.30) = 91)

4. Stub function signatures (each with `raise NotImplementedError` body):
   - `def _load_citation_from_sources_yaml(source_id: str, page_reference: str) -> SourceCitation`
   - `def _build_reporting_obligations(bear_artifact: dict) -> list[ReportingObligation]`
   - `def _build_regulation_reporting_links(reporting_obligations: list[ReportingObligation], bear_artifact: dict) -> list[RegulationReporting]`
   - `def _assert_reporting_obligation_count_within_guard(written: int) -> None`
   - `def _assert_regulation_reporting_count_within_guard(written: int) -> None`
   - `def _parse_args(argv: list[str] | None) -> argparse.Namespace`
   - `def _configure_logging(verbose: bool) -> None`
   - `def main(argv: list[str] | None = None) -> int`
   - `if __name__ == "__main__":\n    sys.exit(main())`

5. **State-agnostic posture:** the adapter MUST NOT import from any other state-adapter module (`ingestion/states/<other_state>/*`). It MAY import from `ingestion/states/montana/load_regulation_records.py` only if it needs `_load_citation_from_sources_yaml` — but mirror S03.8's discipline: prefer to replicate the helper inline rather than cross-import (see `load_draw_specs.py` lines 145-230 which replicates the citation loader rather than importing from `load_regulation_records.py`). DO replicate, do not import.

**Verify:** `cd ingestion && .venv/bin/ruff check states/montana/load_reporting_obligations.py && .venv/bin/mypy ingestion/lib/ states/montana/load_reporting_obligations.py` — clean. (The mypy invocation includes `ingestion/lib/` because it's the project-canonical pattern from S03.7's mypy-discipline pitfall.)

**UI:** no

---

### T3: Implement `_build_reporting_obligations` with dispatch dictionary (~5 min)

**Files:** `ingestion/states/montana/load_reporting_obligations.py`

**Depends on:** T2

**Action:** Implement the entity-row builder. Reads `bear_artifact["reporting_obligations"]` (a list of 3 entries), constructs 3 `ReportingObligation` Pydantic instances via a dispatch dictionary keyed by `(region_scope, kind_hint)`.

**Details:**

1. Define module-private `_REPORTING_ROW_SPEC: dict[tuple[str, str], dict]` — a frozen dispatch dict with exactly 3 entries, keyed by `(region_scope, kind_hint)` tuples. Each entry contains the structured-field values from the working note. Use this exact structure:

   ```python
   _REPORTING_ROW_SPEC: Final[dict[tuple[str, str], _RowSpec]] = {
       ("STATEWIDE", "harvest_report"): {
           "id_suffix": "harvest-report-48hr-statewide",
           "kind": "harvest_report",
           "deadline": "48 hours",
           "deadline_hours": 48,
           "submission_method": "phone",
           "submission_url": "https://fwp.mt.gov",
           "submission_phone": "1-877-FWPWILD",
           "applies_to_regions": None,
           "what_to_present": None,
       },
       ("R1", "tooth_submission"): {
           "id_suffix": "tooth-submission-r1-10day",
           "kind": "tooth_submission",
           "deadline": "10 days",
           "deadline_hours": 240,
           "submission_method": "agency_office",
           "submission_url": None,
           "submission_phone": None,
           "applies_to_regions": ["R1"],
           "what_to_present": ["both premolar teeth"],
       },
       ("R2-7", "hide_skull_presentation"): {
           "id_suffix": "hide-skull-r2to7-10day",
           "kind": "hide_skull_presentation",
           "deadline": "10 days",
           "deadline_hours": 240,
           "submission_method": "in_person_check_station",
           "submission_url": None,
           "submission_phone": None,
           "applies_to_regions": ["R2", "R3", "R4", "R5", "R6", "R7"],
           "what_to_present": ["hide", "skull"],
       },
   }
   ```

   Use a `TypedDict` (`_RowSpec`) for the value shape. Mark the dict as `Final` (typing import).

2. Define `_ID_PREFIX = "mt-bear"` as a **module-level constant** (place it with the other module constants in the constants section near the top of the file, NOT inside the function). It is read by both `_build_reporting_obligations` (T3) for the obligation `id` slug AND by `_build_regulation_reporting_links` (T4) when deriving the 3 known obligation ids from `_REPORTING_ROW_SPEC` — both functions read it from module scope. The obligation `id` = `f"{_ID_PREFIX}-{spec['id_suffix']}"`.

3. Implement `_build_reporting_obligations(bear_artifact: dict) -> list[ReportingObligation]`:
   - Validate `bear_artifact.get("reporting_obligations")` is a non-empty `list`. Defensive shape check matches S03.6's bear-artifact pattern (see `load_regulation_records.py:322-335`).
   - Validate `bear_artifact.get("sources")` is a non-empty `list` and build a `sources_by_id: dict[str, dict]` lookup (mirror S03.6 `load_regulation_records.py:336`).
   - For each entry in `reporting_obligations`:
     - Extract `region_scope`, `kind_hint`, `source_id`, `source_publication_date`, `page_reference`, `verbatim_rule`.
     - Look up `(region_scope, kind_hint)` in `_REPORTING_ROW_SPEC`. If missing, raise `RuntimeError` naming the unknown combo + listing the 3 expected combos (fail-loud per S03.7/S03.8 precedent).
     - Look up `source_id` in `sources_by_id`. If missing, raise `RuntimeError` naming the unknown id.
     - Build `SourceCitation(id, agency, title, url, publication_date, document_type, supersedes=None, page_reference=pdf.page_reference_to_str(page_reference))`. The `supersedes=None` is correct for reporting obligations (they come from the booklet, not the correction PDF — Probe 1 confirmed the correction is hound-training-prose-only).
     - Build `ReportingObligation(id=f"{_ID_PREFIX}-{spec['id_suffix']}", kind=spec["kind"], deadline=spec["deadline"], deadline_hours=spec["deadline_hours"], submission_method=spec["submission_method"], submission_url=spec["submission_url"], submission_phone=spec["submission_phone"], applies_to_regions=spec["applies_to_regions"], what_to_present=spec["what_to_present"], verbatim_rule=verbatim_rule, source=citation)`.
     - Pydantic's `frozen=True, extra="forbid"` validates at construction; non-empty `verbatim_rule` validator runs automatically.
   - Return list of 3 `ReportingObligation` in source-list order.

4. Add a sanity post-condition: `assert len(result) == _EXPECTED_REPORTING_OBLIGATION_COUNT, ...` — but use `RuntimeError`, not bare `assert` (asserts get stripped under `-O`). The actual guard is at the count-guard step; this is belt-and-suspenders against an empty source list.

**Conventions:**

- Use `pdf.page_reference_to_str` from `ingestion.lib.pdf` (already imported via S03.6 patterns).
- Use `cast(Literal[...], spec["kind"])` if mypy doesn't narrow from the TypedDict.

**Verify:** `cd ingestion && .venv/bin/ruff check states/montana/load_reporting_obligations.py && .venv/bin/mypy ingestion/lib/ states/montana/load_reporting_obligations.py` — clean.

**UI:** no

---

### T4: Implement `_build_regulation_reporting_links` (~4 min)

**Files:** `ingestion/states/montana/load_reporting_obligations.py`

**Depends on:** T3

**Action:** Build 70 `RegulationReporting` link rows by iterating bear regulation_records and matching each to the right subset of the 3 obligations via the per-BMU `hd_region` field.

**Details:**

1. Implement `_build_regulation_reporting_links(reporting_obligations: list[ReportingObligation], bear_artifact: dict) -> list[RegulationReporting]`:
   - Look up the 3 obligations by their canonical `id`. `ReportingObligation` does NOT carry `region_scope` or `kind_hint` (those are dispatch-dict inputs, not model fields), so build `obligation_by_id: dict[str, ReportingObligation]` directly from the input list as `{ob.id: ob for ob in reporting_obligations}`. Then derive the 3 known IDs once, up front, from `_REPORTING_ROW_SPEC`: `statewide_id = f"{_ID_PREFIX}-{_REPORTING_ROW_SPEC[('STATEWIDE','harvest_report')]['id_suffix']}"` (and similarly for `r1_id` and `r2to7_id`). If any of the 3 expected ids is missing from `obligation_by_id`, raise `RuntimeError` naming the missing id(s) — this catches an upstream T3 regression.
   - Validate `bear_artifact.get("rows")` is a non-empty `list` (same shape as S03.6 bear-row consumption).
   - Iterate `bear_artifact["rows"]`:
     - For each row, derive `jurisdiction_code = f"MT-HD-bear-{row['bmu_number']}"` (MUST match `load_regulation_records.py:366` byte-for-byte; deviation = silent FK miss).
     - Read `hd_region` from the row. Validate it is `"R1"` ∪ ... ∪ `"R7"`; raise on unknown.
     - Append a `RegulationReporting(state="US-MT", jurisdiction_code=jurisdiction_code, species_group="bear", license_year=2026, reporting_obligation_id=<statewide_harvest_report.id>)` — applies to EVERY bear row → 35 links.
     - If `hd_region` is "R1": append a 2nd link to `<r1_tooth_submission.id>` → 14 more links.
     - Else (R2-R7): append a 2nd link to `<r2to7_hide_skull.id>` → 21 more links.
   - Return list. Expected length: 35 + 14 + 21 = 70.

2. Add a sanity post-condition: `if len(result) != _EXPECTED_REGULATION_REPORTING_COUNT: raise RuntimeError(f"expected {_EXPECTED_REGULATION_REPORTING_COUNT} regulation_reporting rows, got {len(result)}")`. The OQ7 guard is the formal check; this is fast-fail.

3. Defensive validation against duplicates: ensure no two `RegulationReporting` rows share the same `(state, jurisdiction_code, species_group, license_year, reporting_obligation_id)` composite key. The construction logic guarantees this (each BMU produces 2 distinct obligation_ids); add `assert len({(r.state, r.jurisdiction_code, r.species_group, r.license_year, r.reporting_obligation_id) for r in result}) == len(result)` BUT use `RuntimeError` not assert.

**Verify:** `cd ingestion && .venv/bin/ruff check states/montana/load_reporting_obligations.py && .venv/bin/mypy ingestion/lib/ states/montana/load_reporting_obligations.py` — clean.

**UI:** no

---

### T5: Implement OQ7 guards + main() with three-phase transaction (~5 min)

**Files:** `ingestion/states/montana/load_reporting_obligations.py`

**Depends on:** T3, T4

**Action:** Wire up the row-count guards, the CLI args parser, the logging configurator, and the `main()` function with the three-phase transaction structure inherited from S03.6/S03.7/S03.8.

**Details:**

1. Implement `_assert_reporting_obligation_count_within_guard(written: int) -> None`:
   - Read `(lo, hi)` from `_REPORTING_OBLIGATION_COUNT_GUARD_BAND`.
   - If `written < lo or written > hi`: raise `RuntimeError(f"reporting_obligation row count {written} outside ±30% band [{lo}, {hi}] (expected {_EXPECTED_REPORTING_OBLIGATION_COUNT}); ...")`. Include hint: "If the source PDF changed, update `_EXPECTED_REPORTING_OBLIGATION_COUNT` and rerun. If unexpected, inspect `extracted/black-bear-2026.json`."

2. Implement `_assert_regulation_reporting_count_within_guard(written: int) -> None` — same pattern, different constants.

3. Implement `_load_citation_from_sources_yaml(source_id: str, page_reference: str) -> SourceCitation` — replicate from `load_regulation_records.py`. Read `sources.yaml`, find entry by `id == source_id`, construct `SourceCitation` with `page_reference=page_reference` and `supersedes=None`. Fail loud on missing id. (This is the alternative path to T3's artifact-based citation construction; KEEP T3's artifact-based path since the bear artifact already carries the source dicts inline. Document this helper as "currently unused but kept for parity with S03.6/S03.8 — DELETE if not exercised by a test." Actually — simpler — DROP this helper entirely; T3 reads from `bear_artifact["sources"]` which is sufficient. Remove the stub from T2's scaffold.)

   **REVISION TO T2/T5 SCAFFOLD:** drop `_load_citation_from_sources_yaml` from the stub list and from this task. T3 reads citation metadata directly from `bear_artifact["sources"]` (S03.6's bear path also does this). Confirmation that `sources.yaml` is NOT needed for S03.9 because the bear artifact embeds the citation metadata.

4. Implement `_parse_args(argv: list[str] | None) -> argparse.Namespace`:
   - `--dry-run` flag: builds rows + fires guards but does NOT open a DB connection.
   - `-v` / `--verbose` flag: sets logger level to `DEBUG`.

5. Implement `_configure_logging(verbose: bool) -> None` — match the boilerplate from `load_draw_specs.py:663-668`.

6. Implement `main(argv: list[str] | None = None) -> int`:
   - **Phase 0 — setup:** parse args, configure logging.
   - **Phase 1 — build:**
     - Read + parse `black-bear-2026.json` (use `pathlib.Path.read_text()` then `json.loads`).
     - Call `_build_reporting_obligations(bear_artifact)` → list of 3.
     - Call `_build_regulation_reporting_links(obligations, bear_artifact)` → list of 70.
     - Log counts at INFO.
   - **Phase 2 — guards (pre-`db.connect()`):**
     - `_assert_reporting_obligation_count_within_guard(len(obligations))`
     - `_assert_regulation_reporting_count_within_guard(len(links))`
   - **Phase 3 — short-circuit on dry-run:** if `args.dry_run`: log "DRY RUN — would write N+M rows" and return 0.
   - **Phase 4 — open conn, write, commit:**
     - `with db.connect() as conn:` (mirrors S03.6/S03.7/S03.8 pattern).
     - `try:`
       - Phase 4a: for obligation in obligations: `db.upsert_reporting_obligation(conn, obligation)`.
       - Phase 4b: for link in links: `db.write_regulation_reporting(conn, link)`.
       - `conn.commit()`. Log "committed".
     - `except Exception:`
       - `conn.rollback()`. Log "rolled back due to error". Re-raise.
   - Return 0 on success.

7. Add `if __name__ == "__main__": sys.exit(main())` at file end.

**Conventions:**

- Logging at INFO for high-level milestones (build counts, guard passes, commit); DEBUG for per-row.
- Three-phase main mirrors `load_draw_specs.py:671-836` structurally.

**Verify:**

```bash
cd ingestion && .venv/bin/ruff check states/montana/load_reporting_obligations.py && .venv/bin/mypy ingestion/lib/ states/montana/load_reporting_obligations.py
.venv/bin/python states/montana/load_reporting_obligations.py --dry-run -v
```

The dry-run must (a) print "DRY RUN — would write 3 reporting_obligation rows and 70 regulation_reporting rows" (or similar wording), (b) NOT attempt a DB connection, (c) exit 0.

**UI:** no

---

### T6: Tests for db.py helpers (~3 min)

**Files:** `ingestion/tests/test_db.py`

**Depends on:** T1

**Action:** Add two test classes to `test_db.py` covering the new helpers. Follow the pattern of existing `TestUpsertSeasonDefinition` / `TestWriteLicenseSeason` test classes (find by inspection).

**Details:**

1. `class TestUpsertReportingObligation:` with at least these tests:
   - `test_upsert_new_row_inserts_all_columns` — fixture: mock psycopg cursor; verify the SQL executed contains the expected column list and the param tuple matches the obligation's fields in declared order. Use `pytest.MonkeyPatch` or a `MagicMock` for the connection.
   - `test_upsert_existing_row_runs_update_clause` — same pattern; verify the ON CONFLICT path is exercised by inspecting the SQL string.
   - `test_no_internal_commit` — verify the helper does NOT call `conn.commit()` (caller-controlled txn).
   - `test_json_wrap_for_source` — verify `obligation.source` is serialized via `Json(...)` (mock the `Json` constructor and assert it's called with the dict form of source). `Json` is imported at `db.py:40` from `psycopg.types.json`.

2. `class TestWriteRegulationReporting:` with:
   - `test_insert_emits_expected_sql_and_params` — verify the INSERT statement contains all 5 PK columns and the param tuple matches the link's fields.
   - `test_no_internal_commit`.
   - `test_idempotent_on_conflict_do_nothing` — verify the SQL contains `ON CONFLICT DO NOTHING` (not `DO UPDATE`).

3. Test fixtures should use a real `ReportingObligation` and `RegulationReporting` instance constructed with the V1 dispatch values (one valid Pydantic instance per class).

**Conventions:**

- Test class style: `class TestXxx:` (PEP-styled; no `unittest.TestCase`); pytest function tests inside.
- Mock connections with `unittest.mock.MagicMock(spec=psycopg.Connection)` — mirror existing pattern in `test_db.py`.

**Verify:** `cd ingestion && .venv/bin/pytest tests/test_db.py::TestUpsertReportingObligation tests/test_db.py::TestWriteRegulationReporting -v` — all pass; `mypy` + `ruff` clean.

**UI:** no

---

### T7: Tests for load_reporting_obligations.py (~5 min)

**Files:** `ingestion/tests/test_load_reporting_obligations.py`

**Depends on:** T3, T4, T5

**Action:** Create the full test suite. Mirror the structure of `test_load_draw_specs.py` and `test_load_regulation_records.py`.

**Details:**

Implement these test classes (each with multiple test functions). At minimum:

1. **`class TestNoLibImports:`** — AST guard. Walk the file `load_reporting_obligations.py` and assert no imports from sibling state adapters (`from ingestion.states.<other>...` is forbidden; `from .` relative imports forbidden; `import states.colorado...` forbidden — etc.). Mirror the equivalent class in `test_load_draw_specs.py:1401`.

2. **`class TestBuildReportingObligations:`**
   - `test_builds_three_rows_from_artifact` — load real `black-bear-2026.json`, call `_build_reporting_obligations`, assert `len() == 3` and the 3 `(region_scope, kind_hint)` keys are covered.
   - `test_statewide_harvest_report_fields` — assert specific structured-field values from the dispatch dict (id, kind, deadline_hours=48, submission_method="phone", submission_phone="1-877-FWPWILD", applies_to_regions=None, what_to_present=None).
   - `test_r1_tooth_submission_fields` — assert `applies_to_regions == ["R1"]`, `what_to_present == ["both premolar teeth"]`, `submission_method == "agency_office"`, `deadline_hours == 240`.
   - `test_r2to7_hide_skull_fields` — assert `applies_to_regions == ["R2","R3","R4","R5","R6","R7"]` (exact list, exact order), `what_to_present == ["hide","skull"]`, `submission_method == "in_person_check_station"`.
   - `test_verbatim_rule_preserved_byte_faithful` — assert `\n` line breaks preserved, no whitespace collapse (compare to artifact's raw verbatim_rule).
   - `test_unknown_region_scope_kind_combo_raises` — synthesize an artifact entry with `region_scope="R8"`, assert `RuntimeError` mentions the unknown combo + the 3 valid combos.
   - `test_missing_source_id_raises` — synthesize an entry pointing to an unknown `source_id`, assert `RuntimeError` names the bad id.
   - `test_no_confidence_column` — assert the constructed instances have no `confidence` attribute (Pydantic `extra="forbid"`). Inverse: try constructing a ReportingObligation with `confidence="medium"`, assert ValidationError.
   - `test_cwd_sample_kind_uses_schema_literal` — Locked-with-a-test (per scope lock-in from PM): construct a `ReportingObligation(kind="cwd_sample", ...)` and assert it validates. Construct with `kind="cwd_sampling"` and assert `ValidationError`. This protects against epic-narrative drift slipping into a future row.

3. **`class TestBuildRegulationReportingLinks:`**
   - `test_link_count_total_70` — load real artifact, build obligations + links, assert `len() == 70`.
   - `test_link_count_per_obligation_35_14_21` — group by `reporting_obligation_id`, assert exactly 35 + 14 + 21 distribution.
   - `test_r1_bmu_list_locked` — assert the 14 R1 bmu_number values in the resulting tooth_submission links match the locked set `{100,101,103,104,110,120,121,122,123,130,140,141,150,170}` (canonical M1 success-criterion-#2-style baseline lock).
   - `test_jurisdiction_code_matches_s03_6_pattern` — for every link, assert `jurisdiction_code` starts with `"MT-HD-bear-"` (S03.6 pattern lock).
   - `test_unknown_hd_region_raises` — synthesize a bear row with `hd_region="X"`, assert `RuntimeError`.
   - `test_no_duplicate_link_composite_keys` — assert no two `RegulationReporting` rows share the same PK tuple.
   - `test_statewide_obligation_links_all_35_bear_rows` — partition links by obligation, assert STATEWIDE harvest_report has exactly 35 distinct `jurisdiction_code`s.

4. **`class TestCountGuards:`**
   - `test_reporting_obligation_band_passes_at_3` — should not raise.
   - `test_reporting_obligation_band_raises_below_2` — call with 1 → RuntimeError.
   - `test_reporting_obligation_band_raises_above_4` — call with 5 → RuntimeError.
   - `test_regulation_reporting_band_passes_at_70` — should not raise.
   - `test_regulation_reporting_band_raises_below_49` — call with 48 → RuntimeError.
   - `test_regulation_reporting_band_raises_above_91` — call with 92 → RuntimeError.

5. **`class TestMain:`**
   - `test_dry_run_exits_zero_without_db_connect` — monkeypatch `db.connect` to raise if called; run `main(["--dry-run"])`; assert exit code 0.
   - `test_count_guard_fires_pre_db_connect_in_main` — monkeypatch `_build_reporting_obligations` to return 1 row; assert `RuntimeError` raised BEFORE `db.connect` is called (monkeypatch `db.connect` to record-and-raise).
   - `test_db_failure_rolls_back` — monkeypatch `db.upsert_reporting_obligation` to raise `psycopg.OperationalError` mid-loop; assert `conn.rollback()` is called and `conn.commit()` is NOT.
   - `test_real_artifact_smoke_dry_run` — invoke `main(["--dry-run"])` end-to-end against the real artifact; assert returns 0 and logs the expected counts.

**Conventions:**

- Fixture data: prefer real `black-bear-2026.json` for happy-path tests; synthesize minimal dicts for error-path tests.
- Use `pytest.raises(RuntimeError, match="...")` with a substring of the expected error message.
- Use `caplog` for log-content assertions.

**Verify:**

```bash
cd ingestion && .venv/bin/pytest tests/test_load_reporting_obligations.py -v
.venv/bin/ruff check tests/test_load_reporting_obligations.py
.venv/bin/mypy ingestion/lib/ states/montana/load_reporting_obligations.py tests/test_load_reporting_obligations.py
```

All pass. Test count for this file: ~25-30 tests across 5 classes.

**UI:** no

---

## Blast Radius

**Do NOT modify (outside scope of S03.9):**

- Any file in `ingestion/states/montana/` other than the new `load_reporting_obligations.py` (T2-T5).
- Any extraction module (`extract_*.py`) — S03.4 artifact is the input, not the target.
- Any other state adapter (V1 has only Montana).
- `supabase/migrations/` — no schema changes for S03.9.
- `CLAUDE.md` — updated at Stage 8 wrap-up, NOT during Stage 5.
- `.roughly/known-pitfalls.md` — updated at Stage 8 wrap-up.
- `mcp-server/`, `web/`, `plugin/` — TS serving stack untouched.

**Side artifacts already written in Stage 3a (do NOT re-touch):**

- `docs/planning/epics/E03-confidence-findings/S03.9.md`
- `docs/planning/epics/E03-deferred-items/cwd-sampling-modeling.md`
- `docs/planning/epics/E03-deferred-items/README.md`
- `docs/open-questions.md` (Q18)
- `docs/planning/epics/E03-regulation-text-ingestion.md` (S03.6.1 + merge-order)

**Watch for side effects:**

- `db.py` is read by ALL existing adapters; T1's append-only addition must not change any existing signature.
- `test_db.py` test additions must not break any existing test.
- Real `black-bear-2026.json` content drives the row counts — if S03.4's artifact regenerates with a different `reporting_obligations` count, the guards fire. Operator action: update `_EXPECTED_REPORTING_OBLIGATION_COUNT` and the guard band.

## Conventions

- ADR-001 (authority preserved): no fabricated verbatim_rule text; everything sourced from the bear artifact.
- ADR-008 (verbatim discipline): `verbatim_rule` preserved byte-faithful (no whitespace collapse, no normalization). Cleanup-rules section of the module docstring is empty for S03.9.
- ADR-009 (open-questions): Q18 filed (CWD modeling); epic line 807 diff queued for PM at Stage 8.
- ADR-010 (decomposed entities): `regulation_reporting` link table separates obligation rows from regulation_record FKs.
- ADR-017 (confidence inheritance): no `confidence` column on `ReportingObligation` — child entities inherit from parent regulation_record.
- ADR-018 (E03 schema additions): no overlap with S03.9 (those columns were added for license_season + geometry.legal_description, both touched by S03.7/S03.6).

**Cross-cutting pitfalls (from `.roughly/known-pitfalls.md`):**

- `db.update_legal_description`-style fail-loud on `cur.rowcount == 0` — NOT applicable here since UPSERT/INSERT ON CONFLICT DO NOTHING don't update by PK. But the principle applies: if a future helper uses targeted UPDATE, it must fail loud on no-op.
- Adapter-module tests use direct `from ingestion.states.montana.load_reporting_obligations import ...`, NOT `importlib.util.spec_from_file_location` (per S03.6 pitfall).
- Pydantic `frozen=True, extra="forbid"` rejects unknown fields — confirm by enumerating required fields when constructing.
- mypy invocation MUST include `ingestion/lib/` to catch helper-signature regressions (per S03.7 closure note: agent's "ruff + mypy clean" claim was wrong; running mypy on the adapter alone misses cross-module type errors).

**FK construction discipline (load-bearing for S03.9):**

The bear `jurisdiction_code` MUST be `f"MT-HD-bear-{row['bmu_number']}"` — this is `load_regulation_records.py:366`'s exact pattern. Any drift = silent FK miss (the link will fail to insert and the operator won't see it because ON CONFLICT DO NOTHING is silent). Mitigation: T4 includes a sanity check that all 70 `RegulationReporting.jurisdiction_code` values start with `"MT-HD-bear-"`.

## End-state acceptance checklist (cross-reference epic § S03.9 ACs)

| Epic AC | Plan task(s) | Notes |
|---|---|---|
| `load_reporting_obligations.py` exists; deterministic load order | T2-T5 | source-list order is deterministic; obligation_by_key dispatch produces a stable iteration |
| Statewide rows for CWD/Bear-ID/mandatory reporting (3-4 rows per epic) | **AC scope-down** — see working note | V1 ships 3 rows (Bear ID Test → S03.6.1; CWD → Q18 deferred; mandatory reporting → bear STATEWIDE row covers it) |
| R1 split into 2 + R2-7 1 row | T3 dispatch dict | 3 dispatch entries, locked by `_REPORTING_ROW_SPEC` |
| Each R1 row carries verbatim_rule per source-sentence split | T3 | each obligation row has its own `verbatim_rule` from the artifact |
| `regulation_reporting` link table populated for every pair | T4 | 70 links computed deterministically |
| CWD-sampling obligation links only to CWD-overlapping HDs | **N/A — deferred to M2** | working note + Q18 record the disposition |
| UPSERT idempotency | T1, T6 | UPSERT on entity; ON CONFLICT DO NOTHING on link; tests verify SQL |
| Working note records HD→region mapping for R1 | working note already includes the 14-BMU R1 list | done in Stage 3a |
| `ruff check`, `mypy` clean | T2-T7 verify commands | each task's verify command runs both |
| Unit tests cover applies_to_regions, R1 split, R2-7 row, CWD-overlap regulation_record selection | T7 | CWD-overlap-selection AC is N/A (deferred); other 3 covered |

## Post-implementation gates

- Stage 6 (review): expect 0-2 cubic-review iterations on dispatch-dict completeness, fail-loud RuntimeError messages, ADR-017 inheritance compliance (no confidence column anywhere).
- Stage 7 (verify): full project `.venv/bin/pytest` should land at ~935-940 (was 910 + 1 skipped post-S03.8; +25-30 from T7 + T6).
- Stage 8 (wrap-up): commit message references S03.9 close; 3 new pitfalls in `.roughly/known-pitfalls.md`; CLAUDE.md updated with row-count summary + epic line 807 PM diff surfaced.

## Estimated total work

7 tasks, ~29 minutes implementation time (assumes happy path; review/verify cycles may add 10-30 minutes).
