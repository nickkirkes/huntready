> **Status:** Historical — implemented and merged in commit 38a0666457a6d958c98952d50ce3f46244dbc088 on 2026-05-30. This plan was an active build/fix artifact; treat as historical reference only.

# Implementation Plan: S04.4 — M1 UAT runbook hygiene fixes (six edits + mandatory 7th annotation + adopted 7th `jurisdiction_binding` row)

Plan-format-version: 1

**Story spec:** `docs/planning/epics/E04-m1-carry-forward.md` lines 308–353
**Handoff source-of-truth:** `docs/planning/handoffs/M1-to-M2-handoff.md` §8 "Runbook fixes needed before next run" items #1–#6
**Single target file:** `docs/runbooks/M1-uat.md`
**Story posture:** documentation-only; no code, schema, Pydantic, TypeScript, `architecture.md`, or test edits. Test suite must hold at **1166 passed + 2 skipped** (verified at story close; no Python touched in this story).
**Adoption decision (PM-approved):** edit #4 adds a 7th row `jurisdiction_binding 788 build → 788 DB (no collapse — id-keyed UPSERT)` per epic L330 PM recommendation.
**Audit-trail AC:** §6 Sign-Off table operator marks, initials, dates, sign-off paragraph (L388–L405) must be byte-identical pre/post except the single criterion #7 row Notes cell that T7 modifies.

## File Table

| File | Action | Task(s) |
|------|--------|---------|
| docs/runbooks/M1-uat.md | Edit | T1, T2, T3, T4, T5, T6, T7 |

(One file edited across seven discrete tasks. No new files created. No other files touched.)

## Tasks

### T1: Extend §2 deviation note #3 (handoff §8 fix #1) (~3 min)

**Files:** `docs/runbooks/M1-uat.md`
**Action:** Replace the single-sentence deviation note #3 with the spec-prescribed extended version naming HD 262's absence cause and the HD 124 + HD 170 substitution map.
**Details:** Edit the bullet at line 31. Use `Edit` (not `Write`):
- `old_string` (verbatim, byte-exact): `3. HD 170 (Flathead River) is the asymmetric A/B demonstrator, not HD 262 (HD 262 elk lacks the asymmetric pattern).`
- `new_string`: `3. HD 262 has no elk regulation_record at all (not just no A/B asymmetric pattern; the 2026 DEA booklet publishes no elk section for HD 262, only a deer section that fans out to mule_deer + whitetail). HD 124 substitutes for criterion #1 and criterion #2 part (a); HD 170 substitutes for criterion #2 part (b).`

The replacement string is byte-identical to the handoff §8 #1 prescribed text (no paraphrasing). No surrounding context needed because the source string is unique in the file.

**Verify:** `grep -nc "HD 262 has no elk regulation_record at all" docs/runbooks/M1-uat.md` returns `1`; `grep -c "HD 170 (Flathead River) is the asymmetric A/B demonstrator" docs/runbooks/M1-uat.md` returns `0` (the old phrase is gone); `git diff docs/runbooks/M1-uat.md` shows a single-line replacement at L31 only.
**UI:** no

---

### T2: Update §4 criterion #1 SQL + heading + framing (handoff §8 fix #2) (~5 min)

**Files:** `docs/runbooks/M1-uat.md`
**Depends on:** none (operates on a disjoint region from T1)
**Action:** Change criterion #1's heading and SQL WHERE clause from HD 262 to HD 124 with a footnote pointer; leave the PRD-text quote at L68 intact (PRD prose is source-of-truth — only its *framing* changes).
**Details:** Three sub-edits inside criterion #1 (§4):

**Sub-edit 2a — heading at L66:**
- `old_string`: `### Criterion #1 — regulation_record lookup (HD 262 elk 2026)`
- `new_string`: `### Criterion #1 — regulation_record lookup (HD 124 elk 2026 — HD 262 substitution[^9])`

**Sub-edit 2b — SQL WHERE clause at L79:**
- `old_string`: `  AND jurisdiction_code = 'MT-HD-deer-elk-lion-262'` (note the **two leading spaces** matching the SQL block's indent; the literal at L110 has different surrounding indentation — keep the unique two-space form for L79)
- `new_string`: `  AND jurisdiction_code = 'MT-HD-deer-elk-lion-124'`

**Sub-edit 2c — add new footnote `[^9]` at end of file (under "## Footnotes" L409 section):**
- Use `Edit` against the existing footnote `[^8]` block (L425, the last footnote) to append `[^9]`. Use the `[^8]:` footnote line as the unique anchor for old_string; new_string appends two blank lines + the new `[^9]` footnote.
- `old_string` (last footnote, verbatim from current file): `[^8]: ADR-017 status is `Accepted` (unmodified) per the S03.11 FINALIZE verdict (2026-05-27). The synthesis report at `docs/planning/epics/E03-confidence-calibration-synthesis.md` (~360 lines) is the durable audit record — it lives outside the `E03-confidence-findings/` working-notes deletion target and survives past the m1 tag per ADR-017 §6.`
- `new_string`: same content as old_string PLUS, after it, a blank line followed by: `[^9]: HD 262 elk has no regulation_record (the 2026 DEA booklet publishes no elk section for HD 262 — see §2 deviation note #3). HD 124 substitutes for criterion #1 and criterion #2 part (a); HD 170 substitutes for criterion #2 part (b) (see §2 note #3 and footnote [^3]). Criterion #3 still uses HD 262 because the HD 262 geometry exists and is the canonical test-point fixture in `spatial-test-points.json`.`

**Do NOT change:** L29 (§2 deviation #1 — different topic: format reference); L68 (PRD-text quote — source-of-truth verbatim); L72 (§4 criterion #1 format-deviation note — different topic); L178 (§4 criterion #3 — HD 262 geometry still used); L373/L374/L376 (§5 Results Summary — out of spec scope); L390 (§6 Sign-Off — audit-trail-locked).

**Verify:** `grep -n "HD 124 elk 2026 — HD 262 substitution" docs/runbooks/M1-uat.md` finds line ~66; `grep -c "MT-HD-deer-elk-lion-124" docs/runbooks/M1-uat.md` returns `1` (T3 will add the second occurrence); `grep -c "MT-HD-deer-elk-lion-262" docs/runbooks/M1-uat.md` returns `5` (down from 6 — only the criterion #1 WHERE clause changed); `grep -c '^\[\^9\]:' docs/runbooks/M1-uat.md` returns `1`.
**UI:** no

---

### T3: Update §4 criterion #2 part (a) SQL + framing (handoff §8 fix #3) (~3 min)

**Files:** `docs/runbooks/M1-uat.md`
**Depends on:** T2 (because both edits target HD 262 → HD 124 strings and footnote `[^9]` introduced by T2 is referenced here)
**Action:** Change criterion #2 part (a)'s SQL WHERE clause from HD 262 to HD 124 and reframe the heading + expected-result line; cross-reference footnote `[^9]`.
**Details:** Three sub-edits inside criterion #2 part (a) (§4):

**Sub-edit 3a — Part (a) heading at L100:**
- `old_string`: `**Part (a) — HD 262 elk: basic regulation_record → license_tag join (confirms HD 262 has data):**`
- `new_string`: `**Part (a) — HD 124 elk substitution: basic regulation_record → license_tag join (confirms HD 124 has data; HD 262 substituted per footnote [^9]):**`

**Sub-edit 3b — SQL WHERE clause at L110:**
- `old_string`: `  AND rr.jurisdiction_code = 'MT-HD-deer-elk-lion-262'` (note: this form has the `rr.` table-alias prefix — distinguishes it from L79's unprefixed form, so both edits' old_strings remain unique without `replace_all`)
- `new_string`: `  AND rr.jurisdiction_code = 'MT-HD-deer-elk-lion-124'`

**Sub-edit 3c — Expected-result line at L116:**
- `old_string`: `**Expected result (part a):** ≥1 row showing the elk licenses applicable to HD 262.`
- `new_string`: `**Expected result (part a):** ≥1 row showing the elk licenses applicable to HD 124 (substituting for HD 262 per footnote [^9]).`

**Do NOT change:** L98 (§4 criterion #2 deviation note about HD 262 → HD 170 — different topic, the asymmetric-pattern explanation; footnote `[^3]` is the canonical reference and remains valid).

**Verify:** `grep -c "MT-HD-deer-elk-lion-262" docs/runbooks/M1-uat.md` returns `4` (down from 5 after T2 — the criterion #2(a) WHERE clause changed); `grep -c "MT-HD-deer-elk-lion-124" docs/runbooks/M1-uat.md` returns `2`; `grep -n "HD 124 elk substitution" docs/runbooks/M1-uat.md` shows hits at ~L66 (from T2) and ~L100 (this task).
**UI:** no

---

### T4: Add build-vs-DB collapse footnote to §4 criterion #6 expected-counts table + adopted `jurisdiction_binding` row (handoff §8 fix #4 + PM-approved 7th row) (~7 min)

**Files:** `docs/runbooks/M1-uat.md`
**Depends on:** T2 (sub-edit 4b appends after the `[^9]` footnote that T2 writes at end-of-file; the implementation agent must re-read the file to capture T2's byte-exact `[^9]:` text before appending `[^10]`). Sub-edit 4a (the criterion #6 table at L266–L280) is independent of T1–T3 and could in principle run earlier, but the strict T1→…→T7 ordering keeps footnote append-ordering deterministic.
**Action:** Replace the existing expected-counts table block (L266–L280) with an annotated version: add a leading footnote pointer to the "Expected counts (post-batch-run):" header line; update vague band values to the post-UPSERT-collapse DB numbers from handoff §8 #4; add the adopted `jurisdiction_binding 788 build → 788 DB (no collapse — id-keyed UPSERT)` row formatting consistent with the other rows; add a new footnote `[^10]` explaining the build-vs-DB distinction with concrete deltas.

**Details:** Two sub-edits.

**Sub-edit 4a — replace the expected-counts header + table at L266–L280:**

- `old_string` (verbatim block from current file):
```
**Expected counts (post-batch-run):**

| Table | Expected count | Notes |
|---|---|---|
| regulation_record | 437 | 32 high, 405 medium, 0 low |
| season_definition | ≈978 | |
| license_tag | ≈1225 | |
| draw_spec | ≈278 | composite PK collapses 388 pre-UPSERT rows |
| reporting_obligation | 3 | STATEWIDE + R1 + R2-7 |
| jurisdiction_binding | ∈ [400, 1100] | T16-empirical; band narrows post-UAT |
| license_season | ≈3040 | |
| regulation_season | ≈1385 | |
| regulation_license | ≈1914 | |
| regulation_reporting | 70 | 35 + 14 + 21 |
| geometry | 350 | 349 V1 rows + MT-STATEWIDE-geom |
```

- `new_string`:
```
**Expected counts (post-batch-run)[^10]:**

| Table | Build count | Post-UPSERT DB count | Notes |
|---|---|---|---|
| regulation_record | 437 | 435 | entity; `INSERT … ON CONFLICT DO UPDATE` (32 high, 405 medium, 0 low at build time) |
| season_definition | ≈978 | ≈978 | entity; no observed collapse delta |
| license_tag | 1225 | 825 | entity; PK-keyed UPSERT collapses cross-listed B-Licenses |
| draw_spec | 388 | 278 | entity; composite PK `(state, hunt_code, year)` collapses |
| reporting_obligation | 3 | 3 | STATEWIDE + R1 + R2-7 |
| jurisdiction_binding | 788 | 788 | no collapse — id-keyed UPSERT; matches S03.10 T16 empirical 788; S04.2's `_BINDING_COUNT_GUARD_BAND = (552, 1024)` is centered on this value |
| license_season | 3040 | 2411 | link; `ON CONFLICT DO NOTHING` collapses (license_tag_id, season_definition_id) duplicates |
| regulation_season | 1385 | 1381 | link; `ON CONFLICT DO NOTHING` |
| regulation_license | 1914 | 1279 | link; `ON CONFLICT DO NOTHING` |
| regulation_reporting | 70 | 70 | 35 + 14 + 21; no observed collapse |
| geometry | 350 | 350 | 349 V1 rows + MT-STATEWIDE-geom |
```

(11 rows preserved; column count grows from 3 to 4 to expose the build-vs-DB distinction; `jurisdiction_binding`'s vague band `∈ [400, 1100]` is replaced by the concrete `788 / 788` pair per the PM-approved adopted 7th row.)

**Sub-edit 4b — append new footnote `[^10]` after `[^9]` in the Footnotes section:**

After T2 added `[^9]`, the file ends with `[^9]:` as the last footnote. Use that as the unique anchor.

- `old_string`: the full `[^9]:` footnote text added by T2 (verbatim) — implementation agent re-reads the file before edit to capture the exact bytes T2 wrote.
- `new_string`: same `[^9]:` content PLUS, after it, a blank line followed by:

```
[^10]: Build counts are what the loaders report at `--dry-run` time (pre-database UPSERT); DB counts are what `SELECT COUNT(*)` returns after the live batch-run completes. Entity tables collapse via `INSERT … ON CONFLICT DO UPDATE` when the same PK appears in multiple builder paths (e.g., cross-listed B-Licenses); link tables collapse via `ON CONFLICT DO NOTHING` when the same (left_id, right_id) tuple appears multiple times in the build set. Both numbers are correct in their respective contexts: build counts validate the extraction/builder pipeline; DB counts validate the loaded state. Idempotency (criterion #6) is judged against DB counts pre/post re-run, NOT build counts. `jurisdiction_binding` shows no collapse because the loader UPSERTs by deterministic id and the cross-product produces no duplicate ids (the 788 figure matches S03.10 T16 empirical measurement and is the center of S04.2's narrowed `_BINDING_COUNT_GUARD_BAND = (552, 1024)`). **Provenance note for `draw_spec`:** the S03.8 closure (recorded in `CLAUDE.md`) is authoritative at 278 post-UPSERT DB rows; `docs/planning/handoffs/M1-to-M2-handoff.md` §8 #4 enumerated the delta as `388 → 276 DB` (a transcription error noted parenthetically in the same handoff bullet, which then cites the correct 388→278 collapse). The runbook value tracks the S03.8 closure.
```

**Do NOT change:** L249–L263 (SQL block — column names match the table semantics; SQL has its own UNION ALL row labels). The SQL output paste-slots at L283–L294 remain untouched.

**Verify:** `grep -n '\| Build count \| Post-UPSERT DB count' docs/runbooks/M1-uat.md` finds the new header row in the table; `grep -c '\| 788 \| 788 \|' docs/runbooks/M1-uat.md` returns `1` (the adopted jurisdiction_binding row); `grep -c '^\[\^10\]:' docs/runbooks/M1-uat.md` returns `1`; `grep -c '∈ \[400, 1100\]' docs/runbooks/M1-uat.md` returns `0` (the vague band is gone).
**UI:** no

---

### T5: Add §1 Prerequisites footnote for supabase-CLI substitute (handoff §8 fix #5) (~3 min)

**Files:** `docs/runbooks/M1-uat.md`
**Depends on:** T4 (sub-edit 5b appends after the `[^10]` footnote that T4 writes at end-of-file; the implementation agent must re-read the file to capture T4's byte-exact `[^10]:` text before appending `[^11]`). Sub-edit 5a (the `[^11]` anchor on the L14 Prerequisites bullet) is independent of T1–T4 and could in principle run earlier.
**Action:** Add a footnote `[^11]` anchor at the end of the `psql` Prerequisites bullet at L14, with the footnote text documenting the supabase-CLI substitute that was actually used in the 2026-05-28 UAT run. Spec L332 names this the canonical preference over the `brew install libpq` alternative.

**Details:** Two sub-edits.

**Sub-edit 5a — anchor the new footnote on the `psql` Prerequisites bullet at L14:**

- `old_string`: ``- **`psql` connected via service-role `DATABASE_URL`** (RLS bypass for read; anon/authenticated are denied all tables).``
- `new_string`: ``- **`psql` connected via service-role `DATABASE_URL`** (RLS bypass for read; anon/authenticated are denied all tables).[^11]``

**Sub-edit 5b — append new footnote `[^11]` after `[^10]` in the Footnotes section:**

- `old_string`: the full `[^10]:` footnote text added by T4 (verbatim — implementation agent re-reads the file before edit to capture the exact bytes T4 wrote).
- `new_string`: same `[^10]:` content PLUS, after it, a blank line followed by:

```
[^11]: If `psql` is not installed locally (it is not part of the standard macOS toolchain), the 2026-05-28 UAT run used the Supabase CLI substitute: `supabase db query --db-url "$DATABASE_URL" "<sql>"`. This connects via the same service-role DSN, returns the same shape of result rows, and bypasses RLS the same way `psql` does. Alternative: `brew install libpq && brew link --force libpq` to make `psql` available; pick whichever fits the operator's environment. The supabase-CLI form is the canonical preference because it matches what was actually run on 2026-05-28 (see [`docs/runbooks/M1-uat-results-2026-05-28.md`](M1-uat-results-2026-05-28.md)).
```

**Verify:** `grep -nE '^- \*\*`?psql' docs/runbooks/M1-uat.md` shows the Prerequisites bullet now ends with `[^11]`; `grep -c '^\[\^11\]:' docs/runbooks/M1-uat.md` returns `1`; the supabase-CLI command string appears once.
**UI:** no

---

### T6: Fix §4 criterion #8 bash regex (handoff §8 fix #6) (~2 min)

**Files:** `docs/runbooks/M1-uat.md`
**Depends on:** none
**Action:** Replace the broken regex in the criterion #8 bash command with the spec-prescribed simpler form that actually matches ADR-017's `**Status:** Accepted` heading (verified via direct read of `docs/adrs/ADR-017-confidence-calibration.md:4`).

**Details:** Single edit at L350.

- `old_string`: `grep -A1 -E '^(\*\*Status\*\*|Status):' docs/adrs/ADR-017-confidence-calibration.md | head -3`
- `new_string`: `grep '^\*\*Status:\*\*' docs/adrs/ADR-017-confidence-calibration.md`

The simpler form `grep '^\*\*Status:\*\*'` (without `-A1` and without `head -3`) matches the spec's parenthetical "or simpler" preference at epic L334 and returns exactly the one matching line.

**Verify:** `grep -n "^grep '^\\\\\*\\\\\*Status" docs/runbooks/M1-uat.md` (or simpler: open the file and inspect L350) shows the new command; `grep -c "grep -A1 -E '\\^(\\*\\*Status\\*\\*\\|Status):'" docs/runbooks/M1-uat.md` returns `0` (the broken regex is gone). Sanity-execute the new command in the repo: `grep '^\*\*Status:\*\*' docs/adrs/ADR-017-confidence-calibration.md` should print `**Status:** Accepted`.
**UI:** no

---

### T7: Mandatory criterion #7 annotation in §6 Sign-Off table (audit-trail-preserving) (~3 min)

**Files:** `docs/runbooks/M1-uat.md`
**Depends on:** none (T7 modifies a region disjoint from T1–T6)
**Action:** Append `— RESOLVED M2-W1 via 20260530132727_rls_license_season.sql` to the Notes cell of the criterion #7 row at L396. This is the **only** allowed change to §6 per the audit-trail-preservation AC. Every other cell in §6 (operator initials, PASS/FAIL marks, dates, milestone-sign-off paragraph, signature line, post-signature note) must be byte-identical pre/post.

**Details:** Single edit.

- `old_string`: `| #7 — RLS / pg_roles | FAIL — accepted as M2 week 1 carry-forward per S03.12 pitfall #1 (`license_season` RLS gap; does NOT block m1 tag) | 2026-05-28 |`
- `new_string`: `| #7 — RLS / pg_roles | FAIL — accepted as M2 week 1 carry-forward per S03.12 pitfall #1 (`license_season` RLS gap; does NOT block m1 tag) — RESOLVED M2-W1 via `20260530132727_rls_license_season.sql` | 2026-05-28 |`

(The S04.1 migration timestamp `20260530132727` is verified from `supabase/migrations/20260530132727_rls_license_season.sql` existing on the current branch — discovery confirmed it is present. The annotation text format matches the spec's literal `RESOLVED M2-W1 via <timestamp>_rls_license_season.sql` template at epic L336/L348.)

**Verify:**
1. `grep -c "RESOLVED M2-W1 via " docs/runbooks/M1-uat.md` returns `1`.
2. `git diff docs/runbooks/M1-uat.md | grep -E '^[+-]' | grep -v '^[+-][+-][+-]' | grep -A0 -B0 '#6 Sign-Off\\|Sign-off:\\|Signed:\\|Milestone sign-off\\|Note:\\|After UAT'` returns no `[+-]` lines for these locked sections.
3. Detailed audit-trail check: `awk '/^## 6\\. Sign-Off/,/^---$/' docs/runbooks/M1-uat.md | diff <(git show HEAD:docs/runbooks/M1-uat.md | awk '/^## 6\\. Sign-Off/,/^---$/') -` shows exactly one changed line (the criterion #7 row).
**UI:** no

---

## Blast Radius

- **Do NOT modify:** any file other than `docs/runbooks/M1-uat.md`. No code, no schema, no Pydantic, no TypeScript, no `architecture.md`, no tests, no migrations, no ADRs.
- **Do NOT modify within `docs/runbooks/M1-uat.md`:** L29 (§2 deviation #1), L68 (PRD-text quote), L72 (§4 criterion #1 jurisdiction_code deviation note), L98 (§4 criterion #2 HD 262→170 deviation note + footnote `[^3]`), L178 (§4 criterion #3 prose — HD 262 geometry still used), L373/L374/L376 (§5 Results Summary table — out of spec scope; row labels stay stale by design), L413 (existing footnote `[^2]`), and the entirety of §6 Sign-Off (L386–L405) **except** the single criterion #7 row Notes cell that T7 touches.
- **Watch for:** the 6 occurrences of `MT-HD-deer-elk-lion-262` — only the 2 at L79 and L110 may change; the other 4 are by-design references and must remain. Each `Edit` call must use the unique surrounding indent / `rr.` prefix to disambiguate. No `replace_all`.
- **Side-effect-free:** no SQL is executed; no Python is imported; no Supabase API is called; no migrations run. The test suite must remain at 1166 passed + 2 skipped after this story closes (no Python touched).
- **Footnote ordering:** new footnotes `[^9]`, `[^10]`, `[^11]` must appear in the Footnotes section in order after `[^8]`. T2 writes `[^9]`, T4 writes `[^10]` (appending after `[^9]`), T5 writes `[^11]` (appending after `[^10]`). The strict task order T1→T2→T3→T4→T5→T6→T7 keeps footnote append-ordering deterministic.

## Conventions

- **ADR-001** (Authority preserved, not replaced) — handoff §8 text is the authority; new prose must be byte-identical to the handoff text where the spec quotes it verbatim. No paraphrasing of the handoff §8 #1 prescribed extension.
- **ADR-009** (Documentation as handoff mechanism) — this entire story is an ADR-009 instance: M1 UAT discovered runbook drift, captured it in handoff §8 as flag-and-carry-forward, and S04.4 lands the carry-forward edits.
- **Audit-trail-preservation discipline** (spec L338) — §6 Sign-Off table operator marks, initials, dates, and the milestone-sign-off paragraph must be byte-identical pre/post except for the single criterion #7 row Notes cell that T7 touches. Verified via `git diff` AC #5.
- **Spec citation discipline** (per S04.2's third PM-noted pitfall about spec line-number drift) — when this plan cites epic line numbers (308–353), those are line numbers as of 2026-05-30. The plan-review subagent should re-grep the section heading `### S04.4:` if line numbers have drifted.
- **Editor mechanics** (Roughly skill convention) — use `Edit` (not `Write`) for all changes; never use `replace_all` (every `old_string` is intentionally unique by surrounding context); always `Read` the file first when the `old_string` was written by a prior task in this same pipeline (T2→T4→T5 chain on footnotes).
- **Commit-message convention** (epic L349) — the final commit message must cite each landed handoff §8 item number, e.g., `runbook: M1-uat §2 #3 extension (handoff §8 fix #1)`, `runbook: M1-uat §4 criterion #1 HD 124 substitution (handoff §8 fix #2)`, etc. Single squash commit at story close.

## Final-State Acceptance Checklist (mirrors epic L344–L353 ACs)

- [ ] S04.1 has merged to `main` before this PR opens (verified: `20260530132727_rls_license_season.sql` is on `main` per CLAUDE.md project status and the file exists on the current feature branch).
- [ ] All six handoff §8 #1–#6 edits applied to `docs/runbooks/M1-uat.md` (T1–T6).
- [ ] Mandatory 7th annotation: criterion #7 row extended with `RESOLVED M2-W1 via 20260530132727_rls_license_season.sql` (T7).
- [ ] Each edit cites its handoff §8 item number in the commit message (final commit at story close).
- [ ] Audit-trail integrity preserved: §6 Sign-Off table operator marks, initials, dates, and milestone-sign-off paragraph byte-identical pre/post except the criterion #7 row Notes cell.
- [ ] No SQL executed during the story.
- [ ] No code, schema, Pydantic, TypeScript, `architecture.md`, or test edits.
- [ ] `cd ingestion && .venv/bin/pytest tests/` reports **1166 passed + 2 skipped** (test baseline preservation; runnable at Stage 7 verify-all).
