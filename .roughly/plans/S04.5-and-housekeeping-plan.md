# Implementation Plan: S04.5 PRD 001 reconciliation + 2 doc-only housekeeping bundles

> **Status:** Historical — implemented and merged in commit 37bc86a on 2026-05-31. This plan was an active build/fix artifact; treat as historical reference only.

Plan-format-version: 1

## Context

User-driven build with three independent doc-only commits on `feat/S04.5-PRD-001-sequencing-language-reconciliation`. Every substitution is content-anchored and explicitly drafted in the user's prompt. All anchors grep-verified at Stage 2: PRD anchors hit count 1 (×3), handoff path-drift hit count 7 (2 file + 5 directory), handoff §3 header hit count 1, handoff §8 #4 transcription anchor hit count 1, handoff §8 #4 entity-collapse anchor hit count 1, handoff §8 #7 anchor hit count 1, pitfalls section header at line 739 with the section trailing the file (EOF append).

## File Table

| File | Action | Task(s) |
|------|--------|---------|
| docs/planning/prds/001-M1-montana-ingestion.md | Edit (3 substrings) | T1 |
| .roughly/known-pitfalls.md | Edit (append 2 entries) | T2 |
| docs/planning/handoffs/M1-to-M2-handoff.md | Edit (5 substitutions) | T3 |

## Tasks

### T1: S04.5 — apply 3 PRD 001 substring replacements (~2 min)
**Files:** docs/planning/prds/001-M1-montana-ingestion.md
**Action:** Apply the three line-substring replacements from the E04 §S04.5 drafted diff (lines 90, 96, 111). Line 48 intentionally unchanged.
**Details:**
- Line 90: replace E02 Outcome `…loaded into the geometry table with correct jurisdiction_binding rows expressing their overlay relationships. Spatial queries work.` with `…loaded into the geometry table, and a geometry-overlays.json fixture captures their overlay relationships for E03 to consume. Spatial queries work.`
- Line 96: replace `jurisdiction_binding rows correctly express HD → BMU overlay, HD → CWD-zone overlay, HD → Portion overlay relationships` with `a geometry-overlays.json fixture captures HD → Portion, HD → CWD-zone, and HD → restricted-area overlay relationships for E03's S03.6.1 + S03.10 to consume when writing jurisdiction_binding rows`
- Line 111: replace `E03 writes regulation_record, license_tag, reporting_obligation, and other rows that foreign-key to jurisdiction_binding. Those bindings must exist before E03 can write.` with `E03 writes regulation_record, license_tag, reporting_obligation, jurisdiction_binding, and other rows. The jurisdiction_binding rows FK to geometry(id) (a hard FK to a table E02 populates) and to regulation_record (the binding's anchor, written earlier within E03 itself). Bindings cannot be written until both FK targets exist — geometry from E02 and regulation_record from E03's earlier stories.`
**Verify:** `git diff --stat docs/planning/prds/001-M1-montana-ingestion.md` shows 3 line-diff hunks; `grep -c "with correct .jurisdiction_binding. rows expressing"` returns 0 post-edit; `grep -c "fixture captures their overlay"` returns 1 post-edit.
**UI:** no

### T2: Bundle A — append 2 new pitfall entries at EOF (~2 min)
**Files:** .roughly/known-pitfalls.md
**Action:** Append two new `### ` entries at the file end (after line 811, inside trailing § "Conventions — Documentation & planning discipline" section, no subsequent section to displace).
**Details:**
- Entry 1: "Spec-prescribed string substitutions silently invalidate coupled references elsewhere in the document" (S04.4 T2/T3 HD 262→124 trap)
- Entry 2: "Authoritative numbers drift between canonical documents — name the source-of-truth before copying" (S04.1 ~3040-vs-2411 + S04.4 276-vs-278)
**Verify:** post-edit line count is 811 + 2 entries' line count; `grep -c "^### Spec-prescribed string substitutions"` returns 1; `grep -c "^### Authoritative numbers drift"` returns 1.
**UI:** no

### T3: Bundle B — apply 5 handoff substitutions (~4 min)
**Files:** docs/planning/handoffs/M1-to-M2-handoff.md
**Action:** Apply 5 content-anchored substitutions per the user's prompt.
**Details:**
- Edit 1: path-drift `replace_all` for `docs/planning/epics/E03-confidence-calibration-synthesis.md` → `docs/planning/epics/completed/E03-confidence-calibration-synthesis.md` (2 occurrences) AND `docs/planning/epics/E03-deferred-items/` → `docs/planning/epics/completed/E03-deferred-items/` (5 occurrences). Use `replace_all: true` on Edit tool for each.
- Edit 2: §3 header (line 43) — insert "Convention note" blockquote between header and table.
- Edit 3: §8 #7 (line 188) — append RESOLVED 2026-05-30 clause to logging.basicConfig bullet.
- Edit 4: §8 #4 (line 182) — replace `draw_spec 388 build → 276 DB (S03.8 closure already noted the 388→278 collapse)` with the corrected 278 phrasing.
- Edit 5: §8 #4 (line 182, same bullet) — extend entity-collapse sentence with jurisdiction_binding 788→788 row.

Apply in this order: Edit 1 first (different anchor), then Edits 4 + 5 sequentially on line 182, then Edits 2 + 3 (independent of each other).
**Verify:** `grep -c "docs/planning/epics/E03-confidence-calibration-synthesis"` returns 0 unprefixed-post-edit (only `completed/`); `grep -c "docs/planning/epics/E03-deferred-items"` same; `grep -c "draw_spec 388 build → 278 DB"` returns 1; `grep -c "RESOLVED 2026-05-30 via S04.3"` returns 1; `grep -c "Convention note"` returns 1; `grep -c "jurisdiction_binding 788 build → 788 DB"` returns 1.
**UI:** no

## Blast Radius
- Do NOT modify: anything outside the 3 named doc files
- No code changes, no test changes, no schema changes
- Test suite is expected to remain at 1166 passed + 2 skipped baseline (doc-only)

## Conventions
- Per S04.5 AC: PRD edits authored by the user's git identity (`nick@rowdycloud.io`) — user has explicitly delegated apply
- Three separate commits in the order: PRD (commit 1) → pitfalls (commit 2) → handoff (commit 3)
- Standing rule: do NOT push (user-handled)
