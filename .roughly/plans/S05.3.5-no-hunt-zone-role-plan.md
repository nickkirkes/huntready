> **Status:** Historical — implemented and merged in commit d5c9aa7f78897e429d0a4240000880e225277e3c on 2026-06-03. This plan was an active build/fix artifact; treat as historical reference only.

# Implementation Plan: S05.3.5 — `jurisdiction_binding.role` no_hunt_zone migration + MT V1 reclassification

Plan-format-version: 1

**Story:** S05.3.5 (docs/planning/epics/E05-colorado-geometry-ingestion.md L304-398)
**ADR:** ADR-021 (implements; flips Proposed → Accepted)
**Scope:** Add `'no_hunt_zone'` as 8th value to `jurisdiction_binding.role` CHECK constraint; five-place sync; MT V1 reclassification of 3 rows (via migration UPDATE); MT loader + test updates; ADR status flip + README index row.

All line numbers below are **grep-verified against the real file state 2026-06-03** (per Stage-2 discovery). Where the spec's cited line numbers drifted, the REAL numbers are used here. Implementation subagents must still content-anchor (not trust line numbers blindly) per Bundle A pitfall #1.

## File Table
| File | Action | Task(s) |
|------|--------|---------|
| supabase/migrations/20260603000000_jurisdiction_binding_no_hunt_zone_role.sql | Create | T1 |
| ingestion/ingestion/lib/schema.py | Edit | T2 |
| ingestion/ingestion/lib/overlays.py | Edit | T3 |
| mcp-server/src/types/schema.ts | Edit | T4 |
| docs/architecture.md | Edit | T4 |
| ingestion/states/montana/load_jurisdiction_bindings.py | Edit | T5 |
| ingestion/tests/test_load_jurisdiction_bindings.py | Edit | T6 |
| docs/adrs/ADR-021-jurisdiction-binding-no-hunt-zone-role.md | Edit | T7 |
| docs/adrs/README.md | Edit | T7 |

## Tasks

### T1: DDL migration — extend CHECK constraint + reclassify 3 MT rows (~4 min)
**Files:** supabase/migrations/20260603000000_jurisdiction_binding_no_hunt_zone_role.sql (new)
**Action:** Create a new transactional migration that extends the `jurisdiction_binding.role` CHECK constraint to 8 values and reclassifies the 3 MT no-hunt-zone binding rows.
**Details:**
- Timestamp prefix `20260603000000` is strictly greater than the latest existing migration `20260530132727` — confirmed. Keep this exact filename.
- Mirror the structure/header style of `supabase/migrations/20260504032424_e03_schema_additions.sql` (read it first for the comment-block convention). Header block: title `HuntReady: jurisdiction_binding role no_hunt_zone`, story line `S05.3.5 — ADR-021 implementation`, a short description of the two operations, and `Relevant ADRs: 006 (sync), 010 (enum-not-flag), 021 (this migration).`
- Body, wrapped in `BEGIN; ... COMMIT;`, **in this exact order**:
  1. Comment noting the auto-generated constraint name `jurisdiction_binding_role_check` (Postgres `<table>_<column>_check` convention for the inline column-level CHECK at `20260425000000_initial_schema.sql:201-206`).
  2. `ALTER TABLE jurisdiction_binding DROP CONSTRAINT jurisdiction_binding_role_check;`
  3. `ALTER TABLE jurisdiction_binding ADD CONSTRAINT jurisdiction_binding_role_check CHECK (role IN ('primary_unit', 'portion', 'restricted_area', 'cwd_management_zone', 'bear_management_unit', 'block_management_area', 'other_overlay', 'no_hunt_zone'));` — keep the existing 7 values BYTE-IDENTICAL (same single-quote style, same grouping as the initial_schema constraint) and append `'no_hunt_zone'` as the 8th.
  4. `UPDATE jurisdiction_binding SET role = 'no_hunt_zone' WHERE geometry_id IN ('MT-restricted-bigame-glacier-national-park-geom', 'MT-restricted-bigame-sun-river-game-preserve-geom', 'MT-restricted-bigame-yellowstone-national-park-geom');` — these 3 IDs are grep-verified from `build_overlay_fixture.py:236-242` (EXPECTED_RA_ORPHAN_IDS). Add a comment above the UPDATE noting it keys on `geometry_id` (catches all binding rows per zone regardless of HD fan-out) and that the binding `id` column still encodes `other_overlay` as a substring post-update — this is an accepted V1 artifact (id is immutable; do NOT update the id column).
- The DROP-then-ADD-then-UPDATE order is mandatory: the UPDATE must come after ADD CHECK because `'no_hunt_zone'` violates the old 7-value constraint.
**Verify:** File is created and is valid SQL by inspection (no DB connection in this environment — Group B operator applies it). Confirm the 7 pre-existing values are byte-identical to `20260425000000_initial_schema.sql:201-206`.
**UI:** no

### T2: Pydantic GeometryRole Literal (~2 min)
**Files:** ingestion/ingestion/lib/schema.py
**Action:** Add `"no_hunt_zone"` to the `JurisdictionBinding.role` Literal.
**Details:** Content-anchor on the `role: Literal[` block inside the `JurisdictionBinding` model (real location ~L426-434). It currently ends with `"other_overlay",` as the last value. Insert `"no_hunt_zone",` immediately after `"other_overlay",` (before the closing `]`). Match existing indentation/trailing-comma style exactly.
**Verify:** `cd ingestion && .venv/bin/ruff check ingestion/lib/schema.py && .venv/bin/mypy ingestion/lib/`
**UI:** no

### T3: overlays.py GeometryRoleForE03 Literal + docstring sync (~4 min)
**Files:** ingestion/ingestion/lib/overlays.py
**Action:** Add `"no_hunt_zone"` to the `GeometryRoleForE03` Literal alias AND update the two sync-reminder docstrings.
**Details:**
- Content-anchor on `GeometryRoleForE03 = Literal[` (real location ~L28-36). It ends with `"other_overlay",`. Insert `"no_hunt_zone",` immediately after, before the closing `]`. Match indentation/comma style.
- Update the MODULE docstring (~L12-17): change the count word "seven" → "eight" in the phrase "duplicates the seven Literal values"; update the stale line citation `ingestion/ingestion/lib/schema.py:424-432` → `ingestion/ingestion/lib/schema.py:426-435` (the schema.py Literal block grows by one line after T2, so the range is now :426-435 — verify against the actual post-T2 file; if T2 isn't applied yet, cite the range covering the 8 values). NOTE: this task may run before T2 — cite `schema.py:426-435` as the post-edit target range regardless.
- Update the INLINE docstring on the Literal (~L37-45): change "Seven roles" → "Eight roles"; update the stale citation `ingestion/ingestion/lib/schema.py:424-432` → `ingestion/ingestion/lib/schema.py:426-435`. Verify the SYNC REQUIRED docstring still points at `schema.py` (it does — just refresh the line range).
- Grep the file for any remaining `424-432` or the word "seven"/"Seven" referring to the role count and fix all occurrences.
**Verify:** `cd ingestion && .venv/bin/ruff check ingestion/lib/overlays.py && .venv/bin/mypy ingestion/lib/`
**UI:** no

### T4: TypeScript union + architecture.md type (~3 min)
**Files:** mcp-server/src/types/schema.ts, docs/architecture.md
**Action:** Add `"no_hunt_zone"` to the `GeometryRole` union in both the TS source and the architecture.md schema-types doc.
**Details:**
- `mcp-server/src/types/schema.ts` (~L30-37): content-anchor on `export type GeometryRole =`. The last member is `| "other_overlay";`. Change it to `| "other_overlay"` and add a new line `  | "no_hunt_zone";` (move the semicolon to the new last member). Match the `  | "value"` indentation style.
- `docs/architecture.md` §"Schema types" (~L281-288): the plain-text `type GeometryRole =` block. Same edit — append `| "no_hunt_zone";` as the new last member, moving the semicolon. Do NOT touch the response-shape example at ~L401 (`overlays: { role: GeometryRole; ... }`) — it does not enumerate values.
**Verify:** `cd mcp-server && npx tsc --noEmit` (confirm clean; the union is consumed at schema.ts:258 and elsewhere — adding a value is non-breaking).
**UI:** no

### T5: MT loader — docstring + hardcoded default + cast Literal (~5 min)
**Files:** ingestion/states/montana/load_jurisdiction_bindings.py
**Depends on:** T3
**Action:** Update three sites in the MT binding loader so the no-hunt-zone path emits `role='no_hunt_zone'` and mypy stays clean against the extended `GeometryRoleForE03`.
**Details:** Grep `other_overlay` across the whole file first and reconcile against these three known sites (content-anchor each; real line numbers given as a guide only):
1. **Docstring in `_build_no_hunt_zone_bindings` (~L596-609):** revise the prose that currently claims `role='other_overlay'` is "the only DDL-permitted role for this semantic (the 7-value role enum does not include 'no_hunt_zone' ...)". Rewrite to state that these bindings now use `role='no_hunt_zone'` per ADR-021 (the 8-value enum includes it as of migration `20260603000000`). Remove the now-false "only DDL-permitted role" / "7-value enum does not include 'no_hunt_zone'" claims.
2. **Hardcoded default (~L637):** `role: Literal["other_overlay"] = "other_overlay"` → `role: Literal["no_hunt_zone"] = "no_hunt_zone"`.
3. **Cast Literal in `_build_overlay_bindings` (~L517-521)** — SPEC GAP surfaced in discovery: there is a `cast(Literal["primary_unit", "portion", "restricted_area", "cwd_management_zone", "bear_management_unit", "block_management_area", "other_overlay"], role_e03)`. Add `"no_hunt_zone"` to this cast's Literal so it matches the now-8-value `GeometryRoleForE03` that `role_e03` is typed as. Match the existing value grouping/wrapping.
- Do NOT change binding-id derivation or the `_JURISDICTION_BINDING_ID_FORMAT`. The id strings for the 3 reclassified rows keep `other_overlay` as a substring (accepted V1 artifact).
**Verify:** `cd ingestion && .venv/bin/ruff check ingestion/states/montana/load_jurisdiction_bindings.py && .venv/bin/mypy ingestion/states/montana/load_jurisdiction_bindings.py`
**UI:** no

### T6: MT tests — 4 assertion sites + grep sweep (~5 min)
**Files:** ingestion/tests/test_load_jurisdiction_bindings.py
**Depends on:** T5
**Action:** Update the test assertions that lock the old 7-value enum and the `other_overlay`-only no-hunt-zone behavior so they reflect the new `no_hunt_zone` role.
**Details:** Grep `other_overlay`, `_valid_roles`, and `test_role_is_other_overlay_only` across the whole file first. Known sites (content-anchor; real lines as guide):
1. **Set-equality (~L1025):** `assert {b.role for b in bindings} == {"other_overlay"}` → `== {"no_hunt_zone"}`.
2. **`test_role_is_other_overlay_only` (~L1053-1064):** rename the function to `test_role_is_no_hunt_zone`; update the body assertion `assert b.role == "other_overlay"` → `assert b.role == "no_hunt_zone"`. Update any docstring/comment in the test that references the old role.
3. **`_valid_roles` frozenset (~L1896-1899):** extend the 7-value frozenset with `"no_hunt_zone"` as the 8th value.
4. **Any other `other_overlay` occurrence** surfaced by the grep that asserts on `.role` of no-hunt-zone bindings (discovery flagged the real-artifact regression test ~L1875 calling `_build_no_hunt_zone_bindings` — check whether it asserts `.role` and update if so). Patches like `patch.object(_ljb, "_build_no_hunt_zone_bindings", ...)` that replace the function entirely need NO change.
**Verify:** `cd ingestion && .venv/bin/ruff check ingestion/tests/test_load_jurisdiction_bindings.py && .venv/bin/pytest tests/test_load_jurisdiction_bindings.py -q` (must pass; was 105 passed pre-change, expect 105 still — rename, not addition).
**UI:** no

### T7: ADR-021 status flip + README index row (~3 min)
**Files:** docs/adrs/ADR-021-jurisdiction-binding-no-hunt-zone-role.md, docs/adrs/README.md
**Action:** Flip ADR-021 to Accepted and add it to the ADR index table.
**Details:**
- ADR-021 front matter (L4): `**Status:** Proposed` → `**Status:** Accepted`.
- ADR-021 status-note blockquote (L8): revise from the "flips to Accepted when the migration story closes" future-tense note to past-tense reflecting the flip has fired — e.g. note that the five-place sync + MT data reclassification shipped via migration `20260603000000_jurisdiction_binding_no_hunt_zone_role.sql` in the S05.3.5 PR. Keep the `docs/adrs/README.md §"Status"` reference.
- `docs/adrs/README.md`: the index table currently ends at ADR-020 (discovery: ~L82, ADR-021 missing). Add a new table row for ADR-021 matching the existing row format (number, title `Jurisdiction-Binding no_hunt_zone Role Enum`, Status `Accepted`, and whatever other columns the table uses — read the table header + the ADR-020 row to match the column set exactly).
**Verify:** Both files render as valid markdown by inspection; ADR-021 status reads Accepted; README table has an ADR-021 row consistent with sibling rows.
**UI:** no

## Blast Radius
- **Do NOT modify:** `ingestion/states/colorado/` (zero CO code per AC #12 — `git diff --stat ingestion/states/colorado/` must be empty); `ingestion/ingestion/lib/db.py` (no new helpers); `ingestion/states/montana/build_overlay_fixture.py` (EXPECTED_RA_ORPHAN_IDS is read-only reference); the binding `id` column / `_JURISDICTION_BINDING_ID_FORMAT` (immutable; slug-drift on the 3 rows is accepted).
- **Watch for:** (a) mypy mismatch if T3 extends `GeometryRoleForE03` but T5's cast Literal at ~L517-521 is missed — discovery's spec-gap site; (b) the response-shape example in architecture.md L401 must NOT be edited; (c) byte-identical preservation of the 7 existing CHECK values in the migration; (d) line-citation drift — content-anchor, don't trust the spec's stale `:599-608` / `:424-432`.
- **No production-DB writes** this session (Group B is operator-driven). The migration file is created but NOT applied.

## Conventions
- Migration pattern: mirror `20260504032424_e03_schema_additions.sql` (BEGIN/COMMIT, comment blocks, DROP+ADD CHECK for inline column constraints).
- Five-place sync discipline: ADR-006 + ADR-021 Negatives §. All five surfaces must carry `no_hunt_zone` or the enum is inconsistent.
- ADR-010 enum-not-flag: `no_hunt_zone` is a role value, not a boolean.
- Bundle A pitfall #1 (line-citation drift): every spec-prescribed line number is content-anchored, never trusted blindly.
- Test baseline: 1234 + 2 skipped pre-change; expected delta N=0 (all 4 test edits are rewrites/renames, not additions).
- Source-of-truth for the 7 existing values: `supabase/migrations/20260425000000_initial_schema.sql:201-206`.
