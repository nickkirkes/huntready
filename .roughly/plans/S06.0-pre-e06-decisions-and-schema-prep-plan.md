> **Status:** Historical — implemented and merged in commit 5c8b2bece0e6bb21e99e8592b3782489e036df96 on 2026-06-08. This plan was an active build/fix artifact; treat as historical reference only.

# Implementation Plan: S06.0 — Pre-E06 decisions + schema-prep gate

Plan-format-version: 1

## Summary

S06.0 is a **decision-gate + documentation story** (`UAT: no`). The 5 pre-registered decisions + the conditional 6th were collected from the human at S06.0 open (this session). Every decision resolved to a path that ships **no code**: no schema gap fires (so no migration), and the conditional `db.update_geometry_verbatim` helper was deferred to S06.5 (where a test exercises it). Therefore S06.0 ships **exactly two file changes**: the decision memo `S06.0.md` (new) and the epic file's S06.0 AC/status ticks.

### Decisions captured this session (verbatim outcomes)

- **D1 (Q18 CWD disposition):** **(c) license-keyed** [PM rec]. 0 typed CWD `reporting_obligation` rows; CWD text lives in `regulation_record.additional_rules`. No new table. PRD 002 §"Open decisions resolved during M2" L174 trigger does not fire.
- **D2 (Known Issue #6 — `_VALID_ROLE_FOR_E03`):** **Hardcoded path** [PM rec]. CO mirrors MT's `_build_no_hunt_zone_bindings` (`ingestion/states/montana/load_jurisdiction_bindings.py:592`); the gate frozenset (`:417`) stays narrow at 4 values. Zero MT changes.
- **D3 (`_STATE` vs `CO_STATE_CODE`):** **`_STATE` wins** [PM rec]. Migration of the 4 CO loaders (`load_state_boundary.py:72`, `load_gmus.py:74`, `load_restricted_areas.py:78`, `build_overlay_fixture.py:73`) `CO_STATE_CODE → _STATE` ships **alongside S06.10**, not in S06.0. `CO_STATE_SLUG` companions untouched.
- **D4 (CPW Big Game brochure URL):** **Defer as operator-pending.** Do NOT pin a URL in S06.0. AC #132/#133 stay unticked with an operator-pending annotation. Breadcrumbs recorded for the operator: landing page `https://cpw.widen.net/s/5wvx7rggrd/colorado-big-game-hunting-brochure` (Widen DAM — operator grabs direct-download URL from the download button) + corrections PDF `https://spl.cde.state.co.us/artemis/nrserials/nr1431internet/nr14312026corrinternet.pdf` (for the later ADR-019 doc-type-precedence merge). **Only operator-pending item gating S06.1.**
- **D5 (multi-source provenance for the 10 no-hunt zones):** **(b) accept split-provenance.** PAD-US wins the `geometry.source` field (`document_type='gis_layer'`); CPW text reaches the rows via `verbatim_rule` only. No migration, no `source` mutation. Grep confirmed zero runtime consumers of `geometry.source.document_type` (only the type def at `mcp-server/src/types/schema.ts:50`), so option (a)'s consequence list is moot — but recorded.
- **D6 (conditional — `db.update_geometry_verbatim`):** **Helper built in S06.5, not S06.0.** S06.5 populates `verbatim_rule` via a dedicated targeted UPDATE helper that fails loud on `cur.rowcount == 0` (mirroring `update_legal_description` at `db.py:362` / `update_license_tag_draw_spec_key`), NOT by reusing `upsert_geometry`. No cross-story encoding contract to lock (unlike S03.6.1's `upsert_jurisdiction_binding`), so no early introduction. No `db.py` code in S06.0.

### Schema-gap read-through result: NO GAP FIRES

All 7 candidate gaps evaluated against live schema (Pydantic + TS), with file:line evidence:

1. `PointSystem.kind` — `preference_linear` already present (`schema.py:154`, `schema.ts:100`). CO V1 uses only this. No gap.
2. `AllocationPool.selection` — `rank_ordered_by_points` + `unweighted_random` already present (`schema.py:194–199`, `schema.ts:116–120`). No gap.
3. `ReportingObligation.kind` — 6 values incl. `other` already present (`schema.py:341–346`, `schema.ts:207–212`). No gap.
4. `SourceCitation.document_type` — 5 values already present (`schema.py:74–75`, `schema.ts:51–55`). No gap.
5. `ClosurePredicate.effective_after` — absent (`schema.py:97–112` has no such field), but this is a **data-triggered** ADR-candidate (`closure-temporal-anchors.md`); fires only if CO extraction shows a quota+calendar-gate closure. S06.0 is pre-CO-data → structurally cannot fire at read-through. Deferred to extraction time.
6. New `public.*` table for Q18 — only fires on option (a)/(b); D1=(c) chosen → no table.
7. `draw_spec` structured fields (`successor_hunt_code_key` composite `DrawSpecKey` `schema.py:317`; `application_deadline` `:318`; `purchase_only_code: str|None` `:157`; `inactive_forfeit_years: int|None` `:158`) — all present in Python + TS. No gap.

Conclusion: S06.0 ships **no migration**. Test baseline holds at **1346 + 2 skipped** (no Python edits).

## File Table

| File | Action | Task(s) |
|------|--------|---------|
| docs/planning/epics/E06-confidence-findings/S06.0.md | Create | T1 |
| docs/planning/epics/E06-colorado-regulation-text-ingestion.md | Modify | T2 |

## Tasks

### T1: Write the S06.0 decision memo (~5 min)
**Files:** docs/planning/epics/E06-confidence-findings/S06.0.md (create; `mkdir -p` the directory first)
**Action:** Create the decision memo capturing all 6 decisions verbatim, the schema-gap read-through, and the AC-status disposition.
**Details:** The directory `docs/planning/epics/E06-confidence-findings/` does not exist yet — create it. The memo follows the E05-confidence-findings house format (title `# S06.0 — ...`, `## Status`, then content sections). Required sections:
- **`## Status`** — Decisions captured 2026-06-08; memo is Group-A-at-merge; D4 (CPW URL) operator-pending. Note S06.0 ships no migration and no db helper.
- **`## Pre-registered decisions (5 + conditional 6th)`** — one subsection per decision (D1–D6) with: the question, the human's chosen option (verbatim outcome from the list in this plan's Summary), PM rationale, and **downstream spec implications** (D1→S06.9; D2→S06.10; D3→S06.10 rename; D4→S06.1 unblock; D5→S06.5; D6→S06.5). Use the exact decision text from the Summary above. For D4, include both candidate URLs + the operator procedure (grab direct-download URL from Widen download button, HEAD-verify Content-Type `application/pdf` + Last-Modified + Content-Length, open PDF + confirm cover reads "2026", pin URL + SHA-256, capture in follow-up doc-only commit). For D5, note option (a)'s consumer-grep finding (zero runtime consumers; only `mcp-server/src/types/schema.ts:50` type def) even though (b) was chosen. For D6, state the helper builds in S06.5 with fail-loud `cur.rowcount==0`, touching only `verbatim_rule` (leaves `source` untouched, consistent with D5=(b)).
- **`## Schema-gap read-through`** — enumerate all 7 candidate gaps with the live-schema file:line evidence and the per-candidate verdict (all "no gap" except #5 data-trigger-deferred and #6 N/A under D1=(c)). State the sources read: PRD 002 (`docs/planning/prds/002-M2-colorado-ingestion.md` §"Decisions already made" L150–165 + §"Open decisions resolved during M2" L167–182), `docs/architecture.md` § Schema types, `docs/research/colorado-draw-schema-proposal.md` (§1 L10, §6 L78, §8 L276). Conclude: no migration; baseline 1346 + 2 skipped unchanged.
- **`## Cross-references to PRD 002 open decisions`** — note D1 leaves Q18 (PRD 002 L174) a deferred ADR-candidate with a recorded V1 disposition; D5 leaves "Multi-source geometry provenance" (PRD 002 L176) a deferred ADR-candidate (split-provenance accepted for V1, no new field). The formal `docs/open-questions.md` Q18 flip happens at S06.9 spec-draft time (S05.3 precedent: the formal decision is E06's, recorded when the dependent story drafts) — S06.0 does NOT edit `open-questions.md`.
- **`## Acceptance-criteria disposition`** — a table mapping each S06.0 AC (#131–142, epic L131–142) to status: satisfied-at-merge vs operator-pending. AC #132/#133 = operator-pending (D4). All others satisfied by this memo.
- **`## References`** — link the epic, PRD 002, the draw-schema proposal, the relevant ADRs (001, 006, 017, 018, 019, 020, 021), and the E05 Known Issues #6 source.
**Verify:** `test -f docs/planning/epics/E06-confidence-findings/S06.0.md && echo OK`; grep the memo for each decision id D1–D6 to confirm all six are present.
**UI:** no

### T2: Tick the epic's S06.0 ACs + set status (~3 min)
**Files:** docs/planning/epics/E06-colorado-regulation-text-ingestion.md
**Depends on:** T1
**Action:** Update the S06.0 story block (L79–142) to reflect closure.
**Details:**
- Change `**Status:** Not Started` (L81) → `**Status:** Complete` (or the project's closure phrasing; match how prior completed stories in epics read — verify against an E05 completed story before editing).
- Tick the satisfiable ACs (L131, L134–142) by changing `- [ ]` → `- [x]` and appending a terse parenthetical pointing at the S06.0.md memo / the decision outcome where useful.
- Leave AC #132 (L132) and AC #133 (L133) as `- [ ]` and append an **operator-pending** annotation (D4 — CPW URL resolution deferred; ticked in a follow-up doc-only commit per E05 Group-B pattern).
- Do NOT alter any other story's text, the epic header, or AC wording beyond the checkbox + a short parenthetical. Do NOT edit `docs/open-questions.md` (out of S06.0 scope per the memo's cross-reference section).
**Verify:** `grep -n "S06.0\|Status:" docs/planning/epics/E06-colorado-regulation-text-ingestion.md | head`; confirm exactly two ACs (#132, #133) remain unticked under S06.0 and carry the operator-pending note.
**UI:** no

## Blast Radius
- **Do NOT modify:** any `ingestion/` source or test file (no code ships); `supabase/migrations/` (no migration); `ingestion/ingestion/lib/db.py` (D6 → S06.5); `ingestion/ingestion/lib/schema.py` / `mcp-server/src/types/schema.ts` / `docs/architecture.md` (no schema gap → no three-place sync); any CO or MT loader (D3 rename → S06.10); `docs/open-questions.md` (Q18 flip → S06.9).
- **Watch for:** line-citation drift (documented project failure mode) — every file:line in the memo was grep-verified this session; if re-verifying, anchor on symbol text not line number. Keep the epic edit to checkboxes + short parentheticals; do not reflow AC prose.

## Conventions
- House format: mirror `docs/planning/epics/completed/E05-confidence-findings/S05.4.md` heading structure.
- Documentation-as-handoff per ADR-009; the memo is the authoritative AC record per S06.0's deliverables list (epic L117–121).
- No production-DB writes (AC #142). No code, no migration, no helper this story.
- PM does not edit thinking-layer docs (`open-questions.md`) without explicit approval — deliberately out of scope; the memo records the cross-reference instead.
