# Changelog

## E04 — M1 Carry-Forward and Colorado Schema Preparation — Complete + Audited 2026-05-31

**Post-implementation audit closed 2026-05-31** via PR #52 squash-merged to main as `b168d28` from `test/E04-implementation-audit` (2 pre-squash commits: `7478ea6` S04.1 migration header `public.`-qualifier rationale documentation + `c25d160` 221-LOC audit report at `docs/planning/epics/E04-m1-carry-forward-audit.md`, mirroring the E02 audit's durable-artifact pattern). **Audit verdict: 49 ACs reviewed across 5 stories — 47 MET on direct file/commit inspection, 2 operator-asserted (S04.1 Group B already live-verified 2026-05-30; S04.2 A11 production row counts structurally guaranteed by constant-narrowing-only nature), 0 NOT MET, 0 blocking findings.** Hard-locked merge order S04.2 (`e46fc5a` 05-29) → S04.1 (`616b5fb` 05-30 10:03) → S04.3 (`5de83c3` 05-30) → S04.4 (`0c7425e` 05-30 19:21) → S04.5 (`693e63b` 05-31) verified via PR-merge timestamps; all three cross-story dependency handoffs satisfied (S04.4 mandatory 7th annotation → S04.1 timestamp `20260530132727`; S04.5 Bundle B §8 #7 RESOLVED → S04.3 PR SHA `5de83c3`; S04.5 Bundle B §8 #4 fix-up → S04.4 `draw_spec [^10]` footnote); test baseline +1 from S04.2 (1165 → 1166 via `TestCountGuard::test_band_locked_to_t16_empirical`) consistently propagated through every later story's AC; no regressions. **Single actionable finding** — S04.1 migration header missing `public.`-qualifier rationale from the base migration — **resolved at `7478ea6` as a comment-only header edit (4-line "Style note" block inserted between the three-security-layers paragraph and the "Relevant ADRs" line); zero DDL impact, the migration's production behavior is byte-identical pre/post the comment addition; no re-apply needed; no `supabase migration repair` needed.** One originally-filed finding ("Bundle B path-drift count off by one") retracted in the audit report itself after `git show 37bc86a` re-verification — pre-commit file held 1 `completed/` reference (L162, pre-existing) and post-commit holds 8, a delta of exactly 7 new fixes byte-matching Bundle B's commit message and the existing CLAUDE.md prose. **Emergent theme captured (informational)**: 9 post-merge fix-up commits across E04 (4 in S04.2, 4 in S04.4, 1 in S04.5) — all caught pre-production by reviewers/cubic, none escaped to production; the convention-hardening response is already on disk (S04.2's 3 new pitfalls + S04.5 Bundle A's 2 new entries grew `.roughly/known-pitfalls.md` § "Documentation & planning discipline" from 6 → 11 entries across M2-W1 as deliberate convention-maturing, not recurring quality issues). Quality gates clean on every pre-squash audit commit (cubic returned `{"issues": []}` on both; pre-commit detect-secrets passed; tsc/ruff skipped — no relevant files; no Python touched so pytest baseline structurally preserved at 1166 + 2 skipped). **Post-implementation-audit standard locked for E05 and beyond**: each future epic closes with a post-implementation audit recorded under `docs/planning/epics/completed/E0X-audit.md` before `/plan-next-epic` is invoked for the following epic; PM refuses-and-flags any `/plan-next-epic` invocation where the prior epic's `Audited:` field is unpopulated.

## E04 — M1 Carry-Forward and Colorado Schema Preparation — Complete 2026-05-31

All 5 E04 stories closed across 5 calendar days (2026-05-29 → 2026-05-31). S04.6 evaluated and omitted at planning time per the CO research read-through. All M1 carry-forward items from `docs/planning/handoffs/M1-to-M2-handoff.md` §8 resolved in production: `license_season` deny-all RLS live (S04.1); `_BINDING_COUNT_GUARD_BAND` narrowed to `(552, 1024)` per T16 empirical 788 (S04.2); `logging.basicConfig` on `load_jurisdiction_bindings.py:main()` (S04.3); seven runbook hygiene edits to `docs/runbooks/M1-uat.md` with PM-approved adopted 7th row for `jurisdiction_binding 788 build → 788 DB` completing the build-vs-DB convention (S04.4); PRD 001 sequencing language reconciliation (S04.5) plus two parallel housekeeping bundles co-landed (2 new `.roughly/known-pitfalls.md` entries under Conventions — Documentation & planning discipline; 5 hygiene edits to the M1→M2 handoff making §3, §8 #4, and §8 #7 self-consistent). Test baseline 1166 + 2 skipped held throughout (baseline shifted 1165 → 1166 at S04.2 close via the post-merge `TestCountGuard::test_band_locked_to_t16_empirical` contract-lock test — a deliberate quality addition, not a regression). No ADRs created across E04; no schema changes; no Colorado data loaded; no CO-specific code in `ingestion/states/colorado/`. The recurring-RLS-gap M2 open-question candidate (E04 § "Known Issues to Escalate" #1) remains unchanged in scope — none of the 5 E04 stories needed to resolve it; it persists as a flag for the human to land via event-trigger migration, CI check, or discipline-only mitigation path. M2 next: E05 (Colorado geometry ingestion) — awaits `/plan-next-epic` invocation.

- S04.5: PRD 001 sequencing language reconciliation per the PM-drafted diff (lines 90/96/111 reconciled; line 48 unchanged per PM review). 5-commit chain via `/roughly:build` delegation under the user's git identity: `bf9bfa9` PRD 001 core; `3445017` **Bundle A** — 2 entries appended to `.roughly/known-pitfalls.md` § "Conventions — Documentation & planning discipline" (section grew 6 → 8 entries) — Entry 1 "spec-prescribed string substitutions silently invalidate coupled references" (S04.4 T2/T3 HD 262 → HD 124 case) and Entry 2 "authoritative numbers drift between canonical documents — name the source-of-truth before copying" (S04.1 + S04.4 numeric-drift cases); a third S04.4-surfaced candidate (cubic-as-advisory-not-gating on doc diffs) was deliberately excluded as operational reality of cubic, not a recurring trap; `37bc86a` **Bundle B** — 5 hygiene edits to `docs/planning/handoffs/M1-to-M2-handoff.md` (path-drift to `completed/` 7 occurrences for the E03 synthesis report + deferred-items directory; §3 build-vs-DB clarifying preamble pointing at runbook footnote `[^10]`; §8 #7 RESOLVED annotation citing S04.3 / PR #49 / `5de83c3`; §8 #4 transcription error corrected `draw_spec 276 → 278` per S03.8 closure authoritative; §8 #4 incompleteness — `jurisdiction_binding 788 build → 788 DB (no collapse — id-keyed UPSERT)` row added to memorialize the shipped runbook table's PM-approved scope expansion); `91bde52` plan-historical marker; `eb803db` cubic post-merge fix-up correcting sentence-initial "A" capitalization on PRD 001 line 96 (content-anchored grep verification cannot catch grammar — refinement of Bundle A Entry 1; PM judged too narrow for a new pitfall entry, recorded as refinement). **PM judgment override accepted for the delegation deviation**: S04.5 story shape originally specified human-applies-directly with no implementation-agent edit to PRD 001. User explicitly invoked `/roughly:build` with S04.5 in scope, delegating the apply to the build agent running under the user's git identity. Build-agent intake flagged this at Stage 1 rather than silently widening scope. PM accepts the trailer-collaboration form as satisfying the AC's intent — the no-autonomous-PRD-edit rule's purpose is explicit human control over PRD edits, satisfied by the explicit `/roughly:build` invocation under the user's git identity. Quality gates: pytest 1166 + 2 skipped (no delta, 14.34s); cubic clean post-fix-up; pre-commit detect-secrets passed on all 5 commits. No ADRs, no schema, no Python touched.

## E04 — M1 Carry-Forward and Colorado Schema Preparation — Story-level log (chronological)

E04 closes the five M1 carry-forward items from `docs/planning/handoffs/M1-to-M2-handoff.md` §8 and prepares the codebase for Colorado ingestion in E05/E06. Five stories planned (S04.1–S04.5; S04.6 evaluated and omitted at planning time per the CO research read-through). Hard-locked merge order: S04.2 → S04.1 → S04.3 → S04.4 → S04.5. Planned + validated 2026-05-29; epic at `docs/planning/epics/E04-m1-carry-forward.md`.

- S04.4: All seven M1 UAT runbook hygiene fixes per handoff §8 "Runbook fixes needed before next run" #1-#6 (owned by S04.4) + the mandatory criterion #7 sign-off annotation referencing S04.1's `20260530132727_rls_license_season.sql` (the 7th in S04.4 scope; item #7's code-side basicConfig fix was owned by S04.3). Single target file `docs/runbooks/M1-uat.md` (+37/−28 LOC across the full PR). **Fourth M2 PR chronologically.** Edits: §2 deviation note #3 extended (HD 262 has no elk regulation_record; HD 124 substitutes for criterion #1 + #2(a); HD 170 substitutes for #2(b)); §4 criterion #1 + #2(a) SQL substitutions (HD 262 → HD 124 with new footnote `[^9]` explaining substitution and naming criterion #3 as the canonical HD 262 test-point); §4 criterion #6 "Expected counts" table reshape from 3-col to 4-col with `Build count | Post-UPSERT DB count` distinction + new footnote `[^10]` documenting the build-vs-DB convention and **draw_spec 278-vs-276 provenance** (handoff §8 #4's 276 was a transcription error noted parenthetically in the same bullet; S03.8 closure authoritative at 278; shipped runbook tracks 278); §1 Prerequisites with new footnote `[^11]` pointing to the `supabase db query --db-url` substitute (the actual method used in 2026-05-28 UAT); §4 criterion #8 grep regex simplified to `grep '^\*\*Status:\*\*'`; §6 Sign-Off table criterion #7 row extended with the annotation `— RESOLVED M2-W1 via 20260530132727_rls_license_season.sql`. **PM-approved adopted 7th row** to the build-vs-DB table: `jurisdiction_binding 788 build → 788 DB (no collapse — id-keyed UPSERT)` — well-grounded against S03.10 T16 + S04.2 + S04.3 empirical 788 confirmations; user-approved at story open. **Four Stage-6 review-fix corrections** (round-1 reviewers caught these as logical consequences the original plan missed): L99 inline criterion #2 deviation note's stale HD 262 claim rewritten; §5 Results Summary L374/L375 row labels updated; L416 footnote `[^3]` updated; §1 Prerequisites migration count 4 → 5 to surface S04.1's migration as the 5th. **One user-approved AC-interpretation override** (commit `3358166` cubic P1 review-fix): 2-line italic preamble between §6 header and the table documenting the audit-trail-preservation convention up-front — cubic flagged the §6 row's RESOLVED annotation as contradictory with the milestone-sign-off paragraph's frozen 2026-05-28 "Criterion #7 FAILed — 14 privilege leaks ... zero RLS policies" language for cold-reading operators; root cause was the convention being documented in the epic not the runbook itself; user chose Option-1 (italic preamble) over Option-2 (footnote-on-row) after operator-UX vs. strict-AC-compliance tradeoff analysis; expands §6 diff scope beyond AC's literal "criterion #7 row only" verification clause but protected historical content (operator marks, initials, dates, sign-off paragraph) remains byte-identical pre/post. **Two plan-file P3 corrections** (commit `0b010a3` post-implementation cubic-fix): T4 and T5 in the historical plan declared `Depends on: none` when sub-edits 4b/5b state-depend on the prior task's footnote-append; T4 now declares `Depends on: T2`; T5 now declares `Depends on: T4`; plan still historical, no impact on shipped runbook. **Stage-6 review history**: round-1 code-reviewer REQUEST CHANGES BLOCKER on L99/L416; silent-failure-hunter W1/W2/I2 on §5 + §1 migration count; static-analysis PASS; round-2 code-reviewer APPROVE; post-merge cubic P2 invalid (435/825 values were genuine handoff/runbook references, false-positive without context) + P1 valid (§6 preamble) + 2× P3 valid (T4/T5 plan dep declarations). **Quality gates**: ruff clean; mypy 8 source files no issues; pytest 1166 + 2 skipped (zero delta from S04.3 baseline; documentation-only PR); pre-commit detect-secrets clean. **Three new pitfall candidates surfaced — none auto-landed (`.roughly/` is implementation territory; PM flags-and-defers for user decision)**: (1) most load-bearing — "spec-prescribed string substitutions silently invalidate coupled references elsewhere in the document; grep the file for every coupled reference before review catches it" (T2/T3 HD 262 → HD 124 swap case caught by Stage-6 review triad after Stage-3 plan and Stage-4 plan-review both excluded §5 by design); (2) "authoritative numbers drift between canonical documents — name the source-of-truth before copying" (draw_spec 278/276 case; extends S04.1 build-vs-DB lesson; can fold into #1); (3) "cubic-review on documentation diffs can flag values it lacks context to validate; treat as advisory not gating" — PM recommends NOT landing (operational reality of cubic). No open questions touched; no ADRs created. **Squash-merged from `feat/S04.4-m1-uat-runbook-hygiene-fixes` 2026-05-30 (4-commit pre-squash chain).**

- S04.3: 4-line multi-line `logging.basicConfig(level=logging.INFO, format="%(asctime)s %(name)s %(levelname)s %(message)s")` inserted as the first executable statement of `main()` in `ingestion/states/montana/load_jurisdiction_bindings.py` (lines 785-788), immediately before the pre-existing `logger = logging.getLogger(__name__)` at line 789. Format string byte-identical to the canonical peer pattern at `load_regulation_records.py:714-717`. Closes the M1-UAT-2026-05-28 runpy-wrapper workaround per handoff §8 item #7 — `ingestion/.venv/bin/python ingestion/states/montana/load_jurisdiction_bindings.py --dry-run` now emits 25 INFO lines on stderr including the 22-row species×role cross-tab and `TOTAL: 788 bindings` (matches S04.2's T16 empirical 788 — re-confirms `_BINDING_COUNT_GUARD_BAND = (552, 1024)` remains well-centered as of 2026-05-30). Two style decisions baked in (neither rises to ADR threshold): double-quoted format string for project-wide parity across the 9 sibling Montana loaders (spec at L286 had single-quoted; functionally identical); multi-line `basicConfig(...)` form matching majority of peers (cleaner for ruff line-length; spec's code block at L282-289 also multi-line). No regression test added per epic L291. **Spec discrepancy surfaced (documentation-only; not propagated to fix)**: epic prose at L278/L293/L304 cites "other 6 loaders" — actual count is 9 sibling loaders (10 total) per implementation-time grep across `ingestion/states/montana/load_*.py`. Fix-side scope unaffected (`load_jurisdiction_bindings.py` was the sole `main()` without `basicConfig`); PM declined to land a cosmetic-only correction now (S04.3 is closed; folding into S04.4 scope would be creep). No ADRs created; no schema or three-place-sync changes; no new pitfalls; no open questions touched; test suite stable at **1166 + 2 skipped** (no delta — `git diff --stat` showed exactly one code file +4/-0 plus the plan-marker doc file). **PM-noted optional enhancement surfaced for S04.4 (flag-and-discuss with user before adopting)**: handoff §8 #4's build-vs-DB count table to be added at `docs/runbooks/M1-uat.md` §4 criterion #6 omits `jurisdiction_binding`; the cleanest seventh-row addition is `jurisdiction_binding 788 build → 788 DB (no collapse — id-keyed UPSERT)`. Spec expansion requires user approval per the no-autonomous-scope-creep discipline. **Third M2 PR chronologically.** Squash-merged from `feat/S04.3-add-logging-basicConfig` 2026-05-30 (PR #49 / `5de83c3`).

- S04.1: New RLS migration `supabase/migrations/20260530132727_rls_license_season.sql` (33 LOC including banner) closes the M1 UAT criterion #7 leak surface confirmed 2026-05-28 (14 privilege leaks across `{SELECT, INSERT, UPDATE, DELETE, REFERENCES, TRIGGER, TRUNCATE} × {anon, authenticated}` + 0 RLS policies on `license_season`). Body runs `ALTER TABLE public.license_season ENABLE ROW LEVEL SECURITY` + `ALTER TABLE public.license_season FORCE ROW LEVEL SECURITY` (FORCE included for parity with base migration's posture across all 10 original tables; handoff §8 example body had omitted it), two deny-all policies for `authenticated` (declared first, matching `20260425000001_rls_deny_all.sql:29-32` grep ordering) then `anon`, and `REVOKE ALL ON TABLE public.license_season FROM anon, authenticated`. One PM-flagged spec deviation documented in the migration's header: `public.` qualifier on every table reference per the spec's literal-string ACs vs. the base migration's unqualified table names — both forms are functionally equivalent with the default `search_path`; AC-string parity wins over base-migration grep parity per the resolved plan-review must-fix. Plan review: 1 round NEEDS REVISION → fixes applied (qualifier choice; zsh-vs-bash shell idiom in verify block) → PASS. Code-review subagent: 1 WARNING about `CREATE POLICY` non-idempotency on re-apply, not fixed because it matches the base migration's posture and fail-loud on re-apply is the correct posture for a security migration; recovery path documented in `.roughly/known-pitfalls.md` under `supabase migration repair --status applied <timestamp>`. Silent-failure-hunter subagent: PASS with 3 informational observations, no fixes. Static-analysis subagent: clean across ruff + mypy + tsc. Group A file-level ACs satisfied at merge (timestamp ordering, 5 byte-exact SQL strings, authenticated-first policy ordering, test suite holds at 1166 + 2 skipped, no Pydantic / TypeScript / `architecture.md` edits — `license_season` already exists in DDL + Pydantic from S03.0). **Group B operator-driven verification ACs closed 2026-05-30** via live verification against the production Supabase project — Q1 info_schema_privileges returned 0 rows; Q2 pg_policies returned 2 rows with byte-exact deny-all policy names and `cmd=ALL`; Q3 pg_class_rls_flags returned both `relrowsecurity` and `relforcerowsecurity` true; Q4 row_count returned **2411** (the post-UPSERT-collapse DB baseline per the S04.4 spec § "Expected counts" footnote at this epic's L280, not the ~3040 build count from handoff §3 which the verification prompt had cited in error). The S04.1 migration is DDL-only (zero data-modifying statements) so the row count is structurally preserved; AC #11 is satisfied by the unchanged-value evidence. Verbatim outputs locked in `docs/planning/epics/E04-m1-carry-forward.md` § "Group B verification record — 2026-05-30". M1 UAT criterion #7 leak surface (14 privilege leaks + 0 RLS policies on `license_season` confirmed at M1 UAT 2026-05-28) is now structurally closed in production. **Discipline note for future Group B-style verification prompts** (recommended for a future implementation agent to land as a one-line entry in `.roughly/known-pitfalls.md` under § "Conventions — Documentation & planning discipline"; PM does not touch `.roughly/` autonomously): when authoring expected-shape tables that name post-migration row counts, cite the post-UPSERT-collapse DB baseline (S04.4 spec L280 is the durable source-of-truth) rather than the build count from handoff §3 — handoff §3 lists pre-collapse projections that don't match what a `SELECT COUNT(*)` returns. This is the same handoff-document inconsistency surfaced earlier by cubic and flagged as a hygiene-edit candidate on the read-only handoff. No ADRs created; no schema or three-place-sync changes; no `db.py` touches; no production-database writes from the build session (RLS posture change, not data change). The recurring-RLS-gap M2 open-question candidate (E04 § Known Issues to Escalate #1) is unchanged in scope. **Second M2 PR chronologically** per the hard-locked merge order. Squash-merged from `feat/S04.1-license-season-rls-migration` 2026-05-30.

- S04.2: Narrowed `_BINDING_COUNT_GUARD_BAND` in `ingestion/states/montana/load_jurisdiction_bindings.py` from `(400, 1100)` → `(552, 1024)` (±30% around T16's empirical 788 measured 2026-05-28). Three prose locations in the loader updated to name T16 / 2026-05-28 / 788 / ±30% / handoff §8 sixth bullet — spec named two (module comment lines 108-109; function docstring lines 679-680), Stage 6 static-analysis reviewer caught a third at module-docstring lines 26-27. `TestCountGuard` refactored along the recommended arithmetic-derivation path: `LOW, HIGH = _BINDING_COUNT_GUARD_BAND`; `_IN_BAND_SAMPLE_COUNT = 788` extracted with a T16 / 2026-05-28 inline comment; 5 boundary cases derive as `LOW-1 / LOW / _IN_BAND_SAMPLE_COUNT / HIGH / HIGH+1`; 9 unrelated `770` literals elsewhere in the file swept to `_IN_BAND_SAMPLE_COUNT` for project-wide consistency. **Test baseline shifted 1165 → 1166 + 2 skipped** via a new post-merge `TestCountGuard::test_band_locked_to_t16_empirical` contract-lock test closing a circularity gap (the 5 arithmetic-derived boundary cases derived their inputs from the band itself, so accidental widening to e.g. `(0, 9999)` would silently pass; lock test pins the tuple's numeric value to `(552, 1024)`). AC #1087 in closed E03 epic carries `(552, 1024)` tuple plus a rewritten suffix prose ("±30% around T16's empirical 788 measured 2026-05-28") — the as-shipped suffix would have contradicted the narrowed tuple. `[^row-count-correction]` footnote retains its 2026-05-23 paragraph byte-identical and carries an appended **T16 narrowing 2026-05-29.** paragraph (correct 4-space footnote-continuation indent). 4 post-merge cubic-fix iterations across the build session (the third "pending T16" prose location, the AC #1087 suffix-prose contradiction, the test-class circularity gap, a branch-local SHA-reference portability issue). Three new pitfalls under `.roughly/known-pitfalls.md` § "Conventions — Documentation & planning discipline": spec-named location lists must be independently grep-verified; spec line-number citations drift between spec authoring and execution (content-anchored verification beats absolute line-number lookup); reviewer convergence is hard evidence that a finding is valid (dismissing on "the spec didn't require it" grounds is wrong — the spec is a floor, not a ceiling). No ADRs, no schema or three-place-sync changes, no `db.py` touches, no production-DB writes; Montana row counts in Postgres unchanged (constant change is import-time only). **First M2 PR shipped** per handoff §8 sixth bullet chronology lock. Squash-merged from `feat/S04.2-narrow-binding-count-guard-band` 2026-05-29.

## E03 — Montana Regulation Text Ingestion — 2026-05-27

14 stories shipped (13 original + S03.6.1 carve-out). Final V1 row counts: 437 regulation_record + 978 season_definition + 1225 license_tag + 278 unique draw_spec + 3 reporting_obligation + 3040 license_season + 70 regulation_reporting + ~1000 projected jurisdiction_binding (T16 live UAT operator-pending at close) + 350 geometry. 4 migrations live; 11 tables operational. Confidence calibration framework (ADR-017) accepted via FINALIZE verdict at S03.11 close — Q11 RESOLVED, ADR-017 unmodified. Doc-type precedence (ADR-019) accepted mid-E03 at S03.4 close. Q19 (id text-PK UPSERT slug-drift) carries forward as pre-M2 blocker. M1 closure UAT runbook (`docs/runbooks/M1-uat.md`) + M1→M2 handoff document (`docs/planning/handoffs/M1-to-M2-handoff.md`) shipped at S03.12 close.

- S03.0: Schema preparation for Montana regulation text ingestion — migration `supabase/migrations/20260504032424_e03_schema_additions.sql` ships ADR-018's three coordinated schema additions in a single transaction: new `license_season` link table (composite PK `(license_tag_id, season_definition_id)` + reverse-lookup index on `season_definition_id`) for explicit per-license season coverage; new nullable `geometry.legal_description text` column for FWP-published boundary prose, semantically distinct from `verbatim_rule`; `geometry.kind` CHECK constraint extended from 8 values to 9 with the new `'state'` value supporting state-level boundary polygons. Three-place sync per ADR-006: Pydantic `LicenseSeason` BaseModel + `Geometry.legal_description: str | None` + `Geometry.kind` Literal extended in `ingestion/ingestion/lib/schema.py`; matching changes in `mcp-server/src/types/schema.ts` (`tsc --noEmit` clean); architecture.md §"Schema types" + supporting prose absorbed in commit `615120c` (broader M1-reality alignment pass that landed ahead of S03.0's PR). Added `ingestion/states/montana/load_state_boundary.py` (idempotent UPSERT load script) ready to write the single `MT-STATEWIDE-geom` row from the Montana State Library MSDI Framework Boundaries layer 9 — operator-pending step closes via `cd ingestion && DATABASE_URL=... .venv/bin/python states/montana/load_state_boundary.py`. **Source decision deviates from ADR-018's two listed options.** ADR-018 §3 listed FWP-published as first preference, US Census TIGER 2020 as fallback; T7's investigation found a third option that strictly dominates both — Montana State Library MSDI Framework Boundaries layer 9 (gisservicemt.gov), state-published and GCDB-aligned at 1:24,000, qualifies as `document_type='gis_layer'` per ADR-014 with no `'reference_boundary'` Literal extension needed. Verified empty: no state-boundary layer on FWP on-prem (`fwp-gis.mt.gov`) or AGOL `MtFishWildlifeParks` org. Pinned URL + SHA-256 + `2026-01-01` publication date in load script and `docs/planning/epics/E03-confidence-findings/S03.0.md`; live-MapServer SHA drift fails-loud per ADR-001 (`_verify_sha256` returns observed digest on success, raises `RuntimeError` with both digests on mismatch). Working-artifact directories created with retention-policy READMEs: `docs/planning/epics/E03-confidence-findings/` (deleted at `m1` tag commit per ADR-017 §6) and `docs/planning/epics/E03-deferred-items/` (survives past M1 close as M2 promises; initial files include `draw-mechanics.md` for Q12 deferrals). Two new pitfalls institutionalized in `.roughly/known-pitfalls.md` (commit `dcef99b`): MT GIS layers live on **three** hosts not two — FWP on-prem + AGOL MtFishWildlifeParks + Montana State Library MSDI; one-off `requests.get()` against ArcGIS or MapServer endpoints must explicitly check `data.get("error")` envelopes before parsing for the expected shape (200-with-error-body is the ArcGIS pattern). Five cubic-review iteration commits applied (8d06d1a, 0e64750, 3c04815 baked into the ADR-001 fail-loud discipline plus the architecture.md "every regulation-bearing entity" qualifier; three findings rejected with rationale documented). 332/332 ingestion tests green; ruff/mypy/cubic clean; PR review passed 2026-05-04. **AC #5 closed 2026-05-04 16:25 PT** — `MT-STATEWIDE-geom` written to production via the load script: single-part valid MultiPolygon, `area_km2 ≈ 380,840` (within 0.01% of Montana's published total), `license_year=NULL`, `source.id='mt-msdi-framework-boundaries-9-2026'`, `document_type='gis_layer'`, `publication_date=2026-01-01`. Idempotent UPSERT confirmed. S03.10's binding generation now has its FK target. Live-load surfaced a third institutional pitfall added to `.roughly/known-pitfalls.md`: `supabase db push` against a project whose schema was bootstrapped via the dashboard SQL editor (or via `db push` from a different machine) can fail with `relation already exists` because `supabase_migrations.schema_migrations` is empty even though the schema is fully populated. Fix is `supabase migration repair --status applied <timestamp>` for each pre-existing migration before pushing — applied during S03.0 deployment for the three E01/E02 migrations. Plan deviations: TIGER 2020 path never built (T7 chose MSDI; future state-boundary stories without a state-published source should reopen ADR-018's `'reference_boundary'` Literal-extension question); `geometry.legal_description` and `license_season` ship with zero rows (population is S03.5/S03.6/S03.7 work).
- S03.1: PDF fetch infrastructure — shipped `ingestion/ingestion/lib/pdf_fetch.py` (~310 LOC, state-agnostic per ADR-005; public API `fetch_pdf` / `PdfMetadata` / `PdfFetchError`) and `ingestion/states/montana/fetch_pdfs.py` (~230 LOC, fail-loud aggregating orchestrator with distinct buckets for failures vs. pending entries). `ingestion/states/montana/sources.yaml` carries 4 entries (DEA booklet, Black Bear booklet, Legal Descriptions, Black Bear correction) with full `SourceCitation` field set; correction PDF carries `pending: true` since URL was genuinely TBD at story open. SHA-256 drift on re-fetch raises `PdfFetchError` and writes a `<id>-<publication_date>-pending-reextraction.flag` marker consumed as a block signal by downstream stories. State-agnostic-lib invariant enforced via AST-based test guard. 33 new unit tests (332 → 365 total). **Key pitfall:** canonical FWP PDF URLs for all 4 sources required live discovery against `https://fwp.mt.gov/hunt/regulations` before pinning — the spec's URL guesses all returned 404; `expected_sha256` in `sources.yaml` is documentation/intent, not a runtime check (runtime compares against the on-disk manifest). AC #4 (manifest commits) infrastructure-met but fixture-deferred to first ops-run. Merged 2026-05-05.
- S03.2: PDF extraction primitives shared library — shipped `ingestion/ingestion/lib/pdf.py` (332 LOC, state-agnostic per ADR-005) wrapping `pdfplumber` 0.11.x with 12 public exports: `PdfDocument`, `open_pdf`, `iter_pages`, `extract_text`, `extract_tables` (returns `TableMatch` records with bbox + headers + rows via `page.find_tables()` — not `page.extract_tables()` which drops bbox), `find_section`, `PageReference` TypedDict + `page_reference_to_str`, `ConfidenceTier(str, Enum)` mixin so instances are strings with no `.value` access at write-sites, `min_tier` (most-uncertain-wins MIN with `key=lambda t: t.rank` to guard against the lexicographic trap where naive `min(["high","low","medium"])` returns `"high"`), `demote_one_tier` (ADR-017 correction-touched rule, floor-clamped), and `PdfExtractionError`. 39 new tests (332 → 404); includes `test_lexicographic_trap_explicit` and `test_no_layout_true_regression_guard`. Two pdfplumber pitfalls recorded: `extract_text` collapses repeated spaces (not byte-exact verbatim; `page.chars` is the byte-exact escape hatch); `page.extract_tables()` drops bboxes. Merged 2026-05-06.
- S03.3: DEA booklet extraction — shipped `ingestion/states/montana/extract_dea.py` (~1545 LOC, state-agnostic-clean per AST guard) emitting `ingestion/states/montana/extracted/dea-2026.json`. Final artifact: **272 sections / 1190 rows / 601 high + 589 medium confidence, 0 low** (129 deer + 112 elk + 22 antelope per-HD sections + 1 STATEWIDE antelope overlay `900-20`; 8 Region 7 portions sections — HDs 700-705 — added after PM UAT). **Six UAT defects fixed post-initial-PR:** D1 (P0) per-row General Season window divergence collapsed to first-row value; D2 (P0) Region 7 "Portions of HDs" sub-sections missing; D3 (P1) STATEWIDE 900-20 archery date mapped by column-position rather than extras text; D4/D5/D6 (P2/P3) STATEWIDE extras and license_code handling. Three fixes: deleted `_aggregate_section_season_windows` (per-row windows inline); added `_parse_portions_hd_list`; routed `900-20` through shared `_rows_to_license_extractions` machinery. Fail-loud guard for STATEWIDE emit at 3 distinct stages (Phase 7). 9 real-PDF discoveries baked in (one-table-per-page, `-` universal absence sentinel, license-code carry-forward via `None`, species banners embedded mid-table, antelope column format differs, `_ANTELOPE_PAGES = (136, 141)`, etc.). 4 pdfplumber pitfalls added to `.roughly/known-pitfalls.md`. Test suite 488 passed + 1 skipped. Merged 2026-05-08 (12 initial commits + UAT fix commit `cb3fb24`); PR review passed.
- S03.4: Black Bear booklet + correction PDF handling — shipped `ingestion/states/montana/extract_black_bear.py` (~1820 LOC) implementing a three-pass date-arbitrated extraction into `black-bear-2026-base.json` (Pass 1: 35 BMU rows + 2 closures + 3 reporting candidates), `corrections-2026-03-18.json` (Pass 2: 35 per-BMU CorrectionOperation dicts), and `black-bear-2026.json` (Pass 3 merged — downstream consumer). All 35 rows tagged `applied_correction=true`, confidence demoted HIGH→MEDIUM (correction touches every BMU). **ADR-019 accepted** at `docs/adrs/ADR-019-doc-type-precedence-multi-source-merge.md` — formalizes Option B (doc-type precedence wins over date): `document_type='correction'` always wins over `'annual_regulations'` regardless of date; date is tiebreaker within same doc-type only. Three PM decisions accepted at closure: (a) quota-closure BMU count 9→8 (BMU 530 absent from 2026 PDF); (b) reporting obligations count 2→3 (STATEWIDE 48-hour + R1/R2-7 split); (c) Option B replaces spec's MAX-date Option A. Two cubic-review correctness fixes: `_merge_with_corrections` refactored to avoid per-(bmu, field) loop overwriting row-level `source_id` + `demote_one_tier` moved to Stage 3 to fire exactly once per touched BMU per ADR-017 §4. Test suite 574 passed + 2 skipped (+85 from S03.4). Merged PR `ab09e82` 2026-05-12.
- S03.5: Legal Descriptions extraction — shipped `ingestion/states/montana/extract_legal_descriptions.py` (~1372 LOC, state-agnostic-clean per AST guard; single-writer contract locked by `test_no_db_imports`) emitting `ingestion/states/montana/extracted/legal-descriptions-2026.json`. Final artifact: **228 matched** (226 HD + 2 CWD, all `extraction_confidence=high`) + **31 by-design unmatched** (portion sub-headings) + **119 unlinked** (9 HD genuinely absent from PDF + 55 portion + 54 restricted_area + 1 STATEWIDE); regression rate 0.000. PM-run pre-merge review surfaced 1 P1 (verbatim spillage — column-crop x-range too narrow at 145pt) + 5 P2 findings; matched count rose 156→228 and bleed cases dropped 35→0 across the fix cycle. 11 real-PDF discoveries baked in (three-column layout, rotated chapter-label sidebars, CWD heading line-wrap, 8 FWP heading variants, running footer leak corrected from 20pt to 50pt). 7 new pdfplumber pitfalls added to `.roughly/known-pitfalls.md`. Test suite 657 passed + 2 skipped (+83 from S03.5; `TestArtifactRegression` locks no-cross-namespace-bleed and no-footer-leak). Merged PR `b2ad20b` 2026-05-14.
- S03.6: Regulation_record ingestion + `geometry.legal_description` population — shipped `ingestion/states/montana/load_regulation_records.py` (~590 LOC, state-agnostic-clean) as the first E03 database-write adapter. Writes **436 regulation_record rows + 228 `geometry.legal_description` updates** in one atomic transaction (129 mule_deer + 129 whitetail + 112 elk + 31 pronghorn + 35 bear; DEA `deer` fans out to `mule_deer` + `whitetail` at the DB layer). New DB helpers `db.upsert_regulation_record` (UPSERT-by-composite-PK, `ingested_at` excluded from UPDATE) and `db.update_legal_description` (fails loud on `rowcount==0`). **OQ1 resolved** — `regulation_record` has no `verbatim_rule` column; section text decomposes to `season_definition.verbatim_rule` + `license_tag.verbatim_rule` downstream (S03.7); HD-wide NOTE lines land in `regulation_record.additional_rules`. OQ7 row-count fail-loud guards (±30% band) fire BEFORE `db.connect()` so a regression aborts with no partial writes — precedent for S03.7-S03.10. 7 new pitfalls + 2 M2-deferred items recorded. Test suite 709 passed + 2 skipped (+52 from S03.6). Merged PR `c0c1b77` 2026-05-15.
- S03.7: `season_definition` + `license_tag` + `license_season` ingestion — shipped `ingestion/states/montana/load_seasons_and_licenses.py` (~1180 LOC, state-agnostic-clean) writing **978 season_definition + 1225 license_tag + 3040 license_season + 1385 regulation_season + 1914 regulation_license = 8542 entity/link rows + 20 closure_predicate jsonb values** atomically. **M1 success criterion #2 satisfied at the data layer** (HD 170 elk asymmetric A/B license_season coverage; shifted from HD 262 which lacked the pattern; locked by `TestBuildDeaLinkRows::test_license_season_asymmetric_coverage_m1_criterion`). 11 decisions baked in (OQ-S7-1 through OQ-S7-11) covering species granularity, season-name rendering, bear closure_predicate attachment, license_tag.kind 5-branch heuristic, window parser 4-format variants, verbatim_rule fallback chain, residency/quota_range handling, and `purchase_url`. `license_tag.draw_spec_key=None` on every row — S03.8 backfills per ADR-012. Two cubic fixes (P2: DEA builders gate on `season_coverage` truth; P1: `_license_code_slug` includes lowercased type prefix to resolve 6 cross-listing collisions). 3 PM surgical mypy fixes at closure (loop-variable type pollution; `key` shadowing; `weapon_type` narrowing — agent's "ruff+mypy clean" claim was wrong). Test suite 832 passed + 2 skipped (+121 from S03.7). Merged PR `20db0fc` 2026-05-16.
- S03.8: `draw_spec` ingestion — shipped `ingestion/states/montana/load_draw_specs.py` (~720 LOC, state-agnostic-clean per AST guard) writing **388 draw_spec rows (278 unique after UPSERT composite-PK collapse) + 388 `license_tag.draw_spec_key` backfills** atomically. **S03.7's `_DEA_LICENSE_KIND_HEURISTIC` amended in the same PR** — the original `license_code`-only heuristic mis-classified 162 OTC B Licenses as `limited_draw`; amendment inspects `apply_by` cross-row AND cross-HD (`apply_by="OTC:\nJun 15"` cells are load-bearing OTC discriminators). Three new S03.8 patterns established for S03.9+: (1) cross-listing consistency validator with `"key" in dict` presence detection; (2) defensive safety-net lookup with RAISE on runtime firing; (3) override-dispatch table `_KNOWN_CROSS_LISTING_OVERRIDES` for known-irreconcilable real-PDF inconsistencies with rationale strings + page citations. `900-20` correctly has no `draw_spec` (Q5/Q12 deferral). Q17 opened (per-HD allocation caps; HD 210 case). Test suite 910 passed + 2 skipped (+78 from S03.8). Merged PR `323836f` 2026-05-19.
- S03.9: `reporting_obligation` ingestion — shipped `ingestion/states/montana/load_reporting_obligations.py` (~600 LOC, state-agnostic-clean) writing **3 reporting_obligation rows + 70 regulation_reporting link rows** atomically from the bear artifact (1 STATEWIDE `harvest_report` 48-hour + 1 R1 `tooth_submission` 10-day + 1 R2-7 `hide_skull_presentation` 10-day; link arithmetic 35+14+21=70). Three originally-spec'd row types carved out via source-audit probe: Bear ID coursework (wrong target table → S03.6.1); CWD sampling (zone-keyed join would miss license-keyed artifact rows → Q18 + M2 deferred-items file); general mandatory reporting for deer/elk/pronghorn (authoritative FWP list is bear/wolf/marten/swans only — spec was wrong). 6 review-triad findings applied (1 HIGH, 1 MEDIUM, 4 P2). Q18 + Q19 added to `docs/open-questions.md` (Q19 — id text-PK UPSERT slug-drift — is a **pre-M2 blocker** affecting 3 helpers in `db.py`). Test suite 981 passed + 2 skipped (+71 from S03.9 across 7 cubic-review rounds). Merged PR `195ac8b` 2026-05-21.
- S03.6.1: MT-STATEWIDE-bear anchor + Bear ID Test + `jurisdiction_binding` to `MT-STATEWIDE-geom` — carve-out story (from S03.9's Bear ID coursework probe). Extraction amendment to `extract_black_bear.py` adds page-2 right-column extraction via `_extract_statewide_rules()` with two regex anchors; new top-level field `statewide_rules: list[StatewideRuleCandidate]` on both bear JSON artifacts (corrections artifact unchanged). `load_regulation_records.py` amended to add `_build_statewide_bear_record()` + `_build_statewide_bear_binding()` writing **437 regulation_record rows + 1 jurisdiction_binding row + 228 legal_description updates** (prior S03.6 count was 436). New DB helper `db.upsert_jurisdiction_binding()` introduced for S03.10 reuse (UPSERT-by-id, all 9 columns in EXCLUDED clause, fails loud on `rowcount==0`). Deterministic binding id `"US-MT-MT-STATEWIDE-bear-bear-2026-primary_unit-MT-STATEWIDE-geom"` locked by `TestBuildStatewideBearBinding::test_id_encoding_is_deterministic`; S03.10 must import the format constant to preserve UPSERT no-op. Test suite 1024 passed + 2 skipped (+43 from S03.6.1; 8 new test classes). Merged PR #40 2026-05-22.
- S03.10: `jurisdiction_binding` generation — shipped `ingestion/states/montana/load_jurisdiction_bindings.py` (~870 LOC, state-agnostic-clean per `TestNoLibImports` AST guard) as the first cross-cutting binding loader. Reads committed `geometry-overlays.json` fixture × `regulation_record` cross product, applies per-species filtering, walks 4 binding sources (statewide / overlay self-row / overlay portions / no-hunt-zone Option A), and UPSERTs atomically via `db.upsert_jurisdiction_binding` (no `db.py` changes). Projected ~1000 bindings (2 statewide + ~940 overlay + ~59 no-hunt-zone) inside `[400, 1100]` guard band. **T16 live UAT operator-pending** at story close (regulation_record table was empty at T0 probe 2026-05-23; requires batch run of S03.0/S03.6/S03.6.1/S03.7/S03.8/S03.9 first). 4 PM-approved spec deviations footnoted in the epic (id format imports from S03.6.1; count band intentionally wide; source attribution via adapter-local SELECT; no-hunt-zone `ST_DWithin` boundary-to-boundary on native geography — spec's centroid-to-centroid returned 0 matches for all 3 orphan zones). 18 fail-loud guards added across review-triad + 4 PR-review iterations. 7 new pitfalls recorded (notably: `geometry.geom` column not `geog`; PostGIS lives in `extensions` schema; centroid-to-centroid `ST_DWithin` produces zero matches for large polygons; per-builder dedup sets miss cross-builder collisions). Bear binding from S03.6.1 stable via 3 byte-identity locks. Test suite 1128 passed + 2 skipped (+104 from S03.10; `test_load_jurisdiction_bindings.py` ~1530 LOC). Merged PR `e83ef2d` 2026-05-26.
- S03.11: Confidence calibration audit + ADR-017 finalization — **Verdict: FINALIZE. ADR-017 active file unmodified. Q11 RESOLVED 2026-05-27.** Audit shape: 50-row stratified sample × 10 documented edge cases (EC1-EC10); artifact-based (regulation_record table was empty at T0 probe per S03.10). Strict 4-step anti-circularity discipline enforced (pull F12 inputs → apply ADR-017 rules independently → reveal artifact value → compare). Pass-rates scoped per ADR-017 §3: `regulation_record.confidence` 39/39 = 100%; EC8 closure_predicate parent-inheritance 6/6 = 100%; EC9 license_tag row-level extraction_confidence 5/5 = 100%; combined 50/50 = 100%. Tier distribution across projected 437 regulation_record rows: `high=32 (7.3%) medium=405 (92.7%) low=0 (0.0%)`. Defer-or-finalize arc: PM's original recommendation was PARTIAL DEFER under LITERAL reading of ADR-017 §7 Trigger 2 (`low=0` literally fires); independent architectural review returned LAND-WITH-EDITS; PM applied 4 refinements; user decision overrode to FINALIZE under INTENT reading (`low=0` is absence-by-data-property, not absence-by-framework-gap). ADR-017 amendment DRAFT created, reviewed, refined, then rejected and deleted per FINALIZE workflow. Synthesis report at `docs/planning/epics/E03-confidence-calibration-synthesis.md` (~360 lines, survives m1 tag). 8 stale-reference fixes in same PR (root cause: prior `docs/plans/` → `.roughly/plans/` migration). No test changes (doc-only PR; suite holds at 1128 + 2 skipped). Merged PR #42 2026-05-27.
- S03.12: M1 UAT preparation + handoff to M2 — shipped `docs/runbooks/M1-uat.md` (operator UAT runbook with 8 SQL blocks per PRD success criteria + operator batch-run sequence); shipped `docs/planning/handoffs/M1-to-M2-handoff.md` (M2 inheritance + Q11 RESOLVED + Q19 pre-M2 blocker + 4 deferred-items files + 7 known issues); deleted `docs/planning/epics/E03-confidence-findings/` per ADR-017 §6 + `.gitignore` update; CLAUDE.md + planning README + this CHANGELOG updated; m1 tag ready for user push. 6 PRD-vs-actual deviations footnoted in M1-uat.md. Test suite holds at 1128 + 2 skipped (doc-only).

## E02 — Montana Geometry Ingestion — 2026-05-03

All 8 stories merged. 349 V1 Montana geometry rows loaded (235 HDs + 55 portions + 57 restricted areas + 2 CWD zones). Schema additions ADR-014 (`SourceCitation.document_type='gis_layer'`) and ADR-015 (`geometry.verbatim_rule` + REG+COMMENTS separator) completed in S02.0; ADR-016 (digitization-tolerant containment via three-band area-ratio discriminator) added during S02.6. Geometry layer is fully ingested, fingerprinted via per-fetch manifests for drift detection, and verifiable via the operator runbook at `docs/runbooks/E02-geometry-verification.md`. Two E03 handoff items recorded in the epic: `restricted_area` kind conflates internal HD restrictions with no-hunt zones; jurisdiction_binding fan-out is higher than naive estimates (median ~3 parents per child, max 16). E03 (Montana regulation text ingestion) can now proceed.

Post-implementation audit completed 2026-05-03 ([`docs/planning/epics/E02-audit.md`](docs/planning/epics/E02-audit.md), 8 parallel review subagents, one per story): 89 ACs total — 86 MET, 3 PARTIAL, 0 NOT MET. The 3 partials were all P3 cosmetic — dead `MT_FWP_HOST` constant in the shared `arcgis` library, asymmetric atomicity between `_write_features_fixture` (direct write) and `_write_manifest_fixture` (tmp+rename), and a stale Kalispell CWD OBJECTID (970 → 968) in S02.5's epic AC #3. All three fixed in commit `0093e88` (constant deleted, both fixture writers made atomic with paired tmp.unlink cleanup, OBJECTID corrected in epic + this CHANGELOG entry). 310/310 tests pass; cubic clean. No regressions, no NOT-MET ACs, no story-to-story drift.

- E02 S02.7: Spatial query verification + epic exit — extended `arcgis.fetch_features` (+162 / -39 LOC) to write a paired `*-manifest-*.json` (~5 KB) alongside every gitignored `*-features-*.geojson` payload. Manifest fields: `features_count`, `layer_hash` (sha256 over sorted per-feature canonical hashes), `hash_distribution` (256-bucket histogram), `fetched_at`, `source_url`, `source_layer_max_record_count`, `source_layer_object_id_field`. Atomic tmp+rename writer; deterministic JSON. Empty-layer branch writes a marker manifest (zero count, sha256(b"") hash, full zero histogram) so silence ≠ no data — a real S02.7 pitfall now in `.roughly/known-pitfalls.md`. Per-feature OID extraction routes through `_read_objectid` with "no resolvable OID" surfaced as `ArcGISError`; `compute_feature_hash`'s `objectid` parameter widened to `int | str`. Test count 300 → 311. Added `ingestion/states/montana/backfill_manifests.py` (313 LOC, one-shot CLI) regenerating manifests for the 10 layers ingested S02.2-S02.5 from local features GeoJSON; pairs metadata-with-features by latest-metadata-at-or-before-features-timestamp (single fetch run, not mismatched snapshots); `fetched_at` parsed from filename so re-runs are byte-identical; per-layer error handling with `[MISSING]`/`[INVALID]` summary; exits 1 on any failure; `--dry-run` flag. Verification artifacts: `ingestion/states/montana/fixtures/spatial-test-points.json` (11 named test points across 5 kinds — 3 HDs incl. the named multi-part `MT-HD-deer-elk-lion-690-geom`, 1 portion, 4 restricted areas incl. all 3 `EXPECTED_RA_ORPHAN_IDS`, 2 CWD zones, 1 outside-all-zones negative control; every coordinate is a real `shapely.representative_point()` from loaded geometry, none invented). Operator runbook at `docs/runbooks/E02-geometry-verification.md` (264 LOC) mirrors E01's structure: prerequisites → 7 numbered sections → cleanup; all geometry-only PostGIS functions (`ST_IsValid`, `ST_NumGeometries`, `ST_Equals`, `ST_Normalize`, `ST_AsBinary`) round-trip through `ST_GeomFromText(ST_AsText(geom), 4326)` because Supabase's bundled PostGIS rejects direct `geom::geometry` casts; wipe step is scoped `DELETE FROM geometry WHERE state = 'US-MT'`; ID-parity check is symmetric; all `subprocess.run(["psql", ...])` calls use `check=True`. Two new pitfalls in `.roughly/known-pitfalls.md`: detect-secrets scan only sees git-tracked files (use `git add --intent-to-add` before refreshing baseline for new artifacts); drift-signal artifacts must write a marker for the empty case. 6 cubic-review iteration commits applied (12 findings) — most defensive correctness improvements; the `ST_Equals`-on-geography (Supabase rejects) and bare `TRUNCATE geometry` (would destroy other states) findings would have errored on first operator use. 10 manifest artifacts committed; 311 tests pass; ruff clean, mypy strict clean (6 source files), pre-commit green; PR review passed 2026-05-03
- E02 S02.6: Geometry overlay fixture — added `ingestion/states/montana/build_overlay_fixture.py` and shared `ingestion/ingestion/lib/overlays.py` (`OverlayFixtureRow` + `DroppedOverlayPair` + `ROLE_FOR_E03_BY_CHILD_KIND` TypedDicts/mapping for E03 import). Output: `ingestion/states/montana/fixtures/geometry-overlays.json` (kept) and `geometry-overlays-dropped.json` (audit log) — both deterministic, sorted, paired-atomic write. **Architectural shift confirmed during implementation:** the original spec's PostGIS cross-join SQL approach hit Supabase's role-locked 2-min `statement_timeout` on real MT data (~12k candidate pairs × ~113 KB MultiPolygons; per-row geometry detoasting dominated even with `geometry_geom_gix` reachable). Implementation moved spatial work off SQL entirely to local shapely + STRtree against a single bulk `SELECT id, kind, ST_AsText(geom) FROM geometry WHERE state = 'US-MT'` — total ~5 sec end-to-end for 349 geometries / ~26k candidate pairs across all relationship types. Three-band area-ratio discriminator (codified as **ADR-016**, accepted) replaced binary `ST_Covers`: `overlap_pct = parent.intersection(child).area / child.area`; `>=0.99 → "covers"` (digitization-tolerant), `<0.01 → drop` (boundary-touching artifact, written to audit log), otherwise `"intersects"` (genuine partial overlap). Threshold calibration is empirical to MT data; `child.area` denominator is asymmetric and serves the consumer's question ("what HDs is this child a member of?"). Pre-fix UAT showed 411 portion `intersects` rows of which 262 (<0.1% overlap) were boundary-touching noise and 102 (≥99%) were de facto covers rejected by strict `ST_Covers` — only 23 of 55 portions strictly contained. Post-fix: ~390 fixture rows (was 967), all 55 portions get ≥1 `covers`, epic invariant satisfied. Per-ID `EXPECTED_RA_ORPHAN_IDS` allowlist (Glacier NP, Sun River Game Preserve, Yellowstone NP) tolerates 3 documented no-hunt zones with code-edit + human-review gate; first implementation tolerated all RA orphans at the kind level and was rejected as too coarse during review. Two E03 handoff items recorded in the epic: (a) `kind='restricted_area'` conflates internal HD restrictions with no-hunt zones — E03 will likely need a discriminator subtype or a multi-bind role mechanic; (b) jurisdiction_binding fan-out is higher than naive estimates — median ~3 parent HDs per child after threshold filtering, with one RA tied to 16 parents. Threshold-edge tests (`overlap_pct = 0.989/0.990/0.011/0.009`) lock in the contract. Ruff/mypy/cubic clean; PR #25 merged 2026-05-01
- E02 S02.5: CWD zone discovery and ingestion — **Path A taken: standalone GIS layer found.** Investigation systematically exercised all four discovery paths (per epic spec) and documented results in `ingestion/states/montana/cwd-source-discovery.md` (189 lines): (1) MapServer root catalog scan of all 9 folders on `fwp-gis.mt.gov` plus all 39 `huntingDistricts` layers and all 27 `bigGameDistribution` layers — zero CWD matches, confirming the empirical finding from S02.4's live load; (2) Hub-catalog query via `arcgis.com/sharing/rest/search` (Hub v3 API was returning 502 Bad Gateway and opendata.arcgis.com v2 was timing out — sharing REST search worked) — surfaced 9 MT-FWP-owned CWD items including the authoritative `Chronic Wasting Disease Hunt Areas` Feature Service (ArcGIS Online item `8837c07298054f5e8be2e072681d870c`); (3) Layer #2 exclusion-filter path skipped (S02.4 had already proven zero matches); (4) Hand-traced GeoJSON fallback skipped (Path A succeeded). Selected Feature Service URL: `https://services3.arcgis.com/Cdxz8r11hT0MGzg1/arcgis/rest/services/ADMBND_HD_CWD/FeatureServer`. Added `ingestion/states/montana/load_cwd_zones.py` (214 lines) loading the FeatureServer's 2 features into `geometry` with `kind='cwd_zone'` and IDs `MT-CWD-zone-{areaname-slug}-geom`. Live load 2026-05-01 (`6fdb11b`) ingested 2 CWD zones for `REGYEAR='2026'`: Libby CWD Management Zone (OBJECTID 967) and Kalispell CWD Management Zone (OBJECTID 968). UAT documented spec-permitted name substitution: the live FWP layer publishes only 2 zones for 2026 rather than 3 (Libby, South-Central, Northeast) named in the original epic spec — the spec explicitly permits this with documentation. UAT covers Libby positive (test point `-115.555, 48.388` PASS), Kalispell positive, and an outside-zone negative-control point in eastern Montana (`-106.500, 46.800` → 0 features, PASS) so both positive and negative `ST_Covers` semantics are exercised on the same dataset. UPSERT idempotency confirmed (second run identical row counts). The shared `ingestion/states/montana/cwd_discriminator.py::is_cwd_feature` from S02.4 stays as the single definition even though the discriminator path was not taken (verified via grep). 41 unit tests covering Path A only (`tests/test_load_cwd_zones.py` 674 lines). Layer metadata fixture committed at `fixtures/ADMBND_HD_CWD-0-metadata-20260501T141952Z.json`. After S02.5: 235 hunting_district + 55 portion + 57 restricted_area + 2 cwd_zone = 349 total `geometry` rows ready for S02.6's overlay fixture. Plus a chore on this branch: `.ruckus/` renamed to `.roughly/` for workflow tooling consistency. Ruff/mypy/cubic clean; PR review passed 2026-05-01
- E02 S02.4: Restricted Areas with verbatim text — added `ingestion/states/montana/load_restricted_areas.py` (277 lines) loading MT FWP layers #2 (Big Game Restricted Areas) and #15 (Elk Restricted Areas) into the `geometry` table with `kind='restricted_area'`, deterministic IDs `MT-restricted-{bigame|elk}-{PORTIONNAME_slug}-geom`, and the five-case `verbatim_rule` combination rule from ADR-015 (`_extract_verbatim_rule_combined` covering REG only / COMMENTS only / both-differ / both-identical / both-empty cases with the literal `\n\n--- COMMENTS ---\n\n` HuntReady-introduced separator). Live load 2026-04-30 ingested 57 rows (53 from layer #2 with `license_year=NULL` since the layer carries no REGYEAR; 4 from layer #15 with `license_year=2026`); 55 of 57 rows exercise the differ-and-separator branch; 100% geometry validity. UPSERT idempotency confirmed by running the load twice with identical row counts. Added shared `ingestion/states/montana/cwd_discriminator.py::is_cwd_feature` (38 lines) imported by both S02.4 (inverse filter applied to layer #2) and pre-staged for S02.5; predicate matches `COMMENTS ILIKE '%CWD%' OR PORTIONNAME ILIKE '%chronic wasting%' OR REG ILIKE '%CWD%'`. Empirical finding: layer #2's 53 features include zero CWD-discriminator matches, so the S02.4↔S02.5 exclusion-filter path will not produce CWD zones; S02.5 must use the Hub-catalog standalone-layer search or fall through to the hand-traced GeoJSON fallback. 216 unit tests passing across S02.4 + dependents (`tests/test_load_restricted_areas.py` 748 lines, `tests/test_cwd_discriminator.py` 101 lines). Metadata fixtures committed for layers #2, #15 at `ingestion/states/montana/fixtures/huntingDistricts-{2,15}-metadata-20260501T*.json`. State-adapter README updated with field-specific CWD predicate and corrected post-load AC SQL using the Supabase ST_GeomFromText/ST_AsText workaround. Ruff/mypy/cubic clean; PR review passed 2026-04-30
- E02 S02.3: Portions ingestion — added `ingestion/states/montana/load_portions.py` (352 lines) loading MT FWP layers #4 (Antelope), #12 (Mule Deer), #13 (Whitetail), #14 (Elk) into the `geometry` table with `kind='portion'` and species-prefixed deterministic IDs (`MT-HD-antelope-`, `MT-HD-mule-deer-`, `MT-HD-whitetail-`, `MT-HD-elk-`). Live load 2026-04-30 ingested 55 portions (4 + 11 + 13 + 27); per-layer slug strategy (SHAPECODE preferred, slugified PORTIONNAME fallback, layer-wide selection) introduced in commit `ed2a05c` to handle real SHAPECODE collisions surfaced in layer #12 (`mdPt312` shared by East/West halves of district 312, `mdPt388` shared by Inside/Outside weapons-restriction halves of 388). Pre-upsert collision check fails loud listing up to 5 duplicates. SHAPECODE format gate rejects values outside `[A-Za-z0-9_-]` so malformed codes fail loud. Empty-features condition hardened from WARN to RAISE in commit `c4809bb` (silent-failure-hunter P2): zero features from a portion layer is a real fault (server-side filter, projection drift, OID resolution) and the soft-fail risked operators missing data loss in INFO-level output. `verbatim_rule` populated from `REG` for layer #14; NULL where absent. Per-feature `SourceCitation` construction with REGYEAR-derived `publication_date`, mirroring S02.2. UPSERT semantics confirmed via shared `lib/db.py::upsert_geometries` plus 661 lines of `tests/test_load_portions.py`. All four layer metadata fixtures committed; iterative live-load feature fixtures preserved for drift-detection corpus. Known-pitfalls.md deduped and reorganized in commit `a776465`. Ruff/mypy/cubic clean; PR review passed 2026-04-30
- E02 S02.2: Hunting District ingestion — added `ingestion/states/montana/load_hds.py` (190 lines) and shared `ingestion/ingestion/lib/db.py` (116 lines, `connect` + `upsert_geometries`) to load all V1 Montana HDs into the `geometry` table with `kind='hunting_district'` and species-prefixed deterministic IDs (`MT-HD-antelope-`, `MT-HD-bear-`, `MT-HD-deer-elk-lion-`). Live load 2026-04-30 ingested 235 HDs (61 + 35 + 139) from MT FWP layers #3, #10, #11; first feature fixtures committed alongside the metadata fixtures from S02.1's write path. Per-feature `SourceCitation` construction with `publication_date` derived from the row's `REGYEAR` (commit `e78a1c4`, replacing the initial implementation that used a single citation per layer). `Geometry.license_year` stays REGYEAR-or-NULL; `SourceCitation.license_year` falls back to `fetch_year` when REGYEAR is absent so every citation carries an annual cycle. `verbatim_rule` populated from `REG` source attribute; NULL where absent. UPSERT-by-id semantics confirmed via 186 lines of `tests/test_db.py`; `tests/test_load_hds.py` adds 435 lines covering ID derivation, REGYEAR extraction, error handling, and the full load pipeline. `requests.Session` lifecycle fixed via context manager (commit `4ab7cfc`). Three real-world findings institutionalized in `.ruckus/known-pitfalls.md`: Supabase PostGIS cluster-config quirk requiring `ST_GeomFromText(ST_AsText(geom), 4326)` cast for `ST_IsValid`/`ST_NumGeometries` queries; `shapely.make_valid` may emit `GeometryCollection [Polygon, LineString]` for self-intersecting source polygons (handled via area-preservation rule in `geojson_to_multipolygon_wkt`, observed on antelope HD 556 OBJECTID 385); style anchor for adding nullable text columns. State-adapter import path documented in `ingestion/states/montana/README.md` (118 lines). Named multi-part HD anchor for S02.7's verification suite: `MT-HD-deer-elk-lion-690-geom` (`ST_NumGeometries = 12`). Ruff/mypy/cubic clean; PR review passed 2026-04-30
- E02 S02.1: ArcGIS fetch infrastructure shared library — added `ingestion/ingestion/lib/arcgis.py` (state-agnostic per ADR-005) with public API: `LayerMetadata`, `ArcGISError`, `fetch_layer_metadata`, `fetch_features`, `geojson_to_multipolygon_wkt`, `build_source_citation`, `compute_feature_hash`. Pagination is metadata-driven (`outFields`/`maxRecordCount`/`object_id_field`/`spatial_reference_wkid` from `extent.spatialReference`), terminates on any empty page regardless of `exceededTransferLimit`, and has an absolute iteration cap. ArcGIS error envelope retry on transient codes (500/504/504001); HTTP 408/429 transient with `Retry-After` honored up to 30s; per-host throttling at 500ms; operator-configurable User-Agent contact via `HUNTREADY_INGESTION_CONTACT` (no PII baked into source). Projection guard: mixed-batch refusal, EPSG:3857 valid-extent pre-check, declared-CRS cross-check with WARNING for the origin-near corner case. `publication_date` per ADR-014 (Jan 1 of REGYEAR, not `lastEditDate`). Source-fixture write path (metadata + features) implemented; first fixtures land in S02.2. Promoted `pyproj` to direct dependency; added `types-shapely`/`types-requests` to dev. 74 unit tests; ruff/mypy/cubic clean; PR review passed 2026-04-29
- E02 S02.0: Schema preparation for Montana geometry ingestion — added `verbatim_rule text` (nullable) to `geometry` via migration `supabase/migrations/20260428000000_geometry_verbatim_rule.sql`; extended `SourceCitation.document_type` Literal/union with `"gis_layer"` (type-layer enforcement only per ADR-014, no SQL CHECK); three-place sync across Pydantic (`ingestion/ingestion/lib/schema.py`), TypeScript (`mcp-server/src/types/schema.ts`), and `docs/architecture.md` per ADR-006; merged 2026-04-29 (PR #18); no data loaded

## E01 — Schema Migrations, RLS, and Quality Gates — 2026-04-28

- S01.1: Installed pre-commit hooks (TypeScript `tsc --noEmit`, Python `ruff check`, secrets scanning via `detect-secrets`); secrets baseline at `.secrets.baseline`, config at `.pre-commit-config.yaml`
- S01.2: Initial migration creating all 10 entity and link tables with composite/text PKs, jsonb soft FKs, `geography(MultiPolygon, 4326)` columns, `int4range` quota ranges, CHECK constraints on every enum-like field, and 4 supporting indexes (GiST spatial + 3 query-pattern); migration at `supabase/migrations/20260425000000_initial_schema.sql`
- S01.3: RLS deny-all migration applying three-layer defense-in-depth (ENABLE + FORCE RLS, deny-all policies for `authenticated` and `anon`, explicit REVOKE) on all 10 tables; service-role bypass preserved; migration at `supabase/migrations/20260425000001_rls_deny_all.sql`
- S01.4: Pydantic models for all 18 entity and jsonb sub-model types in `ingestion/ingestion/lib/schema.py`, mirroring DDL one-to-one with `Literal` types matching CHECK constraints and `exclude_none=True` serialization for optional jsonb fields
- S01.5: TypeScript interfaces in `mcp-server/src/types/{schema.ts,index.ts}` matching architecture.md exactly; `tsc --noEmit` clean; no `any` types
- S01.6: Migration reproducibility verified against two fresh Supabase projects; cross-language type checks pass (`tsc --noEmit`, `ruff check`, `mypy`); runbook at `docs/runbooks/E01-migration-verification.md`
- E01 audit (#15) ran against all 64 ACs (53 MET, 4 PARTIALLY, 1 NOT) and applied 4 fixes: clarified `verbatim_rule` nullable convention for `jurisdiction_binding`, added missing `authenticated` curl test to runbook, replaced invalid Supabase CLI command in runbook, documented `exclude_none=True` convention in `schema.py`

## M0 — Scaffold — 2026-04-22

- Verified `.gitignore` covers Node, Python, Next.js, Supabase, OS files, and `.env*` patterns; documented gaps for pnpm and broader `.env.*` coverage
- Created `docs/planning/` directory with milestone index and M0 epic file
- Added `.env.example` documenting all required environment variables (Supabase, Postgres, Mapbox) with no real values
- Scaffolded `mcp-server/` with TypeScript, MCP SDK, Supabase client; hello-world compiles and runs
- Scaffolded `ingestion/` with Python 3.11+, pyproject.toml, pdfplumber/geopandas/shapely toolchain; package installs and imports
- Scaffolded `web/` with Next.js 15, React 19, App Router; production build succeeds
- Scaffolded `plugin/` with Claude Code plugin conventions per ADR-009; two placeholder skills (regulation-lookup, ingest-state) deferred to M5
- Created `supabase/` directory with config placeholder and migrations directory; provisioned Supabase free-tier project with PostGIS enabled
- Drafted README, updated CLAUDE.md to reflect M0 completion, created this changelog
