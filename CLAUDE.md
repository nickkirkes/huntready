# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

HuntReady is a regulatory data platform for licensed hunting in the US. Given a coordinate, species, and date, it returns applicable regulations with source citations, license/tag requirements, season windows, reporting obligations, and agency contacts. It routes hunters to authoritative state agency sources — it does not interpret or paraphrase regulations.

## Project status

**M1 complete (E01 + E02 + E03 all closed; S03.12 M1 UAT + handoff closed 2026-05-27; UAT runbook at `docs/runbooks/M1-uat.md`; M1→M2 handoff at `docs/planning/handoffs/M1-to-M2-handoff.md`; ADR-017 stands FINALIZE; Q11 + Q19 both RESOLVED — Q19 closed 2026-05-29 via PR #45 `ccbe085` per ADR-020). M2 (Colorado) in progress — E04 Complete + Audited 2026-05-31 (5 stories closed across 5 calendar days 2026-05-29 → 2026-05-31; S04.6 evaluated and omitted at planning time per CO research read-through; epic at `docs/planning/epics/completed/E04-m1-carry-forward.md`; audit at `docs/planning/epics/completed/E04-m1-carry-forward-audit.md`). **S04.2 closed 2026-05-29** via squash-merge from `feat/S04.2-narrow-binding-count-guard-band` — first M2 PR shipped per handoff §8 sixth bullet chronology lock; `_BINDING_COUNT_GUARD_BAND` in `ingestion/states/montana/load_jurisdiction_bindings.py` narrowed `(400, 1100)` → `(552, 1024)` (±30% around T16's empirical 788 measured 2026-05-28); test baseline shifted **1165 → 1166 + 2 skipped** via a new `TestCountGuard::test_band_locked_to_t16_empirical` post-merge contract-lock test closing a circularity gap in the arithmetic-derivation `TestCountGuard` refactor (the 5 boundary cases derive from the band itself, so accidental widening to e.g. `(0, 9999)` would silently pass; lock test pins the tuple's numeric value); 4 post-merge cubic-fix iterations addressed a third "pending T16" prose location at module-docstring lines 26-27 missed by spec/discovery/plan, an AC #1087 suffix-prose contradiction post-narrowing, the test-class circularity gap, and a branch-local SHA-reference portability issue; 3 new pitfalls under `.roughly/known-pitfalls.md` § "Conventions — Documentation & planning discipline" (spec-named location lists need grep-verification; spec line-number citations drift between spec authoring and execution; reviewer convergence is hard evidence not noise). No ADRs, no schema or three-place-sync changes, no `db.py` touches, no production-DB writes. AC #1087 footnote in closed E03 epic carries 2026-05-23 paragraph byte-identical plus appended **T16 narrowing 2026-05-29.** paragraph. **S04.1 closed at-merge 2026-05-30** via squash-merge from `feat/S04.1-license-season-rls-migration` — new migration `supabase/migrations/20260530132727_rls_license_season.sql` (33 LOC) ENABLES + FORCES RLS on `public.license_season` with two deny-all policies (authenticated first then anon, grep-parity with base `20260425000001_rls_deny_all.sql:29-32`) + `REVOKE ALL ON TABLE … FROM anon, authenticated`. Closes the M1 UAT criterion #7 leak surface confirmed 2026-05-28 (14 privilege leaks + 0 RLS policies). **Group A file-level ACs satisfied at merge** (timestamp ordering; 5 byte-exact SQL strings; authenticated-first policy ordering; test suite holds at 1166 + 2 skipped — no Python edits; no Pydantic / TS / `architecture.md` / `db.py` touches). **Group B operator-driven verification ACs closed 2026-05-30** — operator ran the four verification queries against the production project via `supabase db query --db-url "$DATABASE_URL"` (service-role DSN); all four returned the expected shape (Q1 info_schema_privileges 0 rows; Q2 pg_policies 2 rows with byte-exact deny-all policy names; Q3 pg_class_rls_flags both true; Q4 row_count = 2411 which is the post-UPSERT-collapse DB baseline per S04.4 spec L280 — not the ~3040 build count from handoff §3 which the verification prompt had cited in error; DDL-only migration so row count is structurally preserved). Verbatim outputs locked in the E04 epic § "Group B verification record — 2026-05-30". M1 UAT criterion #7 leak surface (14 privilege leaks + 0 RLS policies on `license_season`) is now structurally closed in production. Discipline note for future Group B-style verification prompts: cite the post-UPSERT DB baseline (S04.4 spec L280 is the durable source-of-truth) not the build count from handoff §3. One PM-flagged spec deviation captured in the migration's header: `public.` qualifier on every table reference per the spec's literal-string ACs vs. unqualified names in the base migration — both functionally equivalent with default `search_path`, AC-string parity wins. No ADRs created; no schema or three-place-sync changes; the recurring-RLS-gap M2 open-question candidate (E04 § Known Issues #1) is unchanged in scope. PM recommendation surfaced to user 2026-05-30: run `supabase db push` now, ahead of S04.3 — user accepted; operator ran the migration the same day and Group B verification queries returned clean within hours; the exploit surface is now structurally closed and S04.4 has concrete verification-output evidence available for its criterion #7 sign-off annotation. **S04.3 closed at-merge 2026-05-30** via squash-merge from `feat/S04.3-add-logging-basicConfig` as PR #49 (`5de83c3`). 4-line `logging.basicConfig(level=logging.INFO, format="%(asctime)s %(name)s %(levelname)s %(message)s")` inserted at the top of `main()` in `ingestion/states/montana/load_jurisdiction_bindings.py` (lines 785-788), parity with the 9 sibling Montana loaders (spec said 6 — actual count is 9, fix-side scope unaffected). Closes M1-UAT-2026-05-28 runpy-wrapper workaround per handoff §8 item #7. `--dry-run` now emits 25 INFO lines on stderr including the 22-row species×role cross-tab and `TOTAL: 788 bindings` (matches S04.2 T16 empirical 788 — re-confirms S04.2's `_BINDING_COUNT_GUARD_BAND = (552, 1024)` remains well-centered as of 2026-05-30). All 6 ACs satisfied; test suite stable at 1166 + 2 skipped (no delta). No ADRs, no schema changes, no new pitfalls, no open questions touched. **S04.4 closed 2026-05-30** via squash-merge from `feat/S04.4-m1-uat-runbook-hygiene-fixes` (fourth M2 PR; 4-commit chain — impl `38a0666` + plan-marker `b277571` + §6 audit-trail-preamble cubic P1 review-fix `3358166` + T4/T5 plan-dep P3 fixes `0b010a3`). Single target file `docs/runbooks/M1-uat.md` (+37/−28 LOC). 7 prescribed edits (handoff §8 #1-#6 + mandatory criterion #7 sign-off annotation referencing S04.1's `20260530132727`) + 4 Stage-6 review-fix corrections (HD 262 → HD 124 swap silently invalidated 4 downstream references caught by review triad; §1 migration count 4 → 5 to surface S04.1's migration to future operators) + 1 user-approved AC-interpretation override (§6 audit-trail-preamble between header and table to disambiguate the frozen milestone-sign-off paragraph from current production state; cubic P1 finding; user chose Option-1 italic preamble over Option-2 footnote-on-row; protected historical content byte-identical pre/post). **PM-approved adopted 7th row** to handoff §8 #4 build-vs-DB count table: `jurisdiction_binding 788 build → 788 DB (no collapse — id-keyed UPSERT)` — well-grounded against S03.10 T16 + S04.2 + S04.3 empirical 788 confirmations. **draw_spec 278-vs-276 provenance**: handoff §8 #4 cited 276 (transcription error noted in same bullet); S03.8 closure authoritative at 278; shipped runbook tracks 278 with footnote `[^10]` documenting discrepancy. Quality gates clean (1166 + 2 skipped, zero delta; ruff/mypy/cubic all clean). **Three pitfall candidates surfaced for user decision (none auto-landed — `.roughly/` is implementation territory)**: most load-bearing is "spec-prescribed string substitutions silently invalidate coupled references — grep before review catches it" (T2/T3 case); two others extend S04.1's build-vs-DB lesson and flag cubic-as-advisory-not-gating on doc diffs. **Five handoff hygiene-edit candidates** now accumulated from M2 work (path-drift; §3 build-vs-DB inconsistency; §8 #7 RESOLVED annotation; §8 #4 transcription error 276→278; §8 #4 incompleteness missing jurisdiction_binding row). No ADRs, no schema, no open questions touched. **S04.5 closed 2026-05-31** via `/roughly:build` delegation under the user's git identity (commit author `Nick Kirkes <nick@rowdycloud.io>`, Co-Authored-By trailer naming Claude — PM judgment: the no-autonomous-PRD-edit rule's purpose is explicit human control over PRD edits; the user's explicit `/roughly:build` invocation with S04.5 in scope IS explicit human control; AC reframed accordingly). 5-commit chain: `bf9bfa9` PRD 001 lines 90/96/111 reconciled (line 48 unchanged per PM review); `3445017` Bundle A appended 2 entries to `.roughly/known-pitfalls.md` § "Conventions — Documentation & planning discipline" (Entry 1 spec-prescribed string substitutions silently invalidate coupled references; Entry 2 authoritative numbers drift between canonical documents — name the source-of-truth before copying; 3rd S04.4-surfaced candidate deliberately excluded as operational reality of cubic); `37bc86a` Bundle B applied 5 hygiene edits to the M1→M2 handoff (path-drift to `completed/` 7 occurrences; §3 build-vs-DB clarifying preamble; §8 #7 RESOLVED annotation; §8 #4 transcription error 276 → 278; §8 #4 incompleteness — jurisdiction_binding 788 build → 788 DB row added); `91bde52` plan-historical marker; `eb803db` cubic post-merge fix-up correcting sentence-initial "A" capitalization on PRD 001 line 96 (content-anchored grep verification cannot catch grammar — refinement of Bundle A Entry 1). Quality gates clean: pytest 1166 + 2 skipped (no delta, 14.34s); cubic clean post-fix-up; pre-commit detect-secrets passed on all 5 commits. **E04 Complete 2026-05-31** — all 5 stories closed across 5 calendar days 2026-05-29 → 2026-05-31; all M1 carry-forward items from handoff §8 resolved in production; M1→M2 handoff is now self-consistent at §3, §8 #4, and §8 #7. The recurring-RLS-gap M2 open-question candidate (E04 §"Known Issues to Escalate" #1) remains unchanged in scope — none of the 5 E04 stories needed to resolve it. No ADRs created across E04; no schema changes; no Colorado data; no CO-specific code. Post-implementation audit closed 2026-05-31 (squash-merged as `b168d28` from `test/E04-implementation-audit`; 2 pre-squash commits — `7478ea6` S04.1 header fix + `c25d160` audit report). 49 ACs reviewed across 5 stories; 47 MET on direct file/commit inspection, 2 operator-asserted with internally-consistent documented stdout; no blocking findings. Hard-locked merge order S04.2 → S04.1 → S04.3 → S04.4 → S04.5 verified via PR-merge timestamps; all three cross-story dependency handoffs satisfied; no regressions. Single actionable finding (S04.1 migration header missing `public.`-qualifier rationale from base migration) resolved at `7478ea6` as a comment-only header edit — zero DDL impact, the migration's production behavior is unchanged. Audit report at `docs/planning/epics/completed/E04-m1-carry-forward-audit.md` mirrors the E02 audit's durable-artifact pattern. Emergent theme captured: 9 post-merge fix-up commits across E04, all caught pre-production by reviewers/cubic; the convention-hardening response is already on disk (S04.2's 3 new pitfalls + S04.5 Bundle A's 2 new entries grew `.roughly/known-pitfalls.md` § "Documentation & planning discipline" from 6 → 11 entries across M2-W1 as deliberate convention-maturing, not recurring quality issues). **Post-implementation-audit standard locked** for E05 and beyond: each future epic closes with a post-implementation audit recorded under `docs/planning/epics/completed/E0X-audit.md` before `/plan-next-epic` is invoked for the following epic; PM refuses-and-flags any `/plan-next-epic` invocation where the prior epic's `Audited:` field is unpopulated. **E05 (Colorado geometry ingestion) planned + validated 2026-05-31** via `/plan-next-epic`. Epic at `docs/planning/epics/E05-colorado-geometry-ingestion.md` (688 LOC; 8 stories S05.0–S05.7; recommended first story S05.0 — write `CO-STATEWIDE-geom`). Validation triad (Spatial Correctness + ArcGIS Fidelity + Schema Stress-Test) returned LAND-WITH-EDITS; 14 MUST-FIX + 9 SHOULD-FIX findings applied at draft time. **Pre-implementation review closed 2026-05-31** (verdict Ready/LAND-WITH-MINOR-EDITS; review artifact at `docs/planning/epics/E05-colorado-geometry-ingestion-review.md`); **7 review-fix edits applied to the epic** in PR #53 (`fd989b0`): S05.6 line-citation drift correction (`load_jurisdiction_bindings.py:111 → :110`; `:587/740 → :587` — `:740` is `_load_non_statewide_reg_records`, not the binding-query); S05.5 `OverlayChildKind` snippet correction across 3 sites (existing 4 values incl. `hunting_district` → proposed 5 adding `gmu`); defensive defaults on `EXPECTED_CO_RA_ORPHAN_IDS` (defaults to `frozenset()` if S05.4 outcome (b) fires); S05.7 multipart fixture-read N/A documented for empty list; docstring-sync guidance; AC edge-test pattern note. Cubic clean post-edits. **Validation-triad output was unusually-hardened** — residual review work was minor (cross-cutting risk surfaced: line-citation drift is the most likely E05 failure mode; recommend Stage-0 grep audit at each story open per Bundle A pitfall #1). **S05.0 closed at-merge 2026-06-01** via PR #54 / `7f3b071` from `feat/S05.0-write-co-statewide-geom-row` (5 pre-squash commits: 1 implementation + 1 plan-historical + 1 pitfalls + 2 cubic-fixes; first E05 PR). **Source choice locked**: Tier 3 — US Census TIGER 2024 state shapefile (`https://www2.census.gov/geo/tiger/TIGER2024/STATE/tl_2024_us_state.zip`) with SHA-256 `ad00cbe66c7177091b668cee202e93d4a1ddcee271c28d1c9f9874af59c04b92` <!-- pragma: allowlist secret --> pinned per ADR-001 fail-loud; `SourceCitation.id='co-census-tiger-state-2024'`; `publication_date='2026-01-01'` (REGYEAR-anchored per ADR-014, NOT TIGER vintage 2024-01-01); Tiers 1 (CPW-published) and 2 (Colorado state library) were not repo-confirmed within investigation budget — fell through to Tier-3 fallback (inverse of S03.0's MSDI-beats-listed-tiers deviation). **Stage-gate trajectory**: Stage-4 plan review Round 1 NEEDS REVISION → Round 2 PASS (Blocker: bare `gdf.to_crs(4326)` would have produced ~100m systematic NAD83-as-WGS84 offset undetectable by AC #8 area check; fixed by explicit reassignment + load-bearing comment block; Concern: `urllib.request` → `requests.Session` for project-convention parity). Stage-6 parallel review caught 7 actionable findings landed in 1 review-fix cycle (P0 detect-secrets baseline; P1 ZipFile resource leak; P1 empty body diagnostic; P1 multiple .shp silent first-alphabetical; P1 NULL geometry; P2 missing STATEFP column; P3 overbroad test exception tuple). **2 cubic-fix iterations**: Iter 1 removed `--service-url` override creating silent-lie citation surface; Iter 2 added missing variable binding in pitfall example. **9 fail-loud guards** in the loader (7 + 1 SHA + 1 non-MultiPolygon post-make_valid). **4 new pitfalls landed** in `.roughly/known-pitfalls.md` (2 under Integration — Census TIGER + geopandas covering the `to_crs` reassignment trap + TIGER native EPSG:4269; 2 under Conventions — Pre-commit & secrets covering detect-secrets baseline interactions). **Test baseline shifted: 1166 → 1184 + 2 skipped** (+18 from new `test_load_co_state_boundary.py` unit tests — deliberate quality addition, AC carve-out satisfied; new 1184 baseline holds across S05.1-S05.7). **Group B operator-driven verification ACs remain open** (AC #7 post-write verification queries + AC #8 area_km2 within 1% of ~269,837 km²) pending operator-run loader against production Supabase service-role DSN — mirrors S04.1's operator-verifies-live pattern; operator runbook in `docs/planning/epics/E05-confidence-findings/S05.0.md` (working-notes directory; M2 analog of M1's `E03-confidence-findings/`; deletes at `m2` tag per ADR-017 §6). **Two M2 hygiene candidates queued** (flagged to user for decision; PM does NOT touch `ingestion/` autonomously): (1) `db.upsert_geometry` missing `cur.rowcount == 0` fail-loud guard (inconsistent with post-S03.6 pattern; low-risk today but surfaces if DDL ever changes to ON CONFLICT DO NOTHING); (2) MT `load_state_boundary.py --service-url` provenance silent-lie (same latent issue cubic-fixed in CO). **No ADRs created**; **no schema or three-place-sync changes**; **no `db.py` touches**; **no production-database writes from the build session**. **S05.1 closed at-merge 2026-06-02** via PR #55 / `ba6ef1c` from `feat/S05.1-cpw-arcgis-fetch-infra` (3 pre-squash commits: 1 implementation + 1 plan-historical + 1 doc-writer pitfalls update; second E05 PR). **Scaffold-only story**: 5 deliverable files (CO `__init__.py` verify-only; `README.md` 113 LOC / 8 h2 sections; `sources.yaml` 35 LOC using `gis_layers:` top-level key — deliberate divergence from MT's `pdfs:`-only structure since CO at S05.1 has no PDFs; `fixtures/.gitignore` 17 LOC trimmed from MT's 45-line template; `ingestion/tests/test_arcgis.py` appended with `TestNoColoradoLeakIntoSharedLib` class at +95 insertions). **Hard constraint held — `git diff --stat ingestion/ingestion/lib/` empty across all 3 pre-squash commits** (AC #6 + the broader zero-lib-edit constraint; the new leak-guard test locks this property going forward as the canonical ADR-005 enforcement mechanism for CO). **Stage-gate trajectory**: Stage-4 plan review PASS on first iteration with 4 minor findings (2 plan edits pre-Stage-5: line-number citation removed; detect-secrets pattern flipped from inline pragma to baseline-merge per S05.0 precedent). Stage-6 triad: **4 actionable findings landed in 1 review-fix cycle** (LOW `-> list` → `-> list[Path]`; W1 class-body lowercase invariant for `_CPW_HOST_LITERALS`; W2 inline non-empty guards for pytest-randomly safety; INFO inline anti-direct-read YAML comment on `id_template`). **1 mid-Stage-6 cubic-fix**: `-> list[Path]` annotation triggered ruff F821 because Path was only imported in method body — PEP 563 makes the annotation a string at runtime but ruff's static analysis still flags; fix hoisted `from pathlib import Path` to module-level imports. **1 post-merge cubic finding empirically invalidated**: cubic flagged `assert all(s == s.lower() for s in _CPW_HOST_LITERALS)` at class-body level as a P1 NameError-at-import-time risk; empirically demonstrated invalid via isolated 4-line repro that DID raise AssertionError on a mixed-case entry — generator expression's leftmost iterable is evaluated in class scope per Python language reference § 6.2.4 (only the inner expression + subsequent for/if clauses run in implicit function scope); pattern correct, finding rejected. **PM-approved spec deviations baked in** (both validated at Stage 1 + Stage 2 discovery; flagged MINOR-not-blocking at Stage 4): (1) Epic AC text `_STATE = 'US-CO'` convention — S05.0's actual code uses `CO_STATE_CODE = "US-CO"`; README documents the current name with explicit S05.6 forward-reference rather than pre-documenting a constant that doesn't exist yet (preserves doc/code alignment); (2) Epic AC text `from ingestion.lib.arcgis import ...` — codebase-actual convention is `from ingestion.lib import arcgis, db` (verified in both `load_state_boundary.py` files); README uses codebase-actual form. **4 new pitfalls landed in `.roughly/known-pitfalls.md`** (file grew 887 → 923 LOC; doc-writer dispatched via Stage 8 step 6): (1) Conventions — Python (new section): PEP 563 deferred annotations do not satisfy ruff F821 — hoist annotation-only names to module level; (2) Conventions — Testing (new section), entry 1: Class-body tuple invariants must be locked by an inline `assert all(...)` immediately after the tuple; (3) Conventions — Testing (new section), entry 2: Multi-method test classes — each scan method must be self-sufficient with an inline `assert <collection>, …` guard rather than relying on a sibling sanity-check running first (pytest-randomly would break definition-order assumptions silently); (4) Conventions — Ingestion adapters (existing section): YAML fields with placeholder substrings (`{license_year}`-style) need an inline comment warning against direct read — surfaced during silent-failure hunt on `sources.yaml`'s `id_template` field. **Test baseline shifted: 1184 → 1187 + 2 skipped** (+3 from the new `TestNoColoradoLeakIntoSharedLib` class — deliberate quality addition, satisfies AC carve-out; new 1187 baseline holds going forward across S05.2-S05.7). **Quality gates final state at-merge**: ruff clean across `ingestion/` + `tests/`; mypy clean on `ingestion/lib/` (8 source files) AND `ingestion/states/colorado/` (2 source files); pytest 1187 + 2 skipped in ~14s; pre-commit detect-secrets passed on all 3 commits (with 1 routine baseline line-number refresh on the pitfalls commit). **No ADRs created**; implementation refines ADR-003 (ingestion upstream + offline), ADR-005 (the new `TestNoColoradoLeakIntoSharedLib` is the canonical enforcement mechanism for ADR-005 going forward for CO), and ADR-014. Q19 stays RESOLVED via ADR-020. **`_STATE` constant codification deferred to S05.6** — README documents `CO_STATE_CODE` as current; S05.6's spec carries the unification across all CO loaders. **Two M2 hygiene candidates from S05.0 still pending** (unchanged in scope; not S05.1 scope): `db.upsert_geometry` rowcount guard + MT `load_state_boundary.py --service-url` removal. **S05.2 closed at-merge 2026-06-02** via PR #56 / `d7ba731` from `feat/S05.2-GMU-ingestion` (2 pre-squash commits: `0b38758` implementation + tests; `f8087e1` plan-historical marker; third E05 PR). Single new loader at `ingestion/states/colorado/load_gmus.py` (409 LOC) + comprehensive test suite at `ingestion/tests/test_load_gmus.py` (733 LOC, 47 tests). Mirrors `montana/load_hds.py` structure with single-layer simplifications. **Loader shape** (state-agnostic-clean; no `ingestion/lib/` edits; `TestNoColoradoLeakIntoSharedLib` from S05.1 continues to lock): pure helpers `_extract_gmuid` / `_extract_county`; `_feature_to_geometry` normalization; `_collect_multipart_gmus` + `_write_multipart_fixture`; guards `_duplicate_ids` + `_check_count_band`. **Fetch+build split from DB write** (`_fetch_and_build` / `_write_geometries`) so count-band + duplicate-id guards fire BEFORE `db.connect()` per OQ7 discipline. `main()` carries function-level logger, `logging.basicConfig`, no `--service-url` flag (S05.0 cubic-fix silent-lie-citation precedent honored). **Group A satisfied at-merge** (loader code + 47 mocked tests; all quality gates green; no live network; no DB write). **Group B operator-pending**: live run against CPW FeatureServer + production service-role DSN to (a) commit real metadata + manifest + populated `multipart-gmus.json` fixtures; (b) `returnCountOnly=true` row-count cross-check; (c) post-insert `ST_IsValid` round-trip verification — mirrors S05.0 Group B. Operator capture in `docs/planning/epics/E05-confidence-findings/S05.2.md` when run. **Key decisions baked in**: (1) `id` derived from `GMUID` (NEVER `OBJECTID` / `metadata.object_id_field`); locked by spec-required regression test `test_geometry_id_derived_from_gmuid_not_objectid`. (2) `license_year=None` on `Geometry` row (CPW layer 6 has no per-feature REGYEAR); `SourceCitation.license_year=2026` → `publication_date='2026-01-01'` per ADR-014; citation id `co-cpw-arcgis-CPWAdminData-6-2026` (service slug **case-preserved** from URL — NOT the lowercase `id_template` in `sources.yaml`, which remains documentation-only per S05.1's anti-direct-read warning). (3) **AC #235 satisfied as LOG-ONLY (documented deviation)** — CPW `EDIT_DATE`/`INPUT_DATE` emitted at INFO for forensics but not persisted; `SourceCitation` is frozen with `extra="forbid"` (no `notes`/extension field) and E05 forbids schema migrations; silent-failure-hunter independently confirmed this is a genuine constraint, not a silent omission; recurrence of S02.5 obs #1016 finding. (4) `ColoradoGeometryError` defined new in the loader; per-feature loop catches shared lib's `ArcGISError` and re-raises with `GMUID` prepended (exc chain preserved). (5) Count-band guard `[167, 205]` (186 ±10%) fires pre-`db.connect()`. (6) `multipart-gmus.json` schema `{gmuid, part_count, total_area_sq_km}` with `total_area_sq_km=None` deliberately — planar WKT degrees can't yield faithful km²; authoritative `ST_Area(geom::geography)/1e6` deferred to S05.7's PostGIS analytical layer. **Stage-gate trajectory**: Plan review Round 1 NEEDS REVISION (2 Blockers: stale `args.service_url_const` → AttributeError; missing detect-secrets baseline step — both fixed; 3 Concerns: shapely-wkt import threading, `_load_layer` retraction ambiguity, ruff path) → Round 2 PASS. Stage-6 triad: all actionable findings fixed in 1 review-fix cycle, then cubic clean. Fixes: [Critical] docstring `kind='hunting_district'` → `kind='gmu'`; [P1] removed dead module-level `_logger`; [Critical/SFH] `_collect_multipart_gmus` wrapped `wkt.loads()` + `int(gmuid)` in fail-loud try/except; [P1] `Connection[Any]` → `Connection[tuple[object, ...]]`; [Info] docstring "DEBUG" → "INFO"; [Tests +3] `test_main_no_commit_on_duplicate_id_guard`, `test_main_no_commit_on_count_band_guard` (lock OQ7 ordering), `test_no_other_state_adapter_imports` (broadened ADR-005 isolation guard beyond Montana). **detect-secrets baseline refresh proved unnecessary** — CPW org ID in URL string isn't flagged (same ID already ships in S05.1's `sources.yaml`/`README.md`), and this loader pins no SHA-256. **Test baseline shifted: 1187 → 1234 + 2 skipped** (+47 from `test_load_gmus.py` — deliberate quality addition; new 1234 baseline holds going forward across S05.3-S05.7). **No ADRs created**; refines ADR-001 (fail-loud), ADR-005 (CO isolation — `TestNoLibImports` / broadened guard), ADR-010 (MultiPolygon/geography), ADR-014 (gis_layer + REGYEAR-anchored `publication_date`). **No schema or three-place-sync changes** (`geometry.kind='gmu'` already in the Literal); no `db.py` touches; no production-DB writes from the build session. Q18 (CWD sampling target-table) untouched — S05.3 territory. Q19 stays RESOLVED via ADR-020. **Two M2 hygiene candidates from S05.0 still pending** (unchanged in scope; not S05.2 scope): `db.upsert_geometry` rowcount guard + MT `load_state_boundary.py --service-url` removal. **S05.3 closed as documented gap 2026-06-03** via PR #57 / `9fd5c61` from `feat/S05.3-cwd-zone-discovery` (3 pre-squash commits: `c539fac` deliverables + `06022f7` plan-historical + `026e322` pitfalls/baseline; fourth E05 PR). **Decisive finding**: live investigation across all 3 spec'd paths conclusively found CPW publishes **no CWD-zone geometry anywhere** — Path 1 (CPWAdminData service-root catalog, 30 layers enumerated; none match `cwd|chronic|disease|wasting` — GMU/DAU/District/Area/Region boundaries + properties/trails/aquatic only) = NO; Path 2 (ArcGIS Online org-scoped search `orgid:ttNGmDvKQA7oeDQ3` + full ~200-service CPW hosted-org listing — only `CPW_ANS_Sampling_*` Aquatic Nuisance Species / `CPWHPH*` near-misses; broad public search returns third-party academic/USGS only) = NO; Path 3 (PDF hand-trace) = nothing polygonal to trace — Colorado manages CWD by **hunt code / GMU**, not mapped zone polygons (2026: no mandatory deer testing; mandatory elk submission by specific rifle hunt codes per Big Game Brochure pp. 41-52; USGS reports CO CWD positives by wildlife-management-unit). Hand-tracing a prevalence/monitoring map rejected per ADR-001 (not an authoritative regulatory boundary). Path (c) (GMU-attribute filter) pre-recorded NO; confirmed — no CWD flag on GMU layer 6. **Outcome = documented gap** (analog of S05.4 outcome (b)): zero `geometry` rows written; no loader created (`load_cwd_zones.py` not added); `sources.yaml` not extended; no tests; no schema/three-place-sync; no `db.py` touch; no production-DB write. Investigation report committed at `ingestion/states/colorado/cwd-source-discovery.md` — 3-path investigation report (live evidence: 30-layer catalog table, org-scan, USGS/CPW unit-keyed confirmation), MT contrast table (MT had 2 discrete zones Libby + Kalispell on dedicated `ADMBND_HD_CWD` service; CO's model is fundamentally unit/hunt-code-keyed), prominent ## Decision, Q18 finding, UAT-N/A disposition. Lives in the adapter dir (survives `m2` tag), matching MT's placement. Downstream S05.5/S05.7 `cwd_zone` coverage invariants are **vacuously satisfied** (zero rows). Test suite **unchanged at 1234 + 2 skipped**. **Stage-gate trajectory**: plan review NEEDS REVISION → PASS (1 blocker: T3 omitted S05.5 `cwd_zone` orphan-invariant lines 425/443; fixed). Doc accuracy/consistency review (adapted Stage-6 for doc-only change) = PASS with 2 nits fixed (ANS service-name accuracy; 2 drift-prone line-citations → text anchors per the project's own line-drift pitfall); reviewer independently re-ran live ArcGIS probes confirming the report byte-accurate. verify-all clean. **3 new pitfalls landed in `.roughly/known-pitfalls.md`** (923 → 949 LOC): (1) gap-closure-is-valid (zero-row outcomes are first-class story closures, not failure modes); (2) "one of A/B" ACs omit the gap branch (third path must be enumerated when investigation can find nothing); (3) empirical-trigger-evidence-not-status-change (Q18 status deliberately left Open; the formal decision is E06's). **Q18 empirically resolved** per its named trigger ("second CWD-state lands (Colorado)"): the license/unit-keyed CWD model is the general pattern, not an MT quirk, and zone-keyed binding is **structurally unavailable** for CO (no zone to key on); evidence breadcrumb dated in `docs/open-questions.md`. **PM recommendation surfaced for E06**: retain Q18's V1 license-keyed disposition (0 typed CWD `reporting_obligation` rows; text in `regulation_record.additional_rules`); final call is E06's, to be made BEFORE E06's reporting-obligation loader specs draft. **No ADRs created**; refines ADR-001 (no-invented-data — hand-trace rejection). Q19 stays RESOLVED via ADR-020. The recurring-RLS-gap M2 open-question candidate (E04 §"Known Issues to Escalate" #1) is unchanged in scope — S05.3 added no `public.*` tables. **Two M2 hygiene candidates from S05.0 still pending** (unchanged in scope; not S05.3 scope): `db.upsert_geometry` rowcount guard + MT `load_state_boundary.py --service-url` removal. **S05.2 Group B (live CPW fetch + DB write)** remains operator-deferred / open. **S05.4 remains hard-blocked** on the `colorado-restricted-areas-evaluation.md` research-doc prerequisite — human or research-drafting session deliverable; S05.5 / S05.6 / S05.7 cannot start until S05.4 resolves (S05.5 depends on S05.2-S05.4 outputs per epic §"Parallelization Notes"). **S05.4 hard precondition resolved 2026-06-03** — research doc landed at `ed721c4` (`docs/research/colorado-restricted-areas-evaluation.md`, outcome (c): 10 V1-scoped CO no-hunt zones via PAD-US 4.1 Federal Fee Managers Authoritative FeatureServer; 4 NPs + 5 NMs + AFA = 10 zones, Curecanti NRA dropped per 36 CFR §2.2). **ADR-021 landed 2026-06-03** at `fa1da38` (Status: Proposed pre-implementation; flips to Accepted on S05.3.5 migration ship) — extends `jurisdiction_binding.role` CHECK constraint enum 7 → 8 by adding `'no_hunt_zone'`, with 5-place sync (DDL + Pydantic + TypeScript + architecture.md + `ingestion/ingestion/lib/overlays.py` `GeometryRoleForE03` Literal alias) and MT V1 reclassification of 3 existing no-hunt-zone rows (`MT-restricted-bigame-glacier-national-park-geom` / `MT-restricted-bigame-sun-river-game-preserve-geom` / `MT-restricted-bigame-yellowstone-national-park-geom`) from `'other_overlay'` → `'no_hunt_zone'`. **E05 epic extended 2026-06-03 to 9 stories** via S05.3.5 carve-out — ADR-021's migration is a pre-work story for S05.4, mirroring the S03.6.1 pattern (MT-touching + schema-extending in one PR; CO-loading in the next). Merge order becomes `S05.0 → S05.1 → S05.2 → S05.3 → **S05.3.5** → S05.4 → S05.5 → S05.6 → S05.7`. Group A/B AC split mirrors S04.1 (file-level/static at-merge; operator-driven CHECK-constraint shape + reclassification + row-count verification post-`supabase db push`). **S05.3.5 closed at-merge 2026-06-03** via PR #58 / `3344971` from `feat/S05.3.5-no-hunt-zone-role` (3 pre-squash commits: `d5c9aa7` implementation + `bc018bc` plan-historical + `de21196` pitfalls/E06-candidate; fifth E05 PR; carve-out story mirroring S03.6.1). ADR-021 implemented end-to-end: new migration `supabase/migrations/20260603000000_jurisdiction_binding_no_hunt_zone_role.sql` (single atomic `BEGIN; … COMMIT;` mirroring S03.0's `_e03_schema_additions.sql` pattern — `DROP CONSTRAINT jurisdiction_binding_role_check` + `ADD CONSTRAINT … CHECK (8 values)` + `UPDATE … SET role='no_hunt_zone' WHERE geometry_id IN (3 ids)`, in that order — UPDATE must follow ADD because the new value violates the old constraint; UPDATE keys on `geometry_id` not binding `id` to catch all fan-out binding rows per zone; migration count 5 → 6). **Five sync surfaces, all 8 values consistent order**: DDL migration + `ingestion/ingestion/lib/schema.py` `JurisdictionBinding.role` Literal + `ingestion/ingestion/lib/overlays.py` `GeometryRoleForE03` alias (plus 2 docstring citations refreshed `schema.py:424-432` → `:426-435` and "seven"/"Seven" → "eight"/"Eight") + `mcp-server/src/types/schema.ts` `GeometryRole` union + `docs/architecture.md` §"Schema types" `GeometryRole` type. **MT V1 reclassification**: 3 federal no-hunt-zone bindings (`MT-restricted-bigame-glacier-national-park-geom` / `MT-restricted-bigame-sun-river-game-preserve-geom` / `MT-restricted-bigame-yellowstone-national-park-geom`) reclassified `'other_overlay'` → `'no_hunt_zone'`; ids byte-identical to `build_overlay_fixture.py:236-242` `EXPECTED_RA_ORPHAN_IDS`. **MT loader** `load_jurisdiction_bindings.py`: `_build_no_hunt_zone_bindings` docstring de-staled (removed false "only DDL-permitted role" / "7-value enum" claims); hardcoded default `Literal["other_overlay"]="other_overlay"` → `["no_hunt_zone"]="no_hunt_zone"`; `cast(Literal[…], role_e03)` in `_build_overlay_bindings` extended for mypy correctness (spec-gap closure — see below). **MT tests**: 4 sites reworked (set-equality assertion; `test_role_is_other_overlay_only` → `test_role_is_no_hunt_zone` rename + body; `_valid_roles` frozenset → 8 values; role-enum docstring claims); **N=0 test delta** (all rewrites, no additions). **ADR-021 flipped Proposed → Accepted** (status-note blockquote refreshed; ADR-021 added to `docs/adrs/README.md` index table which had been missing it). **Spec gap closed at Stage-2 discovery**: the spec's 5 named sync sites missed a 6th consumer — the `cast(Literal[…7 roles…], role_e03)` in `_build_overlay_bindings`. Once `GeometryRoleForE03` grew to 8, this cast had to grow too. Discovery caught it; folded into T5; recorded as new pitfall under § "Conventions — Documentation & planning discipline" (enum-extension sync surfaces: spec-named sites are necessary-not-sufficient — grep for all `cast` / `Literal` / `frozenset` consumers). **`_VALID_ROLE_FOR_E03` deliberately NOT widened**: this 4-value frozenset is an intentional subset gating overlay-fixture rows. MT no-hunt zones are orphans on a separate hardcoded builder, never fixture rows — zero MT impact, and widening would mask a future bug. Stage-4 plan review explicitly excluded it. Logged as E05 epic Known Issues item #6 (E06 candidate): E06's CO binding-loader must decide whether CO no-hunt zones flow through its overlay-fixture path (gate must admit the value) or a separate hardcoded path (gate stays narrow). Cross-referenced as second new pitfall under § "Conventions — Ingestion adapters" (subset gates are intentionally narrower than the full enum — document the exclusion, don't reflexively widen). **Binding-id slug drift = accepted V1 artifact**: the 3 reclassified rows keep `other_overlay` as an `id` substring while `role` becomes `no_hunt_zone`. Confirmed inert: grep across `ingestion/` + `mcp-server/` found zero code parsing `role` out of `id`; UPSERT omits `role` from `ON CONFLICT SET` so re-runs stay consistent. Id deliberately untouched. **Line-citation drift held minimal**: Pydantic `:424-432`→`:426-434` (+2); loader docstring `:599-608`→`:596-609` (+3); all others exact; grep-verified per Bundle A pitfall #1; drift caused zero misses. **Quality gates (at-merge)**: ruff clean; mypy `ingestion/lib/` (8 files) + MT loader clean; pytest **1234 passed + 2 skipped** (no delta — baseline holds across S05.3 + S05.3.5); tsc `mcp-server` + `web` exit 0; cubic review `{"issues": []}`; pre-commit (tsc/ruff/detect-secrets) green on all 3 commits (1 routine `.secrets.baseline` line-number refresh on the docs commit). CO scope empty (`git diff --stat ingestion/states/colorado/` empty — AC #12 held). No production-DB writes from the build session. **No new ADRs created** — ADR-021 flipped status; this is a five-place sync (stricter superset of ADR-006's three-place discipline). Q19 stays RESOLVED via ADR-020 (`upsert_jurisdiction_binding` already excludes identity fields from UPDATE; no Q19 drift-guard needed here). **2 new pitfalls landed in `.roughly/known-pitfalls.md`** (949 → 964 LOC; doc-writer flagged the file for future reorg/dedup). **Group B operator-pending** (per S04.1 "operator verifies live" pattern; not blocking close): after `supabase db push`, verify (1) `information_schema.check_constraints` shows the 8-value `jurisdiction_binding_role_check`, (2) `SELECT DISTINCT role` ⊆ the 8-value set, (3) the 3 MT rows now read `role='no_hunt_zone'`, (4) row count unchanged pre/post (DDL + 3-row UPDATE; no INSERT/DELETE). Capture in `docs/planning/epics/E05-confidence-findings/S05.3.5.md` § "Group B verification record"; PM ticks the Group B boxes in a follow-up doc-only commit once captured. This live verification is the hard precondition that lets S05.4's PR open with the migration confirmed live (can run in parallel with S05.4 implementation). **S05.4 now unblocked at the schema layer** — DDL CHECK permits `role='no_hunt_zone'`; research-doc prerequisite (`docs/research/colorado-restricted-areas-evaluation.md`, outcome (c): 10 V1 CO no-hunt zones via PAD-US 4.1 Federal Fee Managers Authoritative FeatureServer; 4 NPs + 5 NMs + AFA; Curecanti NRA dropped per 36 CFR §2.2) landed at `ed721c4`. **S05.4 closed at-merge 2026-06-04** via PR #59 / `dc9d5b2` from `feat/S05.4-restricted-area-no-hunt-zone-overlay-discovery` (4 pre-squash commits: `5427285` implementation + `f81ab14` plan-historical + `3c21fc1` pitfalls + `2715bb9` post-review malformed-properties fix; plus `31cbc24` discovery-report accuracy fix folded in pre-merge; sixth E05 PR). **Outcome (c) realized**: 10 V1 federal no-hunt zones ingested from the USGS PAD-US 4.1 Federal Fee Managers Authoritative FeatureServer (`services.arcgis.com/v01gqwM5QqNysAAi/.../Federal_Fee_Managers_Authoritative_PADUS/FeatureServer/0`). New loader `ingestion/states/colorado/load_restricted_areas.py` (~520 LOC, state-agnostic-clean per AST guard) writes **10 `geometry` rows** with `kind='restricted_area'`, ids `CO-restricted-{slug}-geom`, `geography(MultiPolygon, 4326)` + valid. The 10: 4 NPs (Rocky Mountain, Mesa Verde, Great Sand Dunes, Black Canyon of the Gunnison) + 5 NMs (Dinosaur, Colorado NM, Florissant Fossil Beds, Hovenweep, Yucca House) + 1 DOD (United States Air Force Academy). The WHERE clause returns 11 (10 NPS incl. Curecanti NRA + 1 DOD AFA); **Curecanti dropped post-fetch** per 36 CFR §2.2 (NPS NRAs permit hunting) → 10 written. **Geometry rows ONLY — no `jurisdiction_binding` writes** (E06 territory per epic §"PRD/schema correction"; there is no `role` column on `geometry`). The `role='no_hunt_zone'` assignment is E06's binding-loader job; S05.4 documents the boundary-to-boundary nearby predicate `extensions.ST_DWithin(zone.geom, gmu.geom, 5000)` and seeds the orphan-id list for downstream use. **Key decisions baked in**: (1) **`verbatim_rule=None` for V1 (PM-confirmed deferral)** — PAD-US carries geometry only; the CPW Big Game brochure (the would-be `verbatim_rule` source) URL is unresolved (404 on 2026-06-03). No text fabricated per ADR-001/ADR-008. The brochure is documented as a precondition for future text-population (E06), including the unresolved single-source-field provenance question (PAD-US geometry vs CPW brochure text). Live URL resolution + text population deferred. (2) **`source.document_type='gis_layer'`** per AC #450 (geometry from PAD-US/ArcGIS). Single shared `SourceCitation` for all 10 rows: id `co-usgs-padus-arcgis-Federal_Fee_Managers_Authoritative_PADUS-0-2026` (service slug case-preserved; derive-and-asserted in test, not hardcoded blind), `publication_date='2026-01-01'` (fetch-year Jan 1 per ADR-014, NOT `editingInfo.lastEditDate`), `agency="U.S. Geological Survey (PAD-US)"`. (3) **Research-doc count correction 9 → 10** — the research doc's prose said "9 zones" in three places (lines 17/64/79 — a 4+5+1 arithmetic slip; the enumerated named list + fetch-minus-Curecanti math both give 10). Resolved to 10 per the "name the source-of-truth before copying numbers" pitfall; documented in the discovery report's count-reconciliation section. **Count-band guard is exact `(10, 10)`** — 9 or 11 fails loud (11 = Curecanti leak). (4) **Zero `ingestion/lib/` edits** (ADR-005). `fetch_features` hardcodes its WHERE clause (`{oid}>=0`) so it can't apply the V1 filter; rather than edit lib or replicate the pagination loop, the loader composes existing private `arcgis` primitives (`_build_session`, `_request_with_retry`, `_check_and_fix_projection`, `_write_features_fixture`, `_write_manifest_fixture`, `compute_feature_hash`, `_read_objectid`, `_utc_timestamp`) into a bounded single-page fetch with an `exceededTransferLimit` + count-match assert (valid because the V1 set is ~11 features << max_record_count). CO-leak guard test locks `ingestion/lib/` empty. No `db.py` edits; `db.upsert_geometries` reused as-is. (5) **No `--service-url` flag** (S05.0 cubic-fix silent-lie-citation precedent). Guards (`_duplicate_ids`, `_check_count_band`, `_assert_curecanti_dropped`, `_V1_EXPECTED_IDS` identity check) all fire pre-`db.connect()` per OQ7. **Stage-gate trajectory**: Plan review (Stage 4) PASS first iteration with 2 inline-applied concerns (timestamp threading for fixture writers; 9→10 count documented across all 3 research-doc slip sites); reviewer independently verified the 10-count arithmetic, `_slug_from_service` citation-id derivation, all 10 id slugs, every composed lib signature. **Stage-6 triad caught a convergent Critical** (both code-reviewer + silent-failure-hunter): `_V1_EXPECTED_IDS` existed with a module-load length-assert + isolated test but was never wired as a runtime guard in `main()` — a PAD-US `Unit_Nm` rename would write a wrong-id row silently. 1 review-fix cycle resolved 2 Criticals: (C1) wired the `_V1_EXPECTED_IDS` set-equality guard into `main()`; (C2) moved the Curecanti drop **before** the fixture/manifest writes so fixtures, manifest `features_count`, and DB all agree at 10 (was 11 vs 10). Plus P1 (explicit missing-`features`-key → `ArcGISError`) and P2 (`.strip()`-hardened Curecanti drop). Cubic clean `{"issues": []}` on first pass. **Two post-merge-review fixes** (external PR review, both valid): (a) `_feature_to_geometry` raw-subscripted `feature["properties"]` and forwarded to `.get()` — a malformed/null-`properties` feature escaped the fail-loud contract as a bare `KeyError`/`AttributeError`; now guarded → `ColoradoGeometryError` with context (`2715bb9`, +2 tests). (b) Discovery-report MT-contrast paragraph wrongly claimed MT geometry flows through USGS PAD-US (contradicting its own table) — MT's 3 zones were ingested in E02 from MT FWP's own ArcGIS layers (MtFishWildlifeParks AGOL / `fwp-gis.mt.gov`), NOT PAD-US; corrected to state the true shared pattern (split-provenance) and the differing geometry source (`31cbc24`). **Quality gates (final, on main)**: ruff clean (`ingestion/` + `tests/`); mypy clean (`ingestion/lib/` 8 files + CO loader); pytest **1283 passed + 2 skipped** (baseline shifted **1234 → 1283**, +49 from `test_load_co_restricted_areas.py`; new 1283 baseline holds across S05.5–S05.7); tsc `mcp-server` + `web` exit 0; pre-commit detect-secrets passed (2 routine `.secrets.baseline` updates for citation-id literal in test + plan, recorded as false positives). No production-DB writes from the build session. **No new ADRs created** — refines ADR-001 (fail-loud), ADR-005 (CO isolation — composed private lib primitives, zero lib edits), ADR-010 (MultiPolygon/geography), ADR-014 (`gis_layer` + REGYEAR-anchored `publication_date`), ADR-015 (`verbatim_rule` nullable). ADR-021 is context-only (E06 uses `no_hunt_zone` for the binding role; S05.4 writes geometry only). **No schema or three-place-sync changes** (`geometry.kind='restricted_area'` already in the Literal). **3 new pitfalls landed in `.roughly/known-pitfalls.md`** (964 → 995 LOC; doc-writer flagged 995 LOC for future reorg/dedup): (i) Integration — ArcGIS: `fetch_features` can't apply a server-side WHERE filter — compose private primitives in the adapter for small fixed sets; (ii) Conventions — Ingestion adapters: post-fetch row drops must precede fixture/manifest writes or manifest/DB counts diverge; (iii) Conventions — Testing: an enumerated expected-set constant is only a guard if compared against runtime output at the write boundary (length-check at import ≠ identity-check at runtime). **Group B operator-pending** (per S05.0 / S05.2 pattern; not blocking close): live PAD-US fetch + production service-role DB write + `returnCountOnly` cross-check (expect 11→10 written) + `ST_IsValid` round-trip + `GIS_Acres` ±10% acreage sanity, then commit live-run fixtures. Runbook at `docs/planning/epics/E05-confidence-findings/S05.4.md`. **Three M2 hygiene items recorded** (PM cannot land autonomously; `ingestion/` is implementation territory): (1) existing `db.upsert_geometry` rowcount==0 guard (S05.0 carry-forward); (2) MT `load_state_boundary.py --service-url` removal (S05.0 carry-forward); (3) **NEW** — `load_gmus.py` template carries the same raw-`feature["properties"]` subscript pattern that S05.4 fixed; hygiene-pass candidate. **One research-doc accuracy item** for PM-decision surface: `docs/research/colorado-restricted-areas-evaluation.md:249` still carries the softer "same federal-authoritative chain" MT-contrast phrasing (separate already-merged artifact at `ed721c4`); left out of this branch for scope cleanliness — PM may want a one-line accuracy fix there (PM does not edit research docs autonomously per the no-autonomous-research-doc rule). **S05.5 closed at-merge 2026-06-05** via PR #60 / `e7ba3d4` from `feat/S05.5-geometry-overlays-fixture-build` (6 pre-squash commits: `d066975` implementation + `2e42cf6` plan-historical + `23dd903` pitfalls + `5b4d146` runbook-path-portability fix + `fe1fcd3` E05 Known-Issues #7 tech-debt candidate; plus post-build cubic-review-fix pair folded in pre-merge; seventh E05 PR). **CO `geometry-overlays.json` builder shipped as faithful port of MT S02.6**. New `ingestion/states/colorado/build_overlay_fixture.py` (~390 LOC) — local shapely + STRtree against `SELECT id, kind, ST_AsText(geom) FROM geometry WHERE state='US-CO'` (geography-native, no `::geometry` cast); ADR-016 three-band discriminator (0.99 relabel / 0.01 drop / `_OVERLAP_PCT_PRECISION=6`) carried forward unchanged as starting calibration (recalibration check is a Group B operator step); deterministic two-phase tmp+rename write. CO deltas vs MT: `gmu` parent/child kind; no portion handling (CO has none); `cwd_zone` retained but vacuously empty (S05.3 documented gap — zero CWD rows); fail-loud no-GMU guard. **`ingestion/ingestion/lib/overlays.py` extended state-agnostic-clean** (NOT a schema change): `OverlayParentKind → Literal["hunting_district", "gmu"]`; `OverlayChildKind` adds `"gmu"`; new canonical export `ROLE_FOR_BINDING_BY_CHILD_KIND` (adds `"gmu" → "primary_unit"`, preserves `"hunting_district" → "primary_unit"`); `ROLE_FOR_E03_BY_CHILD_KIND` retained as same-object deprecated alias for MT compat (zero disruption for MT's `build_overlay_fixture.py` which imports the old name). `OverlayFixtureRow.role_for_e03` field name preserved (MT fixture-data compat). `TestNoColoradoLeakIntoSharedLib` stays green (only the kind name `"gmu"` added; one comment reworded Colorado→CO to keep the guard clean). This resolves epic §"Architectural commitments" item #1 (`role_for_e03` naming semantically wrong for M2). **`EXPECTED_CO_RA_ORPHAN_IDS`** — the 10 S05.4 federal no-hunt zones (byte-identical to `load_restricted_areas.py` `_V1_EXPECTED_IDS`; `assert len()==10`). Per `restricted-area-discovery.md:170`, all 10 are **expected orphans** (NPs/NMs/AFA adjacent to, not contained by, GMUs — exactly the MT `EXPECTED_RA_ORPHAN_IDS` pattern). Coverage invariant: `gmu` self-rows = `primary_unit`; `cwd_zone` vacuously satisfied; `restricted_area` children OR on allowlist, else fail loud. **`ingestion/tests/test_build_co_overlay_fixture.py` NEW** — 48 pure-function tests (MagicMock-stubbed, no live DB) incl. the four threshold edge-locks (0.989/0.990/0.011/0.009), allowlist/orphan cases, determinism, `TestExpectedOrphanConstant` + `TestLibraryExtension` (alias-identity + `get_args` locks). **Stage-gate trajectory**: Stage-4 plan review PASS first iteration (2 non-blocking concerns, both folded in: explicit `parent_kind=="gmu"` substitution note for `_validate_coverage`; confirmed MT-importer-via-alias safety). Stage-6 triad caught **no Critical findings**; landed in 1 review-fix cycle — W1 (convergent code-reviewer + silent-failure-hunter) strengthened `test_all_children_covered_no_raise` with a `caplog` assertion that the orphan INFO log is absent when an RA is genuinely GMU-covered (because all 10 CO RAs are allowlisted, a broken `parent_kind` filter would otherwise pass vacuously — **the absence-of-log is the lock**); I1/I2 doc-accuracy fixes to `DroppedOverlayPair.parent_kind` and `OverlayFixtureRow.role_for_e03` docstrings. W2 **proposed-and-rejected** (documented): a `if ras and not ra_rows: raise` count guard would false-positive-fail the expected CO case (zero RA pairs is correct — all RAs are orphans); the existing no-GMU guard covers the catastrophic empty-table case. Cubic review `{"issues": []}`. **Post-build PR-review P3 pair**: hardcoded personal path → generic placeholder (fixed at `5b4d146`); overlay-builder duplication → assessed as deliberate ADR-005 per-state pattern, deferred to E05 Known Issues #7 at `fe1fcd3`. **Quality gates (at-merge)**: ruff clean (`ingestion/` + `tests/` + `states/`); mypy clean (`ingestion/lib/` 8 files + `states/colorado/` 5 files); pytest **1283 → 1331 + 2 skipped** (+48 from new CO test file — deliberate quality addition; new 1331 baseline holds going forward across S05.6–S05.7); cubic clean; detect-secrets passed (1 routine `.secrets.baseline` line-number refresh on the pitfalls commit). tsc untouched — serving stack has zero diffs. **No new ADRs** — refines ADR-005 (state-adapter isolation; the lib extension is a state-agnostic expansion, same pattern as ADR-014/ADR-018 lib additions), ADR-010 (geography/MultiPolygon), ADR-016 (three-band discriminator carried forward unchanged), ADR-017 §2 (no `confidence` column). **No schema / three-place-sync / migration changes** (`geometry.kind='gmu'` already in the Literal). No `db.py` touches; no MT-file touches; no production-DB writes from the build session. Q18/Q19 untouched (Q19 stays RESOLVED via ADR-020). **2 new pitfalls landed in `.roughly/known-pitfalls.md`** (995 → 1015 LOC; doc-writer re-flagged 1015 LOC for future reorg/dedup — recurring flag since S05.3.5/S05.4): (i) Conventions — Testing: "Allowlisted-orphan coverage checks can pass vacuously — assert the orphan log is ABSENT for items that should be covered" (the W1 lesson; extends S05.4's enumerated-expected-set pitfall); (ii) Conventions — Ingestion adapters: "Count-band / row-count guards are not portable across states — re-derive from that state's discovery doc" (the W2 lesson). **New E05 Known Issue #7** (post-E05 tech-debt candidate; not S05.5's to land): narrow overlay-builder shared-lib extraction. Scope when taken up: hoist ONLY the two pure low-divergence primitives (`_build_overlay_pairs` discriminator + `_write_outputs` serializer) into `lib/overlays.py` and migrate both MT+CO; leave orchestration / thresholds / allowlists per-state (ADR-016 §4 anticipates per-state threshold recalibration — forcing convergence would fight legitimate divergence). Post-E05; touches merged code so needs its own review; suggested fold into the M2 hygiene sweep alongside the three carried `db.py`/MT-`--service-url`/`load_gmus.py`-raw-properties candidates. **Line-citation-drift flags** (recorded for the PM's E05 drift ledger; both corrected in this PM closure commit): the epic's S05.5 prose snippet at lines 502-513 carried two illustrative inaccuracies the implementation corrected against actual `overlays.py` — (1) typed the mapping `Mapping[OverlayChildKind, GeometryRole]` (neither name exists in the module; actual/shipped is `dict[OverlayChildKind, GeometryRoleForE03]`); (2) the snippet dropped the `"hunting_district"` entry, but AC #548 requires preserving it (shipped dict keeps both). Both were illustrative-snippet drift, not design errors — the ACs (547-563) described correct behavior. **Group B operator-pending** (per S05.0 / S05.2 / S05.4 pattern; not blocking close): the live fixture JSON cannot be generated at-merge because the CO geometry rows S05.5 reads (S05.2 GMUs + S05.4 restricted areas) are themselves operator-pending Group B writes; `geometry-overlays.json` + `geometry-overlays-dropped.json` NOT committed; will be generated + committed in the operator's batched S05.2 + S05.4 + S05.5 live-write session. Runbook at `docs/planning/epics/E05-confidence-findings/S05.5.md`. **S05.6 closed at-merge 2026-06-05** via PR #61 / `1b55bfd` from `feat/S05.6-cross-state-binding-reference` (4 pre-squash commits: `e46fe06` deliverables + `65c1566` plan-historical + `98df5e9` pitfalls/baseline + `9b1fa7d` post-review ORDER BY determinism fix; eighth E05 PR). **Scaffold/reference-only story** — zero DB writes, zero `jurisdiction_binding` rows anywhere in E05 (binding writes are E06 territory per PRD 002 §"Why sequential"; bindings FK to BOTH `regulation_record` (E06) AND `geometry` (E05)). **Deliverables**: (1) `ingestion/states/colorado/load_jurisdiction_bindings.py` (new, ~100 LOC, import-only scaffold; no `main()`, no argparse, no `db.connect()`, no network) holds `_STATE: Final[str] = "US-CO"` (mirrors MT `load_jurisdiction_bindings.py:110`), `_NO_HUNT_ZONE_NEARBY_DISTANCE_M: Final[int] = 5000` (S03.10 Option A inheritance), module-level `_QUERY_NEARBY_GMUS_FOR_ZONE_SQL` (boundary-to-boundary `extensions.ST_DWithin` on geography with state + zone-geom + distance all `%s`-bound, `kind = 'gmu'` filter, `ORDER BY gmu.id`), and a never-called reference function `query_nearby_gmus_for_zone(conn, zone_geom_wkt)` mirroring MT's `_query_nearby_hds_for_zone` 1:1 as E06's drop-in; (2) `ingestion/tests/test_co_binding_reference.py` (9 tests in `TestCoBindingReferenceSql`) incl. the spec-mandated headline `test_co_binding_loader_sql_filters_by_state_co_pollution_guard`; MagicMock-only, no live DB; (3) `docs/planning/epics/E05-confidence-findings/S05.6.md` closure note (deletes at m2 tag per ADR-017 §6). All 9 ACs satisfied at-merge (no Group A/B split — scaffold-only, SQL never executes at runtime in E05). **Key design decisions baked in**: (a) **Inline `_STATE`, no `_constants.py`** — mirrors MT's actual precedent (MT keeps `_STATE` inline; no `_constants.py` exists in the MT adapter); AC-1's "or equivalent" clause permits it. (b) **Module-level SQL constant** — deliberate departure from MT (MT inlines SQL in-function) so the regression test imports/inspects it directly; the reference function still exists so param-binding can be locked by test. (c) **`_STATE` vs `CO_STATE_CODE` naming tension** — the 4 existing CO loaders use `CO_STATE_CODE = "US-CO"`; this scaffold introduces `_STATE` to mirror the MT binding-loader convention E06 inherits. Two-name inconsistency logged as an E06 cleanup candidate, deliberately not retrofitted (scaffold-only scope). **Stage-gate trajectory**: Stage-4 plan review PASS first iteration (no required edits). Stage-6 review triad converged on **one Critical** (code-reviewer + silent-failure-hunter W1; static-analysis concurring): the spec's illustrative SQL hardcoded the `5000` literal while `_NO_HUNT_ZONE_NEARBY_DISTANCE_M` sat unused — a silent-drift trap (E06's anticipated recalibration would not flow into the query). Fixed in 1 review-fix cycle by binding distance as a 3rd `%s` param, mirroring MT's `cur.execute(sql, (..., _NO_HUNT_ZONE_NEARBY_DISTANCE_M, ...))` at `:588`; tests tightened (I1 assert `%s`-bound not hardcoded state; I2 kill vacuous-zero in `extensions`-prefix count; lock `params[2]`). Cubic clean `{"issues": []}`. **One post-merge-review P3 fix** (external PR review, valid, folded into the branch pre-merge at `9b1fa7d`): reference SQL omitted `ORDER BY`, giving no PostgreSQL row-order guarantee across runs (E06 binding output could be nondeterministic); added `ORDER BY gmu.id` mirroring MT's `ORDER BY hd.id`, locked by new `test_sql_orders_by_gmu_id_for_determinism`. **Quality gates (at-merge)**: ruff clean (`ingestion/` + `tests/` + `states/`); mypy clean (`ingestion/lib/` 8 files + `states/colorado/` 6 files); pytest **1331 → 1340 + 2 skipped** (+9 from the new test file — deliberate quality addition; new 1340 baseline holds going into S05.7); cubic `{"issues": []}`; pre-commit detect-secrets passed (1 routine `.secrets.baseline` line-number refresh on the pitfalls commit). `git diff --stat ingestion/ingestion/lib/` empty (ADR-005 + CO-leak guard held); no `db.py` touches; no schema / three-place-sync changes; no MT-file touches; no TS-stack diffs; no production-DB writes. **1 new pitfall landed in `.roughly/known-pitfalls.md`** (1015 → 1029 LOC; doc-writer re-flagged 1029 LOC for future reorg/dedup): under "Conventions — Ingestion adapters" — "Spec-provided reference SQL can hardcode a value a sibling named constant is meant to own — bind the constant as a `%s` param, don't copy the literal" (the Stage-6 Critical lesson; extends the S04.x "name the source-of-truth before copying numbers" family). **No ADRs created** — refines ADR-004 (Supabase + PostGIS, `extensions.`-prefixed `ST_*`), ADR-005 (state-adapter isolation — no lib edits), ADR-010 (decomposed entities). Q18/Q19 untouched (Q19 stays RESOLVED via ADR-020). **Epic SQL reference snippet at lines 602-612 corrected to byte-parity with shipped code** in this PM closure commit: distance is now `%s`-bound to `_NO_HUNT_ZONE_NEARBY_DISTANCE_M` (was hardcoded `5000`); `ORDER BY gmu.id` clause added. **Three M2 hygiene candidates from S05.0/S05.4 still pending** (unchanged in scope; not S05.6's): `db.upsert_geometry` rowcount==0 guard + MT `load_state_boundary.py --service-url` removal + `load_gmus.py` raw-`feature["properties"]` subscript. **New E06 candidate**: `_STATE` vs `CO_STATE_CODE` unification across all 5 CO loaders. **E05 Known Issue #7** (narrow overlay-builder shared-lib extraction post-E05 candidate) unchanged. **S05.7 closed at-merge 2026-06-06** via PR #62 / `83e4c32` from `feat/S05.7-spatial-query-verification` (6 pre-squash commits: 1 implementation + 1 plan-historical + 1 pitfalls + 2 post-PR-review SQL-correctness fixes + 1 pitfall-broadening; ninth + final E05 PR). **Documentation + verification only** — no Python modules, no schema, no DB writes; test baseline unchanged at **1340 + 2 skipped** (held through S05.7). **E05 is now Complete + Audited 2026-06-06.** Three new docs shipped: (1) `docs/runbooks/E05-colorado-geometry-verification.md` (new, ~365 LOC) — CO operator runbook, analog of E02's. Prerequisites + Section 0 (fixture generator) + 7 verification sections (incl. the CO-specific Section 5 `ST_Envelope` bounds check, which has no MT analog) + cross-state-regression subsection + cleanup. Every `ST_*` call `extensions.`-prefixed (the deliberate departure from E02's bare-`ST_*` style); every query carries `AND state = 'US-CO'` (PRD 002 SC#4); WKT round-trip cast workaround for all geometry-only functions; FK-cascade wipe + TRUNCATE guard. (2) `docs/planning/epics/E05-confidence-findings/S05.7.md` (new) — working note with the Group B verification-record table (7 PENDING steps) + the `spatial-test-points.json` generator + decisions log. Deletes at m2 tag per ADR-017 §6. (3) **`docs/planning/epics/completed/E05-audit.md`** (new) — E05 post-implementation audit of all 9 stories (S05.0–S05.7 + S05.3.5). **Verdict: E05 ships clean, no blocking findings.** Tally **~80 MET / ~43 PARTIAL** (Group-B-operator-pending, by design — same posture as E02 S02.6/S02.7 UAT) **/ 0 NOT MET**. **Epic closure** — `docs/planning/epics/E05-colorado-geometry-ingestion.md`: Status → `Complete + Audited 2026-06-05`; `Audited:` field populated (**the gate for `/plan-next-epic` E06**); S05.7 closure block; ACs ticked honestly (Group A `[x]`, Group B `[ ]` + operator-pending annotation); Exit Criteria ticked; plan marked historical. **Cross-state filter regression documented as the UAT step** — `test_co_binding_loader_sql_filters_by_state_co_pollution_guard` (S05.6) passes in CI; no new test added (baseline holds at 1340). **Key decision — Group A / Group B split (Option A)**: the `spatial-test-points.json` fixture and the 7 live spatial-query steps (`ST_Covers`, `ST_IsValid`, multi-part anchor `ST_NumGeometries`, `EXPLAIN ANALYZE`, `ST_Envelope` bounds, wipe+re-ingest) are Group B operator-pending — they require live CO geometry (itself S05.0/S05.2/S05.4 Group B). Option A (fixture is operator-generated from live geometry via `extensions.ST_PointOnSurface`, NOT committed at-merge) was chosen over authoring named coordinates now, to honor AC #2's "real `representative_point()`, no invented points." Mirrors S05.5's `geometry-overlays.json` posture exactly. PM ticks the Group B ACs in a follow-up doc-only commit once the operator captures results in `S05.7.md`. **Post-PR-review fixes (2 SQL-correctness bugs — both caught in review, fixed pre/at-merge)**. Both stem from one root cause: spec-prescribed SQL transcribed into the runbook without validating against the live schema. (a) **`geom::geometry` cast** (`b3189c5`) — Section 5's `ST_Envelope`/`ST_Collect` used the direct geography→geometry cast Supabase rejects. Corrected to the WKT round-trip in the runbook, and re-derived in the canonical source (epic verification-step prose + AC checkbox + working-note record), per the S05.5/S05.6 "byte-parity with shipped code" convention. (b) **`jurisdiction_binding.state` nonexistent column** (`4de80d0`, P1) — the FK-cascade wipe wrote `DELETE FROM jurisdiction_binding WHERE state = 'US-CO'`, but that table has no `state` column (it has `regulation_record_state` + `geometry_id`). Would fail at runtime. Re-derived to the FK-precise form `WHERE geometry_id IN (SELECT id FROM geometry WHERE state = 'US-CO')` (matches the S05.3.5 migration / epic L396 precedent). Fixed in 5 places (runbook executable SQL + epic ×4 + audit ×1); the correct `DELETE FROM geometry WHERE state` refs were untouched. **Pitfalls (`.roughly/known-pitfalls.md`, now ~1045 LOC)**: 3 new/updated entries under Supabase/PostGIS + Documentation discipline: (i) "Spec-prescribed SQL can embed a broken idiom OR a column that doesn't exist — validate against the live DDL, don't transcribe" — recorded for the cast, then **broadened (`8f68857`)** to also cover the nonexistent-column flavor after it bit a second time in the same story. Extends the "name the source-of-truth before copying" family to SQL. (ii) "Explanatory comments that embed a broken-pattern literal trip token-matching reviewers (cubic)" — describe broken idioms in prose, don't embed the literal next to corrected code. (iii) Doc-writer re-flagged the file (~1045 LOC) as a reorg/dedup candidate (recurring since S05.3.5/S05.4/S05.5/S05.6). **E05 epic outcome (for the audit-gate record)**: all 9 stories closed + audited. CO V1 geometry composition: 1 statewide (`CO-STATEWIDE-geom`) + ~186 GMUs + 0 CWD zones (S05.3 documented gap) + 10 restricted areas. Merge order verified S05.0 → S05.7. Test-baseline trajectory **1166 → 1340**, all deliberate. **`Audited:` field is populated → the locked post-implementation-audit gate for E06 is satisfied. E06 (Colorado regulation text ingestion) may be planned via `/plan-next-epic` once you're ready.** **No ADRs created in S05.7**; refines ADR-004, ADR-005, ADR-016, ADR-017 §2. **Open / carried items for PM tracking**: (1) **Operator Group B batched live-write session** outstanding across S05.0/S05.2/S05.3.5/S05.4/S05.5/S05.7 — run `load_state_boundary` → `load_gmus` → `load_restricted_areas` → `build_overlay_fixture` → generate `spatial-test-points.json` → run the 7 verification steps. Then PM ticks Group B ACs + S05.3.5's CHECK-constraint verification in follow-up doc commits. This is the hard precondition feeding E06's binding loader. (2) **3 M2 hygiene candidates** (PM cannot land autonomously — `ingestion/` is implementation territory): `db.upsert_geometry` rowcount==0 guard; MT `load_state_boundary.py --service-url` removal; `load_gmus.py` raw-`feature["properties"]` subscript. **Audit recommends bundling with Known Issue #7** (narrow overlay-builder shared-lib extraction) into one M2 hygiene-sweep PR after E05. (3) **Known Issue #6** (`_VALID_ROLE_FOR_E03` subset gate) — E06 CO-binding-loader decision; resolve before E06's binding-loader spec drafts. (4) **Recurring-RLS-gap M2 open question** — unchanged; E05 added no `public.*` tables. **Next active work-front: E06 (Colorado regulation text ingestion)** — ready for `/plan-next-epic` once user is ready.** M0 scaffold complete. E01 (schema migrations, RLS, quality gates) merged 2026-04-28. **E02 (Montana geometry ingestion) closed and audited 2026-05-03 with all 8 stories merged and all exit criteria met (post-implementation audit at `docs/planning/epics/E02-audit.md`: 86/89 ACs MET, 3 P3 cosmetic findings all fixed in `0093e88`).** Final geometry-table state: 235 HDs (layers #3, #10, #11) + 55 portions (#4, #12, #13, #14) + 57 restricted areas (#2, #15) + 2 CWD zones (dedicated `ADMBND_HD_CWD` FeatureServer) = **349 V1 Montana rows**, all `geography(MultiPolygon, 4326)`, all valid via `shapely.make_valid()`. Companion artifacts: `ingestion/states/montana/fixtures/geometry-overlays.json` + audit log `geometry-overlays-dropped.json` (built via local shapely + STRtree per ADR-016's three-band area-ratio discriminator since Supabase's 2-min `statement_timeout` aborts cross-join SQL on real data); 10 metadata fixtures + 10 per-fetch manifests (~5KB each) committed for cross-operator drift detection while raw features payloads stay gitignored at ~180MB/run; spatial verification suite at `spatial-test-points.json` with 11 named test points covering all 5 kinds; operator runbook at `docs/runbooks/E02-geometry-verification.md` (264 LOC, mirrors E01's structure). Schema additions during E02: `geometry.verbatim_rule text` (nullable) per ADR-015 with HuntReady-introduced `\n\n--- COMMENTS ---\n\n` separator for layer-#2 REG+COMMENTS combination; `SourceCitation.document_type='gis_layer'` per ADR-014. Two E03 handoff items recorded in epic § "Known issues to escalate": `kind='restricted_area'` conflates internal HD restrictions with no-hunt zones (3 V1 cases — Glacier NP, Sun River, Yellowstone) and may need a discriminator subtype or multi-bind role mechanic; jurisdiction_binding fan-out is higher than naive (median ~3 parents per child, max 16) so E03 should expect several thousand binding rows for V1 Montana, not ~349. **E03 (Montana regulation text ingestion) planned 2026-05-03**: 13 stories (S03.0 schema-prep through S03.12 M1 UAT/handoff); validated via the E03 triad (Source Faithfulness, Confidence Calibration, Schema Stress-Test); two new ADRs accepted: ADR-017 (confidence calibration + parent-inheritance rule, resolves Q11) and ADR-018 (E03 schema additions: `license_season` link table + `geometry.legal_description` column + `geometry.kind='state'` value). Working-artifact directories planned at `docs/planning/epics/E03-confidence-findings/` (deleted at m1 tag per ADR-017 §6) and `docs/planning/epics/E03-deferred-items/` (survives past M1, includes `draw-mechanics.md` for `parameters` Q12 deferrals). **E03 S03.0 (schema prep) merged 2026-05-04**: migration `20260504032424_e03_schema_additions.sql` ships ADR-018's three additions (`license_season` link table + `geometry.legal_description` column + `geometry.kind='state'` value) with three-place sync. `MT-STATEWIDE-geom` written to production 2026-05-04 16:25 PT via `ingestion/states/montana/load_state_boundary.py` (single-part valid MultiPolygon, area ≈ 380,840 km²; `license_year=NULL`; `source.id='mt-msdi-framework-boundaries-9-2026'`). Source choice deviates from ADR-018's two listed options — chose **Montana State Library MSDI Framework Boundaries layer 9** (gisservicemt.gov) as a third option that strictly dominates: state-published, GCDB-aligned at 1:24,000, fits `document_type='gis_layer'` cleanly. Pinned URL + SHA-256 + `2026-01-01` publication date. Three new entries in `.roughly/known-pitfalls.md` from S03.0's investigation + deployment: (a) MT GIS layers live on **three** hosts (FWP on-prem + AGOL MtFishWildlifeParks + Montana State Library MSDI), not two — extends the prior S02.5 pitfall; (b) one-off `requests.get()` against ArcGIS endpoints must explicitly check `data.get("error")` envelopes (200-with-error-body is the ArcGIS pattern); (c) `supabase db push` against a dashboard-bootstrapped project fails with "relation already exists" — fix is `supabase migration repair --status applied <timestamp>` per pre-existing migration before pushing. Working-artifact directories live: `docs/planning/epics/E03-confidence-findings/` (deleted at m1 tag) and `docs/planning/epics/E03-deferred-items/` (survives past M1). 332/332 tests green. **E03 S03.1 (PDF fetch infrastructure) shipped 2026-05-05** on `feat/S03.1-pdf-fetch-infrastructure` (8 commits, `b16dc9d..ef3f467`): `ingestion/ingestion/lib/pdf_fetch.py` (~310 LOC, state-agnostic per ADR-005 — public API `fetch_pdf` / `PdfMetadata` / `PdfFetchError`), `ingestion/states/montana/fetch_pdfs.py` (~230 LOC, fail-loud aggregating orchestrator with distinct buckets for failures vs. pending entries), `ingestion/states/montana/sources.yaml` (4 entries with full SourceCitation field set; correction PDF carries `pending: true` since URL is genuinely TBD — exit-0 still blocked until populated), per-PDF-manifest gitignore policy mirroring S02.1's pattern, and 33 new unit tests (332 → 365). SHA-256 drift on re-fetch raises `PdfFetchError` and writes a `<id>-<publication_date>-pending-reextraction.flag` marker; downstream stories (S03.3-S03.5) check the marker and refuse to proceed until an operator deletes it. State-agnostic-lib invariant enforced via AST-based test guard (not just substring scan). Two new pitfalls in `.roughly/known-pitfalls.md`: (a) state-adapter scripts must be invoked as `python <path>` not `python -m` (the editable-install layout makes `states/` a non-subpackage); (b) `expected_sha256` in `sources.yaml` is documentation/intent — runtime drift detection compares against the on-disk manifest, not the YAML field. AC #4 (manifest commits) infrastructure-met but fixture-deferred to first ops-run, same posture as E02 S02.1 → S02.2. Two ADR-adjacent design choices baked in but not promoted: `pending: true` YAML semantics (adapter-specific; revisit if a second state adopts the pattern) and the drift-marker convention as canonical fail-loud-and-block-downstream signal (already encoded in S03.1 epic prose; ADR-001's parent discipline covers the principle). **S03.3 unblocked 2026-05-07** after the implementation agent reported all four `sources.yaml` URLs returning 404 against the live FWP CDN. Canonical URLs located on `https://fwp.mt.gov/hunt/regulations`, verified live (HEAD → 200 application/pdf), and committed to `sources.yaml`. DEA citation id renamed `mt-fwp-dea-2026-2027-booklet` → `mt-fwp-dea-2026-booklet` to match the URL-truthful annual cadence (FWP publishes only `2026-dea-regulations`, not a `2026-2027` biennial — cover-page cadence to be confirmed at first extraction). All four `publication_date` values were revised to each PDF's HTTP `Last-Modified` header (DEA + Black Bear booklet: 2026-04-27; Legal Descriptions: 2026-02-03; correction stays 2026-03-18, matching the citation id). Black Bear correction URL also located in the same pass, **resolving S03.4's S03.1-inherited precondition** (no separate operator action remains). New pitfall recorded under "Conventions — Ingestion adapters": "`sources.yaml` URL slug ≠ publication cadence — confirm cadence by reading the PDF, not the URL." Surfaced learning: spec tables encoding URL guesses are pre-validated assumptions; live discovery + HEAD-verification before pinning is the right S03.X pre-implementation gate. **E03 S03.3 (DEA booklet extraction) code-complete 2026-05-08** on `feat/S03.3-dea-booklet-extraction` (12 commits, `e1a047f..69d98f7`, cleared PR review): `ingestion/states/montana/extract_dea.py` (~1545 LOC, state-agnostic-clean per AST guard) emits `ingestion/states/montana/extracted/dea-2026.json` (264 sections / 1178 rows / **perfect 50/50 high-medium confidence split, 589 each, no low**). Composition: 129 deer + 112 elk + 22 antelope per-HD sections + 1 STATEWIDE antelope overlay (`900-20`, quota=5600, range="1-7500", archery window Aug 15-Nov 8). All 4 first-fetch baseline manifests committed in `7b1ec54` (DEA, Black Bear booklet, Legal Descriptions, Black Bear correction); raw PDFs gitignored. **AC #342 (UAT faithfulness) is operator-pending** — the single open AC; does NOT gate downstream stories. **The S03.3 plan's research notes did not survive contact with the actual PDF — 9 real-PDF discoveries baked into the implementation:** (1) one table per page, not per HD; HD-section delimiters are embedded data rows; (2) column headers carry `... SEASON DATES` suffix; (3) `-` is a universal absent-cell sentinel — review caught dash leaking into apply_by/quota_range/extras (675/971/521 rows pre-fix); (4) species banners embedded mid-table as `['DEER', None, ...]` rows; (5) license-code carry-forward via `None` in LICENSE/PERMIT (merged-cell artifact); (6) antelope tables use a different column format ("License/Opportunity/Quota/Archery Season Dates/Season Dates"); (7) `_ANTELOPE_PAGES = (136, 141)` — actual PDF is 141 pages, not 142 as spec claimed; (8) `900-20` is a repeated table row, not a prose paragraph; (9) multi-page HDs repeat heading with ` - Continued` suffix — first-match-wins dedup added after 20 HDs were duplicated. **Three new pitfalls in `.roughly/known-pitfalls.md` under Integration — pdfplumber:** (a) FWP DEA-style tables: one-table-per-page assumption; (b) `-` as universal absence sentinel; (c) merged-cell sub-row license-code carry-forward via `None`. Schema additions for downstream: 5 TypedDicts (`SeasonCoverage`, `SeasonWindow`, `DeaRowExtraction`, `DeaSectionExtraction`) define the contract S03.6-S03.9 read directly from `dea-2026.json`. 74 tests in `test_extract_dea.py`; suite total ≈478. Three deferred follow-ups for downstream review (recorded in epic § S03.3 closure note): per-row page-accurate `page_reference` for multi-page HDs (S03.6); row-level `weapon_types` default refinement (S03.7); window divergence per row, with "Elk B License: 699-01" as the most-prolific divergence case (S03.7). **S03.3 closed 2026-05-08 after UAT-driven fix cycle.** Original PR (12 commits, `e1a047f..69d98f7`) cleared code review on 2026-05-08 but PM-run UAT against four candidate sections (deer HD 124 / elk HD 170 / antelope HD 690 / antelope STATEWIDE 900-20) surfaced six defects: **D1 (P0)** per-row General Season window divergence collapsed to first-row's value across ≥143 multi-row sections (HD 124 deer alone had 3 distinct General windows in PDF; artifact had only 1); **D2 (P0)** Region 7 "Portions of HDs ..." sub-sections absorbed into HD 690 with 4 phantom rows (`007-21`, `007-31`, `007-20`, `007-30`), actual portions sub-sections entirely missing; **D3 (P1)** STATEWIDE 900-20 row's `Aug. 15-Nov. 08` date in PDF's Season Dates column was remapped to `archery_only` based on extras-text interpretation rather than column position; **D4 (P2)** STATEWIDE extras "First and only choice. ArchEquip only." dropped; **D5+D6 (P3)** STATEWIDE `license_code` prefix and `quota_range` comma stripped inconsistently with per-HD rows. Implementation agent applied three fixes in single commit `cb3fb24`: deleted `_aggregate_section_season_windows` / `_row_season_windows` and rewrote `_rows_to_license_extractions` to read each row's own season-column cells inline (per-row windows; divergence-WARNING removed); added `_parse_portions_hd_list` / `_is_portions_subsection_row` / `_PORTIONS_HEADER_RE` and rewrote the antelope orchestrator to walk pages directly with three delimiter types (HD-numbered, Portions, Region N), emitting one `DeaSectionExtraction` per listed HD per option-(a) (rows duplicated; license_code disambiguates at S03.6); deleted `_extract_statewide_antelope_overlay` / `_ANTELOPE_900_ROW_RE` and routed the 900-20 row through the same `_rows_to_license_extractions` machinery with `is_statewide_overlay=True` flag for column-faithful emission. Added an extras-only `re.sub(r"\s+", " ", ...)` collapse at the write-site (one documented deviation from the directive) so multi-line extras prose collapses to single-space-joined text without affecting `_normalize_cell`'s HD-heading detection. PM re-ran the UAT workbench against the re-extracted artifact: **all six defects resolved**. Final artifact: **272 sections / 1190 rows** (+8 sections / +12 rows from Region 7 portions: HDs 700-705 each with `007-*` rows; HDs 701 and 703 appear in both North and South Yellowstone portions); confidence distribution 589 medium + 601 high. Test delta: −7 stale, +17 new = net +11 in `test_extract_dea.py` (73 → 84); full suite **488 passed + 1 skipped**; ruff + mypy + cubic clean. **Four pdfplumber pitfalls in `.roughly/known-pitfalls.md`**: (a) FWP DEA-style tables: one-table-per-page; (b) `-` as universal absence sentinel; (c) merged-cell sub-row license-code carry-forward via `None`; (d) "Section-level first-observation-wins for per-row variable fields is interpretive, not faithful" (UAT-discovered). Working artifacts at `docs/planning/epics/E03-confidence-findings/S03.3.md` (UAT fix log) and `S03.3-uat-fixes.md` (directive); both delete at m1 tag per ADR-017 §6. **Docstring parity restored 2026-05-08:** the new extras-only `\s+ → " "` collapse rule was added to the module-level "Cleanup rules" section at `extract_dea.py:44-60` with exact regex, scope, rationale, and a pointer to the tests that lock it (`TestStatewideOverlayColumnFaithful`, `TestArtifactRegion7Portions`). AC #338 strict parity established; the pattern — every cleanup rule applied to row cells must appear in the module docstring — is now the canonical S03.X discipline. **Phase 6 plan-realignment 2026-05-09:** five docs(S03.3) commits aligned the implementation plan with the UAT-corrected code so a future re-reader following the plan wouldn't re-create the UAT defects (T7 per-row windows; T8 page-range-bounded inline emit; T1 `_ANTELOPE_PAGES = (136, 141)`; T10 step 1 rewrites referencing the new orchestrator; T12 post-UAT test class enumeration; T9 confidence rules aligned with shipped `_assign_row_confidence`; T13 determinism-check command turned into complete two-step recipe; epic line 337 confidence count corrected from 589/589 to 601/589). **Phase 7 fail-loud guard 2026-05-09:** restructured the STATEWIDE 900-20 emit guard to fail loud at three distinct stages (row-not-captured / row-captured-but-zero-extractions / success); closes a P1 silent-data-loss path. New `TestStatewideOverlayFailLoudOnEmptyExtraction` test (monkeypatches `_rows_to_license_extractions` to return `[]` for the statewide call while letting per-HD calls pass through). PM re-verified all six UAT defects still clear after Phase 6+7 (artifact SHA-256 byte-identical to post-UAT-fix `a41a4fd7…`); 489 tests + 1 skipped, ruff + mypy + cubic all clean. **E03 S03.4 (Black Bear booklet + correction PDF handling) closed 2026-05-12** via PR `ab09e82` (squash of 5 commits on `feat/S03.4-black-bear-extraction`): `ingestion/states/montana/extract_black_bear.py` (~1820 LOC) implements the three-pass date-arbitrated extraction of the 16-page booklet + 1-page correction into three deterministic JSON artifacts: `black-bear-2026-base.json` (Pass 1: 35 BMU rows + 2 closures + 3 reporting candidates), `corrections-2026-03-18.json` (Pass 2: 35 per-BMU CorrectionOperation dicts), and `black-bear-2026.json` (Pass 3 merged — S03.6-S03.9 consume this). All 35 rows tagged `applied_correction=true`, `supersedes=mt-fwp-black-bear-2026-booklet`, confidence demoted HIGH→MEDIUM (correction touches every BMU's hound_training_season cell). **Three PM decisions accepted at closure**: (a) **AC #428 9→8 quota-closure BMUs** — BMU 530 absent from 2026 PDF; spec was stale; drift guard locks 8; (b) **AC #429 2→3 reporting obligations** — STATEWIDE Mandatory Reporting (48-hour harvest) is operationally distinct from the R1/R2-7 inspection split; spec was stale; (c) **AC #430-431 Option B (doc-type precedence) replaces spec's MAX-date Option A** — booklet Last-Modified (2026-04-27) post-dates correction (2026-03-18); Option A would silently no-op; `document_type='correction'` always wins over `'annual_regulations'` regardless of date; date is tiebreaker within same doc-type only. Row-level (not cell-level) source attribution accepted as V1 simplification per the schema-stability discipline. **ADR-019 accepted 2026-05-12** at [`docs/adrs/ADR-019-doc-type-precedence-multi-source-merge.md`](docs/adrs/ADR-019-doc-type-precedence-multi-source-merge.md) — formalizes the doc-type-precedence rule for all future state adapters. Reviewed by a separate architectural sub-agent before sign-off; 3 critical + 4 actionable findings applied (test name corrections; Pass/Stage terminology disambiguation; expanded V1 rank table with fail-loud for `rule_change`/`emergency_order` in regulation merges; two new alternatives — `SourceCitation.supersedes` field and `(value, rank)` pair refactor — explicitly considered and rejected for V1; concrete trigger named for PRD 001 R5 reconciliation; M2 operator-recovery path documented for `CorrectionConflictError`). **E03 S03.5 (Legal Descriptions extraction) closed 2026-05-14** via PR `b2ad20b` (10 commits `ece1ab1..e96a195`): `ingestion/states/montana/extract_legal_descriptions.py` (~1372 LOC, state-agnostic-clean per AST guard; single-writer contract — no DB imports, locked by `test_no_db_imports`) emits `ingestion/states/montana/extracted/legal-descriptions-2026.json`. PM-run pre-merge review surfaced 1 P1 (verbatim spillage — column-crop x-range too narrow at 145pt for col 2, truncating "Those portions" → "Those portio" and defeating the regex anchor; bear-319 absorbed 4 subsequent HDs reaching 8059 chars vs 855 median) + 5 P2 findings (unlinked HD count 98 vs 81 in working note; footer crop 20pt vs 50pt in pitfall; plan unlinked-scope contradiction between summary and task spec; plan HeadedBlock.kind missing "continuation"; CWD canonicalize before consolidation). Agent applied all 6 in 9 follow-up commits before merge; **matched count rose 156 → 228** across the cycle and bleed cases dropped **35 → 0**. Final artifact: **228 matched** (226 HD + 2 CWD, all `extraction_confidence=high`) + **31 by-design unmatched** (portion sub-headings — out-of-V1-matcher-scope) + **119 unlinked** (9 HD genuinely absent from PDF + 55 portion + 54 restricted_area + 1 STATEWIDE; full 347-row V1 geometry surface accounted for); regression rate **0.000** (well below 10% threshold). **11 real-PDF discoveries** baked into the implementation: three-column layout makes full-page `extract_text()` unusable; rotated chapter-label sidebars need `c.get("upright", True)` filter; CWD heading wraps across lines; same heading repeats once per column (CWD multi-column case: D4 longest-body-wins rule); HD heading name capture must exclude newline; pages walked-but-out-of-scope; HD anchor phrase wraps onto continuation lines; running PDF footer leaks at 20pt strip (corrected to 50pt); FWP locator clause stripped via `\b[Bb]eg\w*\s+\w` anchor (Cleanup Rule C; `\s+\w` discriminator avoids matching the closing "the point of beginning"); heading regex overhaul covers **8 FWP heading variants** (canonical, no-locator-clause, "The portion of", truncated anchor word, wrapped name, no-colon, `HD` prefix, period-in-name); CWD consolidation canonicalizes names before deduping (D11 defensive). **Seven new pitfalls in `.roughly/known-pitfalls.md`** under Integration — pdfplumber (three-column layout; rotated sidebars; newline-in-name-capture; multi-column heading repetition; anchor-phrase line wrap; running footer leak; period-pre-wrap lookbehind). Test suite: **657 passed + 2 skipped** (+83 from S03.5; 12 test classes including `TestArtifactRegression` which locks no-cross-namespace-bleed and no-footer-leak end-to-end). **PM UAT spot-check 2026-05-14** on HD 100 North Kootenai (deer-elk-lion, page 19 col 1) + Libby CWD Management Zone (page 19 multi-column, the D4 longest-body-wins case) confirmed faithfulness — no spillage, no foreign content, standard FWP "point of beginning" closing phrase preserved; pdfplumber's column-edge character truncation (e.g., "Kootenai" → "Kooten") preserved verbatim per ADR-008. **No ADRs created** (S03.5 references ADR-001/005/008/017/018). **Four M2-deferred items recorded:** fixture refresh to include `MT-STATEWIDE-geom` (removes the `_load_geometry_lookup` hardcode); 9 genuinely-absent HDs (investigate during M1 UAT); lowercase "beginning" variant pattern (watch for in M2 Colorado); citation slug cadence reconciliation (biennial source vs annual artifact filename). **E03 S03.6 (regulation_record ingestion + `geometry.legal_description` population) closed 2026-05-15** via PR `c0c1b77` (squash of 4 commits on `feat/S03.6-regulation-record-ingestion`): `ingestion/states/montana/load_regulation_records.py` (~590 LOC, state-agnostic-clean) is the first E03 database-write adapter. Reads all three extraction artifacts (`dea-2026.json` + `black-bear-2026.json` + `legal-descriptions-2026.json`) and writes **436 regulation_record rows + 228 `geometry.legal_description` updates in one atomic transaction**. Per-species breakdown: 129 mule_deer + 129 whitetail + 112 elk + 31 pronghorn + 35 bear (DEA species fan-out: artifact `deer` → DB `mule_deer` + `whitetail`). All bear rows confidence=medium (HIGH→MEDIUM demote via S03.4 correction-touched single demote-one-tier passing through MIN aggregation unchanged). Mixed-confidence patterns flagged for S03.12 UAT spot-check: elk has 2 high + 110 medium; pronghorn has 30 high + 1 medium. `MT-STATEWIDE-antelope` is the single confirmed statewide regulation_record in V1 (anchor for the DEA `900-20` row) — per ADR-018 §3 flag-and-surface, no additional statewide candidates surfaced. **DB helpers added**: `db.upsert_regulation_record` (INSERT…ON CONFLICT UPDATE; `ingested_at` excluded from UPDATE clause so re-runs preserve first-ingest time) and `db.update_legal_description` (targeted UPDATE; fails loud on `cur.rowcount == 0` so matcher bugs surface immediately rather than silently no-op). **OQ1 resolved 2026-05-14** (recorded in `docs/open-questions.md` Q15): `regulation_record` has no `verbatim_rule` column in DDL (`supabase/migrations/20260425000000_initial_schema.sql:36-49`) or in the Pydantic `RegulationRecord` model. Option (c) selected — drop section text from `regulation_record` and let it decompose into S03.7's `season_definition.verbatim_rule` + `license_tag.verbatim_rule`; HD-wide `NOTE:` lines land in `regulation_record.additional_rules` via S03.6. Refinement of ADR-008's decomposition story; no new ADR. Epic line 565 amended with strike-through + footnote `[^oq1]`. **OQ7 introduced**: row-count fail-loud guards (±30% band) as a first-of-its-kind pipeline discipline — `regulation_record` band `[359, 668]`, `legal_description` band `[159, 296]`; both guards fire BEFORE `db.connect()` so a regression aborts with no partial writes. Sets precedent for S03.7-S03.10. **Eight real-data findings** baked in: (1) DEA species fan-out via `_DEA_SPECIES_FANOUT` (emit order mule_deer first, whitetail second — locked by test); (2) Bear DB `species_group="bear"`, NOT the artifact's `"black_bear"`; (3) bear path uses `ConfidenceTier(...)` validation at-row, not bare `cast(Confidence, ...)`; (4) bear artifact `sources` and `rows` top-level keys need shape validation (cubic-review P2 + silent-failure-hunter HIGH; both keys now validated with diagnostic RuntimeError); (5) DEA elk/mule_deer/whitetail + `hd_number="STATEWIDE"` is structural fail-loud (only pronghorn has a sanctioned statewide anchor); (6) NOTE-line regex hardened from `^NOTE:\s*` to `^NOTE:[ \t]*` to prevent cross-NOTE absorption via whitespace-only spacer lines (538 NOTEs preserved byte-identical pre/post hardening); (7) `legal_description` empty/whitespace-only strings coerced to SQL NULL (review N4 defensive guard); (8) `MT-STATEWIDE-antelope` confirmed as single V1 statewide regulation_record. **Seven new pitfalls in `.roughly/known-pitfalls.md`** under Conventions — Ingestion adapters (regulation_record has no verbatim_rule column; `db.update_legal_description` fails loud on rowcount==0; DEA species "deer" requires fan-out; Bear DB species_group is "bear" not "black_bear"; ConfidenceTier.min_tier not bare min(); adapter-module tests use direct import not importlib spec_from_file_location) + Conventions — Pre-commit & secrets (detect-secrets baseline line-number updates on every commit growing tracked files). **Review history**: 3-agent review triad (code-reviewer + silent-failure-hunter + static-analysis) applied 7 fixes (3 HIGH + 4 SHOULD-FIX), deferred 3 by design (double-commit pattern matches `load_state_boundary.py` precedent; main() 91 lines linear/readable; `_legal_description_writes` None coercion V1-safe). Cubic-review applied 1 P2 fix (symmetric rows shape guard). Plan-realignment commit post-merge ensured the plan recreates the as-built code (same anti-pattern as S03.3's Phase 6). **Recovery note (PR provenance)**: S03.6 was accidentally pushed directly to main as 3 commits + 1 chore commit; recovery sequence carved a feature branch from the S03.6 tip, force-pushed main back to S03.5 close + chore replay, pushed feature branch, opened and squash-merged PR #36. Single-user repo, `--force-with-lease` for safety, no collateral. Working note at `docs/planning/epics/E03-confidence-findings/S03.6.md` (87 LOC; deletes at m1 tag per ADR-017 §6) captures the cross-tab + mixed-confidence elk/pronghorn findings + the empty "additional statewide candidates" / "free-prose non-NOTE content" sections. Test suite: **709 passed + 2 skipped** (+52 from S03.6; 6 test classes including `TestExtractNoteLines`, `TestBuildDeaRecords`, `TestBuildBearRecords`, `TestLegalDescriptionWrites`, `TestCountGuard`, `TestMain`). **No ADRs created** — OQ1 is a refinement of ADR-008's decomposition story, not a new architectural commitment. **Two M2-deferred items recorded**: free-prose non-NOTE HD-wide content (currently has no structured DB home; V1 Montana DEA expected empty-set, but S03.12 spot-checks); mixed-confidence elk/pronghorn UAT spot-check (identify the 2 elk high HDs + 1 pronghorn medium HD). **S03.7 was queued next** as the load-bearing UAT-yes story for M1 success criterion #2. **E03 S03.7 (season_definition + license_tag + license_season ingestion) closed 2026-05-16** via PR `20db0fc` (5 commits past S03.6 close on `feat/S03.7-season-definition-license-tag-ingestion`): `ingestion/states/montana/load_seasons_and_licenses.py` (~1180 LOC, state-agnostic-clean) is the first E03 multi-link-table adapter. Final state: **978 season_definition + 1225 license_tag + 3040 license_season + 1385 regulation_season + 1914 regulation_license = 8542 entity/link rows + 20 closure_predicate jsonb values** written in one atomic transaction. **M1 success criterion #2 satisfied at the data layer**: spec originally specified HD 262 elk but live extraction showed HD 262 lacked the asymmetric pattern; OQ-S7-3 shifted to HD 170 elk (Flathead River) where A (General Elk License) covers `{archery_only, general, heritage_muzzleloader}` and B (Elk B License: 170-00) covers `{archery_only, general, late}`; A-only = `{heritage_muzzleloader}`, B-only = `{late}`. Locked by `TestBuildDeaLinkRows::test_license_season_asymmetric_coverage_m1_criterion`. End-to-end SQL spot-check deferred to S03.12 post-jurisdiction_binding. Inherits OQ7 row-count guard precedent — **all five guards fire BEFORE `db.connect()`** with ±30% bands. **Eleven decisions baked in (OQ-S7-1 through OQ-S7-11)**: (1/2) species granularity — `license_tag.species` + `season_definition.id` use ARTIFACT-level labels (`deer`/`elk`/`antelope`/`bear`); DEA `deer` fan-out happens ONLY at the link-table layer; (3) UAT HD shift 262 → 170; (4) `_SEASON_NAME_BY_KEY` season-name rendering; (5/6) bear closure_predicate attachment — `sex_threshold` → spring for 4 female-sub-quota BMUs; `quota_threshold` → general AND archery_only for 8 quota-closure BMUs; (7) license_tag.kind 5-ordered-branch heuristic with fail-loud else; (8) window parser handles dotless/dotted/bear-newline/"Sept" 4-letter/year-wrap; (9) verbatim_rule fallback chain; (10/10b) bear → NULL quota_range, DEA → parsed-int range with comma-thousands stripped, residency `"both"`; (11) `purchase_url` evergreen. **`license_tag.draw_spec_key=None` on every row** — S03.8 backfills per ADR-012. **Two cubic fixes**: P2 (`20c0f34`) DEA builders gate on `season_coverage` truth (defensive against S03.3 drift); P1 (`df1b6b0`) `_license_code_slug` includes lowercased license-type prefix to resolve 6 cross-listing collisions (HDs 410/455/555/630/700). **PM applied 3 surgical mypy fixes during closure verification (this commit)**: 8 type errors in `load_seasons_and_licenses.py` — loop-variable type pollution across the 3 sequential final-write loops (renamed `link` → `ls_link`/`rs_link`/`rl_link`); `key` variable shadowing in `_log_summary` (renamed to `lt_key`); `weapon_type` `str | None` not narrowed to `WeaponType | None` (added type annotation + `WeaponType` import). Agent's "ruff + mypy clean" claim was wrong; **discipline reminder filed**: every adapter closure should re-run `mypy ingestion/lib/ states/<state>/<module>.py` before reporting clean. **Five new DB helpers in `ingestion/ingestion/lib/db.py`**: `upsert_season_definition`, `upsert_license_tag` (with `CASE WHEN %s::int IS NULL THEN NULL ELSE int4range(%s, %s, '[]') END` for the quota_range column), `write_license_season`, `write_regulation_season`, `write_regulation_license` — all no-commit (caller-controlled txn), all idempotent (UPSERT on entity tables, ON CONFLICT DO NOTHING on link tables). **Five new pitfalls** in `.roughly/known-pitfalls.md` (DEA "Sept" 4-letter abbreviation in antelope tables; license_code kind heuristic full-artifact profiling discipline; Pydantic frozen+extra="forbid" required-field enumeration; duplicate-license_code-within-section no-pre-dedupe; cross-license slug-prefix collision). **Q16 added to `docs/open-questions.md`** — species granularity revisit for M2. **No ADRs created** — implementation refines ADR-008 (verbatim discipline — section-scoped fallback), ADR-010 (decomposed entities — first multi-link adapter), ADR-012 (soft FK — draw_spec_key=NULL), ADR-017 (confidence inheritance — child entities carry no confidence column), ADR-018 (license_season link table operationalized). Test suite: **832 passed + 2 skipped** (+121 net + 2 cubic-fix tests from S03.7; 14 test classes). Working note at `docs/planning/epics/E03-confidence-findings/S03.7.md` (deletes at m1 tag per ADR-017 §6). **S03.8 was queued next.** **E03 S03.8 (draw_spec ingestion) closed 2026-05-19** via PR `323836f` (8 commits `5b48936..d733482` on `feat/S03.8-draw-spec-ingestion`): `ingestion/states/montana/load_draw_specs.py` (~720 LOC, state-agnostic-clean per AST guard) writes **388 draw_spec rows (278 unique after UPSERT composite-PK collapse via `(state, hunt_code, year)`) + 388 license_tag.draw_spec_key backfills** in one atomic three-phase transaction (build → guards pre-`db.connect()` → conn/phase loops/commit/rollback/close). **S03.7 `_DEA_LICENSE_KIND_HEURISTIC` amended in the same PR**: OQ-S7-7's `license_code`-only heuristic mis-classified 162 over-the-counter B Licenses as `limited_draw`; amendment inspects `apply_by` cross-row AND cross-HD (OTC discriminator is `apply_by="OTC:\nJun 15"`-style cells). Post-amendment DEA breakdown: **388 limited_draw + 162 over_the_counter + 239 general + 1 statewide = 790 unique** license_tag identities (counts unchanged; classification corrected). Without this amendment S03.8 would have over-written `draw_spec` rows for OTC-classified licenses (silent data corruption). `900-20` correctly has NO draw_spec (per Q5/Q12 deferral); `license_tag.draw_spec_key` is NULL for it and for all non-limited-draw rows. **`update_license_tag_draw_spec_key` fail-loud helper** added (RuntimeError on `cur.rowcount == 0`, mirroring `db.update_legal_description` from S03.6). **Inherits OQ7 row-count guard**: 1 guard on draw_spec write count with ±30% band, fires pre-`db.connect()`. **Three new S03.8 patterns established for S03.9+**: (1) **Cross-listing consistency validator** — when multiple constituent rows share an entity PK, validate structural-field agreement; use `"key" in dict` for presence detection (NOT `.get(...) is None` which collapses key-absent with value-None); (2) **Defensive safety-net lookup** — `_DEA_DEADLINE_LOOKUP` is the prototype; fields that COULD be missing from per-row extraction but live in front-matter get an adapter-level fallback whose runtime firing must RAISE (not WARN) so CI catches drift; place the check AFTER the builder + BEFORE the dry-run short-circuit; (3) **Override-dispatch table** — `_KNOWN_CROSS_LISTING_OVERRIDES` is the prototype; for known-irreconcilable real-PDF inconsistencies, document them in a state-adapter-owned dict with rationale strings + page citations; fail loud on undocumented conflicts. **Three real-PDF findings surfaced** (informational for S03.9+): (a) DEA front-matter (pp. 5/9/10/11) carries authoritative species×license-type metadata not repeated per-row — per-row null is sometimes intentional; (b) cross-listed licenses can carry STRUCTURALLY-CONFLICTING data per real per-HD-cap semantics (HD 210: quota=300 home, quota=200 cross-listings); (c) DEA `apply_by="OTC:\nJun 15"` cells are load-bearing OTC discriminators that the original heuristic missed. **Q17 opened in `docs/open-questions.md`** — per-HD allocation caps in `draw_spec`; M2 ADR-candidate; V1 handles via override table. **`draw-mechanics.md` now has 3 entries**: 900-20 "first and only choice" ordering; MT NR-cap (MCA Title 87-2-106) 10% structure not modeled; per-HD allocation caps (HD 210 case). **Four new pitfalls** in `.roughly/known-pitfalls.md` (DEA cross-listed B Licenses can carry CONFLICTING structural fields across HD sections; DEA license_tag.kind requires inspecting `apply_by`, not just `license_code`; DEA front-matter carries authoritative deadlines when per-row `apply_by` is genuinely null; subagent-authored docs can reference stale task IDs from earlier plan drafts). **No ADRs created** — implementation refines ADR-010 (decomposed entities), ADR-012 (sibling entity referenced from license_tag by FK), ADR-018 (license_season + draw_spec composite-PK references operationalized). Test suite: **910 passed + 2 skipped** (+78 from S03.8). New testing patterns: `TestNoLibImports`-style AST guard for state-agnostic-clean; `caplog`-based WARN-log assertion; real-artifact regression locks against pinned counts. Working note at `docs/planning/epics/E03-confidence-findings/S03.8.md` (~280 lines; deletes at m1 tag per ADR-017 §6). **E03 S03.9 (reporting_obligation ingestion) closed 2026-05-21** via PR `195ac8b` (squash-merged from `feat/S03.9-reporting-obligation-ingestion`; pre-squash branch carried 1 main implementation commit + 7 cubic-review fix-up rounds adding ~250 LOC + 18 tests + 1 new open question Q19) after a three-blocker source-audit probe (2026-05-19) reshaped V1 scope. Final state: **3 reporting_obligation rows + 70 regulation_reporting link rows** written atomically from the bear artifact (`black-bear-2026.json` top-level `reporting_obligations` list; no upstream extraction changes). Composition: 1 STATEWIDE `harvest_report` (48-hour) + 1 R1 `tooth_submission` (10-day) + 1 R2-7 `hide_skull_presentation` (10-day). Link arithmetic: 35 + 14 + 21 = 70 (every bear regulation_record gets the STATEWIDE link; R1 BMUs `{100,101,103,104,110,120,121,122,123,130,140,141,150,170}` get tooth_submission; R2-7 BMUs get hide_skull). New adapter `ingestion/states/montana/load_reporting_obligations.py` (~600 LOC, state-agnostic-clean per AST guard) with the established three-phase shape (build → guards pre-`db.connect()` → conn/loops/commit/rollback/close) and a single `_REPORTING_ROW_SPEC` dispatch dict keyed by `(region_scope, kind_hint)` carrying all 3 rows' structured-field interpretation (id slugs, submission_method choices, applies_to_regions, what_to_present). New DB helpers `upsert_reporting_obligation` + `write_regulation_reporting` in `ingestion/ingestion/lib/db.py` (no-commit, caller-controlled txn; `Json(...)` jsonb wrap per project convention). OQ7 row-count guards: `[2, 5]` for reporting_obligation, `[49, 91]` for regulation_reporting, both fire BEFORE `db.connect()`. **Three originally-spec'd row types were carved out or deferred via the three-blocker probe**: (1) **Bear ID coursework** → S03.6.1 carve-out (text exists on page 2 of bear booklet but `reporting_obligation` is the wrong target table since pre-purchase prerequisites are neither post-harvest nor in-season; new `MT-STATEWIDE-bear` regulation_record with the Bear ID Test verbatim in `additional_rules` will land in S03.6.1, mirroring `MT-STATEWIDE-antelope`'s pattern); (2) **CWD sampling** → Q18 + `docs/planning/epics/E03-deferred-items/cwd-sampling-modeling.md` deferred to M2 (license-keyed in artifact, not zone-keyed: HD 103's `Deer Permit: 103-50` carries the sampling rule but isn't inside the Libby CWD-zone overlap — zone-keyed `geometry-overlays.json` join would miss it; rules already in `regulation_record.additional_rules` from S03.6); (3) **General mandatory reporting "for all V1 species"** → epic line 807 is the bug (FWP authoritative list is bear / wolf / marten / migratory swans; no mandatory reporting for deer/elk/pronghorn; the FWP "Harvest Survey" is a voluntary telephone sampling program, not a reporting obligation); draft diff surfaced at S03.9 close, pending PM approval (not silently edited). **Six review-triad findings applied during Stage 6** (1 HIGH, 1 MEDIUM, 4 P2): duplicate-source_id guard (silent-failure-hunter HIGH; raises if `sources_by_id` collapses); `supersedes` now read from `source_dict.get("supersedes")` instead of hardcoded `None` (code-review W1); per-row `KeyError` wrapped with diagnostic `RuntimeError` naming row index + present keys (silent-failure-hunter MEDIUM); `_LOGGER: Final[logging.Logger]` annotation dropped (static-analysis P2; matches S03.6/S03.7/S03.8 convention); `_RowSpec` `kind` and `submission_method` tightened to `Literal[...]` types (static-analysis P2; eliminates 2 `# type: ignore[arg-type]` suppressions); test_db.py fixtures aligned with production `_REPORTING_ROW_SPEC` values exactly (code-review W2; `_make_r1_obligation` now has `deadline_hours=240`, `what_to_present=["both premolar teeth"]`, IDs match production slugs; `test_upsert_handles_nullable_fields` reworked to cover R1 + STATEWIDE sub-cases). **Four new pitfalls in `.roughly/known-pitfalls.md`** under Conventions — Ingestion adapters: (a) E03 story discovery must source-audit upstream artifacts for every epic-required row type before planning (pattern bit twice — S03.8 B5, S03.9 three-blocker); (b) `reporting_obligation.kind` semantic boundary — post-harvest/in-season only; pre-purchase prerequisites belong in `regulation_record.additional_rules` keyed by a STATEWIDE anchor; (c) `submission_method` interpretation for multi-modal source text — pick the headlined modality; the `verbatim_rule` preserves the full source faithfully so the structured-field choice is non-lossy; (d) `id text`-PK UPSERTs carry a latent slug-drift risk when slug-encoded fields can be UPDATEd (added during cubic-review round 3; local module-load drift guard added as belt-and-suspenders; project-wide fix is Q19). **Q18 + Q19 added** to `docs/open-questions.md`: Q18 (CWD sampling target-table modeling; M2 ADR candidate); Q19 (id text-PK UPSERT slug-drift; **pre-M2 blocker** affecting 3 helpers in `db.py` — `_UPSERT_SEASON_DEFINITION_SQL` + `_UPSERT_LICENSE_TAG_SQL` + `_UPSERT_REPORTING_OBLIGATION_SQL`; must land in a single PR before first year-over-year re-ingestion run; ADR required at resolution; leading option is derive-and-assert generalizing S03.9's local pattern). **Seven cubic-review rounds during PR review** added 18 tests + the 4th pitfall + Q19; final test count: **981 passed + 2 skipped** (+71 net from S03.9; +53 at main commit `136454d`, +18 across the 7 cubic-review rounds). **Three new patterns established for S03.6.1/S03.10+**: (1) OQ7 row-count guard is the canonical count authority (in-builder exact-count checks contradict band semantics — remove; build = data construction; bands live in `main()`); (2) defensive fail-loud surface for artifact-list iterations (`isinstance(element, dict)` BEFORE key access; try/except KeyError → RuntimeError wrapping with per-element diagnostics index+keys-present; duplicate-id guard for composite-PK collision risk); (3) `reporting_obligation.kind` semantic boundary (post-harvest/in-season only). Working note at `docs/planning/epics/E03-confidence-findings/S03.9.md` (deletes at m1 tag per ADR-017 §6) records probe findings, design decisions, source-audit method, draft epic line 807 diff. **No ADRs created** — implementation refines ADR-001 (no invented data), ADR-008 (verbatim discipline — empty cleanup-rules section), ADR-010 (decomposed entities + link-table pattern), ADR-017 (confidence inheritance — no confidence column on `reporting_obligation`). **S03.6.1 (MT-STATEWIDE-bear anchor + jurisdiction_binding to MT-STATEWIDE-geom) and S03.10 (jurisdiction_binding generation) are now queued next**; S03.6.1 spec hardened 2026-05-21 with binding scope-add (PM decision) so the statewide-anchor entity ships fully bound at story close. S03.6.1 also introduces the `db.upsert_jurisdiction_binding` helper that S03.10 reuses for its broader overlay-derived binding generation (mirrors the S03.6 → S03.8 helper-introduction pattern with `update_legal_description`). Binding-id encoding locked at story close so S03.10's symmetric derivation UPSERTs as a no-op. Merge order updated to `… → S03.9 → S03.6.1 → S03.10 → …`. **E03 S03.6.1 (MT-STATEWIDE-bear anchor with Bear ID Test + jurisdiction_binding to MT-STATEWIDE-geom) closed 2026-05-22** via PR #40 (squash-merged as `339e213` from `feat/S03.6.1-mt-statewide-bear-anchor`): three modules amended in one story. **Extraction amendment** to `ingestion/states/montana/extract_black_bear.py` adds page-2 right-column extraction via `_extract_statewide_rules(pdf, source_id, source_publication_date, extracted_at)` with two regex anchors (`_BEAR_ID_START_RE` matches "A hunter may purchase only one Black Bear License per year." and `_BEAR_ID_END_RE` matches "fwp.mt.gov/hunt/education/bear-identification"); start-anchor-missing returns `[]` (downstream count guard catches the regression), end-anchor-missing raises loud. New top-level field `statewide_rules: list[StatewideRuleCandidate]` on `black-bear-2026-base.json` and `black-bear-2026.json` (corrections artifact unchanged — page 2 not touched by the 2026-03-18 correction PDF). Determinism verified via two consecutive extraction runs producing byte-identical SHA-256 (`a09aefdb845257c13a85ba6ed5c6e81191e6ab34e5ea16d2e59d7ef2b99a8fb8`); diff against pre-S03.6.1 artifact is `statewide_rules`-only — 35 BMU rows + 3 reporting_obligations + 2 closure_predicates byte-identical. **Adapter amendment** to `ingestion/states/montana/load_regulation_records.py` adds `_build_statewide_bear_record(bear_artifact, bear_citation)` (fail-loud on missing/empty/multi `statewide_rules` field) and `_build_statewide_bear_binding(record)` building one new `JurisdictionBinding` with deterministic id `"US-MT-MT-STATEWIDE-bear-bear-2026-primary_unit-MT-STATEWIDE-geom"` (locked by `TestBuildStatewideBearBinding::test_id_encoding_is_deterministic`). New module constants `_BEAR_BOOKLET_CITATION_ID`, `_MT_STATEWIDE_GEOM_ID = "MT-STATEWIDE-geom"`, `_JURISDICTION_BINDING_ID_FORMAT = "{state}-{jurisdiction_code}-{species_group}-{license_year}-{role}-{geometry_id}"`. Final per-loader counts: **437 regulation_record rows + 1 jurisdiction_binding row + 228 legal_description updates** in a single atomic transaction (FK ordering: regulation_record loop before jurisdiction_binding loop before legal_description loop; single `conn.commit()`). `_SPEC_ESTIMATE_TOTAL` updated 514 → 437 with band `[305, 568]`; new `_JURISDICTION_BINDING_EXPECTED_TOTAL = 1` with exact-match `[1, 1]` band (S03.10 broadens). Three pre-existing `TestCountGuard` tests updated to match the new baseline. **New DB helper** `db.upsert_jurisdiction_binding(conn, binding)` in `ingestion/ingestion/lib/db.py` — no-commit, UPSERT-by-id with all 9 columns in EXCLUDED clause (no `ingested_at` column on the binding table per DDL `supabase/migrations/20260425000000_initial_schema.sql:193-216`); fail-loud on `cur.rowcount == 0`. Mirrors S03.6's introduce-helper-ahead-of-S03.8-reuse pattern (`update_legal_description`); S03.10 will reuse this helper as-is. **id encoding** locked: only PK-equivalent fields (`state`, `jurisdiction_code`, `species_group`, `license_year`) + `role` + `geometry_id` are id-encoded — `verbatim_rule` and `source` are NOT encoded and can update freely on re-ingest without producing a new row (contains Q19 slug-drift risk locally; project-wide derive-and-assert fix still tracked in `docs/open-questions.md` Q19). S03.10 carry-over: must use this exact format string so the bear binding written here is a stable UPSERT no-op when S03.10 re-derives it. **Pre-discovery verification (AC #936)** confirmed: `MT-STATEWIDE-antelope` `additional_rules` carries exactly 2 NOTE entries from `_extract_note_lines` against DEA `900-20` (Boundary Change + New Hunting District); `jurisdiction_binding` table had zero rows pre-S03.6.1 (no adapter wrote to it); `MT-STATEWIDE-geom` present in `geometry` table from S03.0. **No ADRs created** — refines ADR-001 / ADR-008 / ADR-010 / ADR-012 / ADR-017 / ADR-018 / ADR-019. **No new pitfalls or open questions** (Q19 unchanged status; M2-deferred items: `rule_hint` Literal convention if a second-state pre-purchase prerequisite shape emerges; S03.10 must remove the exact-match `[1, 1]` band and replace with broader overlay-fanout-sized band). **`extracted_at` threading discipline reinforced**: `_extract_statewide_rules`'s 4th parameter takes the manifest-derived `extracted_at` string explicitly (was initially using `source_publication_date` as a fallback during T2; T3 fixed to thread the real manifest timestamp through, matching the convention used by `_extract_reporting_obligations`). Test suite: **1024 passed + 2 skipped** (+43 net from S03.6.1; 8 new test classes: `TestExtractStatewideRules`, `TestUpsertJurisdictionBinding`, `TestBuildStatewideBearRecord`, `TestBuildStatewideBearBinding`, `TestJurisdictionBindingCountGuard`, `TestRegulationRecordCount`, `TestAtomicTransaction`, `TestNoLibImports`). Working note at `docs/planning/epics/E03-confidence-findings/S03.6.1.md` (deletes at m1 tag per ADR-017 §6). **S03.10 (jurisdiction_binding generation) is now queued next**; the bear binding is the baseline (count guard starts from 1, not 0). **E03 S03.10 (jurisdiction_binding generation) code-complete 2026-05-26** via PR `e83ef2d` (squash-merged from `feat/S03.10-jurisdiction-binding-generation`; 8 pre-squash commits `3003d46..3a851b5`: 1 main implementation + 7 review-driven fixes from triad + 4 PR-review iteration rounds): `ingestion/states/montana/load_jurisdiction_bindings.py` (~870 LOC, state-agnostic-clean per `TestNoLibImports` AST guard) is the **first cross-cutting binding loader**. Reads the committed `geometry-overlays.json` fixture × `regulation_record` cross product, applies per-species filtering, walks 4 binding sources (statewide / overlay self-row / overlay portions / no-hunt-zone Option A), and UPSERTs in one atomic three-phase transaction via `db.upsert_jurisdiction_binding` reused from S03.6.1 (**no `db.py` changes** — read SELECTs live in the adapter per S03.10 constraint #7 that `db.py` is reserved for write/mutation). **T16 live UAT is operator-pending** — `regulation_record` table was empty at T0 probe (2026-05-23); requires batch run of S03.0/S03.6/S03.6.1/S03.7/S03.8/S03.9 before binding writes can occur. Operator runbook documented in working note. **Projected ~1000 bindings** (2 statewide + ~940 overlay + ~59 no-hunt-zone) inside `[400, 1100]` guard band; narrow to ±30% around observed count after T16. **T16 does NOT gate S03.11 or S03.12 planning** — S03.10 is code-complete; downstream stories can begin. **Four PM-approved spec deviations** baked in and footnoted in the epic (L1045/L1070/L1092/L1311+L1313): (1) **id format** imports `_JURISDICTION_BINDING_ID_FORMAT` from `load_regulation_records.py` — spec L1045's `binding-{geometry_id}-{role}` was stale; importing S03.6.1's locked constant preserves the bear-binding UPSERT no-op contract (3 separate tests lock byte-identity); (2) **count band** `[400, 1100]` intentionally wide pending T16 (spec's 1,500-3,500 was ~2x too high; deer×3-species multiplicity over-counted); (3) **source attribution** via adapter-local SELECT `SELECT id, source FROM geometry WHERE id = ANY(%s)` rather than a new `db.py` helper, with `Pydantic ValidationError` wrapped to surface the failing geometry id (review-fix from triad CRITICAL C2); (4) **no-hunt-zone "nearby" rule** simplified to single-clause `extensions.ST_DWithin(zone.geom, hd.geom, 5000)` on native geography type (boundary-to-boundary, canonical PostGIS pattern) — spec's two-clause `ST_Touches OR ST_DWithin(centroid, centroid, 5000)` returned 0 matches for ALL 3 orphan zones at 5km on real Montana data because park centroids are >5km from every HD centroid (Yellowstone's centroid is ~30km deep in the park). No new ADR required — refines implementation of an existing rule. **18 fail-loud guards** added across review-triad + 4 PR-review iteration rounds (all locked by tests): overlay-row-not-dict; overlay-row-missing-keys (names index + present keys); unknown `role_for_e03` (caught twice — `_VALID_ROLE_FOR_E03` builder gate + independent `is_binding_eligible` check); unknown `species_group`; unhandled `jurisdiction_code` pattern in `_derive_parent_geometry_id`; per-builder duplicate-id (overlay + no-hunt-zone); cross-builder global duplicate-id in `main()` after concat (review-fix C1); per-zone zero-nearby-HDs (AC #1091); empty `regulation_record` table with diagnostic naming prior loaders (review-fix W1); **pair-aware** missing-statewide on `(jurisdiction_code, species_group)` (review-fix-to-review-fix catching wrong-species corruption); symmetric pair-aware unexpected-statewide (catches new anchors added without ADR-018 amendment OR wrong species_group); reg_record's `parent_geometry_id` has no overlay-fixture entries (review-fix P1); missing `source_lookup` for `child_geometry_id` (with diagnostic); geometry source jsonb malformed — names the failing geometry id (review-fix C2); row-count band `[400, 1100]`; SQL filters HDs by `state` (prevents M2 Colorado cross-state silent under-binding; review-fix P2); SQL filters by `license_year` (prevents multi-year fan-out; review-fix P2); SQL excludes `kind='portion'` (post-merge fix — portions don't map to HD-keyed reg_records; including them caused silent skips). **Seven new pitfalls in `.roughly/known-pitfalls.md`** (3 under Integration — Supabase / PostGIS; 4 under Conventions — Ingestion adapters): (A) `geometry.geom` column (geography type), not `geog` — CLAUDE.md prose uses "geog" colloquially; DDL is authoritative; (B) PostGIS lives in `extensions` schema in Supabase projects; all `ST_*` calls must be `extensions.`-prefixed in raw SQL (bare names fail with "function does not exist" — E02 + S03.10 both hit this); (C) centroid-to-centroid `ST_DWithin` produces zero matches for large polygons; use boundary-to-boundary geography `ST_DWithin(geom, geom, distance)` — canonical PostGIS pattern for lat/lon "nearby" queries; (D) `with db.connect()` context manager commits on clean exit + rolls back on exception — explicit `conn.commit()` is the "real" gate; implicit commit-on-exit is a no-op safety net; (E) per-builder dedup sets miss cross-builder collisions; multi-builder adapters need a global dedup check in `main()` before write; (F) `Model.model_validate(...)` inside a dict-comprehension swallows row identity; use explicit for-loop with try/except naming the id; (G) **"Live-DB write is operator-driven"** — closure-note row counts describe dry-run-verified shape; live writes happen only when operator invokes the loader explicitly. Future stories depending on prior loaders' DB state must run them first or mock — they can't assume the DB matches the closure-note narrative. **Q19 NOT propagated to this module**: `db.upsert_jurisdiction_binding` already has a stronger mitigation than the 3 Q19-affected helpers — it excludes identity fields from UPDATE entirely (S03.6.1 OQ-S6.1-4), so identity drift on UPSERT is impossible at the SQL layer. S03.10 does not introduce a local module-load drift guard. **Q19 ADR remains a pre-M2 blocker** for the 3 helpers it does affect (`_UPSERT_SEASON_DEFINITION_SQL` + `_UPSERT_LICENSE_TAG_SQL` + `_UPSERT_REPORTING_OBLIGATION_SQL`). **Bear binding from S03.6.1 stable** — re-derived symmetrically via the imported format constant; UPSERT no-op locked by 3 separate tests (`TestBuildStatewideBindings::test_bear_binding_id_byte_identical_to_s036_1_locked_value`, `TestIdStability::test_statewide_bear_id_locked`, `TestRealArtifactRegression::test_bear_statewide_id_is_upsert_noop_with_s036_1`). **Five M2-deferred items recorded**: (1) `role='other_overlay'` semantic awkwardness for no-hunt-zone bindings (DDL enum has 7 values; none captures "this HD's regulations don't apply inside this zone"; V1 ships with `other_overlay` (only DDL-permitted choice); M2 candidate: add `'no_hunt_zone'` role enum value (requires DDL migration + ADR) or clarify `other_overlay` semantics in schema docs); (2) future portion-keyed reg_records — 3 coordinated touchpoints if any future state introduces them: extend `_derive_parent_geometry_id` to produce portion-shaped ids; re-introduce portions to `_query_nearby_hds_for_zone`'s SQL (currently HD-only by deliberate design); update `TestQueryNearbyHdsForZone::test_sql_excludes_portions_regression_guard`; (3) cross-state spatial pollution in M2+ — when Colorado geometries land, the `hd.state = 'US-MT'` filter on `_query_nearby_hds_for_zone` prevents cross-state silent under-binding; the next-state implementer for Colorado will need a paired adapter that filters by `_STATE = 'US-CO'` (pattern is established); (4) count band narrowing after T16 produces empirical count; narrow `_BINDING_COUNT_GUARD_BAND` to ±30% around observed value and update AC #1087 footnote; (5) cell-level source attribution — V1 attributes binding source from the geometry table (single source per geometry); multi-source provenance schema field needed if a future state has multi-source HDs. **No ADRs created** — implementation refines ADR-010 (decomposed entities — first cross-cutting loader), ADR-014 (source-citation `gis_layer` document type), ADR-016 (overlay fixture provenance), ADR-017 (spatial-confidence carve-out — no `confidence` column on `jurisdiction_binding`), ADR-018 (statewide bindings + `MT-STATEWIDE-geom`). **S03.10 produces no audit signal for S03.11's confidence-calibration framework** — per ADR-017 §2, `jurisdiction_binding` has no `confidence` column. S03.11's audit subjects are the entities from S03.6 / S03.7 / S03.8 (`regulation_record`, `season_definition`, `license_tag`, `draw_spec`). **S03.11 is now unblocked.** Test suite: **1128 passed + 2 skipped** (+104 net from S03.10; new test file `ingestion/tests/test_load_jurisdiction_bindings.py` ~1530 LOC includes coverage for all 18 fail-loud guards + the 3 bear-binding-stability locks). Working note at `docs/planning/epics/E03-confidence-findings/S03.10.md` records T0 probe results, fan-out math, post-review fix logs, M2 follow-ups, operator handoff for T16 (deletes at m1 tag per ADR-017 §6). **Four spec footnotes added** to the E03 epic: `[^id-format-correction]` at L1045 + N4 glossary L1311; `[^nearby-rule-correction]` at L1070 + AC #1090 + Open Question item 3 L1273 + S5 glossary L1313; `[^row-count-correction]` at L1092 (AC #1087 band); plus the existing footnote-style spec-correction at L1092. **S03.11 (Confidence calibration audit + ADR-017 finalization) is now queued next**; S03.10's no-audit-signal carve-out (per ADR-017 §2) means S03.11's audit set is the 4 entities written by S03.6/S03.7/S03.8. **E03 S03.11 (Confidence calibration audit + ADR-017 finalization) closed 2026-05-27** via PR #42 (squash-merged as `b84955c`, on main at merge commit `7021bda` from `feat/S03.11-confidence-calibration`). **Verdict: FINALIZE.** **ADR-017 active file unmodified** (status remains `Accepted`; all 7 numbered sections untouched — verified `git diff --name-only -- docs/adrs/ADR-017-confidence-calibration.md` empty across all S03.11 commits). **Q11 RESOLVED 2026-05-27** in `docs/open-questions.md`. Audit shape: **50-row stratified sample × 10 documented edge cases (EC1-EC10)**, artifact-based per S03.10 T0 probe finding `regulation_record` empty 2026-05-23 (sources `dea-2026.json` + `black-bear-2026.json`). Per-EC: 5 each for EC1/EC2/EC5/EC6/EC7/EC9/EC10; EC3 = 7 (5 stratified + 2 random fill); EC4 = 2 (full population — only 2 V1 statewide overlays); EC8 = 6 (3 `sex_threshold` + 3 `quota_threshold` subtypes). Strict **4-step anti-circularity discipline** enforced (pull F12 inputs → apply ADR-017 rules independently → reveal artifact value → compare). **Pass-rates scoped per ADR-017 §3**: `regulation_record.confidence` 39/39 = 100% (headline); EC8 closure_predicate parent-inheritance 6/6 = 100%; EC9 license_tag row-level extraction_confidence 5/5 = 100%; **combined 50/50 = 100%**. **Tier distribution across projected 437 regulation_record rows**: `high=32 (7.3%) medium=405 (92.7%) low=0 (0.0%)`. **Defer-or-finalize arc**: PM's original recommendation 2026-05-26 was PARTIAL DEFER under LITERAL reading of ADR-017 §7 Trigger 2 (`low=0` literally fires); independent architectural review by `roughly:epic-reviewer` returned LAND-WITH-EDITS verdict on PM's DRAFT; PM applied 4 review-recommended refinements; **user decision 2026-05-27 overrode to FINALIZE under INTENT reading** (`low=0` is absence-by-data-property, not absence-by-framework-gap; the LOW rule exists in `_assign_row_confidence`, is unit-tested by `TestDemoteOneTier`, behaves correctly when input conditions present — V1 Montana data simply doesn't exercise it). ADR-017 amendment DRAFT was created, reviewed independently, refined, then rejected at user-review time and deleted as part of the FINALIZE workflow. PM's full deliberation preserved in synthesis report §5.2 + §6 as audit-trail (not erased by user override). **Synthesis report** at `docs/planning/epics/E03-confidence-calibration-synthesis.md` (~360 lines, PM-owned, **survives m1 tag** because it lives outside `E03-confidence-findings/`): §1 edge-case catalogue (10 cases); §2 sample (50 rows); §3 audit results + scoped pass-rate table; §3.1 tier distribution; §4 open issues; §5/§7 trigger analysis with PM-deliberation/user-decision distinction explicit; §6 verdict (FINALIZE). **F12 retrofits** to 4 prior working notes (delete at m1 per ADR-017 §6): `S03.3.md` (5 row-level DEA examples; count narrative aligned to committed artifact 272 sections / 1190 rows / 601 HIGH + 589 MEDIUM — prior narrative was stale at 264/1178/589+589); `S03.4.md` (3 BMU examples: 100, 411, 700); `S03.6.md` (load-bearing 10-record F12 trace covering deer fan-out + MIN-aggregation + statewide overlay + bear correction-touched + Region 7 portions; HD-XXX → HD 250 backfilled; HD-YYY documented as intentional null-result); `S03.6.1.md` (MT-STATEWIDE-bear/bear with `correction_touched=False`). **One new pitfall** in `.roughly/known-pitfalls.md` under Conventions — Documentation & planning discipline: "Deferred open-question verdicts require an amendment-pending breadcrumb in `docs/open-questions.md`" (generalizable lesson; applies to any future audit that DEFERS rather than RESOLVES, even though this audit went FINALIZE). **Four generalizable lessons captured** for future stories: (1) deferred audits need an open-question breadcrumb that survives m1 working-note deletion; (2) **anti-circularity discipline for framework audits** — derive `framework_predicted` from F12 inputs INDEPENDENTLY of artifact's pre-computed value (artifact is what's being tested, not the source of truth for the test); encoded in synthesis §3 methodology block; (3) **scoped pass-rate calculations** — when sample rows audit different schema layers (regulation_record.confidence vs parent-inheritance vs row-level pre-MIN), the headline pass-rate must be scoped to the canonical entity per ADR-017 §3, not rolled together; synthesis §3 has scoped table distinguishing the three properties; (4) **PM does NOT modify the active ADR autonomously** — the DRAFT-file pattern (`docs/adrs/ADR-XXX-amendment-DRAFT.md`) is the canonical handoff mechanism for proposed amendments; PM's audit-trail reasoning is preserved even when the user overrides. **8 stale-reference fixes** as a separate sweep within the same PR (root cause: prior `docs/plans/` → `.roughly/plans/` migration used `git mv` which preserves history but doesn't grep for path references): `docs/open-questions.md` Q15 Resolution-home link; S03.0/S03.6/S03.7/S03.8 working notes; S03.6 plan self-reference; `ingestion/states/montana/load_seasons_and_licenses.py` module docstring; `ingestion/tests/test_extract_black_bear.py` skip-reason string. **No new ADRs created**; **no test changes** (suite holds at **1128 passed + 2 skipped** — doc-only PR; no regression). **Locked context for S03.12 (final M1 story)**: (1) **M1 UAT criterion #8** ("ADR exists documenting Q11 confidence calibration") satisfied as-is by ADR-017 unmodified; (2) **no amendment-pending state** carries into M1→M2 handoff document; (3) **working-notes deletion** (`git rm -r docs/planning/epics/E03-confidence-findings/`) + `.gitignore` update happen in S03.12's final commit (synthesis report survives); (4) **S03.10 T16 live UAT folds into S03.12** as the prerequisite for PRD success-criteria #1-7 (the operator batch S03.0/S03.6/S03.6.1/S03.7/S03.8/S03.9/S03.10 runbook is already in the S03.10 working note). **S03.12 (M1 UAT preparation + handoff to M2) is now queued next** as the final M1 story. **12 real-PDF discoveries** documented in closure note: rotated table requires `_reverse_cell_text` per cell, table spans pp. 9-12 (not 10-11), BMU 530 absent, correction is prose-only (operations synthesized per BMU), regional map is image-only (`hd_region` derived from in-table column markers), Permit Managed sub-table on p. 11 skipped via state flag, quota cells have 3 distinct word orderings, female-sub-quota BMUs carry `*` footnote suffix, page-7 closure prose is two-column (right-column bbox crop required), phone-number digit-linebreak preserves the structural dash, drift guards first-sentence-scoped. **Two cubic-review iterations during wrap-up** caught and fixed correctness defects in `_merge_with_corrections`: (a) per-(bmu, field) loop overwrote row-level source_id (refactored into 3 stages with row-level state updated exactly once per touched BMU); (b) `demote_one_tier` fired N times for a row touched by N corrections (moved to Stage 3 to fire exactly once per touched BMU, per ADR-017 §4 single-step rule); (c) initial `max(..., key=date)` was iteration-order-dependent on equal-date ties (added two-pass selection with lex-smallest-source_id tiebreaker). All three regressions locked by dedicated unit tests. **Eight new pitfalls in `.roughly/known-pitfalls.md`** (6 from initial implementation + 2 algorithmic patterns surfaced by cubic-review iteration). Test suite: **574 passed + 2 skipped** (+85 tests + 1 new skip). PM-driven UAT 2026-05-12 verified 4 BMUs (411 quota-closure+female-subquota, 300 Spring Closure, 100 unrestricted, 700 quota-closure) against `pdfplumber`-extracted source; closure prose + 3 reporting obligations cross-checked against PDF page 7. One faithfulness caveat documented: rotated-table extraction scrambles `opportunity` / `verbatim_text` word order (all source words preserved; sentence flow not); structured fields are clean and verifiable; meets ADR-008 verbatim discipline. **Two M2-deferred items recorded:** cell-level source attribution (would require `BmuRowExtraction` schema split); Spring Season Closure temporal anchor ("at any point after May 31") needs structured field if pattern recurs in future states (`effective_after: date | null` ADR-candidate at `docs/planning/epics/E03-deferred-items/closure-temporal-anchors.md`). **S03.5 (Legal Descriptions extraction) is now unblocked and is next; S03.6 (regulation_record ingestion) can begin in parallel since `dea-2026.json` + `black-bear-2026.json` are both ready.** **E03 S03.2 (PDF extraction primitives — shared library) shipped 2026-05-06** on `feat/S03.2-pdf-extraction-primitives` (3 commits, `80fcca0..ceaca2c`): `ingestion/ingestion/lib/pdf.py` (332 LOC, state-agnostic per ADR-005) wrapping `pdfplumber` 0.11.x with 12 public exports — `PdfDocument`, `open_pdf`, `iter_pages`, `extract_text`, `extract_tables` (returns `TableMatch` records with bbox + headers + rows; uses `page.find_tables()` not `page.extract_tables()` because only `find_tables()` carries spatial info), `find_section` (regex-anchored heading lookup), `PageReference` TypedDict + `page_reference_to_str` collapse helper, `ConfidenceTier(str, Enum)` mixin so instances ARE strings (no `.value` access at write-sites for `regulation_record.confidence`), `min_tier` (most-uncertain-wins MIN with explicit `key=lambda t: t.rank` — guards against the lexicographic trap where naive `min(["high","low","medium"])` returns `"high"` because h<l<m), `demote_one_tier` (ADR-017 correction-touched rule, floor-clamped), and `PdfExtractionError`. ADR-008 boundary clarified: `extract_text` adds zero wrapper-level normalization on top of pdfplumber's word-grouping (which collapses repeated *internal* whitespace as part of glyph-to-text reconstruction — PDF content streams don't carry literal inter-word spaces); paraphrase prevention is fully met because numeric tokens, units, and lexical words are byte-exact. Defense recorded in `docs/planning/epics/E03-confidence-findings/S03.2.md`. Char-level reconstruction (`page.chars`) remains available as a future helper (`extract_text_chars_raw`) if a downstream story needs byte-exact text — not retrofitted onto `extract_text`. `extract_tables` emits `_logger.warning` when `find_tables()` locates a boundary but `Table.extract()` returns [] (silent-failure-hunter P1 fix; surfaces parse ambiguity rather than recording empty rows). 39 new tests in `test_pdf.py` (404 total; ruff + mypy clean across 7 source files), including `test_lexicographic_trap_explicit` (three assertions locking against rank-ordering regressions on raw strings AND on enum `.value`), `test_no_layout_true_regression_guard` (monkeypatch spy ensures `extract_text` does not silently set `layout=True`), `test_empty_table_emits_warning`, and `TestPdfNoStateAdapterImports` (AST-walk + slug-substring scan). Two new pitfalls in `.roughly/known-pitfalls.md` under "Integration — pdfplumber": (a) `extract_text` collapses repeated spaces (not byte-exact verbatim) — `page.chars` is the byte-exact escape hatch; (b) `page.extract_tables()` drops bboxes; use `page.find_tables()` when bbox is needed. **S03.3 (DEA booklet extraction) is next.**

## Architecture

```
Data Sources (state F&W agencies, PAD-US, BLM)
       ↓
  ingestion/ (Python) — per-state adapters, offline pipeline
       ↓
  Supabase Postgres + PostGIS — single source of truth
       ↓
  mcp-server/ (TypeScript) — canonical interface, 5 MCP tools
     ↓          ↓
  web/ (Next.js)  plugin/ (Claude Code plugin)
```

### Architectural commitments (enforced, not aspirational)

- **MCP server is the canonical interface.** Web and plugin are both clients of the MCP server. No surface bypasses it to query the database directly.
- **Ingestion is upstream and offline.** Python pipeline writes to Postgres; the TypeScript serving stack never imports from `ingestion/` or requires Python. Contributors working on serving can ignore the Python toolchain entirely.
- **Authority preserved, not replaced.** Every regulation record requires a source citation (URL, agency, publication date). Regulation text is carried verbatim — no paraphrasing, no summarization. Records without citations fail validation.
- **Schema versioned from day one.** `regulation_record` and `draw_spec` carry `schema_version`; source provenance is tracked via the `source` jsonb field (which includes `publication_date`). The MCP server rejects records with unsupported schema versions.
- **Server returns structure; clients compose presentation.** No server-side `overview` or `headline` fields. Structured sections with always-present, null-bearing fields (null = "not applicable" vs omitted = ambiguous). Each client composes its own summary because each knows its presentation context.
- **Agentic development is first-class.** The Claude Code plugin (`plugin/`) uses `.claude-plugin/` + `plugins/<name>/skills/<skill>/SKILL.md` convention. Documentation is the primary handoff mechanism between sessions.

### Key constraints

- PostgREST API is disabled via RLS — only service-role credentials can read/write. This prevents an uncontrolled second path to the data.
- `draw_spec.parameters` is a `Record<string, unknown>` escape hatch for state-specific quirks. Shared code (MCP server, web client) must NEVER read this field. Only state adapters in `ingestion/states/<state>/` may use it.
- `draw_spec.pools` and `draw_spec.point_system` are stored as `jsonb`. Pool share sum-to-1.0 and eligibility consistency are validated in application code, not DB constraints.
- All geometries use `geography(MultiPolygon, 4326)` — not `Polygon` — because real state data (CPW GMUs, MT HDs) contains multi-part units along state lines.
- Every geometry goes through `shapely.make_valid()` before insert.
- No `any` types in .tsx files.

## Tech stack

| Layer | Language | Key dependencies |
|-------|----------|-----------------|
| Ingestion (`ingestion/`) | Python | pdfplumber, pypdf, unstructured, geopandas, shapely |
| MCP Server (`mcp-server/`) | TypeScript | Anthropic MCP SDK, Postgres connection pool |
| Web (`web/`) | TypeScript | Next.js, Mapbox GL JS, Tailwind |
| Plugin (`plugin/`) | TypeScript | Claude Code plugin conventions |
| Database | SQL | Supabase Postgres + PostGIS extension |
| Migrations | SQL | `supabase/migrations/` timestamped files |

## Build commands (once implementation exists)

```bash
# Ingestion (Python)
make ingest STATE=montana         # Full pipeline: fetch → extract → normalize → validate → load
make ingest-all                   # All states

# MCP Server (TypeScript)
cd mcp-server && npm install && npm run dev

# Web companion (Next.js)
cd web && npm install && npm run dev

# Database migrations
supabase db push                  # Apply migrations to Supabase
```

## Verification commands

Used by `/ruckus:verify-all` and the Stop hook. Run from the repo root.

```bash
# Python ingestion — lint, type, test
cd ingestion && .venv/bin/ruff check ingestion/ tests/
cd ingestion && .venv/bin/mypy ingestion/lib/
cd ingestion && .venv/bin/pytest tests/

# TypeScript serving (when files exist; mcp-server and web both currently empty)
cd mcp-server && npx tsc --noEmit
cd web && npx tsc --noEmit
```

## Data model (6 entities)

- **`regulation_record`** — anchor entity, keyed by (state, jurisdiction_code, species_group, license_year)
- **`season_definition`** — named date ranges with weapon/residency constraints
- **`license_tag`** — permit instruments with optional draw_spec reference
- **`draw_spec`** — draw mechanics, keyed by (state, hunt_code, year). Sibling entity referenced from `license_tag` by FK. Composes `point_system`, `residency_cap`, `choices`, and `allocation_pool[]` — verified against CO, WY, NM, UT draw systems with no state-specific branches in shared code.
- **`reporting_obligation`** — post-harvest/in-season duties, can be region-specific
- **`geometry` + `jurisdiction_binding`** — polygons and their roles (primary unit, overlays like CWD zones, BMUs)

Entities cross-share: e.g., Montana A and B licenses reference the same `season_definition` rows. Corrections update one row, not duplicated copies.

Schema is defined in three places kept in manual sync: TypeScript types (`mcp-server/src/types/`), Python dataclasses (`ingestion/lib/schema.py`), and Postgres DDL (`supabase/migrations/`). Schema version bumps propagate to all three. The canonical type definitions are in [docs/architecture.md](docs/architecture.md).

## Ingestion adapter pattern

Each state lives in `ingestion/states/<state>/` with isolated files:
- `fetch.py` — retrieve source documents
- `extract.py` — pull structured data from sources
- `normalize.py` — map state-specific fields to shared schema
- `validate.py` — common + state-specific validation
- `load.py` — write to Supabase Postgres
- `sources.yaml` — source document registry

State adapters are isolated from each other. Shared code lives in `ingestion/lib/`. Adding a new state means adding a new directory, not modifying shared code.

**FWP DEA table hazards (apply to any state using a similar table format):**

- The "same `license_code` = same data" assumption is FALSE. Cross-listed B Licenses appear in multiple HD sections with different quota values. Validate structural agreement across same-`license_code` rows before writing `draw_spec` rows — see `.roughly/known-pitfalls.md` "DEA cross-listed B Licenses can carry conflicting structural fields."
- `license_tag.kind` for B Licenses requires inspecting the `apply_by` column, not just the `license_code`. OTC B Licenses have an `apply_by` cell prefixed with `OTC:` — classify them as `over_the_counter`, not `limited_draw`. See `.roughly/known-pitfalls.md` "DEA `license_tag.kind` requires inspecting `apply_by`."

## V1 scope

- **States:** Montana, Colorado (deliberately chosen: MT for moderate complexity, CO for draw-system stress-test)
- **Species:** elk, mule deer, whitetail, pronghorn, black bear
- **MCP tools:** `get_regulations`, `check_land_status`, `list_seasons`, `get_tag_requirements`, `get_agency_contacts`
- **Surfaces:** MCP server, web companion, Claude Code plugin

Explicitly out of scope for V1: mobile app, user accounts, automated ingestion scheduling, B2B API packaging, harvest tracking, license purchase proxying.

## Documentation as handoff mechanism

Per ADR-009, documentation is the primary handoff mechanism between sessions (human or agent). The flow: question arises in `open-questions.md` -> resolved in an ADR -> `architecture.md`/`context.md` updated -> question removed. When hitting a decision point, check `open-questions.md` first; escalate new decisions there rather than making silent calls.

## Key documents

- [docs/context.md](docs/context.md) — product frame, what HuntReady is and is not
- [docs/architecture.md](docs/architecture.md) — system design, schema types, response shapes
- [docs/roadmap.md](docs/roadmap.md) — milestones M0-M5
- [docs/open-questions.md](docs/open-questions.md) — unresolved decisions (check before making architectural calls)
- [docs/adrs/](docs/adrs/) — 20 architecture decision records:
  - ADR-001: Authority preserved, not replaced
  - ADR-002: MCP server as canonical interface
  - ADR-003: Ingestion upstream and offline
  - ADR-004: Supabase Postgres + PostGIS
  - ADR-005: Python for ingestion, TypeScript for serving
  - ADR-006: Schema versioned from day one
  - ADR-007: Montana and Colorado as seed states
  - ADR-008: Verbatim regulation text
  - ADR-009: Agentic development as first-class project feature
  - ADR-010: Decomposed entity model (6 entities)
  - ADR-011: Shape C response envelope (null = not applicable, omitted = never)
  - ADR-012: Draw mechanics as sibling entity
  - ADR-013: Server returns structure, client composes presentation
  - ADR-014: `SourceCitation.document_type='gis_layer'` (type-layer enforcement)
  - ADR-015: `geometry.verbatim_rule` column + REG+COMMENTS handling rule
  - ADR-016: Digitization-tolerant containment for geometry overlays (area-ratio thresholds)
  - ADR-017: Confidence calibration + parent-inheritance rule
  - ADR-018: E03 schema additions — `license_season` + `geometry.legal_description` + `geometry.kind='state'`
  - ADR-019: Doc-type precedence in multi-source regulation merge
  - ADR-020: Derive-and-Assert for id text-PK Slug Drift (resolves Q19; shared `drift_guard.py` two-primitive API)

## Environment variables

- `SUPABASE_URL` — Supabase project URL
- `SUPABASE_SECRET_KEY` — secret key (MCP server + ingestion)
- `SUPABASE_PUBLISHABLE_KEY` — publishable key (web app, scoped by RLS)
- `DATABASE_URL` — direct Postgres connection string (ingestion pipeline + migrations only; not used by serving stack per ADR-003)
- `MAPBOX_ACCESS_TOKEN` — Mapbox GL JS access token (web map)
- `HUNTREADY_INGESTION_CONTACT` — *recommended for ingestion runs.* Email or URL appended to the ArcGIS HTTP `User-Agent` as `(contact: <value>)`. Gives upstream data providers (state F&W agencies) a way to reach the operator if a fetch behaves unexpectedly. Empty/unset → User-Agent omits the contact suffix. Kept in env (not source) so the contact can change without a code edit.
