# HuntReady — Planning Index

**Last Updated:** 2026-06-23 (**S06.6.2 Group A closed at-merge 2026-06-23** via PR #73 / `82e34df` — 10th E06 PR; mid-epic hygiene carve-out closing the second-consecutive PAD-US 4.1 republish drift. **5-commit pre-squash chain** (`161d339` feat → `9878f33` plan-historical → `303fade` pitfalls → `968d86a` epic Group A closure → `c47f41b` memo date fix). **Phase B outcome: (a) documented epsilon relaxation** — Phase A characterization revealed RMNP is the **only** zone entering the GeometryCollection branch (the other 9 V1 zones convert cleanly through the existing `Polygon` / `MultiPolygon` paths). **Phase A root-cause finding**: RMNP carries a ring self-intersection that `make_valid()` repairs into `GeometryCollection([MultiPolygon, ~19m LineString])`. The `parsed.area` is **inflated** (the self-intersection double-counts the overlap region); `recovered.area` (polygonal parts only) matches PAD-US `GIS_Acres` to **0.04%**. The `0.0676%` "gap" between `parsed.area` and `recovered.area` is the **self-intersection artifact being removed, not data loss** — recovery is the correct outcome, not a lossy compromise. Max area-loss across all GC-branch zones = **0.0676% < 0.1% threshold**, so outcome (b) re-source did NOT fire (no ADR, no flag-and-discuss). Outcome (c) `buffer(0)` rejected empirically. **The fix (Phase C)**: `ingestion/ingestion/lib/arcgis.geojson_to_multipolygon_wkt` — GeometryCollection area-preservation epsilon relaxed `rel_tol=1e-6 → 1e-3` (single literal + ~20-line rationale comment naming the PAD-US 2026-06-22 republish trigger + the RMNP self-intersection mechanism + the threshold rationale; docstring + WARNING wording refreshed). **Fail-loud preserved**: anything > 0.1% area-loss still raises (genuine overlap loss e.g. ~12.5% would fire at 125× the new threshold); every recovery still logs a WARNING with feature OBJECTID + non-polygonal artifact count. State-agnostic-clean preserved (ADR-005); MT regression-guard via existing `TestGeojsonToMultipolygonWkt` + MT loader test suite stays green. **Phase D fixtures** (per the spec-drift-corrected convention from PM commit `36e487d`): committed `Federal_Fee_Managers_Authoritative_PADUS-0-metadata-20260623T000955Z.json` (drift-detection metadata) + `Federal_Fee_Managers_Authoritative_PADUS-0-manifest-20260623T000955Z.json` (per-fetch SHA + count) — the canonical drift-detection artifacts per `fixtures/.gitignore` convention; the `~1.4MB` `*-features-*.geojson` payload stays gitignored. **Closes deferred S06.6.1 AC #627 (metadata) + AC #628 (manifest) per convention**; `.secrets.baseline` updated for 2 false positives (`layer_hash` SHA-256 + ArcGIS `serviceItemId`). **Phase E tests** (4 added; baseline **1783 → 1787 + 4 skipped**): two epsilon-boundary tests (−0.05% recovers / −0.2% raises pinning the rel_tol=1e-3 threshold), one real-mechanism test (synthetic self-intersecting `Polygon` → `make_valid()` → `GeometryCollection` — the genuine RMNP path, CI-run), and `TestPadusTenZoneIntegration` (skip-if-absent runs against all 10 fixtures committed in Phase D). **Cubic residual P1 accepted-by-design** (documented-deferral; option (c)): cubic objected to the relaxation admitting up to 0.1% loss — **that is the deliberate, evidence-backed, Stage-4-endorsed, silent-failure-hunter-validated decision**. Recorded in the closure memo § "Cubic review disposition"; AC ticked with explicit "not literally `{issues: []}`" annotation. **Quality gates (final, on main)**: pytest **1787 + 4 skipped**; ruff + mypy clean; cubic clean except the documented-deferral P1 (accepted-by-design); detect-secrets passed (2 routine `.secrets.baseline` updates for the new fixture-derived non-secrets). MT-untouched + schema + `db.py` + TS-stack all empty (PRD 002 SC #9 held). **No new ADRs**; refines ADR-001 (fail-loud preserved with documented threshold + rationale comment), ADR-005 + ADR-022 (state-agnostic-clean lib edit). **Pitfalls** (`.roughly/known-pitfalls.md`, now ~692 LOC): new entry § "Integration — ArcGIS" — *single republish changes multiple things; CRS-shift WARNING = full re-audit signal; characterize before relaxing a fail-loud tolerance*; refreshed the stale `rel_tol=1e-6 → 1e-3` reference at the existing make_valid entry. **No open questions touched.** Closure memo at `docs/planning/epics/E06-confidence-findings/S06.6.2.md` (deletes at `m2` tag per ADR-017 §6). **Group A satisfied at-merge** (with the cubic-deferral annotation); **Group B operator-pending** (operator resumes the M2-build pass from Step 4 → 10 against `main`; `load_restricted_areas.py` writes 10 CO geometry rows; **live-fetch `layer_hash` should match the committed manifest `50e486e3…`**; then Steps 5/7/8 unblock; PM ticks Group B in a follow-up doc-only commit once captured). **🚨 Next active work-front: S06.7** (`season_definition` + `license_tag` + `license_season` ingestion — first multi-link-table adapter; `drift_guard.assert_id_matches` mandatory at every per-row entity-construction site per ADR-020; **Q20 (Season Choice `X` method-group modeling per S06.3 discovery) surfaces here** as flag-and-discuss). S06.7 dry-run path can begin in parallel; live execution still gated on the M2-build operator-pass resume completing Steps 4 → 10. **Test baseline floor for downstream stories: 1787 + 4 skipped** (was 1783 going into S06.6.2; +4 from S06.6.2's epsilon-boundary tests + real-mechanism test + 10-zone integration test). **🚨 Strategic candidate carried for M2-release / M3 PM consideration** (NOT in S06.6.2's scope; recorded in the closure memo + carried since the S06.6.2 carve-out): **PAD-US 4.1 source stability** — two distinct republish drifts in 18 calendar days (2026-06-04 → 2026-06-21 OID drift → 2026-06-22 GeometryCollection drift). Options for human consideration at convenience: pin a PAD-US snapshot fixture + cache forever vs continue patching live-fetch drift vs re-source from NPS Boundary Service / USFWS Cadastral / direct downloads. **Prior entry — kept for audit trail: S06.6.2 carved out 2026-06-22** as a **second consecutive mid-epic carve-out** post-S06.6.1 closure — the M2-build operator-pass resume from Step 4 surfaced a second, distinct PAD-US 4.1 republish drift: the first feature in the fetch loop (Rocky Mountain National Park) cleared S06.6.1's `_require_objectid` strict-helper raise but then hit a different fail-loud in `lib/arcgis.geojson_to_multipolygon_wkt` (`arcgis.py:349-374`) — the existing strict GeometryCollection area-preservation guard fired with "polygonal parts do not preserve area" (raise is **correct per the existing discipline**; `math.isclose(recovered.area, parsed.area, rel_tol=1e-6, abs_tol=1e-12)` — meaningful area loss is a data-quality issue, not benign cleanup). The fix isn't "loosen the epsilon" reflexively — it's **investigate first**. **Critically**: only RMNP was reached before the raise; the other 9 zones (Mesa Verde, Great Sand Dunes, Black Canyon of the Gunnison, Dinosaur, Colorado NM, Florissant Fossil Beds, Hovenweep, Yucca House, AFA) are unvalidated against the republished layer. **Spec scope** (5 phases; investigation-first per S06.3.1 Phase A/B/C precedent): **Phase A** — live-fetch all 10 V1 PAD-US no-hunt zones + characterize per-zone (raw geom type / parsed area / make_valid output type / polygonal-vs-parsed area discrepancy / current-path raises-or-passes) in a 10-row table in `docs/planning/epics/E06-confidence-findings/S06.6.2.md`. **Phase B** — decide outcome (a) documented epsilon relaxation (if all affected zones < 1e-4 rel_tol; lock per-zone area-loss in regression test) / (b) re-source affected zones (if any zone > 0.1% area-loss; triggers multi-source provenance ADR — flag-and-discuss to human) / (c) pre-process geometry differently (e.g., `buffer(0)` before `make_valid`; lib refactor). PM expectation is (a) for sub-percent area-loss; (b) only for genuinely-lossy topology. **Phase C** — apply outcome to `lib/arcgis.py` (state-agnostic-clean preserved; MT-regression-guard via existing MT committed fixtures). **Phase D** — commit the live-fetched 10-zone **manifest + metadata fixtures** (drift-detection artifacts per `ingestion/states/colorado/fixtures/.gitignore` convention; `*-features-*.geojson` payload stays gitignored under the uniform-with-MT discipline), **closing deferred S06.6.1 AC #627/#628 inline per convention** (S06.6.1 AC #627's literal "features-*.geojson committed" wording was spec drift the implementation agent caught during S06.6.2 Phase D planning; S06.6.1's surrounding deferral language correctly framed it as "mirrors S05.0/S05.2/S05.4 posture" — metadata + manifest only). **Phase E** — 10-zone integration test confirms all 10 convert cleanly through updated path. **No schema/migration/three-place-sync/`db.py`/MT-touch/TS-stack changes** (unless outcome (b) requires multi-source-provenance ADR, in which case the migration ships in a sibling PR with three-place sync); **no new ADRs autonomously** (PM surfaces outcome (b)'s ADR trigger flag-and-discuss to human before Phase C). Expected test baseline shift 1783 → ~1788-1798 + 4 skipped (additive, +5 to +15 depending on outcome). Branch convention `feat/S06.6.2-padus-geometrycollection-handling` off `main`. Spec at E06 epic § "S06.6.2: PAD-US GeometryCollection area-preservation handling for 10 V1 no-hunt zones". **E06 estimated stories bumped 14 → 15**; epic Story Status table updated; merge order `… → S06.6 ✓ → S06.6.1 ✓ → S06.6.2 (next active; gates operator-pass resume + closes deferred S06.6.1 AC #627/#628) → S06.7 → …`. **🚨 Strategic flag surfaced for M2-release / M3 PM consideration** (NOT in this story's scope): **PAD-US 4.1 source stability** — two distinct republish drifts in 18 calendar days (2026-06-04 close → 2026-06-21 OID drift → 2026-06-22 GeometryCollection drift). Options for human consideration at convenience: pin a PAD-US snapshot fixture + cache forever vs continue patching live-fetch drift vs re-source from NPS Boundary Service / USFWS Cadastral / direct downloads. **Operator pass halted again at Step 4** on `test/m2-operator-pass` (the branch is at `main` post-S06.6.1-merge + the operator's `main` merge-forward); awaiting S06.6.2 fix merge before resume from Step 4 → 10. Stray untracked fixture `Federal_Fee_Managers_Authoritative_PADUS-0-metadata-20260622T204643Z.json` cleanup authorized (operator removes; not source). Test baseline floor at-S06.6.2-dispatch: 1783 + 4 skipped. **Prior entry — kept for audit trail: S06.6.1 closed at-merge via PR #72 / `0506831`** — 9th E06 PR; mid-epic hygiene carve-out closing the M2-build operator-pass Step 4 PAD-US OBJECTID drift. **4-commit pre-squash chain** (`b25a9e2` feat → `e2ca357` plan-historical → `b6da979` pitfall → `d8bae0a` post-review strict-coercion P1 fix). **What shipped**: 4 source files + closure memo. (1) `ingestion/states/colorado/load_restricted_areas.py` — `_RA_OUT_FIELDS` now includes `"OBJECTID"` (append-not-prepend, multi-line tuple); manifest-hash loop calls `arcgis._require_objectid`. (2) `ingestion/ingestion/lib/arcgis.py` — **new strict `_require_objectid(feature, *, oid_field=None) -> int | str`** scans only `properties`/`attributes` (no `feature["id"]` fallback), raises `ArcGISError` with diagnostic naming failure mode + republish/`outFields` root cause + forensic keys. `fetch_features` manifest-hash loop routed through it. `_read_objectid` is **unchanged** (keeps fallback for 6 error-message-context callers + dedup loop that legitimately tolerate `None`; mutating it would have masked errors). **Strict helper, not a mutation** — Stage-4 + Stage-6 endorsed; AC #624's "or its raising caller" sanctions. Behavior change localized to the two OID-critical manifest-hash loops; both already treated `None` as fatal, now they also refuse the masking `feature["id"]` fallback. Behavior-preserving for GMU/MT in practice (their features carry OBJECTID in `properties`; AST test locks). (3) `ingestion/tests/test_arcgis.py` — new `TestReadObjectidFailsLoud` (**8 tests**, not the spec'd 3 — +5 added across implementation + the post-review P1 fix). (4) `ingestion/tests/test_load_co_restricted_areas.py` — new `TestStateAdapterOutFieldsIncludeObjectid` AST-walk over the entire CO adapter dir. **Post-review P1 fix (`d8bae0a`)**: strict helper inherited `_read_objectid`'s `else str(val)` coercion, so a null/non-scalar OBJECTID became `"None"`/`str(obj)` and was accepted — a manifest hash-collision risk. Now `_require_objectid` accepts **only scalar `int`/`str`**; present-but-unusable candidates fall through to siblings (`{"OBJECTID": null, "FID": 42}` → `42`); fails loud otherwise with enriched diagnostic. **Three spec deviations** (reviewer-confirmed; none blocked at-merge; AC-annotated in the epic file): **AC #622** `_GMU_OUT_FIELDS` does not exist — `load_gmus.py` (and `load_state_boundary.py`) delegate `outFields` to `fetch_features` via `metadata.out_fields` (server-reported `fields[]`), so `OBJECTID` is naturally requested. Only `_RA_OUT_FIELDS` is a hardcoded tuple. **This is why operator-pass Step 3 (GMUs) succeeded while Step 4 (RA) failed** — different fetch shape, different vulnerability surface. **AC #629** pinned-SHA constant does not exist in `test_load_co_restricted_areas.py` (mocks `compute_feature_hash`); inherited convention from extraction-artifact stories, N/A here. **AC #627/#628** fixture + manifest regen deferred to operator-pass-resume — loader has no `--dry-run` flag and no committed PAD-US fixtures exist (S05.4 left them operator-pending Group B); mirrors S05.0/S05.2/S05.4 posture. **Quality gates (final, on main)**: pytest **1774 → 1783 + 4 skipped** (+9: 8 `TestReadObjectidFailsLoud` + 1 `TestStateAdapterOutFieldsIncludeObjectid`; **spec predicted ~1779 — the +4 strictness tests from the post-review fix make it 1783**); ruff + mypy clean (8 lib files); cubic `{"issues": []}`; detect-secrets passed (1 routine `.secrets.baseline` line-number refresh on the pitfall commit). MT-untouched + schema + `db.py` + TS-stack all empty per AC #631-#634. `TestNoColoradoLeakIntoSharedLib` + `TestNoStateAdapterImports` green (ADR-005 state-agnostic-clean preserved despite the `lib/arcgis` edit). **No new ADRs**; refines ADR-001 (fail-loud-over-silent-fallback) + ADR-005 (state-agnostic-clean lib edit). **1 new pitfall** in `.roughly/known-pitfalls.md` § "Integration — ArcGIS" (~lines 107-116; file now ~1270 LOC — doc-writer re-flagged for reorg/dedup): ArcGIS host republish drops top-level `id` unless `OBJECTID` in `outFields`; forensic signal = response CRS shift (PAD-US `4269 → 3857`) captured by `_check_and_fix_projection`; mitigation = `OBJECTID` in every hardcoded `_*_OUT_FIELDS` (locked by AST test) + `_require_objectid` on OID-critical paths. **No open questions touched.** **One M2 hygiene candidate carried for future MT-touching PR**: `ingestion/states/montana/backfill_manifests.py:175-178` carries a now-stale comment ("Mirror the OID-extraction strategy used by `fetch_features`'s manifest path") — the two paths intentionally diverge now (backfill keeps `_read_objectid` because older committed MT fixtures store OID under `feature["id"]`); couldn't fix in S06.6.1 (AC #631 MT-untouched). Fold a one-line comment fix into a future MT-touching PR. **Group A satisfied at-merge** (with AC #622/#627/#628/#629 deviation annotations in the epic file); **Group B operator-pending** (operator resumes M2-build pass from Step 4 against dev; writes 10 CO geometry rows; SHA cross-check; then Steps 5/7/8 unblock). Closure memo at `docs/planning/epics/E06-confidence-findings/S06.6.1.md` (deletes at `m2` tag per ADR-017 §6). **🚨 Next active work-front: S06.7** (`season_definition` + `license_tag` + `license_season` ingestion — first multi-link-table adapter; `drift_guard.assert_id_matches` mandatory at every per-row entity-construction site per ADR-020; **Q20 (Season Choice `X` method-group modeling per S06.3 discovery) surfaces here** as flag-and-discuss). S06.7 implementation can begin against the dry-run path; live execution still gated on the outstanding **M2-build operator-pass resume** (the pass currently lives on `test/m2-operator-pass` with Steps 0–3 success captured + Step 4 halted on the PAD-US OBJECTID drift now fixed; operator resumes Steps 4 → 10 against dev to roll up the Group B captures for S05.0/S05.2/S05.3.5/S05.4/S05.5/S05.7/S06.5/S06.6/S06.6.1 in one batched session). **Test baseline floor for downstream stories: 1783 + 4 skipped** (was 1774 in S06.6 closure). **Prior entry — kept for audit trail: S06.6.1 carved out 2026-06-21** as a mid-epic carve-out post-S06.6 closure during the **M2-build operator pass** — Step 4 (`ingestion/states/colorado/load_restricted_areas.py` against PAD-US 4.1) failed loud after PAD-US was republished between S05.4 close 2026-06-04 and the operator pass 2026-06-21 (forensic signal: response CRS shifted `4269 → 3857`, captured by `lib/arcgis._check_and_fix_projection`); the republished service now omits the top-level GeoJSON `id` field unless `OBJECTID` is in `outFields`, and `_RA_OUT_FIELDS` doesn't include it; `lib/arcgis._read_objectid`'s silent `feature["id"]` fallback masked the convention so every feature resolved to `OBJECTID=None` → fail-loud raise → 0 rows written. **Spec drafted as a new section** in the E06 epic at `epics/E06-colorado-regulation-text-ingestion.md` § "S06.6.1: PAD-US OBJECTID outFields hardening (operator-pass-discovered carve-out post-S06.6 closure)" — mirrors S03.6.1 / S05.3.5 / S06.3.1 mid-epic carve-out precedent (chosen over a standalone hygiene story because the operator-pass-discovered blocker gates E06's critical-path S06.7 dispatch; the bug lives in S05.4 code but E05 is closed + audited, so the carve-out lands in the active epic). Operator pass halted at Step 4 on branch `test/m2-operator-pass` @ `1f52932`; awaiting fix merge to `main` before resume from Step 4 → 10. **Fix scope** (7 T-tasks; small): T1 add `"OBJECTID"` to `_RA_OUT_FIELDS`; T2 audit `_GMU_OUT_FIELDS` + any other CO `_*_OUT_FIELDS` tuples; T3 tighten `lib/arcgis._read_objectid` to fail loud with diagnostic (removes silent fallback); T4 new `TestReadObjectidFailsLoud` test class (3 tests including the behavior-change lock); T5 new `TestStateAdapterOutFieldsIncludeObjectid` AST-walk regression test; T6 regenerate S05.4 PAD-US features fixture + manifest + re-pin SHA in `test_load_co_restricted_areas.py`; T7 new pitfall under `.roughly/known-pitfalls.md` § "Integration — ArcGIS" naming both the failure mode AND the CRS-republish forensic signal. **No schema / migration / three-place-sync / `db.py` / MT-touch / TS-stack changes; no new ADRs** (convention tightening; ADR-001 fail-loud + ADR-005 state-agnostic-clean already cover the principles). Expected test baseline shift **1774 → ~1779 + 4 skipped** (+5 additive). Branch convention `feat/m2-hygiene-padus-oid-hardening` off `main`; merges to `main`; operator branch fast-forwards after. **E06 estimated stories bumped 13 → 14**; E06 Story Status table updated below; **merge order proceeding**: `… → S06.6 ✓ → S06.6.1 (next active; operator-pass blocker fix) → S06.7 → …`. **Two PM-observed runbook prose hygiene items** queued for post-pass doc-only commits (non-blocking): (a) Step 1 "3 vs ~50 binding fan-out" — runbook line 60's "3 MT federal no-hunt-zone bindings" collapses geometry-count and binding-count (3 geometry IDs × ~17 fan-out bindings = ~50; S05.3.5 closure note had this right); (b) Step 0 "437 vs 435 baseline" — dev's M1-build trajectory shows 435 MT regulation_record rows vs prod-baseline expected 437; agent accepted via runbook reconciliation flow's A1 path; not blocking but worth investigating before the M2-release pass. Both retroactive doc fixes from PM post-pass. **Prior entry — kept for audit trail: S06.6 closed at-merge 2026-06-19 via PR #71 / `4ecd47d`** — eighth E06 PR; **the first E06 DB-write story**. New `ingestion/states/colorado/load_regulation_records.py` (**848 LOC**, state-agnostic-clean per AST guard) — structural analog of MT's S03.6 loader but **simpler**: no legal-descriptions artifact, no fan-out dict, no statewide anchors in V1, no `jurisdiction_binding` writes (S06.10 territory). Three-phase shape: build → guards pre-`db.connect()` → upsert loop → single `conn.commit()`. New test file `test_load_co_regulation_records.py` (**1,182 LOC, 72 tests**). Reads two committed extraction artifacts (`big-game-2026.json` + `black-bear-2026.json`); writes **398 `regulation_record` rows** in one atomic transaction. **Composition** (dry-run verified): mule_deer 141 (high=55, medium=86) + elk 115 (38/77) + pronghorn 77 (25/52) + whitetail 19 (2/17) + bear 46 (46/0) = **398 total**; all `document_type='annual_regulations'`; confidence via `pdf.min_tier`; **no `low` rows** (the only `low` was the skipped blank-GMU section); **0 `CO-STATEWIDE` rows**. **Key design decisions (load-bearing for S06.7-S06.10)**: (a) **Collapse by `(gmu_code, species_group)`** — CO artifacts have one section per `(gmu, species, method_group)`; `regulation_record` is the anchor (no `method_group`), so per-method sections collapse **906 sections → 398 records**. This matches the epic's "~300-500" estimate. S06.7 link-table builders must group-and-collapse the same way or key off the written PKs. (b) **No fan-out dict** — CO species are pre-separated in the artifact (`mule_deer`/`whitetail`/`elk`/`pronghorn`; no `deer` label); **Q16 does NOT fire for CO**. (c) `jurisdiction_code = CO-GMU-{int(gmu_code)}` mapping to S05.2's `CO-GMU-{int}-geom` by appending `-geom`. (d) Bear `species_group="bear"` (artifact says `black_bear`); bear artifact is a **flat list with a `record_type` discriminator**. (e) **0 statewide anchors** — no pronghorn `900-20` analog; the 2 bear `statewide_rule` records are informational, NOT Bear-ID-Test prerequisites; flag-and-discuss guard wired. (f) **`_STATE: Final = "US-CO"`** matches MT precedent + S05.6 scaffold; rename of the 4 existing CO loaders remains bundled with S06.10/D3. (g) **`_JURISDICTION_BINDING_ID_FORMAT` defined byte-identical to MT** for S06.10 to import; locked by 3 tests per S03.6.1 pattern. **Deviations recorded at closure**: AC #544 fan-out test reframed (CO pre-separates species — shipped as no-accidental-fan-out test); 1 blank-GMU big-game section skipped-with-warning (GMU 020 elk archery — the S06.3.1 heading-absorption residual; no anchor lost). **Scope guarantees**: no new `db.py` helpers (reuses `upsert_regulation_record`); no `ingestion/lib/` edits; no schema/migration/three-place-sync; no MT-file touches; no TS-stack diffs; no production-DB writes from the build session. **Review history**: 3-agent review found 1 Critical (silent drop on missing/misspelled bear `record_type`) + High/Warning fail-loud-context gaps → fixed in 1 cycle (`record_type` validation + diagnostic-wrapped artifact access + contextualized guards). Cubic → 1 P2 (NOTE page provenance in multi-section groups) → fixed + locked. **Maintainability refactor** — extracted shared `_collapse_sections_to_record` helper (single-module convention preserved per ADR-022; "split the module" companion finding declined). Plus 2 fail-loud fixes: `statewide` guard `isinstance(dict)` hardening; `gmu_code` grouping canonicalization (strip + canonical-collision guard). **Test baseline shifted 1702 → 1774 + 4 skipped** (+72 additive; new 1774 baseline holds going into S06.7). ruff + mypy + cubic + detect-secrets all clean. **No new ADRs**; refines ADR-001/005/008/010/017/018 §3 + Q15/019 §"Decision" item 5 + ADR-022. **3 new pitfalls** in `.roughly/known-pitfalls.md` (1,221 → 1,260 LOC; doc-writer re-flagged for reorg/dedup): CO collapse per-method-group; CO big-game pre-separation; CO bear flat-list with `record_type` discriminator. **Group A satisfied at-merge**; **Group B operator-pending** — shares the outstanding E05 operator Group B geometry-write precondition. Operator must (after E05 Group B lands): live row-count + per-species count + SQL-shape + FK validity verification. **🚨 Next active work-front: S06.7** (`season_definition` + `license_tag` + `license_season` ingestion — first multi-link-table adapter; `drift_guard.assert_id_matches` mandatory at every per-row entity-construction site; **Q20 (Season Choice `X` method-group modeling per S06.3 discovery) surfaces here** as flag-and-discuss). S06.7 implementation can begin against the dry-run path; live execution still gated on outstanding E05 operator Group B writes. **Prior entry — kept for audit trail: S06.5 closed at-merge 2026-06-18 via PR #70 / `207f31d`** — seventh E06 PR; restricted-area `verbatim_rule` population for the 10 S05.4 federal no-hunt zones. New `ingestion/states/colorado/load_restricted_area_verbatim.py` (**297 LOC**, state-agnostic-clean per AST guard) — single-purpose targeted-UPDATE loader reading CPW Big Game brochure page 78 ("Land Closures & Use Restrictions") at runtime via `lib/pdf.open_pdf` + `lib/pdf.extract_text(page, bbox=…)` column crops + regex anchors; three-phase `main()` (build → pre-`db.connect()` OQ7 guards → conn/commit); `--dry-run`; no `--service-url` flag. **NEW `ingestion/ingestion/lib/db.py` helper `update_geometry_verbatim(conn, geometry_id, text)`** (S06.0/D6 delivered) — exact analog of `update_legal_description`: `UPDATE geometry SET verbatim_rule = %s WHERE id = %s`; fail-loud `RuntimeError` on `cur.rowcount == 0`; no commit (caller owns txn). **State-agnostic write-path extension** per the `db.py` extension-point convention — NOT a state-specific lib edit (ADR-005 / ADR-022 hold). **Key decisions**: **D5 = (b) split-provenance honored** — loader UPDATEs only `verbatim_rule`; `source` stays the PAD-US citation (`document_type='gis_layer'`) on all 10 rows. No schema / three-place-sync / migration. **Phrasing case (1) + 9+1 split locked**: CPW publishes one generic sentence for NPS lands shared across the 9 NPS rows (4 NPs + 5 NMs): *"National parks and monuments managed by the National Park Service are closed to hunting. Check park websites for more information."* The **AFA row gets its own distinct page-78 access-rules prose** (regulated-access HUNTING area, NOT a closure). Locked by regression test asserting 9 NPS values share one unique string + AFA differs. **ADR-008 byte-equivalence** preserved (no `layout=True`; column-crop bbox + regex anchor; fail-loud on missing anchor / empty crop / wrong PDF; never hand-edit). **Test baseline shifted 1681 → 1702 + 4 skipped** (+21 additive: `test_load_co_restricted_area_verbatim.py` +17 + `test_db.py` +4 `TestUpdateGeometryVerbatim`); new 1702 baseline holds going into S06.6. **🚩 NEW Known Issue #12 — AFA classification deferral, user-decided 2026-06-17** (cubic's lone P2 = this documented deferral). The Air Force Academy (`CO-restricted-united-states-air-force-academy-geom`, one of the 10 S05.4 rows) is a **regulated-access HUNTING area, NOT a no-hunt zone** — GMU 512 carries CPW hunt codes; CPW publishes escorted rifle deer hunts on AFA land per Big Game brochure p.78. S06.5's `verbatim_rule` for that row now describes how to hunt there, not a closure. **S06.10 must NOT bind AFA `role='no_hunt_zone'`** — doing so would protect a huntable zone as closed in query results, contradicting CPW-published reality and the user's stated intent ("AFA must remain protected as a huntable zone in query results"). **Two resolution paths** for S06.10: (a) bind AFA `role='other_overlay'` (PM-recommended for V1 — preserves geometry row, no schema migration); (b) revisit the S05.4 federal no-hunt-zone classification at the geometry layer (V2 candidate; geometry-side migration + ADR-018-style three-place sync). S06.10 spec now carries an inline forward-note documenting both options. The other 9 (NPS) rows are genuine no-hunt zones; `role='no_hunt_zone'` is correct for them. **Group A satisfied at-merge**; **Group B operator-pending** — shares the outstanding E05 operator Group B geometry-write precondition. Operator must (after E05 Group B lands): `fetch_pdfs.py` → `load_restricted_area_verbatim.py` → SQL spot-check all 10 rows non-null (AC #482), `source` still `gis_layer` (AC #483, D5=b), 9 NPS share the closure sentence, AFA carries its own prose; PM-run spot-check ≥3 zones (AC #486). Runbook + SQL in `docs/planning/epics/E06-confidence-findings/S06.5.md`. **No ADRs created**; refines ADR-001 (fail-loud), ADR-008 (verbatim, no `layout=True`), ADR-005 / ADR-022 (state-agnostic-clean), ADR-015 (REG+COMMENTS combiner N/A — single brochure-page source). **1 new pitfall** in `.roughly/known-pitfalls.md` (now ~1,222 LOC — re-flagged for reorg/dedup): multi-column PDF prose extraction — crop by column, keep crop edges out of text lines. (Post-merge consistency fix corrected an initial over-generalization that contradicted S03.5's header/footer-strip guidance; failure mode is crop-edge-placed-mid-line, NOT "non-zero top"; S03.5's 25pt/50pt strips remain correct; code unaffected.) **Quality gates clean** (ruff + mypy `ingestion/lib/` + pytest 1702/4 + cubic + detect-secrets). No TS-stack diffs. **🚨 S06.6 (regulation_record ingestion — first DB-write story for regulation-text entities) is next active** but **LIVE EXECUTION IS GATED** on the outstanding E05 operator Group B batched live-write session (CO geometry rows must exist in production before any `regulation_record` FKs to them). S06.6 implementation can proceed against the dry-run path (build + tests) without Group B, but the live UPSERT cannot run until E05 Group B lands. **Prior entry — kept for audit trail: S06.3.1 closed at-merge 2026-06-16 via PR #69 / `bfeb60a`** — sixth E06 PR; the mid-epic hygiene carve-out resolving Known Issues #10 + #11 as one coordinated SHA shift. Documentation + extraction-side only — no DB writes, no schema/migration, no `db.py` touches, no `ingestion/lib/` edits. **Phase A — Elk-correction investigation → OUTCOME (b) structurally inert** (NOT outcome (a) as the PM-expected outcome). The big-game extractor has no correction machinery and needs none: the brochure PDF (`publication_date='2026-03-04'`) postdates all four corrections (2026-02-13/17/19), and the correction PDF states the online brochure was updated. The two V1-scope elk corrections (`E-M-059-O1-M` → List A, `E-F-044-O1-R` → List B) are already correct in the committed artifact; the other two corrections are moose (out of V1 scope). **~400 LOC of three-pass infra avoided**. Documented deviation from spec; durable record is the existing S03.4 "correction-PDF-contradicts-forward-note" pitfall. **Phase B — R17 row-fusion split ported as big-game "Rule R16"**. New `_split_fused_block_row` in `extract_big_game.py` (state-agnostic-clean preserved; R16 stays per-extractor like `_split_valid_gmus`; no lib edits). **Adapted, not verbatim from bear**: big-game fusion is partial-column, so the rule is `{N parts → distribute | 1 → broadcast | else → fail-loud}` rather than bear's strict raise-on-any-mismatch. Wired into the single standard-block path in `_parse_table_block`; Season Choice (8-col) deliberately excluded. **4 previously-dropped codes recovered**: `D-M-082-O3-R`, `D-F-107-O1-R`, `A-M-004-O1-M`, `A-F-118-O1-R`. ⚠️ **The S06.4 closure narrative's "9 dropped codes" was an unmeasured estimate** — the empirical count is **4**. Reconciled per the "name the source-of-truth before copying numbers" discipline. **Phase C — Re-extraction + SHA re-pin**: deterministic. **SHA lineage**: `3c2ecd90…015d` (S06.3 close) → `e5c7c33a…6d1e` (S06.4 `valid_gmus` cleanup) → **`9312e2595071a80cc317250504e4ba6a7eaaae33a201a313db275aa0f0c8bb2f`** <!-- pragma: allowlist secret --> (S06.3.1). Counts: sections **737 → 736** (a now-empty mule_deer placeholder section collapsed once R16 gave its fused rows real GMU codes); rows **2758 → 2762** (+4); confidence **high 2170 → 2178** / medium 583 / **low 5 → 1** (per S03.11 FINALIZE this remains data-shape signal, not framework signal — ADR-017 unmodified; the single remaining LOW is section 416 elk-archery heading-absorption residual, documented out of R16 scope, not a silent gap). Re-pinned in `test_extract_co_big_game.py`; S06.3 AC #279 annotated with the full SHA lineage. **PR-review hardening (3 cycles)**: 5 findings — 2 fixed, 3 declined with rationale. Fixed: (a) duplicated hunt-code grammar → single `_HUNT_CODE_GRAMMAR` fragment as the source of truth, both anchored + unanchored regexes derive from it (locked by `TestHuntCodeGrammarSingleSourceOfTruth`); (b) ambiguous shared-cell mis-distribution → continuation-comma fail-loud guard. **NEW: ADR-022 (Accepted) — Single-Module Per-State PDF Extractors** (`docs/adrs/ADR-022-single-module-per-state-extractors.md`). Formalizes the recurring "monolithic extractor module" P2 review finding (raised 3× against S06.3) as a canonical architectural commitment; modularization reopens only via a superseding ADR applied uniformly across all CO + MT extractors. ADR README index updated; `known-pitfalls.md:481` + epic Known Issue #9 updated. **Quality gates**: ruff + mypy clean; full suite **1681 passed / 4 skipped** (+12 additive from `TestFusedRowSplit` + comma-guard + `TestHuntCodeGrammarSingleSourceOfTruth` + `_RULE_COUNT` parity); cubic `{"issues":[]}`; detect-secrets clean. **Known Issues #10 + #11 → RESOLVED via S06.3.1**; **Known Issue #9 → formalized as ADR-022**. **Distinct residual at section 416** (elk archery: `E-M-020-O1-A` / `E-F-020-O1-A` in `verbatim_text` but no row — heading-absorption, NOT row-fusion) documented in closure memo as out of R16 scope; not a silent gap (LOW + WARNING + verbatim preserved). **UAT PM-pending** (AC left unchecked per Group-A/B posture): spot-check the 4 recovered codes against the source PDF + confirm Phase A inertness (no elk rows changed by a merge — verify by inspection that `E-M-059-O1-M` reads List A and `E-F-044-O1-R` reads List B in `big-game-2026.json`). **Next active work-front: S06.5** (restricted-area `verbatim_rule` population for the 10 S05.4 federal no-hunt zones). **S06.5 remains dry-run-decoupled**; S06.6+ still gated on outstanding E05 operator Group-B writes. **Prior entry — kept for audit trail: S06.4 closed at-merge 2026-06-15 via PR #68 / `51f6aa7`** — fourth E06 PR; CPW Black Bear extraction. New `ingestion/states/colorado/extract_black_bear.py` (**3,603 LOC**, state-agnostic-clean) shipped as a separate extractor (NOT folded into S06.3) per AC #344; parses the CPW Big Game brochure's bear section (pp. 72–77) into deterministic committed artifacts via `lib/pdf.write_extraction_artifact`. **Artifacts**: `extracted/black-bear-2026.json` — **172 records / 215 rows; SHA-256 `7b35c202fd614f37fb529c2cc308fbf0004192eefd07fdf6963772dcc867d5f6`** <!-- pragma: allowlist secret --> (determinism-pinned); confidence `{high:215, medium:0, low:0}` + `black-bear-2026-base.json` (Pass-1) + `corrections-2026-02-19.json` (Pass-2 ops `[]` — **inert for bear**). Composition: 137 limited_draw + 78 OTC; hunt-code grammar `B-{sex}-{gmu}-{season}-{method}`; `species_group='black_bear'` (S06.6 maps to DB `'bear'`). **Three-pass arbitration preserved** as inert-confirmation pathway (NOT bypassed). **17 cleanup rules R1–R17** documented including CO-specific **R16** (hunt-code embedded in prose; recovers Plains-OTC `B-E-087-U6-R` "Sales agents only:" prefix) + **R17** (pdfplumber fused-row split). **⚠️ Branch-name caveat**: branch is literally `feat/S03.4-black-bear-extraction` (human typo at branch creation since S03.4 was MT's bear story in E03; commits scoped S06.4 correctly; user acknowledged and kept the name). **⚠️ Cross-story `valid_gmus` cleanup** touched merged S06.3 code: review finding (prose like "private land only" / "Note: No hunting access to GMU 211" contaminating structured `valid_gmus`) was real in both CO extractors. Per "no broken windows" call, fixed in both — `valid_gmus` now holds clean GMU list only; free-text qualifier routed to `extras` and appended to `verbatim_text` (ADR-008 preserved). **`big-game-2026.json` regenerated** — counts UNCHANGED (737/2758); confidence UNCHANGED `{high:2170, medium:583, low:5}`; **SHA shifted `3c2ecd90…015d` → `e5c7c33a728a95f3d2845894ce53d4de664ed20bfc40853eaa421ac8d12e6d1e`** <!-- pragma: allowlist secret --> and re-pinned in `test_extract_co_big_game.py`. The S06.3 closure record's pinned SHA is annotated as superseded; `lib/pdf.py` net-zero change (split logic was moved out of lib into private `_split_valid_gmus` per CPW-specific convention). **Test baseline shifted 1498 → 1669 + 4 skipped** (+171 additive); ruff + mypy + detect-secrets clean. **⚠️ cubic was unavailable during the late post-restart review cycle** — gates rested on ruff/mypy/1669-suite + manual SHA/count/exclusion verification; PM commit run below will re-confirm. **🔴 2 RED-FLAG carry-forwards surfaced** — added to epic Known Issues: **(#10) Latent S06.3 elk-correction-content gap** — the 2026 correction PDF is **moose + elk** (page 2 is `E-M-…` elk-muzzleloader correction p.44), NOT moose-only as the S06.1 forward-note claimed. S06.3 closed treating the correction as moose-only inert and may have missed applying this elk-muzzleloader correction. PM decision needed: re-extract big-game with elk correction applied (re-pin SHA again) vs confirm structurally-outside-V1-scope vs defer to S06.6 ingestion. Worth investigating before E06 proceeds to S06.6. **(#11) Latent big-game R17 row-fusion defect** — the same pdfplumber row-fusion bug S06.4 Rule R17 fixed in bear extraction exists in `extract_big_game.py`; **9 fused cells in `big-game-2026.json` silently dropped codes**. Out of S06.4 scope; R17 logic ports directly. Recommended separate hygiene story (analog of S06.3.1 carve-out, mirroring S03.6.1 / S05.3.5 mid-epic pattern) that ports R17 to big-game, re-runs extractor, re-pins SHA, captures the 9 previously-dropped codes; land before S06.6 to avoid downstream silent gaps. **`.roughly/known-pitfalls.md` +56 LOC** — entries for: per-species column layouts differ; hunt-code-in-prose (R16); pdfplumber row-fusion split (R17, incl. the big-game-latent note); duplicate hunt codes are faithful (loader collapses); correction-PDF-contradicts-forward-note. **No ADRs created**; refines ADR-001 (fail-loud), ADR-005 (state-adapter isolation; lib net-zero), ADR-008 (verbatim discipline), ADR-017 (confidence; `low=0` is data-shape signal not framework signal per FINALIZE), ADR-019 (correction precedence — confirmed inert for bear but see #10 for elk question). No schema/migration/three-place-sync/`db.py`/TS-stack changes; no production-DB writes. **Group A satisfied at-merge**; **UAT (AC #343) PM-pending** — non-blocking for S06.5 dispatch (S06.5 depends on S06.0 provenance + S06.3 brochure, not bear). **Prior entry — kept for audit trail: S06.3 closed at-merge 2026-06-13 via PR #67 / `08bf396`** — third E06 PR; the load-bearing E06 extraction story. New `ingestion/states/colorado/extract_big_game.py` (**2,685 LOC**, state-agnostic-clean) parses the 84-page CPW Big Game brochure into a deterministic committed artifact `extracted/big-game-2026.json` — **737 sections / 2,758 rows; SHA-256 `3c2ecd90…015d`** <!-- pragma: allowlist secret --> (determinism-pinned in tests); confidence split `{high: 2170, medium: 583, low: 5}` (per S03.11 FINALIZE, `low=5` is data-shape signal NOT framework signal). Two PDF manifests committed; raw PDF gitignored. **Cross-cutting infra change** (affects future extractors): mid-story, cubic refused to review the PR (108,187 changed lines; the `indent=2` artifact was 103,854 lines, past cubic's 50k cap; `.gitattributes` did not help — GitHub counts raw diff lines). Resolved by one-record-per-line serialization (→739 lines), then **generalized into a new state-agnostic shared helper** `ingestion/ingestion/lib/pdf.write_extraction_artifact(records, path)` — atomic, deterministic, opt-in (mandatory for new extractors; **S06.4 must use it from the start**). ADR-005 holds — the lib helper is state-agnostic-clean per `TestNoColoradoLeakIntoSharedLib`. **Carry-forward flag**: MT's `dea-2026.json` is 47,956 lines (one bigger brochure from cap) — migrate MT's three extractors to the helper at next re-extraction (format-only, data-unchanged, re-pin SHA); surfaced as new E06 epic Known Issue #8 (post-E06 M2 hygiene candidate alongside E05 Known Issue #7). **5 real-PDF discoveries baked in** (per S03.3 lesson — expected ≥3): (1) CPW hunt-code grammar `{Species}-{Sex}-{GMU}-{Season}-{Method}` (pronghorn species letter `A`; 3- and 4-digit GMUs both handled); (2) Season Choice (method letter `X`) → mapped to `method_group="season_choice"` + `weapon_types=["archery","muzzleloader","any_legal_weapon"]`; **Q20 opened** for the S06.7 modeling decision; (3) plains whitetail detection via `_WHITETAIL_UNIT_RE`; (4) R14 uniform character-doubling recovery at top of table-row loop (CPW PDF artifact); (5) empty-GMU sections page-disambiguated to prevent cross-page row coalescing. **Recurring-review pattern**: "monolithic extractor module" P2 raised 3× against the 2,685-LOC file and declined all three (uniform single-module convention across MT + CO extractors; coupling test-mitigated; modularization is an ADR-level project-wide call) — rationale now in `.roughly/known-pitfalls.md`; surfaced as new E06 epic Known Issue #9. **No ADRs created**; refines ADR-001/005/008/017. Test baseline shifted **1376 → 1498 + 3 skipped** (+122 additive: 1,334-LOC `test_extract_co_big_game.py` + 7 `TestWriteExtractionArtifact` tests in `test_pdf.py`); new 1498 baseline holds going into S06.4. **UAT spot-check is PM-pending** but does NOT block S06.4 dispatch (S06.4 is independent on its source PDF). **S06.4 (Black Bear extraction)** is next active — must use `write_extraction_artifact` from the start. **S06.4 / S06.5 remain dry-run-decoupled**; S06.6+ still gated on the outstanding E05 operator Group-B writes. **Prior entry — kept for audit trail: S06.1 closed at-merge via PR #66 / `abc6c21`** — second E06 PR; CO PDF fetch infrastructure operational. New `ingestion/states/colorado/fetch_pdfs.py` (~285 LOC, fail-loud aggregating orchestrator mirroring MT's `fetch_pdfs.py`) + `sources.yaml` `pdfs:` section with **2 fully-pinned CPW PDFs** (both on the Colorado State Publications Library "Artemis" serial NR14.31/INTERNET; both SHA pins carry `# pragma: allowlist secret`): `co-cpw-big-game-2026-brochure` (`annual_regulations`, 84 pp, SHA-256 `38cf26e1…3582b3`, `publication_date='2026-03-04'`) + `co-cpw-big-game-2026-correction-2026-02-19` (`correction`, 2 pp, SHA-256 `8eddff70…71dbf0`, `publication_date='2026-02-19'`); no `pending: true` entries (both URLs operator-resolved at S06.0/D4). **2 post-merge-review fixes** folded into the branch before merge: (i) **unenforced SHA pin** (P2, fixed root-cause) — the orchestrator never passed `expected_sha256` to `fetch_pdf` and the lib had no param for it; CO was the first state with real pins so this had teeth. Fixed by adding a state-agnostic, opt-in `expected_sha256` param to `pdf_fetch.fetch_pdf` that enforces a real pin before any write on every fetch (incl. first); `"unknown"`/absent skips so MT is unaffected; malformed fails loud. **Complements — does not replace — the manifest re-fetch drift gate** (two-gates model). Locked by 8 new lib tests. (ii) **doc scope contradiction** (P2 doc-only) — closure note claimed "zero lib edits" while also describing the post-review edit; reconciled to shipped state. **ADR-005 holds** despite the lib edit: the edit is state-agnostic-clean (mirrors S05.5's `overlays.py` precedent); `TestNoColoradoLeakIntoSharedLib` + `TestFetchPdfNoStateAdapterImports` green. Test baseline shifted **1346 → 1376 + 2 skipped** (+30 additive: 24 in `test_fetch_co_pdfs.py` + 6 lib pin-gate tests); ruff + mypy clean; tsc exit 0; detect-secrets passed. **`.roughly/known-pitfalls.md`** updated: rewrote the S03.1 `expected_sha256` pitfall into a two-gates model + added a "fork-a-state-adapter-module → port its test classes" pitfall; file is now ~1065 LOC and doc-writer re-flagged for reorg/dedup (recurring since S05.x). **S06.4 forward-note**: the 2026 correction PDF is **moose-only** (two omitted moose hunt codes, p.61) → **out of V1 scope** → ADR-019 `correction > annual_regulations` merge expected to be **CO V1 no-op**. Registered for provenance; S06.4 confirms inert via the three-pass arbitration before declaring it so. **Group A satisfied at-merge; AC #188 (per-PDF manifest commits at first fetch) Group-B operator-pending** per S03.1 "infrastructure-met, fixture-deferred" posture — first-fetch SHA pin is now auto-enforced lib-side so no manual SHA-verification step is needed on the operator's first run. **No ADRs created**; refines ADR-001 (SHA-pin fail-loud), ADR-005, ADR-014 (`document_type`), ADR-019 (correction precedence registered, expected V1 no-op). No schema / three-place-sync changes; no `db.py` touches; no MT-code behavior change; no production-DB writes. **Prior entry — kept for audit trail: S06.0 closed at-merge 2026-06-08 via PR #65 / `5d6ab37`** — first E06 PR; decision-gate / documentation-only; no code, no migration, no schema/three-place-sync, no `db.py` helper, no production-DB writes; test baseline held exactly at 1346 + 2 skipped. All 6 decisions locked verbatim in `docs/planning/epics/E06-confidence-findings/S06.0.md`: **D1 (Q18 CWD)** → (c) license-keyed (S06.9 ships 0 typed CWD `reporting_obligation` rows; text → `regulation_record.additional_rules`; `cwd_sample` enum stays defined-but-unused for CO V1); **D2 (Known Issue #6)** → hardcoded path (S06.10 mirrors MT `_build_no_hunt_zone_bindings` at `load_jurisdiction_bindings.py:592`; `_VALID_ROLE_FOR_E03` gate stays narrow at 4 values; promote S05.6 scaffold's `query_nearby_gmus_for_zone:85` to a live body); **D3 (`_STATE` naming)** → `_STATE` wins (4 CO loaders renamed `CO_STATE_CODE` → `_STATE` as same-PR follow-up in S06.10: `load_state_boundary:72`, `load_gmus:74`, `load_restricted_areas:78`, `build_overlay_fixture:73`; `CO_STATE_SLUG` companions untouched); **D4 (CPW brochure URL)** → **RESOLVED** at the Colorado State Publications Library "Artemis" durable path with SHA-256 byte pin + HEAD baseline + cover-page-2026 confirmation captured verbatim in memo §D4 (Known Issue #7 RESOLVED; S06.1 fully unblocked; large-download flag for S06.1 — brochure is ~96.7 MB / 84 pp / PDF v1.6); **D5 (multi-source provenance for the 10 S05.4 no-hunt zones)** → (b) split-provenance (PAD-US keeps `geometry.source` as `gis_layer`; CPW text reaches rows via `verbatim_rule` only via S06.5; no migration; leaves PRD 002 L176 "multi-source geometry provenance" a deferred ADR-candidate); **D6 (`db.update_geometry_verbatim`)** → helper built in S06.5 not S06.0 (analog of `update_legal_description` at `db.py:362`; fail-loud `cur.rowcount == 0`; no early introduction). **Schema-gap read-through: NO GAP FIRES → no migration** (all 7 candidates evaluated against live Pydantic + TS schema; the only two non-fires were data-triggered: `ClosurePredicate.effective_after` revisits at S06.3/S06.4 extraction if a quota+calendar-gate closure appears; Q18 new-table is N/A under D1=(c)). Companion correction PDF located at `https://spl.cde.state.co.us/artemis/nrserials/nr1431internet/nr14312026corrinternet.pdf` ("Latest Update Feb 19 2026"); S06.1 enters it under `pdfs:` as HEAD-verified or `pending: true` until SHA captured. **One review finding surfaced and was fixed inline**: AC #132 header capture had been deferred to S06.1 instead of captured in-memo — corrected by a live HEAD during build. **No new pitfalls** (the one citation-drift catch — `query_nearby_gmus_for_zone:96 → :85` — is a recurrence of the already-documented lesson). **Prior planning entry** (kept for audit trail): **E06 pre-implementation review closed via PR #64 / `f030405`**; verdict **Ready** (no blockers); review artifact at `epics/E06-colorado-regulation-text-ingestion-review.md`. Roughly `epic-reviewer` (opus) grep-verified E06's load-bearing external claims against the live codebase — MT precedent line numbers (drift_guard call sites `:589/660/926/1100`; id-derivation `:370/387`; `_JURISDICTION_BINDING_ID_FORMAT` at `load_regulation_records.py:115`), the S05.6 CO scaffold's 4 imported symbols (no `main()`), `EXPECTED_CO_RA_ORPHAN_IDS` (`len()==10`), `_VALID_ROLE_FOR_E03` MT-only/4-value, every architecture.md↔schema.py schema-type claim, all four enum value lists, the draw-schema-proposal numbers, Q-statuses in open-questions, and the **1346+2 test baseline** (`pytest` re-confirmed). All claims held — material for a project with documented "line-citation drift" failure mode. **6 actionable findings all addressed** (1 MUST-FIX + 2 SHOULD-FIX + 3 CONSIDER; 2 reviewer findings deemed not needing change with rationale captured in the review artifact's "no change required" note): (1) MUST-FIX S06.0 5-vs-6 decision-count inconsistency reworded to "5 pre-registered + conditional 6th" consistent across lines 91/95/131/779/801; (2) SHOULD-FIX S06.9 drift-guard AC corrected (`helper_name="_REPORTING_ROW_SPEC"` dispatch-dict-name, mandatory `id_field="id_suffix"`, callable aligned to MT's `_derive_expected_id_suffix(kind, deadline_hours, region_scope)` at `load_reporting_obligations.py:295/342-348`); (3) SHOULD-FIX Group A/B split stated explicitly in S06.6 Context + new Exit Criteria bullet (closes "operator-Group-B coupling under-mitigated in AC structure"); (4) CONSIDER S06.8 AC #567 coupling lock softened from a-priori invariant to "observed at extraction; contradictions route to flag-and-discuss"; (5) CONSIDER S06.0 AC #132 `Content-Length` demoted to informational (SHA-256 is the authoritative drift gate); (6) CONSIDER new Known Issue #7 flagging the CPW Big Game brochure URL as the single highest-likelihood epic-stall cause + recommending operator resolve it as the first S06.0 action. **No structural blockers** — E06 is cleared to begin. Resolution block dated at top of the review artifact for audit trail. **Prior planning entry** (kept for audit trail): E06 planned + validated 2026-06-08 via `/plan-next-epic`; epic at `epics/E06-colorado-regulation-text-ingestion.md`; **12 stories** drafted (S06.0 through S06.11; carve-outs may be added at sequencing slots mid-epic per S03.6.1 precedent if surfaced). E06 validation triad ran in parallel (Source Faithfulness + Draw-Mechanics & Confidence + Schema Stress-Test & Drift-Guard); verdicts LAND-WITH-MINOR-EDITS + LAND-WITH-EDITS + LAND-WITH-MINOR-EDITS (no NEEDS-REVISION); **all 11 MUST-FIX findings + key SHOULD-FIX items applied at draft time** — including broken ADR-020 link sweep across 5 occurrences; S06.0 Last-Modified/Content-Length header capture + cover-page confirmation + multi-source provenance option (a)/(c) consequence chains + conditional 6th `db.update_geometry_verbatim` decision; S06.1 `pending: true` drift-marker semantics; S06.3/S06.4/S06.5 ADR-008 paraphrase-prohibition + no-`layout=True` AST guard + docstring grep-parity discipline; S06.4/S06.6/S06.9 `SourceCitation.document_type` silent-widening guard per ADR-019 §"Decision" item 5; S06.6 `_JURISDICTION_BINDING_ID_FORMAT` 3-test lock + statewide-anchor 3rd-candidate flag tighten + ADR-017 FINALIZE lock; S06.7 `drift_guard.assert_id_matches` every-build-function-site language + pure id-derivation function AC + Q16/Q17/closure-temporal-anchors pre-code flag protocols; S06.8 `successor_hunt_code_key` composite-key form + 20%/25% coupling rule + `application_deadline` location lock + `purchase_only_code` per-species string + `inactive_forfeit_years=null` + module-level `Final` constants for `_HYBRID_*` parameters + `draw_spec` composite-PK exclusion AC + Q17 per-GMU caps flag; S06.9 `assert_dispatch_dict_drift_free` module-top timing + pure `_derive_reporting_obligation_id` callable; S06.10 `drift_guard` NOT imported AC + AST guard + `%s`-bound distance lock + 4-builder portion-path preservation. **Recommended first story: S06.0** (pre-E06 decisions + schema-prep gate). **S06.0 surfaces 6 pre-registered decisions** to the human in one flag-and-discuss session before downstream story-spec drafts: Q18 license-keyed disposition (final call); Known Issue #6 `_VALID_ROLE_FOR_E03` subset-gate; `_STATE` vs `CO_STATE_CODE` naming unification; CPW Big Game brochure URL hard precondition (operator must resolve); multi-source provenance for the 10 S05.4 federal no-hunt zones; conditional 6th `db.update_geometry_verbatim` helper decision. **Operator Group B batched live-write session remains the hard precondition** feeding S06.6+ live execution against CO geometry rows (outstanding across S05.0 + S05.2 + S05.3.5 + S05.4 + S05.5 + S05.7); **S06.0 through S06.5 are dry-run-decoupled and can begin without Group B**. Post-E05 M2 hygiene ledger holds at 1 item — Known Issue #7 (narrow overlay-builder shared-lib extraction).)
**Current Milestone:** M2 — Colorado Ingestion (In Progress; E04 Complete + Audited 2026-05-31 with 0 blocking findings; E05 Complete + Audited 2026-06-06 (audit re-run + hygiene fixes merged 2026-06-08), epic at `epics/completed/E05-colorado-geometry-ingestion.md`. **E06 In Progress** — 9 of 15 stories closed (S06.0 decision-gate 2026-06-08; S06.1 CO PDF fetch infra 2026-06-09; S06.3 CPW Big Game extraction 2026-06-13; S06.4 CPW Black Bear extraction 2026-06-15; S06.3.1 mid-epic carve-out 2026-06-16 — Phase A outcome (b) structurally inert + Phase B R17 ported as big-game "R16" with 4 codes recovered + ADR-022 (Accepted) formalizing single-module per-state extractor convention; S06.5 closed 2026-06-18 — restricted-area `verbatim_rule` population + NEW `db.update_geometry_verbatim()` helper + phrasing case (1) + 9+1 split locked + 🚩 Known Issue #12 AFA classification deferral; S06.6 closed 2026-06-19 — first E06 DB-write story; 398 `regulation_record` rows built; **Q16 confirmed does NOT fire for CO**; `_JURISDICTION_BINDING_ID_FORMAT` byte-identical to MT locked for S06.10 import; S06.6.1 closed 2026-06-22 — state-agnostic ArcGIS-fetch hardening via new strict `_require_objectid` helper + `_RA_OUT_FIELDS` workaround + AST-walk convention lock; **S06.6.2 Group A closed at-merge 2026-06-23** via PR #73 / `82e34df` — second-consecutive PAD-US 4.1 republish-drift fix; Phase B outcome (a) documented epsilon relaxation `rel_tol=1e-6 → 1e-3` (RMNP self-intersection mechanism characterized; max area-loss 0.0676% < 0.1% threshold; fail-loud preserved for >0.1%); 4 new tests + 2 committed PAD-US drift-detection fixtures (closes deferred S06.6.1 AC #627/#628 per the metadata+manifest convention); cubic P1 accepted-by-design with documented-deferral rationale; test baseline 1783 → 1787 + 4 skipped; **S06.2 omitted by design** at S06.3 Stage-1 discovery). Epic at `epics/E06-colorado-regulation-text-ingestion.md`. Merge order proceeding: S06.0 ✓ → S06.1 ✓ → S06.2 (omitted) → S06.3 ✓ → S06.4 ✓ → S06.3.1 ✓ → S06.5 ✓ → S06.6 ✓ → S06.6.1 ✓ → S06.6.2 ✓ → **S06.7 (next active; first multi-link-table adapter; `drift_guard.assert_id_matches` mandatory at every per-row entity-construction site; Q20 Season Choice surfaces; dry-run path can begin in parallel — live execution gated on M2-build operator-pass resume completing Steps 4 → 10)** → S06.8 → S06.9 → S06.10 → S06.11. **Post-implementation-audit standard** continues per E02 + E04 + E05 precedent — each future epic closes with a post-implementation audit recorded under `docs/planning/epics/completed/E0X-audit.md` before `/plan-next-epic` is invoked for the following epic; PM refuses-and-flags any `/plan-next-epic` invocation where the prior epic's `Audited:` field is unpopulated). M1 closed 2026-05-27 with `m1` tag at PR #45 (`ccbe085`).
**Overall V1 Status:** 2/6 milestones complete; M2 active

---

## Milestone Status

| Milestone | Name | Status | Validated | Dependencies |
|---|---|---|---|---|
| M0 | Scaffold | Complete | 2026-04-22 | None |
| M1 | Montana Ingestion | Complete | 2026-05-27 | M0 |
| M2 | Colorado Ingestion | In Progress (E04 Complete + Audited 2026-05-31; E05 Complete + Audited 2026-06-06 (audit re-run + hygiene fixes 2026-06-08); **E06 In Progress** — 9 of 15 stories closed (S06.0, S06.1, S06.3, S06.4, S06.3.1, S06.5, S06.6, S06.6.1, S06.6.2 Group A); **S06.6.2 Group A closed at-merge 2026-06-23** via PR #73 / `82e34df` — second-consecutive PAD-US 4.1 republish-drift fix; Phase B outcome (a) documented epsilon relaxation `rel_tol=1e-6 → 1e-3` with ~20-line rationale comment naming the PAD-US 2026-06-22 republish trigger + RMNP self-intersection mechanism + threshold; **fail-loud preserved** (>0.1% area-loss still raises — 125× the new threshold for genuine overlap loss); Phase A characterized RMNP as the only GC-branch zone (parsed.area inflated by ring self-intersection double-count; recovered.area matches PAD-US GIS_Acres to 0.04%; the 0.0676% gap is artifact removal, not data loss); 4 new tests (2 epsilon-boundary + 1 real-mechanism + 1 10-zone integration); test baseline 1783 → 1787 + 4 skipped; **closes deferred S06.6.1 AC #627 (metadata) + AC #628 (manifest) per the metadata+manifest convention** (PM spec-drift fix `36e487d`); cubic P1 accepted-by-design with documented-deferral rationale recorded in closure memo; .secrets.baseline updated for 2 false positives (layer_hash SHA + ArcGIS serviceItemId); state-agnostic-clean preserved; no new ADRs; S06.6.1 closed 2026-06-22; S06.6 closed 2026-06-19 as the first E06 DB-write story (398 regulation_record rows built; Q16 confirmed does NOT fire for CO); Group B operator-pending — operator resumes M2-build pass from Step 4 → 10 against `main`; layer_hash should match committed manifest `50e486e3…`; one batched live-write session captures S05.0/S05.2/S05.3.5/S05.4/S05.5/S05.7/S06.5/S06.6/S06.6.1/S06.6.2 Group B; 🚩 Known Issue #12 still queued for S06.10 (AFA classification deferral); 🚨 **PAD-US 4.1 source stability** surfaced as M2-release / M3 strategic flag (two republish drifts in 18 calendar days — pin vs continue patching vs re-source from NPS/USFWS direct); **S06.7 next active** (`season_definition` + `license_tag` + `license_season` — `drift_guard.assert_id_matches` mandatory; Q20 Season Choice surfaces as flag-and-discuss); dry-run path can begin in parallel; live execution gated on operator-pass resume; E06 implementation + audit ahead) | 2026-05-29 (E04), 2026-05-31 (E05), 2026-06-08 (E06) | M1 |
| M3 | MCP Server | Not Started | — | M1 |
| M4 | Web Companion | Not Started | — | M3 |
| M5 | Claude Code Plugin | Not Started | — | M4 |

---

## Current Milestone: M2 — Colorado Ingestion

M2 delivers Colorado regulations into Supabase Postgres, validated against the same six-entity schema, covering five V1 species across all applicable Game Management Units (GMUs), CWD zones, and overlay geometries. Three sequential epics (PRD 002):

### Epic Status

| Epic | Name | Status | Validated | Completed | Stories |
|---|---|---|---|---|---|
| E04 | M1 Carry-Forward and Colorado Schema Preparation | **Complete (audited) 2026-05-31** (5 of 5 stories closed across 5 calendar days; post-implementation audit closed same day at PR #52 / `b168d28` — 49 ACs reviewed, 47 MET + 2 operator-asserted + 0 NOT MET + 0 blocking findings; single actionable finding [S04.1 migration header `public.`-qualifier rationale] resolved at `7478ea6` as comment-only header edit, zero DDL impact; audit report at `epics/completed/E04-m1-carry-forward-audit.md`) | 2026-05-29 | 2026-05-31 | 5 (S04.6 evaluated and omitted) |
| E05 | Colorado Geometry Ingestion | **Complete (audited) 2026-06-06; audit re-run + hygiene fixes merged 2026-06-08** (9 of 9 stories closed; S05.0 PR #54 `7f3b071`; S05.1 PR #55 `ba6ef1c`; S05.2 PR #56 `d7ba731`; S05.3 PR #57 `9fd5c61` documented-gap close; S05.3.5 PR #58 `3344971` ADR-021 implementation; S05.4 PR #59 `dc9d5b2` outcome (c) — 10 V1 CO no-hunt zones from PAD-US 4.1; S05.5 PR #60 `e7ba3d4` CO overlay-fixture builder + state-agnostic-clean lib extension; S05.6 PR #61 `1b55bfd` cross-state binding-loader reference scaffold; S05.7 PR #62 `83e4c32` spatial query verification + epic exit; **audit re-run + hygiene fixes PR #63 `f612a73`** — independent re-run via 9 parallel per-story review agents superseded the at-merge audit; verdict unchanged (E05 ships clean, no blocking findings); ~85 MET / ~29 PARTIAL Group-B-operator-pending by design / 0 NOT MET; 3 of 4 M2 hygiene candidates retired in the same PR; audit report at `epics/completed/E05-audit.md`; CO V1 geometry composition: 1 statewide + ~186 GMUs + 0 CWD zones (S05.3 documented gap) + 10 restricted areas; epic file migrated to `epics/completed/` 2026-06-08 per standard close-out; test baseline 1166 → 1184 → 1187 → 1234 (held through S05.3 + S05.3.5) → 1283 → 1331 → 1340 (held through S05.7) → **1346** + 2 skipped (+6 from hygiene PR's additive guard tests)) | 2026-05-31 | 2026-06-06 (audit re-run 2026-06-08) | 9 (S05.0–S05.7 + S05.3.5 carve-out) |
| E06 | Colorado Regulation Text Ingestion | **In Progress** (9 of 15 stories closed; **S06.6.2 Group A closed at-merge 2026-06-23** via PR #73 / `82e34df` — second-consecutive PAD-US 4.1 republish-drift fix; Phase B outcome (a) documented epsilon relaxation `rel_tol=1e-6 → 1e-3` with rationale comment + RMNP self-intersection mechanism characterized; fail-loud preserved for >0.1% area-loss; 4 new tests; closes deferred S06.6.1 AC #627/#628 per metadata+manifest convention (PM spec-drift fix `36e487d`); cubic P1 accepted-by-design with documented-deferral rationale; test baseline 1783 → 1787 + 4 skipped; planned + validated 2026-06-08; S06.0/S06.1/S06.3/S06.4/S06.3.1/S06.5/S06.6 closed 2026-06-08 → 2026-06-19; **S06.6.1 closed at-merge 2026-06-22** via PR #72 / `0506831` — state-agnostic ArcGIS-fetch hardening shipped via new strict `_require_objectid(feature, *, oid_field=None)` helper in `lib/arcgis.py` (`_read_objectid` unchanged; the strict helper is routed only into the two OID-critical manifest-hash loops) + `_RA_OUT_FIELDS` `"OBJECTID"` workaround in `load_restricted_areas.py` + AST-walk regression test locking `"OBJECTID"` in every CO `_*_OUT_FIELDS` tuple + 1 new pitfall in `known-pitfalls.md` § "Integration — ArcGIS"; 3 spec deviations reviewer-confirmed and AC-annotated (AC #622 `_GMU_OUT_FIELDS` doesn't exist as a hardcoded tuple — load_gmus delegates outFields via `metadata.out_fields`, explaining why Step 3 succeeded while Step 4 failed; AC #627/#628 fixture + manifest regen deferred to operator-pass-resume — no `--dry-run` flag, no committed PAD-US fixtures; AC #629 pinned-SHA constant doesn't exist — `test_load_co_restricted_areas.py` mocks `compute_feature_hash`); post-review P1 fix added scalar-only `int`/`str` coercion preventing manifest hash-collision risk; test baseline 1774 → **1783 + 4 skipped** (+9 — spec predicted ~1779, the +4 strictness tests from the post-review fix bumped it); ruff/mypy/cubic/detect-secrets all clean; MT-untouched + schema + `db.py` + TS-stack all clean; no new ADRs (refines ADR-001 + ADR-005); one MT hygiene candidate carried for future MT-touching PR (`backfill_manifests.py:175-178` stale comment); Group A satisfied at-merge; Group B operator-pending folds into the M2-build operator-pass resume from Step 4 → 10 against dev; 5 outstanding UAT spot-checks PM-pending for S06.3 / S06.4 / S06.3.1 / S06.5 / S06.6 Group B; **S06.7 next active** (`season_definition` + `license_tag` + `license_season` ingestion — first multi-link-table adapter; `drift_guard.assert_id_matches` mandatory at every per-row entity-construction site; Q20 Season Choice surfaces as flag-and-discuss); dry-run path can begin in parallel; live execution still gated on M2-build operator-pass resume completing Steps 4 → 10 against `main`) | 2026-06-08 | — | 15 (S06.0–S06.11 + S06.3.1 + S06.6.1 + S06.6.2 carve-outs) |

### E04 Story Status

| Story | Name | Status | Owner |
|---|---|---|---|
| S04.1 | license_season RLS migration | **Fully Closed 2026-05-30** (Group A satisfied at-merge; Group B closed via live verification later same day — all 4 queries returned expected shape against the production project; Q4's 2411 vs. the prompt's "~3040" was the post-UPSERT-collapse DB baseline per S04.4 spec L280, not a leak; verbatim outputs captured in E04 epic § "Group B verification record — 2026-05-30"); migration `supabase/migrations/20260530132727_rls_license_season.sql` | Implementation |
| S04.2 | Narrow _BINDING_COUNT_GUARD_BAND to (552, 1024) — **first M2 PR per handoff §8** | Complete (squash-merged to main 2026-05-29; chronology lock satisfied; test baseline shifted 1165 → 1166 via new `TestCountGuard::test_band_locked_to_t16_empirical` contract-lock test; 4 post-merge cubic-fix iterations; 3 new pitfalls under `.roughly/known-pitfalls.md`) | Implementation |
| S04.3 | Add logging.basicConfig to load_jurisdiction_bindings.py main() | **Closed 2026-05-30** (PR #49 / `5de83c3`; third M2 PR chronologically; 4-line multi-line `basicConfig` at `load_jurisdiction_bindings.py:785-788`; `--dry-run` now emits 25 INFO lines including `TOTAL: 788 bindings` re-confirming S04.2's empirical 788; test suite stable at 1166 + 2 skipped, no delta) | Implementation |
| S04.4 | M1 UAT runbook hygiene fixes (six edits + mandatory 7th annotation + PM-approved adopted 7th row for jurisdiction_binding build-vs-DB) | **Closed 2026-05-30** (fourth M2 PR; 4-commit chain — impl + plan-marker + §6 audit-trail-preamble cubic P1 review-fix + T4/T5 plan-dep P3 fixes; 7 prescribed edits + 4 Stage-6 review-fix corrections + 1 user-approved AC-interpretation override; draw_spec 278-vs-276 provenance reconciled per S03.8 closure authoritative; 3 pitfall candidates surfaced for user decision; test suite stable at 1166 + 2 skipped, zero delta) | Implementation |
| S04.5 | PRD 001 sequencing language reconciliation (PM drafts diff; human applies) | **Closed 2026-05-31** (5-commit chain via `/roughly:build` under user's git identity: `bf9bfa9` PRD 001 lines 90/96/111 + `3445017` Bundle A pitfalls + `37bc86a` Bundle B handoff hygiene + `91bde52` plan-marker + `eb803db` cubic sentence-initial-capitalization fix-up; PM-judgment override accepted for the `/roughly:build` delegation as satisfying the no-autonomous-PRD-edit intent — user-initiated delegation under user's git identity is explicit human control) | PM + Human (delegated via `/roughly:build`) |

**E04 merge order is hard-locked:** S04.2 → S04.1 → S04.3 → S04.4 → S04.5 (S04.2-first per handoff §8 sixth bullet; S04.1-before-S04.4 because S04.4's mandatory criterion #7 sign-off annotation references S04.1's migration timestamp). See E04 epic §"Parallelization Notes" for the precondition details.

### E05 Story Status

| Story | Name | Status | Owner |
|---|---|---|---|
| S05.0 | Schema preparation — write `CO-STATEWIDE-geom` | **Closed at-merge 2026-06-01** (PR #54 / `7f3b071`; first E05 PR; source = US Census TIGER 2024 Tier-3 fallback per source-priority framing; SHA-256 pinned; 9 fail-loud guards in loader; 4 new pitfalls landed; test baseline shifted 1166 → **1184** + 2 skipped via +18 new unit tests; Group A satisfied at-merge; Group B operator-pending — AC #7 verification queries + AC #8 area_km2 within 1% — pending live `supabase db push` + loader-run; 2 M2 hygiene candidates queued) | Implementation |
| S05.1 | CPW ArcGIS fetch infrastructure + Colorado adapter scaffold | **Closed at-merge 2026-06-02** (PR #55 / `ba6ef1c`; second E05 PR; scaffold-only — 5 files; zero `ingestion/lib/` edits held; new `TestNoColoradoLeakIntoSharedLib` AST-walk regression test locks ADR-005 going forward for CO; test baseline shifted 1184 → **1187** + 2 skipped; 4 new pitfalls landed; 2 PM-approved spec deviations — `CO_STATE_CODE` vs `_STATE` documented with S05.6 forward-reference; codebase-actual import form `from ingestion.lib import arcgis, db`) | Implementation |
| S05.2 | GMU ingestion (CPW FeatureServer layer 6, ~186 polygons) | **Closed at-merge 2026-06-02** (PR #56 / `d7ba731`; third E05 PR; loader 409 LOC + tests 733 LOC / 47 tests; AC #235 satisfied as LOG-ONLY deviation — `SourceCitation` is frozen with `extra="forbid"`, E05 forbids schema migrations; Group A satisfied; Group B operator-pending pending live CPW fetch + DB write; test baseline shifted 1187 → **1234** + 2 skipped) | Implementation |
| S05.3 | CWD zone discovery + ingestion (Q18 trigger surfaces here) | **Closed (documented gap) 2026-06-03** (PR #57 / `9fd5c61`; fourth E05 PR; live 3-path investigation conclusively found CPW publishes no CWD-zone geometry — CO manages CWD by hunt code / GMU, not mapped zone polygons; zero rows; no loader; investigation report at `ingestion/states/colorado/cwd-source-discovery.md`; Q18 empirically resolved with PM recommendation for E06 to retain V1 license-keyed disposition; test baseline unchanged at 1234 + 2 skipped; 3 new pitfalls landed) | Implementation |
| S05.3.5 | `jurisdiction_binding.role` migration + MT V1 reclassification (ADR-021 implementation; pre-work for S05.4) | **Closed at-merge 2026-06-03** (PR #58 / `3344971`; fifth E05 PR; new migration `20260603000000_jurisdiction_binding_no_hunt_zone_role.sql` extends CHECK constraint 7 → 8 values adding `'no_hunt_zone'`; 5-place sync shipped — DDL + Pydantic + TypeScript + architecture.md + `overlays.py` `GeometryRoleForE03` Literal; 3 MT V1 federal no-hunt-zone bindings reclassified `'other_overlay'` → `'no_hunt_zone'`; ADR-021 flipped Proposed → Accepted; spec gap closed at Stage-2 discovery — `cast(Literal[…], role_e03)` in `_build_overlay_bindings` was a 6th consumer beyond the spec's 5 named sites; `_VALID_ROLE_FOR_E03` deliberately not widened (E06 candidate item #6 in epic § Known Issues); test baseline holds at 1234 + 2 skipped (N=0 delta; all rewrites); 2 new pitfalls landed; Group B operator-pending) | Implementation |
| S05.4 | Restricted-area / no-hunt-zone overlay discovery + ingestion (10 V1 CO no-hunt zones via PAD-US 4.1) | **Closed at-merge 2026-06-04** (PR #59 / `dc9d5b2`; sixth E05 PR; outcome (c) realized — 10 V1 federal no-hunt zones ingested from USGS PAD-US 4.1 Federal Fee Managers Authoritative FeatureServer: 4 NPs + 5 NMs + 1 DOD AFA; Curecanti NRA dropped post-fetch per 36 CFR §2.2; loader composes private `arcgis` primitives — zero `ingestion/lib/` edits; geometry rows only — no `jurisdiction_binding` writes (E06 territory); `verbatim_rule=None` V1 deferral pending CPW Big Game brochure URL resolution; research-doc count corrected 9 → 10; test baseline shifted 1234 → **1283** + 2 skipped (+49); 3 new pitfalls landed; Group B operator-pending) | Implementation |
| S05.5 | `geometry-overlays.json` fixture build (CO analog; library extension state-agnostic-clean) | **Closed at-merge 2026-06-05** (PR #60 / `e7ba3d4`; seventh E05 PR; new `ingestion/states/colorado/build_overlay_fixture.py` ~390 LOC faithful port of MT S02.6; `ingestion/ingestion/lib/overlays.py` extended state-agnostic-clean — `OverlayParentKind`/`OverlayChildKind` add `"gmu"`; new canonical export `ROLE_FOR_BINDING_BY_CHILD_KIND` with `ROLE_FOR_E03_BY_CHILD_KIND` retained as same-object deprecated alias for MT compat; `EXPECTED_CO_RA_ORPHAN_IDS` = 10 S05.4 federal no-hunt zones byte-identical to `_V1_EXPECTED_IDS`; W1 absence-of-orphan-log lock catches the vacuously-passing-coverage-check trap; Group B operator-pending — live fixture depends on S05.2 + S05.4 operator writes; test baseline shifted 1283 → **1331** + 2 skipped (+48); 2 new pitfalls landed; new E05 Known Issue #7 — narrow overlay-builder shared-lib extraction post-E05 tech-debt candidate; epic snippet drift at lines 502-513 corrected to byte-parity with shipped code) | Implementation (UAT: yes — Group B operator-pending) |
| S05.6 | Cross-state spatial discipline + binding-loader reference (`_STATE = 'US-CO'`) | **Closed at-merge 2026-06-05** (PR #61 / `1b55bfd`; eighth E05 PR; scaffold/reference-only — zero DB writes, zero `jurisdiction_binding` rows anywhere in E05; new `ingestion/states/colorado/load_jurisdiction_bindings.py` import-only scaffold with `_STATE` + `_NO_HUNT_ZONE_NEARBY_DISTANCE_M` + module-level `_QUERY_NEARBY_GMUS_FOR_ZONE_SQL` reference for E06's drop-in binding loader; `extensions.ST_DWithin` boundary-to-boundary on geography with all 3 params `%s`-bound — Stage-6 Critical W1 fix caught the spec's hardcoded `5000` literal so E06 recalibration now flows via the named constant; `ORDER BY gmu.id` for deterministic E06 output (post-merge-review P3 fix at `9b1fa7d`); 9 tests including spec-mandated `test_co_binding_loader_sql_filters_by_state_co_pollution_guard`; test baseline shifted 1331 → **1340** + 2 skipped (+9); 1 new pitfall landed; new E06 cleanup candidate — `_STATE` vs `CO_STATE_CODE` naming unification across all 5 CO loaders, deliberately not retrofitted; epic SQL snippet corrected to byte-parity with shipped code) | Implementation |
| S05.7 | Spatial query verification + epic exit (UAT + audit gate for `/plan-next-epic`-to-E06) | **Closed at-merge 2026-06-06** (PR #62 / `83e4c32`; ninth + final E05 PR; documentation + verification only — no Python modules, no schema, no DB writes; test baseline unchanged at 1340 + 2 skipped; three new docs shipped: `docs/runbooks/E05-colorado-geometry-verification.md` CO operator runbook ~365 LOC; `docs/planning/epics/completed/E05-confidence-findings/S05.7.md` Group B verification-record table; `docs/planning/epics/completed/E05-audit.md` post-implementation audit; 2 post-PR-review SQL-correctness fixes — `geom::geometry` cast → WKT round-trip; `DELETE FROM jurisdiction_binding WHERE state` → FK-precise form; 3 new/updated pitfalls; `.roughly/known-pitfalls.md` now ~1045 LOC) | Implementation (UAT: yes — Group B operator-pending) |

**E05 merge order completed**: S05.0 → S05.1 → S05.2 → S05.3 → S05.3.5 → S05.4 → S05.5 → S05.6 → S05.7. S05.3.5 was carved out 2026-06-03 between S05.3 and S05.4 as ADR-021's implementation deliverable — mirrors the S03.6.1 carve-out pattern (MT-touching + schema-extending in one PR; CO-loading in the next). All 9 stories closed 2026-06-01 → 2026-06-06; post-implementation audit closed 2026-06-06 with no blocking findings. E06 is the next active work-front via `/plan-next-epic`.

E05 closed 2026-06-06 (audit re-run + hygiene fixes merged 2026-06-08); E06 planned + validated 2026-06-08. PRD 002 §"Why sequential" — only the E06→E05 dependency is FK-hard (binding rows FK to BOTH `regulation_record` AND `geometry`); the other orderings are operator-discipline ordering.

### E06 Story Status

| Story | Name | Status | Owner |
|---|---|---|---|
| S06.0 | Pre-E06 decisions + schema-prep gate (5 pre-registered + conditional 6th decision for human approval; conditional schema migration if gap surfaces) | **Closed at-merge 2026-06-08** (PR #65 / `5d6ab37`; first E06 PR; decision-gate / documentation-only — no code, no migration, no schema/three-place-sync, no `db.py` helper, no production-DB writes; test baseline held exactly at 1346 + 2 skipped). All 6 decisions locked: **D1**=(c) license-keyed CWD (S06.9 ships 0 typed CWD `reporting_obligation` rows; text → `regulation_record.additional_rules`); **D2**=hardcoded path for CO no-hunt zones (S06.10 mirrors MT `_build_no_hunt_zone_bindings`; `_VALID_ROLE_FOR_E03` gate stays narrow at 4 values); **D3**=`_STATE` wins (4 CO loaders renamed `CO_STATE_CODE` → `_STATE` as same-PR follow-up in S06.10); **D4**=CPW Big Game brochure URL **RESOLVED** (Artemis durable path + SHA-256 + HEAD baseline + cover-page-2026 captured in memo §D4; Known Issue #7 RESOLVED; S06.1 fully unblocked); **D5**=(b) split-provenance (PAD-US keeps `geometry.source`; CPW text reaches rows via `verbatim_rule` only; no migration); **D6**=`db.update_geometry_verbatim` helper built in S06.5, not S06.0. **Schema-gap read-through: no gap fires → no migration**; decision memo at `docs/planning/epics/E06-confidence-findings/S06.0.md` (deletes at `m2` tag per ADR-017 §6); all 12 ACs ticked at-merge | PM + Human + Implementation |
| S06.1 | CPW PDF fetch infrastructure + CO `sources.yaml` `pdfs:` section | **Closed at-merge 2026-06-09** (PR #66 / `abc6c21`; second E06 PR; new `ingestion/states/colorado/fetch_pdfs.py` ~285 LOC + `sources.yaml` `pdfs:` section with **2 fully-pinned CPW PDFs** — Big Game brochure 2026 + Big Game correction 2026-02-19 — both on the CO State Publications Library "Artemis" with SHA-256 byte pins; **2 post-merge-review fixes**: (i) unenforced SHA pin lifted to a **state-agnostic opt-in `expected_sha256` param** on `pdf_fetch.fetch_pdf` (CO was the first state with real pins; first-fetch now auto-enforced; complements but does not replace the manifest re-fetch drift gate — **two-gates model**); (ii) doc scope contradiction reconciled. **ADR-005 holds** despite the lib edit (state-agnostic expansion, mirrors S05.5 precedent); CO-leak guard test green. Test baseline shifted **1346 → 1376** + 2 skipped (+30: 24 in `test_fetch_co_pdfs.py` + 6 lib pin-gate tests). **S06.4 forward-note registered**: the 2026 correction PDF is **moose-only** (out of V1 scope) → ADR-019 `correction > annual_regulations` merge expected to be **CO V1 no-op** (S06.4 confirms inert via three-pass arbitration). Group A satisfied at-merge; Group B operator-pending (AC #188 per-PDF manifest commits at first fetch — S03.1 "infrastructure-met, fixture-deferred" posture) | Implementation |
| S06.2 | pdfplumber primitives extension (conditional — may be omitted if M1 primitives sufficient at S06.3 discovery) | **Omitted 2026-06-13** — S06.3 Stage-1 discovery confirmed M1's `pdfplumber` primitives were sufficient; no CPW-specific pdfplumber extension needed. Closure read-through recorded in S06.3 closure note. (S06.3 did ship a state-agnostic JSON-serialization helper `lib/pdf.write_extraction_artifact` for cubic 50k-line-cap workaround — that's an unrelated JSON-writer convention, not a pdfplumber primitive extension covered by S06.2's trigger condition) | — |
| S06.3 | CPW Big Game brochure extraction (deer + elk + pronghorn) | **Closed at-merge 2026-06-13** (PR #67 / `08bf396`; third E06 PR; **the load-bearing E06 extraction story**; new `extract_big_game.py` 2,685 LOC + `extracted/big-game-2026.json` artifact 737 sections / 2,758 rows / SHA `3c2ecd90…015d` <!-- pragma: allowlist secret --> / confidence `{high:2170, medium:583, low:5}`; **new shared lib helper `ingestion/ingestion/lib/pdf.write_extraction_artifact(records, path)`** — state-agnostic-clean one-record-per-line serializer (cubic 50k-line-cap workaround turned into a reusable primitive); S06.4 must use it from the start; 5 real-PDF discoveries baked in (CPW hunt-code grammar; Season Choice X mapping; plains whitetail detection; R14 character-doubling recovery; empty-GMU page-disambiguation); **Q20 opened** (Season Choice → `season_definition`/`license_tag` modeling; S06.7 decision); ADR-017 unmodified per FINALIZE (`low=5` is data-shape signal); test baseline shifted 1376 → **1498** + 3 skipped (+122 additive: 1,334-LOC `test_extract_co_big_game.py` + 7 `TestWriteExtractionArtifact` tests); UAT spot-check PM-pending — not blocking S06.4 | Implementation (UAT: yes — PM-pending) |
| S06.3.1 | Big Game extractor hygiene — R17 port + elk-correction investigation (carve-out post-S06.4 closure 2026-06-16; mirrors S03.6.1 / S05.3.5 mid-epic carve-out precedent) | **Closed at-merge 2026-06-16** (PR #69 / `bfeb60a`; sixth E06 PR; resolves Known Issues #10 + #11; **Phase A outcome (b) structurally inert** — brochure PDF postdates all 4 corrections so V1 elk corrections already correct in committed artifact, ~400 LOC of three-pass infra avoided; **Phase B R17 ported as big-game "Rule R16"** — partial-column-fusion rule `{N parts → distribute \| 1 → broadcast \| else → fail-loud}`; **4 codes recovered** (`D-M-082-O3-R`, `D-F-107-O1-R`, `A-M-004-O1-M`, `A-F-118-O1-R`) — spec's "9" was unmeasured estimate, empirical is 4; **Phase C SHA shift** `e5c7c33a…6d1e` → `9312e2595071a80cc317250504e4ba6a7eaaae33a201a313db275aa0f0c8bb2f` <!-- pragma: allowlist secret -->; sections 737 → 736 (placeholder collapsed); rows 2758 → 2762 (+4); confidence high 2170 → 2178 / low 5 → 1; **NEW ADR-022 (Accepted): Single-Module Per-State PDF Extractors** formalizes Known Issue #9; 3 PR-review cycles incl. single `_HUNT_CODE_GRAMMAR` source-of-truth + continuation-comma fail-loud guard; test baseline 1669 → 1681 + 4 skipped; UAT PM-pending) | Implementation (UAT: yes — PM-pending) |
| S06.4 | Black Bear extraction (separate brochure OR fold into S06.3 per S06.1 discovery; correction-PDF axis preserved) | **Closed at-merge 2026-06-15** (PR #68 / `51f6aa7`; fourth E06 PR; **shipped as separate extractor**; new `extract_black_bear.py` 3,603 LOC parses bear section pp. 72–77 of CPW Big Game brochure; artifacts `black-bear-2026.json` 172 records / 215 rows / SHA `7b35c202…d5f6` <!-- pragma: allowlist secret --> / confidence `{high:215, medium:0, low:0}` + `black-bear-2026-base.json` (Pass-1) + `corrections-2026-02-19.json` (Pass-2 ops `[]` — **inert for bear**); composition: 137 limited_draw + 78 OTC; **17 cleanup rules R1–R17** documented including CO-specific R16 (hunt-code in prose) + R17 (pdfplumber fused-row split); three-pass arbitration preserved as inert-confirmation pathway (NOT bypassed); **branch-name caveat**: branch is `feat/S03.4-…` (human typo at branch creation; commits scoped S06.4 correctly); **⚠️ cross-story `valid_gmus` cleanup** touched merged S06.3 code — `big-game-2026.json` SHA shifted `3c2ecd90…015d` → `e5c7c33a…6d1e` (counts UNCHANGED 737/2758; confidence UNCHANGED; only field contents moved; re-pinned in `test_extract_co_big_game.py`); test baseline shifted 1498 → **1669** + 4 skipped (+171 additive); cubic was unavailable during late post-restart review cycle (gates rested on ruff/mypy/1669-suite + manual SHA/count/exclusion verification); **2 RED-FLAG carry-forwards** — Known Issue #10 latent S06.3 elk-correction-content gap (correction PDF is moose+elk not moose-only; PM decision pending) + Known Issue #11 latent big-game R17 row-fusion defect (9 fused cells silently dropped codes; recommend separate hygiene story before S06.6); UAT spot-check PM-pending — not blocking S06.5 | Implementation (UAT: yes — PM-pending) |
| S06.5 | Restricted-area `verbatim_rule` population for the 10 S05.4 federal no-hunt zones | **Closed at-merge 2026-06-18** (PR #70 / `207f31d`; seventh E06 PR; new `load_restricted_area_verbatim.py` 297 LOC + NEW `db.update_geometry_verbatim()` helper (S06.0/D6 delivered); D5=(b) split-provenance honored (only `verbatim_rule` updated; `source` stays PAD-US `gis_layer`); phrasing case (1) + **9+1 split** locked — 9 NPS rows share one closure sentence, AFA gets its own access-rules prose; test baseline shifted 1681 → **1702** + 4 skipped (+21 additive); ADR-008 byte-equivalence + no `layout=True` AST guard; **🚩 AFA classification flag** carried forward to S06.10 — AFA is a regulated-access HUNTING area not a no-hunt zone (Known Issue #12); Group A satisfied at-merge; Group B operator-pending shares the E05 Group B precondition) | Implementation (UAT spot-check is Group B operator-pending) |
| S06.6 | `regulation_record` ingestion (5 V1 species × applicable CO GMUs + statewide anchors) | **Closed at-merge 2026-06-19** (PR #71 / `4ecd47d`; eighth E06 PR; first E06 DB-write story; new `load_regulation_records.py` 848 LOC; **398 `regulation_record` rows in one atomic transaction** (mule_deer 141 / elk 115 / pronghorn 77 / whitetail 19 / bear 46; 0 statewide anchors; 0 `low`-tier rows); collapse `(gmu_code, species_group)` design **906 sections → 398 records** locked by `_collapse_sections_to_record` shared helper; **`_JURISDICTION_BINDING_ID_FORMAT` byte-identical to MT** for S06.10 reuse (3 locking tests); AC #544 fan-out reframed at closure (CO species are pre-separated; no `deer` label requiring fan-out); **Q16 does NOT fire for CO**; **`_STATE = "US-CO"`** in this loader (4 existing CO loaders' rename remains bundled with S06.10/D3); 3 new pitfalls (CO collapse + pre-separation + bear flat-list with `record_type` discriminator); test baseline shifted 1702 → **1774** + 4 skipped (+72 additive); Group A satisfied at-merge; Group B operator-pending shares E05 Group B precondition) | Implementation (UAT: no; Group B operator-pending) |
| S06.6.1 | PAD-US OBJECTID outFields hardening (operator-pass-discovered carve-out post-S06.6 closure 2026-06-21; mirrors S03.6.1 / S05.3.5 / S06.3.1 mid-epic carve-out precedent) | **Closed at-merge 2026-06-22** (PR #72 / `0506831`; 9th E06 PR; **strict helper, not a mutation** — new `_require_objectid(feature, *, oid_field=None)` returning `int` or `str` in `lib/arcgis.py` (scans only `properties`/`attributes`; no `feature["id"]` fallback; raises `ArcGISError` with diagnostic); `_read_objectid` unchanged (kept for 6 error-message-context callers + dedup loop that legitimately tolerate `None`); strict helper wired into the two OID-critical manifest-hash loops (`fetch_features` + RA loader); `_RA_OUT_FIELDS` adds `"OBJECTID"` (multi-line tuple, append-not-prepend); new `TestReadObjectidFailsLoud` (8 tests, not the spec'd 3 — +5 from implementation + post-review P1 strict-coercion fix); new `TestStateAdapterOutFieldsIncludeObjectid` AST-walk over the entire CO adapter dir; **post-review P1 fix `d8bae0a`**: scalar-only `int`/`str` coercion (no `str(val)` fallback) prevents `null`-OBJECTID manifest hash-collision risk; **3 spec deviations reviewer-confirmed and AC-annotated**: AC #622 `_GMU_OUT_FIELDS` doesn't exist (load_gmus delegates outFields via `metadata.out_fields` — explains why Step 3 succeeded while Step 4 failed); AC #627/#628 fixture + manifest regen deferred to operator-pass-resume (no `--dry-run` flag, no committed PAD-US fixtures); AC #629 pinned-SHA constant doesn't exist (mocks `compute_feature_hash`); 1 new pitfall in `known-pitfalls.md` § "Integration — ArcGIS" naming both the failure mode AND the CRS-shift forensic signal; test baseline 1774 → **1783 + 4 skipped** (spec predicted ~1779; +9 actual: 8 ArcGIS + 1 AST); ruff/mypy/cubic/detect-secrets all clean; MT-untouched + schema + `db.py` + TS-stack all empty; no new ADRs (refines ADR-001 + ADR-005); one MT hygiene candidate carried for future MT-touching PR (`backfill_manifests.py:175-178` stale comment — backfill keeps `_read_objectid` because older committed MT fixtures store OID under `feature["id"]`); Group A satisfied at-merge; Group B folds into the M2-build operator-pass resume from Step 4 → 10) | Implementation (UAT: no; Group B = operator-pass resume) |
| S06.6.2 | PAD-US GeometryCollection area-preservation handling for 10 V1 no-hunt zones (second operator-pass-discovered carve-out post-S06.6.1 closure 2026-06-22; mirrors S03.6.1 / S05.3.5 / S06.3.1 / S06.6.1 mid-epic carve-out precedent; **second-consecutive PAD-US 4.1 republish drift**) | **Closed at-merge 2026-06-23** (PR #73 / `82e34df`; 10th E06 PR; **Phase B outcome (a) documented epsilon relaxation** `rel_tol=1e-6 → 1e-3` with ~20-line rationale comment naming the PAD-US 2026-06-22 republish trigger + RMNP self-intersection mechanism + threshold; **fail-loud preserved** for any area-loss > 0.1% (125× the new threshold for genuine overlap loss); Phase A characterized RMNP as the **only** GC-branch zone (the other 9 V1 zones convert cleanly through existing `Polygon`/`MultiPolygon` paths); RMNP ring self-intersection inflates `parsed.area`, `make_valid()` repairs it into `GeometryCollection([MultiPolygon, ~19m LineString])`, `recovered.area` matches PAD-US `GIS_Acres` to 0.04% — the 0.0676% gap is artifact removal, not data loss; outcome (b) re-source NOT fired (max area-loss 0.0676% < 0.1%); outcome (c) `buffer(0)` rejected empirically; 4 new tests in `test_arcgis.py` (2 epsilon-boundary at −0.05% recovers / −0.2% raises + 1 real-mechanism synthetic self-intersection + 1 `TestPadusTenZoneIntegration` skip-if-absent); test baseline 1783 → 1787 + 4 skipped; Phase D committed `Federal_Fee_Managers_Authoritative_PADUS-0-{metadata,manifest}-20260623T000955Z.json` per `fixtures/.gitignore` metadata+manifest convention (closes deferred S06.6.1 AC #627 + AC #628; `*-features-*.geojson` stays gitignored); `.secrets.baseline` updated for 2 false positives (`layer_hash` SHA + ArcGIS `serviceItemId`); **cubic P1 accepted-by-design** (documented-deferral; option (c)) — cubic objected to admitting up to 0.1% loss but that is the deliberate Stage-4-endorsed evidence-backed decision recorded in closure memo § "Cubic review disposition"; AC ticked with explicit "not literally `{issues: []}`" annotation; state-agnostic-clean preserved; MT-untouched + schema + `db.py` + TS-stack all empty; no new ADRs (refines ADR-001 fail-loud-with-documented-threshold + ADR-005/022 state-agnostic-clean); 1 new pitfall in `.roughly/known-pitfalls.md` § "Integration — ArcGIS" (~692 LOC) — *single republish changes multiple things; CRS-shift WARNING = full re-audit signal; characterize before relaxing a fail-loud tolerance*; refreshed stale `rel_tol=1e-6` reference; Group A satisfied at-merge; Group B operator-pending — operator resume of M2-build pass from Step 4 → 10 against `main`; live-fetch `layer_hash` should match committed manifest `50e486e3…`) | Implementation (UAT: no; Group B = operator-pass resume) |
| S06.7 | `season_definition` + `license_tag` + `license_season` ingestion (drift_guard.assert_id_matches mandatory at every build function) | Not Started | Implementation |
| S06.8 | `draw_spec` ingestion (CPW preference-point hybrid — Q12/Q17 trigger surface) | Not Started (UAT: yes) | Implementation |
| S06.9 | `reporting_obligation` ingestion (drift_guard.assert_dispatch_dict_drift_free mandatory; Q18 trigger surface) | Not Started (UAT: conditional on CWD rules) | Implementation |
| S06.10 | `jurisdiction_binding` generation (consumes S05.5 fixture + S05.6 scaffold; final E06 DB-write story) | Not Started | Implementation |
| S06.11 | M2 milestone UAT preparation + handoff to M3 (final E06 story) | Not Started (UAT: yes — milestone-level) | Implementation + PM |

**E06 merge order proceeding**: S06.0 ✓ → S06.1 ✓ → S06.2 (omitted by design at S06.3 Stage-1 discovery) → S06.3 ✓ → S06.4 ✓ → S06.3.1 ✓ → S06.5 ✓ → S06.6 ✓ → S06.6.1 ✓ → S06.6.2 ✓ → **S06.7 (next active; first multi-link-table adapter; `drift_guard.assert_id_matches` mandatory at every per-row entity-construction site; Q20 Season Choice modeling surfaces as flag-and-discuss)** → S06.8 → S06.9 → S06.10 → S06.11. **🚨 S06.6 onward LIVE EXECUTION IS GATED** on the outstanding **E05 operator Group B batched live-write session** (CO geometry rows must exist in the M2-target environment before any regulation_record FKs to them — this precondition has been carried since E05 close, gated S06.6's Group B verification, and is now being actively closed via the **M2-build operator pass** which halted twice at Step 4 — first on S06.6.1's OID raise [fixed], then on S06.6.2's GeometryCollection area-preservation raise [fixed at-merge 2026-06-23]; **the pass is now unblocked and ready to resume** — operator merges `main` forward into `test/m2-operator-pass`, re-verifies Step 0 pre-pass DB state, and runs Steps 4 → 10 to completion). S06.7 dry-run path can begin in parallel without Group B but the live UPSERT/UPSERTs cannot run until the M2-build operator pass completes Step 10. **Test baseline floor for downstream stories: 1787 + 4 skipped** (was 1783 going into S06.6.2; +4 from S06.6.2's epsilon-boundary + real-mechanism + 10-zone integration tests). **🚨 PAD-US 4.1 source stability** flagged as M2-release / M3 strategic candidate — two distinct republish drifts in 18 calendar days (2026-06-04 → 2026-06-21 OID drift → 2026-06-22 GeometryCollection drift); options: pin a PAD-US snapshot fixture + cache forever vs continue patching live-fetch drift vs re-source from NPS/USFWS direct; tactical S06.6.1 + S06.6.2 fixes sufficient for the M2-build pass; worth human decision before the M2-release pass. **Q16 → confirmed does NOT fire for CO V1** (S06.6 closure 2026-06-19; CO artifact pre-separates `mule_deer`/`whitetail`/`elk`/`pronghorn` at section level; no fan-out needed). Q20 (Season Choice `X` method-group modeling per S06.3 discovery) surfaces at S06.7 as flag-and-discuss. Known Issues #10 + #11 → **RESOLVED via S06.3.1**; #9 → **formalized as ADR-022**; 🚩 #12 still queued for S06.10 (AFA classification deferral — S06.10 must NOT bind AFA `role='no_hunt_zone'`; S06.10 spec carries inline forward-note with PM-recommended option (a) `role='other_overlay'`). **Convention shifts mid-epic**: (a) `lib/pdf.write_extraction_artifact(records, path)` mandatory for new extractors (S06.3); (b) **ADR-022 (Accepted) — Single-Module Per-State PDF Extractors** locks the convention (S06.3.1); (c) `db.update_geometry_verbatim()` helper landed via S06.5 (state-agnostic write-path extension; S06.0/D6 delivered); (d) `_collapse_sections_to_record` shared helper extracted in S06.6 (single-module convention preserved per ADR-022; "split the module" companion finding declined); (e) **`_JURISDICTION_BINDING_ID_FORMAT` byte-identical to MT locked in S06.6** for S06.10 to import (3 tests per S03.6.1 pattern); (f) **collapse design `(gmu_code, species_group)`** locked in S06.6 — S06.7 link-table builders must group-and-collapse the same way or key off the written PKs.

---

## Past Milestone: M1 — Montana Ingestion (Complete 2026-05-27)

M1 delivered Montana regulations into Supabase Postgres, validated against the six-entity schema, covering five V1 species across all applicable jurisdictions. Three sequential epics:

### M1 Epic Status

| Epic | Name | Status | Validated | Completed | Stories |
|---|---|---|---|---|---|
| E01 | Schema Migrations, RLS, and Quality Gates | Complete | 2026-04-24 | 2026-04-28 | 6 |
| E02 | Montana Geometry Ingestion | Complete (audited) | 2026-04-28 | 2026-05-03 | 8 |
| E03 | Montana Regulation Text Ingestion | Complete | 2026-05-03 | 2026-05-27 | 14 (13 original + S03.6.1) |

### E03 Story Status

| Story | Name | Status | Owner |
|---|---|---|---|
| S03.0 | Schema preparation — license_season + geometry.legal_description + geometry.kind='state' + Montana state geometry | Complete | Implementation |
| S03.1 | PDF fetch infrastructure | Complete | Implementation |
| S03.2 | PDF extraction primitives (shared library) | Complete | Implementation |
| S03.3 | DEA booklet extraction (deer, elk, antelope) | Complete (UAT passed 2026-05-08 after one fix cycle; plan-realignment + fail-loud polish 2026-05-09) | Implementation (UAT: yes) |
| S03.4 | Black Bear booklet extraction + correction PDF handling | Complete (UAT passed 2026-05-12; Option B doc-type precedence accepted) | Implementation (UAT: yes) |
| S03.5 | Legal Descriptions extraction | Complete (UAT passed 2026-05-14 after PM-review-driven fix cycle) | Implementation (UAT: yes) |
| S03.6 | regulation_record ingestion | Complete (closed 2026-05-15; 436 rows + 228 legal_description writes) | Implementation |
| S03.7 | season_definition + license_tag + license_season ingestion | Complete (closed 2026-05-16; M1 criterion #2 satisfied at data layer; 8542 rows + 20 closure_predicates) | Implementation (UAT: yes) |
| S03.8 | draw_spec ingestion | Complete (closed 2026-05-19; 388 draw_spec rows / 278 unique + 388 license_tag.draw_spec_key backfills) | Implementation |
| S03.9 | reporting_obligation ingestion | Complete (closed 2026-05-21; 3 reporting_obligation rows + 70 regulation_reporting link rows; epic line 807 corrected; CWD sampling deferred to Q18; Bear ID coursework carved to S03.6.1) | Implementation |
| S03.6.1 | MT-STATEWIDE-bear anchor with Bear ID Test + jurisdiction_binding to MT-STATEWIDE-geom | Complete (closed 2026-05-22; 437 regulation_record + 1 jurisdiction_binding + new `db.upsert_jurisdiction_binding` helper for S03.10 to reuse) | Implementation (carved out 2026-05-19; binding scope-add 2026-05-21; closed 2026-05-22) |
| S03.10 | jurisdiction_binding generation | Complete (code-complete 2026-05-26 via PR `e83ef2d`; first cross-cutting binding loader; ~870 LOC; 18 fail-loud guards; 4 PM-approved spec deviations; 7 new pitfalls; reuses `db.upsert_jurisdiction_binding` helper from S03.6.1 — no `db.py` changes; T16 live UAT operator-pending; 1128 + 2 skipped tests) | Implementation (UAT T16: operator-pending; does not gate downstream) |
| S03.11 | Confidence calibration audit + ADR-017 finalization | Complete (closed 2026-05-27 via PR #42 squash `b84955c`; verdict FINALIZE; ADR-017 unmodified; Q11 RESOLVED; synthesis report at `docs/planning/epics/E03-confidence-calibration-synthesis.md` survives m1 tag; 50/50 = 100% audit pass-rate; doc-only PR — test suite holds at 1128+2) | Implementation + PM |
| S03.12 | M1 UAT preparation + handoff to M2 | Complete (closed 2026-05-27; M1 UAT runbook at `docs/runbooks/M1-uat.md` + M1→M2 handoff at `docs/planning/handoffs/M1-to-M2-handoff.md`; working notes deleted per ADR-017 §6; synthesis report survives; m1 tag ready for user push) | Implementation + PM |

### E02 Story Status

| Story | Name | Status | Owner |
|---|---|---|---|
| S02.0 | Schema preparation — `document_type='gis_layer'` + `geometry.verbatim_rule` | Complete | Implementation |
| S02.1 | ArcGIS fetch infrastructure (shared library) | Complete | Implementation |
| S02.2 | Hunting District ingestion (#3, #10, #11) | Complete | Implementation |
| S02.3 | Portions ingestion (#4, #12, #13, #14) | Complete | Implementation |
| S02.4 | Restricted Areas with verbatim text (#2, #15) | Complete | Implementation |
| S02.5 | CWD zone discovery and ingestion | Complete | Implementation (UAT: yes) |
| S02.6 | Geometry overlay fixture | Complete | Implementation (UAT: yes) |
| S02.7 | Spatial query verification + epic exit | Complete | Implementation (UAT: yes) |

### E01 Story Status

| Story | Name | Status | Owner |
|---|---|---|---|
| S01.1 | Install pre-commit hooks | Complete | Implementation |
| S01.2 | Initial migration — entity tables, link tables, indexes | Complete | Implementation |
| S01.3 | RLS migration — deny-all policies | Complete | Implementation |
| S01.4 | Python dataclasses matching DDL | Complete | Implementation |
| S01.5 | TypeScript types matching DDL | Complete | Implementation |
| S01.6 | Migration reproducibility verification | Complete | Implementation |

---

## Active Blockers

None. M2 E04 fully closed S04.1 on 2026-05-30 — Group A satisfied at merge (PR #48 squash from `feat/S04.1-license-season-rls-migration`) and Group B closed via live verification later same day. All four verification queries against the production Supabase project returned the expected shape (Q1: 0 info-schema privileges for anon/authenticated; Q2: 2 deny-all policies attached, byte-exact policy names; Q3: `relrowsecurity` + `relforcerowsecurity` both true; Q4: 2411 rows preserved — the post-UPSERT-collapse DB baseline per S04.4 spec § "Expected counts" L280, not the ~3040 build count from handoff §3 which the verification prompt had cited in error). Verbatim outputs locked in `docs/planning/epics/completed/E04-m1-carry-forward.md` § "Group B verification record — 2026-05-30". The M1 UAT criterion #7 leak surface (14 privilege leaks + 0 RLS policies on `license_season` confirmed at M1 UAT 2026-05-28) is now structurally closed in production. **Discipline note for future Group B-style verification prompts**: cite the post-UPSERT DB baseline (S04.4 spec L280 is the durable source-of-truth) not the build count from handoff §3 — recommended a future implementation agent land as a one-line entry in `.roughly/known-pitfalls.md`; PM does not touch `.roughly/` autonomously.

**E04 closed 2026-05-31.** All 5 E04 stories shipped across 5 calendar days (2026-05-29 → 2026-05-31). The two parallel housekeeping bundles surfaced through S04.4 closure both co-landed at S04.5 close via the same `/roughly:build` session: Bundle A (2 of 3 surfaced pitfall candidates landed; 3rd deliberately excluded as operational reality of cubic per PM judgment); Bundle B (all 5 accumulated handoff hygiene candidates landed). The M1→M2 handoff is now self-consistent at §3 (build-vs-DB clarifying preamble pointing at runbook footnote `[^10]`), §8 #4 (draw_spec 276→278; jurisdiction_binding row added), and §8 #7 (RESOLVED annotation). The recurring-RLS-gap M2 open-question candidate (E04 §"Known Issues to Escalate" #1) remains unchanged in scope — none of the five E04 stories needed to resolve it; it persists as a flag for the human to land via event-trigger migration, CI check, or discipline-only mitigation path.

**Active work-front: E05 (Colorado geometry ingestion).** Planned + validated 2026-05-31; pre-implementation review closed 2026-05-31 (PR #53 / `fd989b0`); **S05.0 closed at-merge 2026-06-01** (PR #54 / `7f3b071`) — TIGER 2024 Tier-3 source; Group B operator-pending. **S05.1 closed at-merge 2026-06-02** (PR #55 / `ba6ef1c`) — scaffold-only; `TestNoColoradoLeakIntoSharedLib` locks ADR-005 for CO. **S05.2 closed at-merge 2026-06-02** (PR #56 / `d7ba731`) — GMU loader; AC #235 LOG-ONLY deviation; Group B operator-pending. **S05.3 closed as documented gap 2026-06-03** (PR #57 / `9fd5c61`) — live 3-path investigation conclusively found CPW publishes no CWD-zone geometry; zero rows; Q18 empirically resolved with PM recommendation for E06 to retain V1 license-keyed disposition. **S05.3.5 closed at-merge 2026-06-03** (PR #58 / `3344971`; fifth E05 PR; carve-out story mirroring S03.6.1) — ADR-021 implemented end-to-end: new migration `20260603000000_jurisdiction_binding_no_hunt_zone_role.sql` extends the `jurisdiction_binding.role` CHECK constraint 7 → 8 values adding `'no_hunt_zone'`; 5-place sync shipped (DDL + Pydantic + TypeScript + architecture.md + `overlays.py` `GeometryRoleForE03`); 3 MT V1 federal no-hunt-zone bindings reclassified `'other_overlay'` → `'no_hunt_zone'`; ADR-021 flipped Proposed → Accepted and added to ADR index. **Spec gap closed at Stage-2 discovery**: a `cast(Literal[…], role_e03)` in `_build_overlay_bindings` was a 6th consumer beyond the spec's 5 named sites — folded into T5, recorded as a generalizable pitfall. **`_VALID_ROLE_FOR_E03` deliberately NOT widened** (4-value subset gate is intentional; MT impact zero; E06's CO binding-loader decides whether CO no-hunt zones flow through overlay-fixture path or hardcoded path — logged as E05 epic Known Issues item #6). Test baseline holds at **1234 + 2 skipped** (N=0 delta — all rewrites; baseline unchanged across S05.3 + S05.3.5); 2 new pitfalls landed in `.roughly/known-pitfalls.md` (949 → 964 LOC; doc-writer flagged the file for future reorg/dedup). **Group B operator-pending**: post-`supabase db push`, verify (1) `information_schema.check_constraints` shows the 8-value `jurisdiction_binding_role_check`, (2) `SELECT DISTINCT role` ⊆ the 8-value set, (3) the 3 MT rows now read `role='no_hunt_zone'`, (4) row count unchanged pre/post (DDL + 3-row UPDATE; no INSERT/DELETE). Capture in `docs/planning/epics/completed/E05-confidence-findings/S05.3.5.md` § "Group B verification record". **S05.4 closed at-merge 2026-06-04** (PR #59 / `dc9d5b2`; sixth E05 PR) — outcome (c) realized: 10 V1 federal no-hunt zones ingested from USGS PAD-US 4.1 Federal Fee Managers Authoritative FeatureServer (4 NPs + 5 NMs + 1 DOD AFA; Curecanti NRA dropped post-fetch per 36 CFR §2.2). New loader `ingestion/states/colorado/load_restricted_areas.py` (~520 LOC, state-agnostic-clean) writes 10 `geometry` rows with `kind='restricted_area'`, ids `CO-restricted-{slug}-geom`, `geography(MultiPolygon, 4326)` + valid. **Geometry rows ONLY** — no `jurisdiction_binding` writes (E06 territory; there is no `role` column on `geometry`). `verbatim_rule=None` for V1 per PM-confirmed deferral (CPW Big Game brochure URL unresolved 404 on 2026-06-03; no text fabricated per ADR-001/ADR-008). **Zero `ingestion/lib/` edits** — the loader composes existing private `arcgis` primitives into a bounded single-page fetch (V1 set is ~11 features << max_record_count); CO-leak guard test locks the property. **Research-doc count corrected 9 → 10** (the research doc's prose said "9 zones" in three places — arithmetic slip; enumerated named list + fetch-minus-Curecanti math both give 10); count-band guard is exact `(10, 10)` — 9 or 11 fails loud (11 = Curecanti leak). **Stage-6 triad caught a convergent Critical**: `_V1_EXPECTED_IDS` existed with a module-load length-assert but was never wired as a runtime guard in `main()` — a PAD-US `Unit_Nm` rename would write a wrong-id row silently; 1 review-fix cycle resolved 2 Criticals (set-equality guard wired into `main()`; Curecanti drop moved before fixture/manifest writes so fixtures/manifest/DB all agree at 10). **Two post-merge-review fixes**: (a) `_feature_to_geometry` malformed-`properties` fail-loud at `2715bb9`; (b) discovery-report MT-contrast paragraph corrected at `31cbc24` (MT's 3 zones came from MT FWP's ArcGIS layers, NOT PAD-US). **Quality gates clean**: ruff + mypy + pytest **1283 + 2 skipped** (baseline shifted 1234 → 1283 via +49 from `test_load_co_restricted_areas.py`; new baseline holds across S05.5–S05.7); tsc `mcp-server` + `web` exit 0; cubic `{"issues": []}`. **Group B operator-pending**: live PAD-US fetch + production service-role DB write + `returnCountOnly` cross-check (expect 11→10 written) + `ST_IsValid` round-trip + `GIS_Acres` ±10% acreage sanity, then commit live-run fixtures. Runbook at `docs/planning/epics/completed/E05-confidence-findings/S05.4.md`. **3 new pitfalls landed in `.roughly/known-pitfalls.md`** (964 → 995 LOC; doc-writer flagged 995 LOC for future reorg/dedup): `fetch_features` server-side WHERE filter limitation; post-fetch row drops must precede fixture/manifest writes; enumerated expected-set constants need runtime-output identity-check not just import-time length-check. **S05.5 closed at-merge 2026-06-05** (PR #60 / `e7ba3d4`; seventh E05 PR) — CO `geometry-overlays.json` builder shipped as faithful port of MT S02.6. New `ingestion/states/colorado/build_overlay_fixture.py` (~390 LOC) computes spatial relationships locally via shapely + STRtree against `SELECT id, kind, ST_AsText(geom) FROM geometry WHERE state='US-CO'` (geography-native, no `::geometry` cast); ADR-016 three-band discriminator carried forward unchanged as starting calibration. **`ingestion/ingestion/lib/overlays.py` extended state-agnostic-clean**: `OverlayParentKind → Literal["hunting_district", "gmu"]`; `OverlayChildKind` adds `"gmu"`; new canonical export `ROLE_FOR_BINDING_BY_CHILD_KIND` (adds `"gmu" → "primary_unit"`, preserves `"hunting_district" → "primary_unit"`); `ROLE_FOR_E03_BY_CHILD_KIND` retained as same-object deprecated alias for MT compat — resolves §"Architectural commitments" item #1. **`EXPECTED_CO_RA_ORPHAN_IDS` = the 10 S05.4 federal no-hunt zones** (byte-identical to `load_restricted_areas.py` `_V1_EXPECTED_IDS`; `assert len()==10`); per `restricted-area-discovery.md:170` all 10 are expected orphans (NPs/NMs/AFA adjacent to, not contained by, GMUs). Coverage invariant: `gmu` self-rows = `primary_unit`; `cwd_zone` vacuously satisfied (S05.3 produced 0 rows); `restricted_area` children OR on allowlist, else fail loud. **Stage-6 triad caught a convergent W1**: `test_all_children_covered_no_raise` strengthened with a `caplog` assertion that the orphan INFO log is **absent** when an RA is genuinely GMU-covered — because all 10 CO RAs are allowlisted, a broken `parent_kind` filter would otherwise pass vacuously (the absence-of-log is the lock). W2 proposed-and-rejected (documented): a `if ras and not ra_rows: raise` count guard would false-positive-fail the expected CO case. **Group B operator-pending** — the live fixture JSON cannot be generated at-merge because the CO geometry rows S05.5 reads (S05.2 GMUs + S05.4 restricted areas) are themselves operator-pending Group B writes; `geometry-overlays.json` + `geometry-overlays-dropped.json` NOT committed; will be generated + committed in the operator's batched S05.2 + S05.4 + S05.5 live-write session. Mirrors S05.0/S05.2/S05.4 pattern exactly. Operator runbook at `docs/planning/epics/completed/E05-confidence-findings/S05.5.md`. **Quality gates clean**: ruff + mypy + pytest **1331 + 2 skipped** (baseline shifted 1283 → 1331 via +48 from `test_build_co_overlay_fixture.py`; new baseline holds across S05.6 + S05.7); cubic `{"issues": []}`. **2 new pitfalls landed in `.roughly/known-pitfalls.md`** (995 → 1015 LOC; doc-writer re-flagged 1015 LOC for future reorg/dedup — recurring since S05.3.5/S05.4): allowlisted-orphan coverage checks can pass vacuously (assert orphan log is ABSENT for items that should be covered); count-band guards are not portable across states (re-derive from that state's discovery doc). **New E05 Known Issue #7** (post-E05 tech-debt candidate; not S05.5's to land): narrow overlay-builder shared-lib extraction — hoist ONLY the two pure low-divergence primitives (`_build_overlay_pairs` discriminator + `_write_outputs` serializer) into `lib/overlays.py` and migrate both MT+CO; leave orchestration/thresholds/allowlists per-state (ADR-016 §4 anticipates per-state threshold recalibration). Post-E05; suggested fold into the M2 hygiene sweep. **Epic snippet drift at lines 502-513 corrected to byte-parity with shipped code** in this PM closure commit: `Mapping[OverlayChildKind, GeometryRole]` → `dict[OverlayChildKind, GeometryRoleForE03]`; missing `"hunting_district": "primary_unit"` entry restored. Both were illustrative-snippet drift, not design errors — the ACs (547-563) described correct behavior. **S05.6 closed at-merge 2026-06-05** (PR #61 / `1b55bfd`; eighth E05 PR; **scaffold/reference-only — zero DB writes**). New `ingestion/states/colorado/load_jurisdiction_bindings.py` (~100 LOC, import-only; no `main()`, no `db.connect()`, no network) holds `_STATE: Final[str] = "US-CO"` (mirrors MT `:110`), `_NO_HUNT_ZONE_NEARBY_DISTANCE_M: Final[int] = 5000` (S03.10 Option A inheritance; CO recalibration deferred to E06), module-level `_QUERY_NEARBY_GMUS_FOR_ZONE_SQL` (boundary-to-boundary `extensions.ST_DWithin` on geography with state + zone-geom + distance all `%s`-bound), and a never-called reference function `query_nearby_gmus_for_zone(conn, zone_geom_wkt)` mirroring MT's `_query_nearby_hds_for_zone` 1:1 as E06's drop-in. Plus `ingestion/tests/test_co_binding_reference.py` (9 tests in `TestCoBindingReferenceSql` incl. the spec-mandated headline `test_co_binding_loader_sql_filters_by_state_co_pollution_guard`). **Stage-6 Critical W1** (convergent code-reviewer + silent-failure-hunter; static-analysis concurring): the spec's illustrative SQL hardcoded the `5000` literal while `_NO_HUNT_ZONE_NEARBY_DISTANCE_M` sat unused — silent-drift trap where E06's anticipated recalibration would not flow into the query. Fixed in 1 review-fix cycle by binding distance as a 3rd `%s` param mirroring MT's `cur.execute(sql, (..., _NO_HUNT_ZONE_NEARBY_DISTANCE_M, ...))` at `:588`. **One post-merge-review P3 fix** (folded into the branch pre-merge at `9b1fa7d`): reference SQL omitted `ORDER BY`, giving no PostgreSQL row-order guarantee across runs (E06 binding output could be nondeterministic); added `ORDER BY gmu.id` mirroring MT's `ORDER BY hd.id`, locked by new `test_sql_orders_by_gmu_id_for_determinism`. **Quality gates clean**: ruff + mypy + pytest **1340 + 2 skipped** (baseline shifted 1331 → 1340 via +9 from the new test file; new baseline holds into S05.7); cubic `{"issues": []}`. **1 new pitfall landed in `.roughly/known-pitfalls.md`** (1015 → 1029 LOC; doc-writer re-flagged 1029 LOC for future reorg/dedup): "Spec-provided reference SQL can hardcode a value a sibling named constant is meant to own — bind the constant as a `%s` param, don't copy the literal" (the Stage-6 Critical lesson; extends the S04.x "name the source-of-truth before copying numbers" family). **No ADRs created**; no `db.py` touches; no schema / three-place-sync changes; no MT-file touches; no TS-stack diffs; no production-DB writes. **New E06 cleanup candidate**: `_STATE` (this scaffold) vs `CO_STATE_CODE` (existing 4 CO loaders) naming unification — deliberately not retrofitted in S05.6 (scaffold-only scope); E06 binding-loader spec should pick one and migrate. **Epic SQL reference snippet at lines 602-612 corrected to byte-parity with shipped code** in this PM closure commit: distance is now `%s`-bound to `_NO_HUNT_ZONE_NEARBY_DISTANCE_M` (was hardcoded `5000`); `ORDER BY gmu.id` clause added. **S05.7 closed at-merge 2026-06-06** (PR #62 / `83e4c32`; ninth + final E05 PR) — documentation + verification only; **E05 is now Complete + Audited**. Three new docs shipped: (1) `docs/runbooks/E05-colorado-geometry-verification.md` (CO operator runbook, analog of E02's, ~365 LOC; prerequisites + Section 0 fixture generator + 7 verification sections incl. CO-specific Section 5 `ST_Envelope` bounds check; FK-cascade wipe; WKT round-trip cast workaround; every `ST_*` call `extensions.`-prefixed); (2) `docs/planning/epics/completed/E05-confidence-findings/S05.7.md` (Group B verification-record table with 7 PENDING steps + `spatial-test-points.json` generator; deletes at m2 tag per ADR-017 §6); (3) **`docs/planning/epics/completed/E05-audit.md` post-implementation audit** of all 9 stories — verdict: **E05 ships clean, no blocking findings**; ~80 MET / ~43 PARTIAL Group-B-operator-pending by design / 0 NOT MET; identical posture to E02 S02.6/S02.7 UAT. E05 epic Status flipped to `Complete + Audited 2026-06-05`; `Audited:` field populated — **the locked post-implementation-audit gate for `/plan-next-epic` E06 is now satisfied**. **2 post-PR-review SQL-correctness fixes** during S05.7 review (both root-caused to spec-prescribed SQL transcribed without validating against live schema): (a) `geom::geometry` cast in Section 5's `ST_Envelope`/`ST_Collect` → WKT round-trip (Supabase rejects the direct cast); (b) `DELETE FROM jurisdiction_binding WHERE state = 'US-CO'` → FK-precise `WHERE geometry_id IN (SELECT id FROM geometry WHERE state = 'US-CO')` (`jurisdiction_binding` has `regulation_record_state` + `geometry_id`, no `state` column); both re-derived in 5 places per the S05.5/S05.6 byte-parity convention. **Test baseline unchanged at 1340 + 2 skipped** (documentation-only PR). **No Python modules, no schema, no DB writes** from the build session. **3 new/updated pitfalls** in `.roughly/known-pitfalls.md` (now ~1045 LOC; doc-writer re-flagged for future reorg/dedup — recurring since S05.3.5): "Spec-prescribed SQL can embed a broken idiom OR a column that doesn't exist — validate against the live DDL, don't transcribe" (broadened mid-review after biting twice in the same story); "Explanatory comments that embed a broken-pattern literal trip token-matching reviewers (cubic)". **CO V1 geometry composition** (per audit): 1 statewide (`CO-STATEWIDE-geom`) + ~186 GMUs + 0 CWD zones (S05.3 documented gap) + 10 restricted areas. Test-baseline trajectory 1166 → 1340, all deliberate. **E06 (Colorado regulation text ingestion) is now ready for `/plan-next-epic`** — the locked post-implementation-audit gate is satisfied. **Operator Group B batched live-write session** remains outstanding across **S05.0 + S05.2 + S05.3.5 + S05.4 + S05.5 + S05.7** (six stories) and is the hard precondition feeding E06's binding loader. Recommended sequence: `load_state_boundary` → `load_gmus` → `load_restricted_areas` → `build_overlay_fixture` → generate `spatial-test-points.json` → run the 7 verification steps. Then PM ticks Group B ACs + S05.3.5's CHECK-constraint verification in follow-up doc commits. Worth sequencing before/with E06 spec drafting so E06 references the verified live state, not the dry-run state.

**E05 audit re-run + hygiene fixes merged 2026-06-08** (PR #63 / `f612a73` from `chore/E05-audit-hygiene-fixes`; 3 pre-squash commits: re-run audit → hygiene fixes → S05.4 count fix). **Scope**: independent re-run of the E05 post-implementation audit + remediation of its actionable findings. No story reopened; no schema/migration/three-place-sync change; no production-DB write; no MT-data reclassification. **Audit re-run**: `completed/E05-audit.md` rewritten as an independent re-run (9 parallel per-story review agents + cross-cutting synthesis), superseding the at-merge audit from S05.7's PR. **Verdict unchanged**: E05 ships clean, **0 NOT MET, 0 blocking**. Tally ~85 MET / ~29 PARTIAL (Group B operator-pending) / 0 NOT MET. Includes a post-merge fix to the S05.4 section header (Group B count 4 → 5; the per-story agent had collapsed live-fetch + `returnCountOnly` into one row — headline tally was already correct). **3 of 4 M2 hygiene candidates retired** in the same PR (fail-loud hardening; no behavior change otherwise): (1) `colorado/load_gmus.py` raw `feature["properties"]` subscript guarded → `ColoradoGeometryError` (mirrors the S05.4 `load_restricted_areas` fix); (2) `lib/db.py` `upsert_geometry` `cur.rowcount == 0` schema-drift tripwire added (matches `upsert_jurisdiction_binding`/`update_legal_description`); (3) MT `load_state_boundary.py --service-url` removed (it cited the pinned URL regardless — silent-lie citation surface; MT-touching but data-inert, `main([])` tests unaffected). **P3 test-contract + doc-rot**: +3 guard tests in `test_load_co_state_boundary.py` (multiple `.shp`, missing `STATEFP`, NULL geometry — closes the S05.0 untested-guard gap); +3 tests in `test_load_gmus.py` (missing/null properties; `caplog` lock proving AC #235 EDIT_DATE/INPUT_DATE are INFO-only); `lib/overlays.py` stale `OverlayFixtureRow.parent_geometry_id` docstring refreshed ("always a hunting district" → "HD in MT or GMU in CO"); `colorado/sources.yaml` placeholder comment replaced with actual S05.3-gap / S05.4-PAD-US outcome; `runbooks/E05-colorado-geometry-verification.md` bare `ST_PointOnSurface` reference de-referenced. **Quality gates clean on main**: ruff + mypy + pytest **1340 → 1346 + 2 skipped** (+6 additive, no regressions); detect-secrets passed. Two cubic findings reviewed and rejected as invalid (conflated MT and CO state-boundary loaders — CO uses geopandas/TIGER and pins `agency="US Census Bureau"`, confirmed via targeted tests). **Post-E05 M2 hygiene ledger reduced from 4 → 1** — only Known Issue #7 (narrow overlay-builder shared-lib extraction) remains; it was deliberately untouched as a standalone review-bearing refactor that mutates merged + audited MT code. **No new pitfalls or open questions opened.** **E05 epic file migrated to `epics/completed/` in this PM closure commit** per the standard close-out move (matches E03/E04 precedent).

**E05 validation triad + review summary**: validation triad (Spatial Correctness + ArcGIS Fidelity + Schema Stress-Test) returned LAND-WITH-EDITS; 14 MUST-FIX + 9 SHOULD-FIX findings applied at draft time. One cross-reviewer conflict resolved (Spatial vs. ArcGIS Fidelity on the CO coord-range check — ArcGIS Fidelity won; CO-bounds-specific check moved to S05.7's analytical layer). The 6 review-fix edits applied at review-merge addressed line-citation drift in S05.6 (`load_jurisdiction_bindings.py:111 → :110`; `:587/740 → :587`), `OverlayChildKind` snippet correction in S05.5 across 3 sites, defensive defaults on `EXPECTED_CO_RA_ORPHAN_IDS`, S05.7 empty-multipart-fixture documentation, docstring-sync guidance, and AC edge-test pattern note. **Cross-cutting risk surfaced by the review**: line-citation drift is the most likely E05 failure mode (the review itself caught two off-by-one drifts in a freshly-drafted epic) — recommend Stage-0 grep audit at each story open per Bundle A pitfall #1.

**M2 hygiene ledger (post-E05 hygiene-PR closure 2026-06-08)** — 3 of 4 prior candidates retired in PR #63; **one remaining**:

1. ~~`db.upsert_geometry` `cur.rowcount == 0` fail-loud guard~~ — **RESOLVED 2026-06-08 via PR #63**.
2. ~~MT `load_state_boundary.py --service-url` provenance silent-lie~~ — **RESOLVED 2026-06-08 via PR #63**.
3. ~~`load_gmus.py` raw `feature["properties"]` subscript pattern~~ — **RESOLVED 2026-06-08 via PR #63**.
4. **Overlay-builder narrow shared-lib extraction** (S05.5 Known Issue #7; post-E05 tech-debt candidate; **OPEN**). Hoist ONLY the two pure low-divergence primitives (`_build_overlay_pairs` discriminator + `_write_outputs` serializer) from MT + CO `build_overlay_fixture.py` into `ingestion/ingestion/lib/overlays.py` and migrate both states. Leave orchestration / thresholds / allowlists per-state (ADR-016 §4 anticipates per-state threshold recalibration — forcing convergence would fight legitimate divergence). Touches merged + audited MT code so needs its own review; deliberately untouched in PR #63's audit-hygiene scope.

**Research-doc accuracy item (PM-flagged; PM does NOT touch `docs/research/` autonomously)**: `docs/research/colorado-restricted-areas-evaluation.md:249` still carries the softer "same federal-authoritative chain" MT-contrast phrasing that S05.4's discovery report corrected to state split-provenance accurately (MT's 3 no-hunt zones came from MT FWP's own ArcGIS layers at MtFishWildlifeParks AGOL / `fwp-gis.mt.gov`, NOT PAD-US). Left out of S05.4's branch for scope cleanliness — surface to user for a one-line accuracy fix at the research doc.

**Recurring-RLS-gap M2 open-question candidate** (E04 §"Known Issues to Escalate" #1) — **does NOT fire for E05**: none of the 8 E05 stories add a new public-schema table. The gap persists for any future M2/M3 work that does add a table; user-side mitigation decision (event-trigger migration / CI check / discipline-only) remains pending and is independent of the E05 critical path.

**Post-implementation-audit standard for E05+** (locked at E04 audit close): the E05 epic file carries explicit Exit Criterion gating `/plan-next-epic` for E06 on E05's audit-artifact existence under `epics/completed/E05-audit.md` with a populated `Audited:` field on the E05 header. Same pattern for E06 and beyond. The PM will refuse-and-flag any `/plan-next-epic` invocation where the prior epic's audit is unrecorded — no silent skip.

**M2 open-question candidate surfaced at E04 close**: the base RLS migration `20260425000001_rls_deny_all.sql` uses a flat per-table IN-list that does not auto-extend to subsequently-added tables — the same pattern that produced the M1 `license_season` gap S04.1 closes. Any future migration in M2 / M3 that adds a new `public.*` table will silently inherit the gap unless its inline RLS block is included in the same migration. Surfaced to user via E04 § "Known Issues to Escalate" for decision on mitigation path (event-trigger migration / CI check / discipline-only). PRD 002 §"Decisions already made" already encodes the discipline-only mitigation; the recurring-gap risk is whether to harden further.

See [`docs/planning/handoffs/M1-to-M2-handoff.md`](handoffs/M1-to-M2-handoff.md) § "Known issues to escalate" for the full M1 carry-forward list (all items mapped into E04 or flagged-and-carried per E04 § "Known Issues to Escalate").

---

## Next Actions

- **S03.4 closed 2026-05-12.** PR `ab09e82` squash-merged to main; 35 BMU rows + 2 closures + 3 reporting obligations in `black-bear-2026.json`. UAT cleared (4 BMUs spot-checked against `pdfplumber`-extracted source: BMU 411 quota-closure+female-subquota; BMU 300 Spring Closure; BMU 100 unrestricted; BMU 700 quota-closure). Three spec corrections accepted: (a) AC #428 9→8 quota-closure BMUs (BMU 530 absent from 2026 PDF); (b) AC #429 2→3 reporting obligations (statewide 48-hour mandatory reporting is distinct from R1/R2-7 inspection); (c) AC #430-431 **Option B (doc-type precedence)** replaces spec's MAX-date Option A (booklet Last-Modified 2026-04-27 post-dates correction 2026-03-18, so Option A would silently no-op). 35 BMU rows all confidence=medium (HIGH→MEDIUM demote via correction-touched single demote-one-tier per row). 12 real-PDF discoveries documented in closure note; 8 new pitfalls in `.roughly/known-pitfalls.md`. Test suite: 574 passed + 2 skipped.
- **ADR-019 accepted 2026-05-12** at [`docs/adrs/ADR-019-doc-type-precedence-multi-source-merge.md`](../adrs/ADR-019-doc-type-precedence-multi-source-merge.md). Formalizes the doc-type-precedence rule (`correction` > `annual_regulations`) for all future state adapters. PRD 001 R5 wording reconciliation deferred to the next PM-led PRD review pass or before E04 planning, whichever comes first. The ADR's V1 rank table is exhaustive within the regulation-text merge path: a `rule_change` or `emergency_order` source attempting to participate fails loud at merge time, and adding a rank for either requires an ADR amendment first. `gis_layer` is structurally outside regulation-text merges (it's the geometry-source doc-type per ADR-014) and the rule doesn't apply.
- **S03.5 closed 2026-05-14** via PR `b2ad20b` (10 commits `ece1ab1..e96a195`). Initial implementation surfaced through PM-run review identified 1 P1 (verbatim spillage — column-crop x-range too narrow at 145pt for col 2, truncating "Those portions" → "Those portio" and defeating the regex anchor; bear-319 absorbed 4 subsequent HDs reaching 8059 chars vs 855 median) + 5 P2 findings (S03.5.md count, footer crop 20pt→50pt, plan unlinked-scope contradiction, plan HeadedBlock.kind missing "continuation", CWD canonicalize before consolidation). Agent applied all 6 fixes in 9 follow-up commits before merge; matched count rose 156 → 228 across the cycle and bleed cases dropped 35 → 0. Final artifact: 228 matched (226 HD + 2 CWD) / 31 by-design unmatched / 119 unlinked (full 347-row V1 surface accounted for); regression rate 0.000. Seven new pdfplumber pitfalls in `.roughly/known-pitfalls.md`. Test suite: 657 + 2 skipped (+83 from S03.5). PM UAT spot-check 2026-05-14 on 2 descriptions (HD 100 + Libby CWD) faithful to source.
- **S03.9 closed 2026-05-21** via PR `195ac8b` (squash of 8 pre-squash commits: 1 main implementation + 7 cubic-review fix-up rounds). Final state: **3 reporting_obligation rows (STATEWIDE harvest_report 48hr + R1 tooth_submission 10-day + R2-7 hide_skull_presentation 10-day) + 70 regulation_reporting link rows (35 STATEWIDE + 14 R1 + 21 R2-7)** in one atomic transaction. **Three substantive scope evolutions via the 2026-05-19 source-audit probe** (Three-Blocker discovery): (a) **CWD sampling deferred to Q18** — verbatim text already in `regulation_record.additional_rules` from S03.6; license-keyed pattern means zone-keyed `geometry-overlays.json` join would miss the `Deer Permit: 103-50` case. (b) **Bear ID coursework carved out to S03.6.1** — pre-purchase licensing prerequisite belongs in `regulation_record.additional_rules` via new `MT-STATEWIDE-bear` anchor (mirrors `MT-STATEWIDE-antelope` pattern), NOT in `reporting_obligation` which is post-harvest/in-season only. (c) **Epic line 807 corrected (PM-approved)** — "general reporting obligation for all V1 species" assumption removed; bear is the only V1 species with a statewide mandatory harvest report per FWP authoritative list (bear/wolf/marten/swans). **Two new open questions** in `docs/open-questions.md`: **Q18** (CWD sampling target-table modeling; M2 ADR-candidate when Colorado lands) and **Q19** (id text-PK UPSERT drift; **pre-M2 blocker**; affects 3 helpers in `db.py`; ADR required at resolution; leading option is derive-and-assert generalizing S03.9's local pattern). **Three new patterns for S03.6.1/S03.10+**: (1) OQ7 row-count guard is the canonical count authority (in-builder exact-count checks contradict band semantics — remove; build = data construction); (2) defensive fail-loud surface for artifact-list iterations (`isinstance` type-check BEFORE key access; try/except KeyError → RuntimeError wrapping; per-element diagnostics + duplicate-id guards); (3) `reporting_obligation.kind` semantic boundary — post-harvest/in-season only; pre-purchase prerequisites go in `regulation_record.additional_rules`. **Four new pitfalls** in `.roughly/known-pitfalls.md` (source-audit upstream artifacts before planning; kind semantic boundary; submission_method multi-modal interpretation; id-text-PK UPSERT drift). **7 cubic-review rounds during PR review** added 18 tests + the 4th pitfall; pre-merge test count was 963 (main commit), post-merge is **981 + 2 skipped**. **`cwd-sampling-modeling.md` added** to `docs/planning/epics/E03-deferred-items/` (survives past m1).
- **S03.6.1 closed 2026-05-22** via PR `339e213` (squash-merged from `feat/S03.6.1-mt-statewide-bear-anchor`). Final state: **437 regulation_record (+1) + 1 jurisdiction_binding (first ever written)** in one atomic transaction. **Four PM decisions baked in (OQ-S6.1-1 through OQ-S6.1-4)**: (1) artifact shape `list[StatewideRuleCandidate]` for M2 extensibility; (2) `rule_hint` as `str` (not Literal) for cross-state extensibility; (3) no `NOTE:` prefix on text — raw verbatim per ADR-008; (4) `db.upsert_jurisdiction_binding`'s UPDATE clause excludes 6 identity-encoded fields — silent-repoint protection refining ADR-018 binding semantics. **12 cubic-review cycles + Stage 6 triad** during PR review (review history catalogued in closure note). **Artifact SHA-256 stable across the entire cycle.** Test suite: **1024 + 2 skipped** (+43 net from S03.6.1).
- **S03.10 code-complete 2026-05-26** via PR `e83ef2d` (squash-merged from `feat/S03.10-jurisdiction-binding-generation`; 8 pre-squash commits: 1 main impl + 7 review-driven fixes). First **cross-cutting** binding loader (`ingestion/states/montana/load_jurisdiction_bindings.py` ~870 LOC). Reads `geometry-overlays.json` × regulation_record cross product, applies per-species filtering, walks 4 binding sources (statewide / overlay self-row / overlay portions / no-hunt-zone Option A), UPSERTs in one atomic three-phase transaction via `db.upsert_jurisdiction_binding` reused from S03.6.1 (no `db.py` changes — read SELECTs live in the adapter per S03.10 constraint #7). **T16 live UAT operator-pending** — regulation_record table empty at T0 probe; requires batch run of S03.0/S03.6/S03.6.1/S03.7/S03.8/S03.9 before binding writes can occur. Projected ~1000 bindings (2 statewide + ~940 overlay + ~59 no-hunt-zone) inside `[400, 1100]` guard band. **Four PM-approved spec deviations** footnoted in epic (L1045/L1070/L1092/L1311+L1313): (1) id format imports `_JURISDICTION_BINDING_ID_FORMAT` from S03.6.1's locked constant — bear binding UPSERTs as no-op; (2) count band `[400, 1100]` (spec's 1,500-3,500 was 2x too high); (3) source attribution via adapter-local SELECT against `geometry.source` (no new `db.py` helper); (4) no-hunt-zone "nearby" rule simplified to single-clause `extensions.ST_DWithin(zone.geom, hd.geom, 5000)` on native geography (boundary-to-boundary; spec's two-clause centroid-based rule returned 0 matches for all 3 orphans). **18 fail-loud guards** added across review-triad + 4 PR-review rounds. **7 new pitfalls** in `.roughly/known-pitfalls.md` (3 PostGIS/Supabase + 4 ingestion-adapter conventions). **Q19 NOT propagated to this module** — `db.upsert_jurisdiction_binding` already excludes identity fields from UPDATE (stronger mitigation than the 3 Q19-affected helpers); Q19 remains a pre-M2 blocker for those 3. **Test suite: 1128 + 2 skipped** (+104 net from S03.10). **5 M2-deferred items recorded:** `role='no_hunt_zone'` enum candidate; portion-keyed reg_records (3 coordinated touchpoints); Colorado adapter must filter `_STATE='US-CO'`; count band narrowing after T16; multi-source geometry provenance schema.
- **S03.11 closed 2026-05-27** via PR #42 (squash-merged as `b84955c`, on main at merge commit `7021bda`). **Verdict: FINALIZE.** ADR-017 active file unmodified (status remains `Accepted`; all 7 numbered sections untouched). **Q11 RESOLVED** in `docs/open-questions.md`. Audit shape: 50-row stratified sample × 10 documented edge cases (artifact-based per S03.10 T0 probe finding `regulation_record` empty); 50/50 = 100% pass-rate scoped per ADR-017 §3 (regulation_record.confidence 39/39 + EC8 parent-inheritance 6/6 + EC9 license_tag row-level 5/5). Tier distribution across projected 437 regulation_record rows: `high=32 medium=405 low=0`. **Defer-or-finalize arc**: PM recommended PARTIAL DEFER under LITERAL reading of Trigger 2 (`low=0` literally fires); `roughly:epic-reviewer` returned LAND-WITH-EDITS on PM's DRAFT; PM applied 4 refinements; user overrode to FINALIZE under INTENT reading (`low=0` is absence-by-data-property, not absence-by-framework-gap); DRAFT rejected and deleted. PM deliberation preserved in synthesis §5.2 + §6. **Synthesis report** at `docs/planning/epics/E03-confidence-calibration-synthesis.md` (~360 lines) is the durable audit record — lives outside `docs/planning/epics/E03-confidence-findings/` so it survives the S03.12 working-notes deletion. **F12 retrofits** to 4 prior working notes (S03.3 / S03.4 / S03.6 / S03.6.1) make the audit reproducible. **One new pitfall** in `.roughly/known-pitfalls.md`: "Deferred open-question verdicts require an amendment-pending breadcrumb in `docs/open-questions.md`" (generalizable, applies to future M2 audits). **Four generalizable lessons** in synthesis: (1) deferred-audit breadcrumb requirement; (2) anti-circularity discipline (derive `framework_predicted` from F12 inputs INDEPENDENTLY of artifact's pre-computed value); (3) scoped pass-rate calculations per ADR-017 §3 (don't roll different schema layers together); (4) PM does NOT modify active ADRs autonomously — DRAFT-file pattern is canonical. Test suite holds at **1128 + 2 skipped** (doc-only PR; no regression). 8 stale `docs/plans/` → `.roughly/plans/` references fixed as a separate sweep within the same PR.
- **S03.12 closed 2026-05-27 — M1 milestone complete.** Final M1 story shipped: `docs/runbooks/M1-uat.md` (operator UAT runbook with 8 SQL query blocks per PRD success criteria + operator batch-run sequence extracted from S03.10 working note before deletion); `docs/planning/handoffs/M1-to-M2-handoff.md` (M2 inheritance + Q11 RESOLVED + Q19 pre-M2 blocker + 4 deferred-items files + 7 known issues for escalation); `docs/planning/epics/E03-confidence-findings/` deleted per ADR-017 §6 with `.gitignore` update; CLAUDE.md + this README + CHANGELOG updated to reflect M1 closure. Synthesis report at `docs/planning/epics/E03-confidence-calibration-synthesis.md` survives (lives outside deletion target). S03.10 T16 live UAT folded into M1 UAT runbook as operator-prerequisite batch run. PM hands off to user for the `git tag m1` push. 6 PRD-vs-actual deviations footnoted in `M1-uat.md` (jurisdiction_code format; no `verbatim_text` column; HD 170 substituted for HD 262; no `make ingest STATE=montana` target; `license_season` RLS gap; ADR-017 unmodified satisfies criterion #8). Test suite holds at 1128 + 2 skipped (doc-only PR).

---

## Epic Files

- [M0 — Scaffold](epics/completed/M0-scaffold.md)
- [E01 — Schema Migrations, RLS, and Quality Gates](epics/completed/E01-schema-migrations.md)
- [E02 — Montana Geometry Ingestion](epics/completed/E02-geometry-ingestion.md)
- [E03 — Montana Regulation Text Ingestion](epics/completed/E03-regulation-text-ingestion.md)
- [E04 — M1 Carry-Forward and Colorado Schema Preparation](epics/completed/E04-m1-carry-forward.md)
- [E05 — Colorado Geometry Ingestion](epics/completed/E05-colorado-geometry-ingestion.md)
- [E06 — Colorado Regulation Text Ingestion](epics/E06-colorado-regulation-text-ingestion.md) (planned + validated 2026-06-08)
