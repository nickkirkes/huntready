# S03.11 — Confidence calibration audit & ADR-017 finalization working note

**Status:** Working artifact — deletes at m1 tag per ADR-017 §6.
**Story closed:** 2026-05-26 (code-complete); **user review closed 2026-05-27 with FINALIZE verdict — ADR-017 stands as-is, no amendment landed, DRAFT file deleted.**
**Synthesis report:** [../E03-confidence-calibration-synthesis.md](../E03-confidence-calibration-synthesis.md) (survives m1; durable audit-trail record)
**Amendment DRAFT:** DELETED 2026-05-27 per FINALIZE workflow — was at `docs/adrs/ADR-017-amendment-DRAFT.md`; rejected by user; no content landed in active ADR-017.

## Audit method

Artifact-based (not live-DB) per the S03.10 T0 probe finding that `regulation_record` was empty at 2026-05-23. The deterministic loaders in `load_regulation_records.py` produce byte-equivalent values from the committed DEA + bear artifacts, so reading from the artifacts is equivalent to reading from a live DB. See synthesis report §2 / §3 for full evidence.

## Sample size

50 stratified rows across 10 documented edge cases. Distribution: EC1/EC2/EC5/EC6/EC7/EC9/EC10 = 5 each (canonical); EC3 = 7 (5 minimum + 2 random fill to reach the `max(50, stratified_count) = 50` floor); EC4 = 2 (full population — only 2 statewide overlays exist in V1); EC8 = 6 (stratified across the 2 closure_predicate subtypes). See synthesis report §2.

## Retrofitted working notes

- **S03.3.md** — added F12 sample subsection with 5 row-level examples (DEA HIGH and MEDIUM rows + STATEWIDE antelope `900-20`). Extraction-level only; section-level MIN-aggregation documented in S03.6.md per scoping decision.
- **S03.4.md** — added F12 sample block with 3 BMU examples (100 unrestricted, 411 sex+quota threshold, 700 quota-only). All correction-touched; demote-one-tier exercised.
- **S03.6.md** — load-bearing retrofit: added F12 per-record trace table with 10 records covering deer fan-out symmetry, MIN aggregation outcomes, statewide overlay anchor, bear correction-touched pass-through, Region 7 portion emissions. Includes the HD 124 deer worked example showing section-level MIN. T5 backfilled the two T3-placeholders: HD-XXX → HD 250 (first all-HIGH elk section by ascending HD sort). HD-YYY resolved as an **intentional null-result**: artifact scan confirmed zero numeric-HD pronghorn sections pull to medium in V1 (the only medium pronghorn record is the statewide overlay `MT-STATEWIDE-antelope`, already represented as a separate trace row). Per the T5 plan's explicit contingency ("if the only medium pronghorn section is STATEWIDE, replace the YYY row with explanatory text — do not invent a fake HD number"), the placeholder row was replaced with an italicized cross-reference rather than a fabricated entry. This is documented in `S03.6.md`'s backfill HTML comment and the trace table's row 6 explanatory note; it is not an unresolved marker. Planning lesson: T3 introduced HD-YYY as a backfillable placeholder assuming a numeric-HD case existed; the artifact scan disproved that assumption. Future placeholder-bearing plans should declare both backfill paths (real-match vs. no-match) at plan-write time so the resolution does not require a reader to cross-reference the T5 contingency clause.
- **S03.6.1.md** — added F12 sample block with 1 row for `MT-STATEWIDE-bear / bear` (page-2 prose; `correction_touched=False` because the correction PDF only touched the BMU table, not page 2).

NONE-classified stories (no retrofit needed): S03.0, S03.2, S03.5, S03.7, S03.8, S03.9, S03.10. Per ADR-017 §3, only `regulation_record` carries the confidence column; other entities inherit via parent-inheritance or are spatial (no confidence column).

## Verdict reached in T8 (PM recommendation, 2026-05-26)

> PM recommendation: PARTIAL DEFER under the LITERAL reading of ADR-017 §7 Trigger 2. The LOW tier's real-world calibration is deferred to M2 (Colorado); ADR-017 §1/§2/§3 and the HIGH/MEDIUM tier definitions remain unchanged. The user may override to FINALIZE per the INTENT reading at review time.

## Final disposition (user decision, 2026-05-27)

**FINALIZE.** The user overrode PM's PARTIAL DEFER recommendation to FINALIZE under the INTENT reading of ADR-017 §7 Trigger 2. The decision treats `low=0` as absence-by-data-property rather than absence-by-framework-gap: the LOW rule exists in `_assign_row_confidence`, is unit-tested by `TestDemoteOneTier` in `tests/test_pdf.py`, and behaves correctly when its input conditions are present; V1 Montana data is simply well-formed enough that no row falls through to LOW. ADR-017 stands as-is. No amendment landed in the active ADR-017 file. The amendment DRAFT was deleted as part of the FINALIZE workflow.

## Amendment drafted?

DRAFTED then REJECTED. The DRAFT (76 lines after independent architectural review on 2026-05-26) proposed two coupled changes: Change A appended a calibration-deferred paragraph to ADR-017 §4 (LOW tier), and Change B appended a one-sentence disambiguation to §7 Trigger 2. The DRAFT was reviewed independently by `roughly:epic-reviewer` which returned LAND-WITH-EDITS; PM applied the 4 review-recommended refinements; the user then chose to REJECT both changes at amendment-review time. The DRAFT file was deleted 2026-05-27. Active ADR-017 was never modified by this story (verified via `git diff --name-only -- docs/adrs/ADR-017-confidence-calibration.md` returning empty across all S03.11 commits).

## Q11 status after this story

**RESOLVED** as of 2026-05-27. The Q11 entry in `docs/open-questions.md` was updated to a resolved-status annotation linking to the synthesis report as the durable audit trail. The amendment-pending breadcrumb that was added during Stage 6 review-fix (silent-failure-hunter MEDIUM finding) is superseded by the resolved status. The generalizable lesson — that any audit story whose verdict DEFERS must annotate the open question with an amendment-pending breadcrumb that survives the m1-tag working-notes deletion — remains captured in `.roughly/known-pitfalls.md` § "Conventions — Documentation & planning discipline" because it applies to future audits regardless of this story's specific outcome. S03.12's M1→M2 handoff document records Q11 as resolved.

## M2 carry-forward items

1. **LOW tier real-world calibration.** Confirm whether Colorado data exercises the LOW rule. If yes, close the deferral without further amendment. If no, evaluate whether LOW needs revision.
2. **MEDIUM→LOW demote path real-world exercise.** Unit-tested in `pdf.py` but never fired on V1 Montana data (no MEDIUM rows are also correction-touched). Watch for it during CO bear or correction-PDF ingestion.
