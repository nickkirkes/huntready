# S06.7 closure memo — `season_definition` + `license_tag` + `license_season` ingestion

**Story:** S06.7 (E06 — Colorado regulation text ingestion)
**Status:** Group A complete at-merge 2026-06-24 (branch `feat/S06.7-season-definition-license-tag-ingestion`; `569941c` impl + `b8f0cae` plan-historical; push/PR operator-pending)
**Group B (live DB write):** operator-pending — gated on the M2-build operator pass. The dry-run path is fully verified.

> Working note — deletes at the `m2` tag per ADR-017 §6.

---

## What shipped

- `ingestion/states/colorado/load_seasons_and_licenses.py` (~1700 LOC, state-agnostic-clean, three-phase: build → 5 row-count guards pre-`db.connect()` → atomic write + single `conn.commit()`). First CO multi-link-table adapter; structural analog of MT's `load_seasons_and_licenses.py`.
- `ingestion/tests/test_load_co_seasons_and_licenses.py` (new, **108 tests**).
- No `ingestion/lib/` edits (reused all 5 existing `db` helpers: `upsert_season_definition`, `upsert_license_tag`, `write_license_season`, `write_regulation_season`, `write_regulation_license`). No schema/migration/three-place-sync, no `db.py` changes, no MT touches, no TS-stack diffs, no ADRs.

## Row counts (dry-run, real artifacts) + band guards

| Table | Count | Band (±30%) |
|---|---|---|
| `season_definition` | 2013 | (1409, 2617) |
| `license_tag` | 2470 | (1729, 3211) |
| `license_season` | 2013 | (1409, 2617) |
| `regulation_season` | 2013 | (1409, 2617) |
| `regulation_license` | 2470 | (1729, 3211) |

All five guards fire BEFORE `db.connect()`. `license_season == season_definition` and `regulation_season == season_definition` because each season id encodes `(species, gmu, hunt_code, window_index)` → exactly one tag + one regulation_record; `regulation_license == license_tag` likewise.

## Q20 (Season Choice `X`) — RESOLVED per-window fan-out

Human decision 2026-06-23 (entry-time flag-and-discuss; full rationale in `docs/open-questions.md` Q20 RESOLVED). Each `X` row carries up to 3 method-windows (archery/muzzleloader/rifle) → one `season_definition` per window (weapon_type from that window), one `license_tag` carrying `weapon_types=["archery","muzzleloader","any_legal_weapon"]`, `license_season` links the tag to all its seasons. F-sex X rows with 0 windows → license_tag + WARNING, no seasons (skip-with-warning, not fail-loud). 6 season_choice sections in the 2026 artifact.

## Pre-code trigger verdicts

- **Q16 (species fan-out):** does NOT fire — CO pre-separates `mule_deer`/`whitetail`/`elk`/`pronghorn`; no `deer` label; no fan-out branch added; locked by a no-accidental-fan-out test.
- **Q17 (cross-listing conflicts):** does NOT fire at the season/license layer (repeated hunt_codes are legit multi-GMU listings; regulation_record keys by `(gmu, species)`). Any draw-side conflict is S06.8 territory.
- **closure-temporal `effective_after`:** does NOT fire — no bear closures in CO V1; zero "after [date]" prose; no `closure_predicate` rows written.
- **`apply_by` OTC discriminator:** N/A — `apply_by` is null on every CO row. Big-game kind derives from `list_value` (A→limited_draw / B→over_the_counter / C→limited_draw, fail-loud else); bear kind from the section `license_kind` (4 OTC variants → over_the_counter; fail-loud else). Documented deviation from the spec's "inspect `apply_by`".

## Asymmetric-coverage demonstrator (PRD 002 SC #2)

Locked by `test_license_season_asymmetric_coverage_m2_criterion` on **GMU 001 mule_deer** (real artifact): archery `D-M-001-O1-A` (Sep 2–30) / muzzleloader `D-M-001-O1-M` (Sep 12–20) / rifle `D-M-001-O2-R` (Oct 24–Nov 1) → 3 distinct seasons + 3 tags, all under the single `(CO-GMU-1, mule_deer)` regulation_record; archery vs rifle coverage sets differ.

## ADR-020 drift-guard

`assert_id_matches` (imported bare) at all 4 entity-builder construction sites; the 3 link builders + `_iter_co_entity_rows` are NOT instrumented. AST-locked by `TestDriftGuardCallSites` (4 instrumented builders; a new uninstrumented builder fails) + `TestLinkTableNotInstrumented`.

## Review history (3-agent + 1 fix cycle)

2 Critical + 4 Warning/Info findings, all resolved in one cycle:

1. **(Critical)** ~477 non-X hunt codes (mostly female `-F-` rifle rows) have empty `season_windows` → window-less license_tags with no WARNING. **Fix:** post-loop zero-season WARNING in both season builders + corrected docstring. The root-cause modeling question (should female tags inherit male-row windows?) is **Known Issue #13** — NOT resolved in S06.7 (faithful-load + loud-flag is the S06.7 behavior; inherit-windows is a future extractor carve-out / human decision).
2. **(Critical)** first-occurrence dedup silently discarded genuinely-different content (`E-F-085-P5-R` Oct 14 vs Oct 15 across pages). **Fix:** differing-content WARNING on dedup collision in all 4 entity builders. **Refined post-review-2 (2026-06-24):** the comparison set is now the full regulatory-content field set (opens/closes/weapon_type/**residency**/**verbatim_rule**) via shared `_season_def_lossy_diffs` / `_license_tag_lossy_diffs` helpers (single source of truth across all 4 builders) — the initial subset (dates/weapon only) silently dropped 3 `verbatim_rule`-differing collisions. `page_reference` is deliberately EXCLUDED: 157 of 219 season collisions are the same season recurring across GMU-section pages (benign provenance, not drift) — warning on those would drown the signal. Locked by `test_big_game_builder_warns_on_lossy_dedup_differing_verbatim` + `test_big_game_builder_no_warning_on_page_only_difference`.
3. **(Warning)** empty `weapon_types` could write `LicenseTag(weapon_types=[])`. **Fix:** fail-loud RuntimeError in both license_tag builders + ValueError in `_co_window_weapon_type`.
4. **(Critical, code-quality)** `_co_season_definition_id` carried an unused `season_code` param + a docstring claiming it was encoded. **Fix:** removed the param (4-param signature); `test_season_code_not_a_parameter` locks it.
5. **(Warning)** `_select_co_verbatim_rule` docstring said "trailing only" but uses `.strip()`. **Fix:** docstring corrected to "leading and trailing".
6. **(Info)** hoisted two in-`main()` `Counter` imports to module top.

Deferred (documented): the AST-guard-loop-scope gap (project-wide MT limitation, not introduced here); bear `quota_range=None` (carries a forward-comment; 2026 artifact has no int ranges).

## Quality gates

ruff + mypy clean (`states.colorado` 14 files + `lib` 8 files); **pytest 1787 → 1904 + 4 skipped** (+117 additive, zero regressions; includes the 2 post-review-2 lossy-dedup tests); `--dry-run` exits 0 with the 5 expected counts; pre-commit ruff + detect-secrets passed.

## Group B — operator-pending live verification

After the M2-build operator pass makes CO geometry + regulation_record live, run `load_seasons_and_licenses.py` (no `--dry-run`) against the dev service-role DSN and verify:

- `SELECT count(*)` per table matches the dry-run counts (2013 / 2470 / 2013 / 2013 / 2470) within band.
- `license_tag.draw_spec_key IS NULL` on every CO row (S06.8 backfills).
- FK integrity: every `license_season` / `regulation_season` / `regulation_license` row resolves to an existing `season_definition` / `license_tag` / `regulation_record`.
- Spot-check the GMU 001 mule_deer asymmetric case in the live DB (3 seasons, 3 tags, regulation_season links to all 3).

PM ticks the Group B box(es) in a follow-up doc-only commit once the operator captures results here.

## Carry-forward

- **Known Issue #13** (this memo's review finding #1) — the ~477 female-row empty-windows modeling question. PM-recommended disposition: an `extract_big_game.py` carve-out recovering the merged female-row windows (re-pin SHA), mirroring S06.3.1.
- S06.8 (draw_spec) consumes S06.7's `license_tag` rows for `draw_spec_key` backfill.
